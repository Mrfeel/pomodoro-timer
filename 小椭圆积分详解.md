# 挖洞法中"小椭圆上的积分"详解

## 背景：复习提纲例1-3

原题：计算 $\displaystyle I = \oint_L \frac{(x-y)dx + (x+4y)dy}{x^2+4y^2}$，$L: x^2+y^2=1$（逆时针）。

在 $L$ 内部围绕原点作小椭圆 $L_\varepsilon: x^2+4y^2 = \varepsilon^2$（顺时针），挖洞后得到：

$$I = \frac{1}{\varepsilon^2} \oint_{L_\varepsilon} (x-y)dx + (x+4y)dy$$

---

## 问题：小椭圆上的积分怎么算？

需要计算：

$$\oint_{L_\varepsilon} (x-y)dx + (x+4y)dy$$

其中 $L_\varepsilon$ 是椭圆 $x^2 + 4y^2 = \varepsilon^2$，方向为**顺时针**。

---

## 第一步：在小椭圆上直接用 Green 公式

令 $P = x - y$，$Q = x + 4y$。

计算偏导数：

$$\frac{\partial Q}{\partial x} = \frac{\partial}{\partial x}(x+4y) = 1$$

$$\frac{\partial P}{\partial y} = \frac{\partial}{\partial y}(x-y) = -1$$

所以：

$$\frac{\partial Q}{\partial x} - \frac{\partial P}{\partial y} = 1 - (-1) = 2$$

---

## 第二步：方向问题——顺时针 vs 逆时针

Green 公式的**标准形式**针对的是**逆时针（正向）**边界：

$$\oint_{L\text{（逆时针）}} Pdx + Qdy = \iint_D \left(\frac{\partial Q}{\partial x} - \frac{\partial P}{\partial y}\right) dxdy$$

但本题的 $L_\varepsilon$ 是**顺时针**方向！顺时针是逆时针的反方向，积分值差一个负号：

$$\oint_{L_\varepsilon\text{（顺时针）}} Pdx + Qdy = -\oint_{L_\varepsilon\text{（逆时针）}} Pdx + Qdy = -\iint_D 2 \, dxdy$$

---

## 第三步：计算椭圆面积

椭圆方程 $x^2 + 4y^2 = \varepsilon^2$，化为标准形式：

$$\frac{x^2}{\varepsilon^2} + \frac{y^2}{(\varepsilon/2)^2} = 1$$

半长轴 $a = \varepsilon$，半短轴 $b = \dfrac{\varepsilon}{2}$。

椭圆面积公式 $= \pi a b$：

$$\text{Area}(D) = \pi \cdot \varepsilon \cdot \frac{\varepsilon}{2} = \frac{\pi\varepsilon^2}{2}$$

---

## 第四步：合在一起

$$\oint_{L_\varepsilon} (x-y)dx + (x+4y)dy = -2 \cdot \text{Area}(D) = -2 \cdot \frac{\pi\varepsilon^2}{2} = -\pi\varepsilon^2$$

---

## 第五步：代回原积分

$$I = \frac{1}{\varepsilon^2} \oint_{L_\varepsilon} (x-y)dx + (x+4y)dy = \frac{1}{\varepsilon^2} \cdot (-\pi\varepsilon^2) = -\pi$$

**$\varepsilon$ 消掉了！** 这正是挖洞法的精妙之处——小椭圆的尺寸不影响结果。

---

## 思路回顾

```
          原积分 I（分母有奇点，不能直接用 Green）
                    │
                    ▼
          围绕奇点挖小椭圆洞 L_ε
                    │
                    ▼
       在环形区域用 Green → I = 小椭圆上的积分
                    │
                    ▼
      小椭圆上分母 = ε²，提到积分号外
                    │
                    ▼
    对小椭圆用 Green（注意方向！）= -2 × 椭圆面积
                    │
                    ▼
               ε 刚好消掉 → I = -π
```

---

## 关键公式速记

| 步骤 | 公式 |
|------|------|
| 偏导数差 | $\frac{\partial Q}{\partial x} - \frac{\partial P}{\partial y} = 2$（常数！） |
| 顺时针修正 | 顺时针 = $-$（逆时针）= $-\iint$ |
| 椭圆面积 | $x^2+4y^2=\varepsilon^2$ → 面积 $= \dfrac{\pi\varepsilon^2}{2}$ |
| 最终结果 | $\oint_{L_\varepsilon} = -2 \cdot \dfrac{\pi\varepsilon^2}{2} = -\pi\varepsilon^2$ |

---

## 一句总结

> **小椭圆上的积分 = $-$（偏导数差 × 椭圆面积）。偏导数差是常数 2，椭圆面积是 $\pi\varepsilon^2/2$，方向是顺时针所以加负号。$\varepsilon^2$ 最终和前面的 $1/\varepsilon^2$ 消掉。**
