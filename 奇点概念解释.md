# 奇点（Singularity）— 数分B2中的重要概念

## 什么是奇点？

在数学分析B2（第11-13章）中，**奇点**指的是使**被积函数或其偏导数无定义的点**，最常见的情形是**分母为零**。

---

## 为什么奇点很重要？

Green公式、Gauss公式、Stokes公式都要求被积函数在积分区域内**处处有定义且连续**。如果区域内部有奇点，这些定理**不能直接使用**。

以Green公式为例：

$$\oint_L Pdx + Qdy = \iint_D \left(\frac{\partial Q}{\partial x} - \frac{\partial P}{\partial y}\right) dxdy$$

如果 $D$ 内某点使 $P, Q$ 或其偏导数无定义（分母为零），则公式失效。

---

## 最经典的例子：复习提纲例1-3（2024真题）

$$I = \oint_L \frac{(x-y)dx + (x+4y)dy}{x^2+4y^2}, \quad L: x^2+y^2=1$$

当 $(x,y) = (0,0)$ 时，分母 $x^2+4y^2 = 0$，被积函数**无定义**。

点 $(0,0)$ 在单位圆内部，所以 $(0,0)$ 是奇点，Green公式**不能直接套用**。

---

## 处理方法：挖洞法

核心思路：把奇点"挖掉"。

**步骤**：

1. 在 $L$ 内部围绕奇点作一个小封闭曲线 $L_\varepsilon$（方向与 $L$ 相反）
2. 在 $L$ 与 $L_\varepsilon$ 之间的**环形区域**上，Green公式合法（奇点被排除在外）
3. 计算 $L_\varepsilon$ 上的积分（通常可以直接算）

**直观图示**：

```
    单位圆 L（逆时针）
   ┌─────────────────┐
   │                 │
   │   ┌─────────┐   │
   │   │  (0,0)  │   │   奇点位置
   │   │    ×    │   │
   │   └─────────┘   │   小椭圆 L_ε（顺时针，挖洞）
   │                 │
   └─────────────────┘
   
   环形区域（L 与 L_ε 之间）上 Green 公式合法使用
```

**具体计算**（例1-3）：

在挖洞后的环形区域上：
$$\oint_L - \oint_{L_\varepsilon} = \iint_{\text{环形区域}} \left(\frac{\partial Q}{\partial x} - \frac{\partial P}{\partial y}\right) dxdy$$

由于 $\frac{\partial Q}{\partial x} = \frac{\partial P}{\partial y}$（当 $(x,y) \neq (0,0)$），右边积分为零，所以：
$$\oint_L = \oint_{L_\varepsilon}$$

在 $L_\varepsilon: x^2+4y^2 = \varepsilon^2$ 上，分母 $= \varepsilon^2$，提取出来后用Green公式直接计算：
$$\oint_{L_\varepsilon} \frac{1}{\varepsilon^2}\left[(x-y)dx + (x+4y)dy\right] = -\pi$$

故 $I = -\pi$。

---

## 奇点出现的其他场景

### 1. Gauss公式中的奇点

$$\oiint_S \frac{x\,dydz + y\,dzdx + z\,dxdy}{(x^2+y^2+z^2)^{3/2}}$$

分母在原点为零 → 围绕原点作小球面挖去。

### 2. 保守场判定中的奇点

例1-5中，$\mathbf{v} = \dfrac{-y\mathbf{i} + x\mathbf{j}}{a^2x^2+b^2y^2}$ 在 $(0,0)$ 无定义。

- 在 $D_1: x > 0$（**单连通**且不含原点）→ 是保守场（$\frac{\partial Q}{\partial x} = \frac{\partial P}{\partial y}$ 处处成立）
- 在 $D_2: x^2+y^2 > 0$（**非单连通**，原点被挖掉形成一个"洞"）→ 不是保守场

### 3. 含参变量反常积分中的奇点

被积函数在积分区间内有无穷间断点（如 $\int_0^1 \frac{dx}{\sqrt{x}}$ 在 $x=0$ 处），也称为奇点。

---

## 一句话总结

> **奇点 = 被积函数分母为零的点。遇到奇点在积分区域内，就用挖洞法——围绕奇点作小曲线/小曲面，在环形/壳层区域上合法使用Green/Gauss/Stokes公式。**
