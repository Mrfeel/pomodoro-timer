# 数据结构 · 第6章 图 知识点与考点总结

> 《数据结构及其算法》复习资料 | 杨思辰 · 未来技术学院 · 物理学专业 · PB25992094
>
> 🔥 习题集约 11 题，考试重点：**邻接表/矩阵、DFS/BFS 手算、Prim/Kruskal、Dijkstra/Floyd、拓扑排序、关键路径**

---

## 目录

1. [§1 图的基本概念与术语](#sec1)
2. [§2 图的存储结构](#sec2)
3. [§3 图的遍历](#sec3)
4. [§4 最小生成树](#sec4)
5. [§5 拓扑排序（AOV 网）](#sec5)
6. [§6 关键路径（AOE 网）](#sec6)
7. [§7 最短路径](#sec7)
8. [§8 易错点与速查卡片](#sec8)

---

# §1 图的基本概念与术语 {#sec1}

## 1.1 图的定义

**图（Graph）** 由**顶点**的有限非空集合 $V$ 和**边**（或弧）的集合 $E$ 组成，记为 $G = (V, E)$。

- $|V|$ = 顶点数 $n$，$|E|$ = 边/弧数 $e$
- **无向图**：边 $(v, w)$，无序对
- **有向图**：弧 $<v, w>$，$v$ 为弧尾（起点），$w$ 为弧头（终点）

## 1.2 核心术语速览

| 术语 | 定义 | 备注 |
|:--|:--|:--|
| **完全图** | 无向：$\frac{n(n-1)}{2}$ 条边；有向：$n(n-1)$ 条弧 | 每个顶点与其他所有顶点互联 |
| **度 TD(v)** | 无向图：与 $v$ 关联的边数 | $\sum TD(v) = 2e$ |
| **入度 ID(v)** | 有向图：以 $v$ 为终点的弧数 | $\sum ID = \sum OD = e$ |
| **出度 OD(v)** | 有向图：以 $v$ 为起点的弧数 | |
| **路径** | 顶点序列 $v_1 \to v_2 \to \dots \to v_k$，边存在 | 路径长度 = 边数 |
| **简单路径** | 路径中顶点不重复 | 无重复顶点 |
| **回路/环** | 起点 = 终点的路径 | |
| **连通图** | 无向图：任意两顶点间存在路径 | |
| **连通分量** | 无向图的**极大连通子图** | |
| **强连通图** | 有向图：任意两顶点间存在双向路径 | |
| **强连通分量** | 有向图的**极大强连通子图** | |
| **网** | 边/弧带**权值**（weight）的图 | 边有权 |
| **生成树** | 连通图的**极小连通子图**，含全部 $n$ 个顶点但只有 $n-1$ 条边 | 多了有环，少了不连通 |

### 📌 关键关系

- **无向图**：$0 \le e \le n(n-1)/2$（边数范围）
- **有向图**：$0 \le e \le n(n-1)$（弧数范围）
- 顶点的度之和 = $2e$（无向图握住原理——每条边贡献两个度）
- **生成树**：$n$ 个顶点 $n-1$ 条边

> ⚠️ 连通分量 ≠ 生成树！连通分量是**极大**（不能再大），生成树是**极小**（不能再小）。

---

# §2 图的存储结构 {#sec2}

> 🔥 考试常考"给一个图，画出邻接矩阵 / 邻接表"，以及两种结构的优缺点对比。

## 2.1 邻接矩阵（Adjacency Matrix）

用一个 $n \times n$ 的二维数组存边信息：

- 无权图：`A[i][j] = 1`（边存在）或 `0`（无边）
- 网（有权图）：`A[i][j] = w_{ij}`（边存在且权值为 $w$）或 $\infty$（无边）

```c
#define MAXV 100
typedef struct {
    int vexs[MAXV];              // 顶点表
    int arcs[MAXV][MAXV];        // 邻接矩阵（边表）
    int vexnum, arcnum;          // 顶点数、边数
} MGraph;
```

### 示例

```
无向图:          邻接矩阵（对称！）
  A --- B        A B C D
  |   /         A[0 1 1 0]
  |  /          B[1 0 1 1]
  C --- D       C[1 1 0 1]
                D[0 1 1 0]
```

### 复杂度

| | 邻接矩阵 |
|:--|:--|
| 空间 | $O(n^2)$ |
| 判断边 $(i,j)$ 是否存在 | $O(1)$ |
| 找顶点 $v$ 的所有邻接点 | $O(n)$（扫描一整行） |
| 适用于 | **稠密图**（边数接近 $n^2$） |
| 不适用于 | 稀疏图（浪费大量空间） |

## 2.2 邻接表（Adjacency List）

每个顶点挂一个链表，存储其所有邻接点。

```c
typedef struct ArcNode {
    int adjvex;                  // 邻接点编号
    int weight;                  // 权值（网时用）
    struct ArcNode *next;        // 下一条边
} ArcNode;

typedef struct VNode {
    int data;                    // 顶点数据
    ArcNode *firstarc;           // 第一条边
} VNode, AdjList[MAXV];

typedef struct {
    AdjList vertices;
    int vexnum, arcnum;
} ALGraph;
```

### 示例

```
无向图:          邻接表:
  A --- B        A → B → C
  |   /          B → A → C → D
  |  /           C → A → B → D
  C --- D        D → B → C
```

### 复杂度

| | 邻接表 |
|:--|:--|
| 空间 | $O(n + e)$（无向图每条边存 2 次） |
| 判断边 $(i,j)$ 是否存在 | $O(\deg(v))$（需遍历链表） |
| 找顶点 $v$ 的所有邻接点 | $O(\deg(v))$ |
| 适用于 | **稀疏图** |

### 对比

| | 邻接矩阵 | 邻接表 |
|:--|:--|:--|
| 空间 | $O(n^2)$ | $O(n+e)$ |
| 判边 | $O(1)$ ✅ | $O(\deg(v))$ |
| 找邻接点 | $O(n)$ | $O(\deg(v))$ ✅ |
| 唯一性 | ✅ 唯一 | ❌ 不唯一（链表顺序可变） |
| 适合 | 稠密图 | 稀疏图 |
| 计算入度 | 有向图方便（列扫描）| 有向图不方便（需逆邻接表） |

### 逆邻接表

对有向图，额外维护**入边链表**，方便计算入度。

---

# §3 图的遍历 {#sec3}

## 3.1 深度优先搜索 DFS

**思想**：沿着一条路径**走到底**，无路可走时回溯到上一个分叉点继续探索。类似树的**前序遍历**。

**递归实现**（邻接表版）：

```c
int visited[MAXV] = {0};

void DFS(ALGraph *G, int v) {
    visited[v] = 1;                    // 标记已访问
    // visit(v);                       // 访问顶点
    for (ArcNode *p = G->vertices[v].firstarc; p; p = p->next)
        if (!visited[p->adjvex])
            DFS(G, p->adjvex);         // 递归访问未访问的邻接点
}
```

**非递归实现**（用栈）：

```c
void DFS_NonRec(ALGraph *G, int start) {
    int stack[MAXV], top = -1, visited[MAXV] = {0};
    stack[++top] = start;
    while (top >= 0) {
        int v = stack[top--];
        if (!visited[v]) {
            visited[v] = 1;
            // visit(v);
            for (ArcNode *p = G->vertices[v].firstarc; p; p = p->next)
                if (!visited[p->adjvex])
                    stack[++top] = p->adjvex;  // 邻接点入栈
        }
    }
}
```

**复杂度**（邻接表）：$O(n+e)$ ——每条边被检查一次。

### DFS 生成树 / 生成森林

遍历过程中经过的边形成一棵树（连通图）或森林（非连通图）。

## 3.2 广度优先搜索 BFS

**思想**：从起点出发，**一层一层**向外扩展。类似树的**层序遍历**。

```c
void BFS(ALGraph *G, int start) {
    int queue[MAXV], front = 0, rear = 0;
    int visited[MAXV] = {0};

    queue[rear++] = start;
    visited[start] = 1;

    while (front < rear) {
        int v = queue[front++];        // 出队
        // visit(v);
        for (ArcNode *p = G->vertices[v].firstarc; p; p = p->next)
            if (!visited[p->adjvex]) {
                visited[p->adjvex] = 1;
                queue[rear++] = p->adjvex;  // 入队
            }
    }
}
```

**复杂度**（邻接表）：$O(n+e)$

### BFS 应用

- 求**无权图最短路径**（BFS 逐层扩展的性质）
- 求**连通分量**

## 3.3 DFS vs BFS

| | DFS | BFS |
|:--|:--|:--|
| 数据结构 | 栈（递归/显式） | 队列 |
| 遍历方式 | 一条路走到黑 | 一层层扩展 |
| 与树类比 | 前序遍历 | 层序遍历 |
| 生成树 | DFS 树（可能很深） | BFS 树（宽而浅） |
| 适用 | 回溯、拓扑排序、强连通分量 | 最短路径（无权图）、连通分量 |
| 复杂度 | $O(n+e)$ | $O(n+e)$ |

---

# §4 最小生成树（MST）{#sec4}

> 🔥 必考：给定一个连通网，手算 Prim 或 Kruskal 求 MST。

## 4.1 定义

**生成树**：连通图的极小连通子图（$n$ 个顶点，$n-1$ 条边，无回路）。

**最小生成树（MST）**：所有生成树中**边权之和最小**的那棵。MST 可能不唯一（当存在权值相等的边时）。

## 4.2 Prim 算法（加点法）

**思想**：从任意起点开始，每次选一条连接**已在树中的顶点**和**不在树中的顶点**的**权值最小**的边，将新顶点加入树中。重复直到所有顶点入树。

**贪心策略**：每次选**当前可达**的最小权边。

```
Prim 过程（示例）:
    初始 U = {v₀}
    重复 n-1 次:
        在 U 和 V-U 之间的边中选权值最小的 (u, v)
        U = U ∪ {v}
```

### 数据结构

```c
// lowcost[i] = V-U 中顶点 i 到 U 中顶点的最小距离
// closest[i] = U 中与 i 最近的顶点
int lowcost[MAXV], closest[MAXV];
```

### 复杂度

- 邻接矩阵（朴素）：$O(n^2)$
- 堆优化（邻接表）：$O(e \log n)$

> 💡 稠密图用朴素 Prim（$O(n^2)$），稀疏图用 Kruskal（$O(e \log e)$）。

## 4.3 Kruskal 算法（加边法）

**思想**：将所有边按权值从小到大排序，依次检查每条边——若加入当前边不构成回路（属于不同连通分量），则选择它；否则跳过。共选 $n-1$ 条边。

**贪心策略**：每次都选不构成回路的**权值最小边**。

```
Kruskal 过程:
    1. 将所有边按权值升序排序
    2. 初始化：每个顶点自成一个连通分量
    3. 依次检查每条边(u,v):
        若 u,v 属于不同连通分量:
            选中 (u,v)
            合并两个连通分量
        否则跳过（会形成回路）
    4. 选满 n-1 条边 → 结束
```

判断是否形成回路：**并查集（Union-Find Set）**。

### 复杂度

- $O(e \log e)$（排序主导），稀疏图最优。

## 4.4 Prim vs Kruskal

| | Prim | Kruskal |
|:--|:--|:--|
| 策略 | **加点法** | **加边法** |
| 核心操作 | 找最小边（连接 U 与 V-U） | 排序 + 判回路（并查集） |
| 复杂度 | $O(n^2)$ 或 $O(e \log n)$ | $O(e \log e)$ |
| 适合 | **稠密图** | **稀疏图** |
| 是否从某点开始 | 任意起点 | 不需要起点 |

---

# §5 拓扑排序（AOV 网）{#sec5}

## 5.1 定义

- **AOV 网**：用顶点表示**活动**，有向边表示活动间的**先后关系**
- **拓扑排序**：将 AOV 网中所有顶点排成线性序列，使所有前驱→后继关系成立

> ⚠️ **存在回路（环）的图无拓扑序列**——拓扑排序可以用于检测有向图中是否有环。

## 5.2 算法（删源法）

```
1. 计算每个顶点的入度 indegree[]
2. 将所有入度为 0 的顶点入栈/队
3. while 栈非空:
      a) 弹出栈顶 v，输出 v（或存入结果）
      b) 对 v 的每个邻接点 w:
           indegree[w]--
           if indegree[w] == 0: 入栈
4. 若输出顶点数 < n → 图中有回路！
```

### 代码实现

```c
int TopoSort(ALGraph *G, int topo[]) {
    int indegree[MAXV] = {0}, stack[MAXV], top = -1;
    // ① 计算入度
    for (int i = 0; i < G->vexnum; i++)
        for (ArcNode *p = G->vertices[i].firstarc; p; p = p->next)
            indegree[p->adjvex]++;
    // ② 入度为 0 的顶点入栈
    for (int i = 0; i < G->vexnum; i++)
        if (indegree[i] == 0) stack[++top] = i;
    // ③ 拓扑排序主循环
    int cnt = 0;
    while (top >= 0) {
        int v = stack[top--];
        topo[cnt++] = v;               // 输出顶点 v
        for (ArcNode *p = G->vertices[v].firstarc; p; p = p->next) {
            indegree[p->adjvex]--;     // "删除"v，邻接点入度减 1
            if (indegree[p->adjvex] == 0)
                stack[++top] = p->adjvex;
        }
    }
    return (cnt == G->vexnum) ? 1 : 0; // cnt<n → 有回路
}
```

### 复杂度

- $O(n + e)$（邻接表）

### 拓扑序列不唯一

当存在多个入度为 0 的顶点时，任选一个输出即可，故拓扑序列不唯一。

---

# §6 关键路径（AOE 网）{#sec6}

## 6.1 定义

- **AOE 网**：用**有向边**表示**活动**，边权 = 活动持续时间，顶点 = 事件（时刻点）
- **关键路径**：从源点到汇点的**最长路径**——决定了整个工程的工期。
- **关键活动**：关键路径上的活动，若延迟则**整个工期延迟**。

## 6.2 四个关键量

| 量 | 符号 | 含义 | 计算方式 |
|:--|:--|:--|:--|
| 事件最早发生时间 | $ve(k)$ | 从源点出发**最长路径**到达事件 $k$ 的时刻 | 正向拓扑排序：$ve(k) = \max\{ve(j) + w_{jk}\}$ |
| 事件最迟发生时间 | $vl(k)$ | 不延误工期的前提下事件**最晚**可发生的时刻 | 逆向拓扑排序：$vl(k) = \min\{vl(j) - w_{kj}\}$ |
| 活动最早开始时间 | $ee(i)$ | 活动 $a_i = <j,k>$ 的最早开始 = $ve(j)$ | $ee(i) = ve(j)$ |
| 活动最迟开始时间 | $el(i)$ | 活动 $a_i$ 的最晚开始 = $vl(k) - w_{jk}$ | $el(i) = vl(k) - w$ |

### 关键活动判定

$$\boxed{\text{若 } ee(i) = el(i)\text{，则活动 } a_i \text{ 是关键活动}}$$

### 时间余量

$$\text{Slack}(a_i) = el(i) - ee(i)$$

余量为 0 的活动 = 关键活动。

## 6.3 算法步骤

```
① 正向拓扑排序 → 计算 ve[]
   ve[源点] = 0
   对每个顶点 j 的所有后继 k: ve[k] = max(ve[k], ve[j] + w[j][k])

② 逆向拓扑排序 → 计算 vl[]
   vl[汇点] = ve[汇点]
   对每个顶点 k 的所有前驱 j: vl[j] = min(vl[j], vl[k] - w[j][k])

③ 对每条边 <j,k>:
   ee = ve[j]
   el = vl[k] - w[j][k]
   若 ee == el → 关键活动

④ 关键路径 = 源点到汇点、每条边都是关键活动的路径
```

### 复杂度

- $O(n + e)$（邻接表，需两次拓扑排序）

---

# §7 最短路径 {#sec7}

> 🔥 必考：给一个带权有向图，手算 Dijkstra 或 Floyd 求最短路径。

## 7.1 Dijkstra 算法（单源最短路径）

**适用**：**非负权**有向图，求从源点 $v_0$ 到其他各顶点的最短路径。

**思想**：按最短路径长度**递增**的顺序，逐次确定各顶点的最短路径。贪心策略。

### 算法步骤

```
1. 初始化:
   dist[i] = arcs[v₀][i]    // v₀ 到 i 的直接距离
   S = {v₀}                 // 最短路径已确定的顶点集合

2. 重复 n-1 次:
   a) 在 V-S 中选 dist[] 最小的顶点 u → 加入 S
   b) 用 u 更新其他顶点的 dist[]:
      for 每个 V-S 中的 v:
          if dist[u] + arcs[u][v] < dist[v]:
              dist[v] = dist[u] + arcs[u][v]
              path[v] = u        // 记录前驱
```

### 手算示例

给定网如下（邻接矩阵），求 $v_0$ 到各顶点的最短路径：

| dist[] | $v_0$ | $v_1$ | $v_2$ | $v_3$ | $v_4$ | 已确定 |
|:--|:--:|:--:|:--:|:--:|:--:|:--|
| 初始 | 0 | 10 | $\infty$ | 30 | 100 | $\{v_0\}$ |
| 选 $v_1$(dist=10) | 0 | — | $10+50=60$ | $10+\infty=\infty \to30$ | $10+\infty=\infty\to100$ | $\{v_0,v_1\}$ |
| 选 $v_3$(dist=30) | 0 | — | $30+20=50<60$ ✅ | — | $30+60=90<100$ ✅ | $\{v_0,v_1,v_3\}$ |
| 选 $v_2$(dist=50) | 0 | — | — | — | $50+10=60<90$ ✅ | $\{v_0,v_1,v_3,v_2\}$ |
| 选 $v_4$(dist=60) | 0 | — | — | — | — | 全部 |

**最终**：最短距离 $dist = [0, 10, 50, 30, 60]$。

### 复杂度

- 朴素实现（邻接矩阵）：$O(n^2)$
- 堆优化（邻接表）：$O(e \log n)$

> ⚠️ Dijkstra **不适用于负权边**！有负权边时用 Bellman-Ford 算法。

## 7.2 Floyd 算法（多源最短路径）

**适用**：求**每对顶点之间**的最短路径。**允许负权边，但不允许负权回路**。

**思想**：动态规划。逐个尝试将每个顶点作为**中间点**，看能否缩短其他顶点间的距离。

### 核心代码（三行！）

```c
for (int k = 0; k < n; k++)               // 中间点
    for (int i = 0; i < n; i++)           // 起点
        for (int j = 0; j < n; j++)       // 终点
            if (D[i][k] + D[k][j] < D[i][j])
                D[i][j] = D[i][k] + D[k][j];
```

### 复杂度

- $O(n^3)$——三重循环，空间 $O(n^2)$

## 7.3 Dijkstra vs Floyd

| | Dijkstra | Floyd |
|:--|:--|:--|
| 问题 | **单源**最短路 | **多源**（所有点对）最短路 |
| 负权边 | ❌ 不允许 | ✅ 允许（但无负权回路） |
| 思想 | 贪心 | 动态规划 |
| 复杂度 | $O(n^2)$ 或 $O(e \log n)$ | $O(n^3)$ |
| 适用 | 求某点到其余各点 | 求所有点对之间的最短路径 |

---

# §8 易错点与速查卡片 {#sec8}

## 8.1 常见易错点

| 易错点 | 正确做法 |
|:--|:--|
| 连通分量 vs 生成树 | 连通分量是**极大**连通子图，生成树是**极小**连通子图 |
| Prim 与 Kruskal 的区别 | Prim **加点**（从 U 向外扩展），Kruskal **加边**（全局排序） |
| 拓扑排序唯一性 | **不唯一**（多个入度为 0 时可任选） |
| 存在回路 → 拓扑排序 | 回路导致**无拓扑序列** |
| 关键路径 = 最长路径 | 不是最短！关键路径决定了工程的**最短期** |
| Dijkstra 能处理负权吗 | **不能！** 有负权边时用 Bellman-Ford |
| Floyd 三层循环顺序 | **外层 k**（中间点），内层 i,j（起点、终点）——顺序不能换！ |
| 邻接表的唯一性 | 邻接表**不唯一**（链表排序任意），邻接矩阵**唯一** |
| BFS 求最短路径的前提 | 仅适用于**无权图**（或权值全相等的图） |
| 无向图邻接矩阵对称 | 无向图的邻接矩阵**对称**，可只存上/下三角 |

## 8.2 核心速查

| 知识点 | 核心公式/方法 |
|:--|:--|
| **图的存储** | 邻接矩阵 $O(n^2)$ 适合稠密，邻接表 $O(n+e)$ 适合稀疏 |
| **DFS** | 递归/栈，类似树的先序遍历，$O(n+e)$ |
| **BFS** | 队列，类似树的层序遍历，$O(n+e)$ |
| **Prim** | 加点法，$O(n^2)$ 适合稠密图 |
| **Kruskal** | 加边法 + 并查集判环，$O(e \log e)$ 适合稀疏图 |
| **拓扑排序** | 删源法（入度=0 删去），$O(n+e)$ |
| **关键路径** | 正向求 ve（最早）→ 逆向求 vl（最迟）→ ee=el 为关键 |
| **Dijkstra** | 贪心，每次选最小 dist 确定之，$O(n^2)$ |
| **Floyd** | DP，三重循环 $k/i/j$，$O(n^3)$ |
| **生成树边数** | $n-1$ 条边（多了必有环） |
| **完全图边数** | 无向 $n(n-1)/2$，有向 $n(n-1)$ |

---

<div style="text-align: center; margin-top: 2em;">

—— 第 6 章「图」知识点总结完 ——

> 📚 下一步建议：配合习题集第 7 章（图）约 11 道题逐题攻克，重点练习 Prim/Kruskal/Dijkstra 的手算过程。

</div>
