# 微分流形入门讲义

> 从拓扑到几何的桥梁 | 杨思辰 · 未来技术学院 · 物理学专业
>
> 🔥 核心概念：**拓扑流形、坐标卡、转移函数、微分结构、切向量**

---

# §1 为什么需要流形？{#sec1}

---

## 1.1 从曲线曲面到高维推广

我们熟悉 $\mathbb{R}^2$ 中的曲线和 $\mathbb{R}^3$ 中的曲面。但物理学需要更一般的空间：

- 经典力学的**相空间**（位置 + 动量，$2n$ 维）
- 广义相对论的**时空**（4 维弯曲时空）
- 规范理论中的**纤维丛**（主丛、伴随丛）

> 💡 **核心思想**：一个 $n$ 维流形是这样一个空间——**局部看起来像 $\mathbb{R}^n$**，但全局可以是弯曲的、复杂的。就像地球表面：局部看是平的（你可以用一张平面地图），但全局是一个球面。

---

## 1.2 流形 = 局部欧氏 + 全局粘合

想象你在搭建一个模型：

```
取很多块 ℝⁿ 的"补丁"（开集）
    ↓
用"坐标卡"把它们贴到空间上
    ↓
在重叠区域要求"坐标变换"足够光滑
    ↓
得到一个微分流形
```

> ⭐ **关键**：流形不只是"弯曲的空间"，更重要的是——**你可以在上面做微积分**。这就是"微分流形"中"微分"二字的含义。

---

# §2 拓扑流形 {#sec2}

---

## 2.1 定义

一个 **$n$ 维拓扑流形** $M$ 是满足以下条件的拓扑空间：

1. **Hausdorff**：任意两个不同点可以被不相交的开集分离
2. **第二可数**：具有可数拓扑基
3. **局部欧氏**：每点 $p \in M$ 存在一个邻域同胚于 $\mathbb{R}^n$（或 $\mathbb{R}^n$ 的一个开子集）

> ⚠️ 条件 1 和 2 是技术性条件，排除了一些病态例子（如"带两个原点的直线"不满足 Hausdorff 性）。

## 2.2 例子

| 空间 | 维数 | 说明 |
|------|------|------|
| $\mathbb{R}^n$ | $n$ | 最基本的平凡流形 |
| $S^n$（$n$ 维球面） | $n$ | 如 $S^1$（圆周）、$S^2$（球面） |
| $T^n = S^1 \times \cdots \times S^1$ | $n$ | $n$ 维环面 |
| $\mathbb{RP}^n$（实射影空间） | $n$ | $\mathbb{R}^{n+1}$ 中过原点的所有直线 |
| 任何 $n$ 维向量空间 | $n$ | 选取基后同胚于 $\mathbb{R}^n$ |

---

# §3 坐标卡与图册 {#sec3}

---

## 3.1 坐标卡

设 $M$ 是拓扑空间，$U \subset M$ 是开集。一个 **坐标卡**（chart）是一个同胚映射：

$$\varphi: U \to \varphi(U) \subset \mathbb{R}^n$$

记坐标卡为 $(U, \varphi)$ 或 $(U, x^1, \ldots, x^n)$，其中 $x^i$ 称为**局部坐标函数**。

> 💡 一张坐标卡就像给 $M$ 的某个区域画了一张"地图"——把弯曲区域摊平到 $\mathbb{R}^n$ 里。

## 3.2 图册

一个 **图册**（atlas）$\mathcal{A}$ 是一族坐标卡 $\{(U_\alpha, \varphi_\alpha)\}$，满足：

$$\bigcup_{\alpha} U_\alpha = M$$

即所有坐标卡的定义域覆盖整个流形。

> 💡 图册 = 一本地图集。每张地图只覆盖一部分区域，但合起来覆盖整个地球。

## 3.3 转移函数

设 $(U, \varphi)$ 和 $(V, \psi)$ 是两张坐标卡，$U \cap V \neq \emptyset$。则**转移函数**（transition map）定义为：

$$\boxed{\psi \circ \varphi^{-1}: \varphi(U \cap V) \to \psi(U \cap V)}$$

这是 $\mathbb{R}^n$ 中两个开集之间的映射。

> ⭐ **核心观察**：转移函数是一个 $\mathbb{R}^n \to \mathbb{R}^n$ 的映射——是"普通"的多元函数！我们可以讨论它的光滑性。这是定义微分流形的关键。

---

# §4 微分结构与微分流形 {#sec4}

---

## 4.1 $C^r$ 相容性

两张坐标卡 $(U, \varphi)$ 和 $(V, \psi)$ 称为 **$C^r$ 相容**的，如果：

- $U \cap V = \emptyset$，或
- $U \cap V \neq \emptyset$ 且转移函数 $\psi \circ \varphi^{-1}$ 及其逆都是 $C^r$ 映射

>💡 $C^0$ 相容就是"拓扑相容"；$C^\infty$ 相容就是"光滑相容"。物理中几乎只用 $C^\infty$。

## 4.2 微分结构

若图册 $\mathcal{A}$ 中任意两张坐标卡都 $C^r$ 相容，则称 $\mathcal{A}$ 为 **$C^r$ 图册**。

一个 **极大 $C^r$ 图册**（即把所有与 $\mathcal{A}$ 中坐标卡 $C^r$ 相容的坐标卡都加进去）称为 $M$ 上的一个 **$C^r$ 微分结构**。

## 4.3 微分流形的定义

$$\boxed{\text{微分流形} = \text{拓扑流形} + \text{微分结构}}$$

> ⚠️ **惊人的事实**：同一个拓扑流形上可以有不同的微分结构！例如 $S^7$ 上有 28 种不同的微分结构（Milnor, 1956）。$\mathbb{R}^4$ 上有不可数无穷多种微分结构（Donaldson, 1982）！这种怪异现象只在 4 维以上出现。

---

# §5 光滑映射与微分同胚 {#sec5}

---

## 5.1 光滑映射

设 $M, N$ 是光滑流形，映射 $f: M \to N$ 称为 **光滑的**，如果对任意 $p \in M$，存在 $p$ 附近的坐标卡 $(U, \varphi)$ 和 $f(p)$ 附近的坐标卡 $(V, \psi)$，使得：

$$\psi \circ f \circ \varphi^{-1}: \varphi(U \cap f^{-1}(V)) \to \psi(V)$$

是光滑的（作为 $\mathbb{R}^m \to \mathbb{R}^n$ 的映射）。

> 💡 说人话：$f$ 光滑 $\Leftrightarrow$ 在任意局部坐标下，$f$ 表现为光滑的多元函数。这就是流形的核心思想——**用局部坐标把问题归结为 $\mathbb{R}^n$ 上的微积分**。

## 5.2 微分同胚

若 $f: M \to N$ 是双射，且 $f$ 和 $f^{-1}$ 都光滑，则称 $f$ 为 **微分同胚**。

> ⭐ 微分同胚的流形在微分几何意义下是"相同"的——就像同胚的空间在拓扑意义下相同。

---

# §6 切空间与切向量 {#sec6}

---

## 6.1 三种等价的定义

在流形上没有"位置向量"的概念，如何定义切向量？有三种等价方式：

| 定义方式 | 核心思想 | 记法 |
|----------|----------|------|
| **曲线等价类** | 过 $p$ 的曲线，在局部坐标下导数相同即等价 | $[\gamma]$ |
| **方向导数** | 切向量 = 作用在芽上的线性泛函，满足 Leibniz 规则 | $X_p$ |
| **坐标分量** | 在局部坐标下用分量 $\{X^i\}$ 表示 | $X^i \frac{\partial}{\partial x^i}\big|_p$ |

## 6.2 切空间

$p$ 处的所有切向量构成 $n$ 维向量空间，记作 **$T_pM$**。

$$\boxed{\dim T_pM = \dim M = n}$$

## 6.3 自然基底

在局部坐标 $(U, x^1, \ldots, x^n)$ 下，$T_pM$ 有一组自然基底：

$$\left\{ \frac{\partial}{\partial x^1}\bigg|_p, \frac{\partial}{\partial x^2}\bigg|_p, \ldots, \frac{\partial}{\partial x^n}\bigg|_p \right\}$$

> 💡 这里的 $\frac{\partial}{\partial x^i}$ 不是普通的偏导数算符，而是 $T_pM$ 中的基向量。理解为"沿 $x^i$ 方向的无穷小平移"。

---

# §7 切丛与向量场 {#sec7}

---

## 7.1 切丛

把所有点的切空间"捆"在一起，得到**切丛**：

$$TM = \bigsqcup_{p \in M} T_pM$$

$TM$ 本身是一个 $2n$ 维光滑流形。它带有自然投影：

$$\pi: TM \to M, \quad (p, v) \mapsto p$$

## 7.2 向量场

一个**光滑向量场** $X$ 是一个光滑映射 $X: M \to TM$，满足 $\pi \circ X = \mathrm{id}_M$。在局部坐标下：

$$X = \sum_{i=1}^{n} X^i(x) \frac{\partial}{\partial x^i}$$

其中 $X^i(x)$ 是光滑函数。

> 💡 在物理中，向量场处处可见：电场、磁场、速度场、引力场……它们都是在流形上定义的向量场。

---

# §8 核心概念速查 {#sec8}

---

| 概念 | 一句话解释 |
|------|-----------|
| 拓扑流形 | 局部同胚于 $\mathbb{R}^n$ 的 Hausdorff 第二可数空间 |
| 坐标卡 | 把流形局部映射到 $\mathbb{R}^n$ 的同胚 |
| 图册 | 覆盖整个流形的一族坐标卡 |
| 转移函数 | 坐标卡重叠处的坐标变换 $\psi \circ \varphi^{-1}$ |
| 微分结构 | 极大 $C^r$ 相容图册 |
| 微分流形 | 拓扑流形 + 微分结构 |
| 光滑映射 | 在局部坐标下为光滑函数的映射 |
| 切向量 | 方向导数算符 / 曲线等价类 |
| 切空间 $T_pM$ | $p$ 处所有方向导数的集合，$n$ 维向量空间 |
| 切丛 $TM$ | 所有切空间的并，$2n$ 维流形 |
| 向量场 | 给每点分配一个切向量的光滑映射 |

---

# §9 后续进阶主题预览 {#sec9}

---

1. **余切空间与 1-形式**：切空间的对偶空间，$dx^i$ 为自然余基
2. **张量场**：$(r,s)$ 型张量，物理中的应力张量、电磁张量
3. **外微分与微分形式**：$d$ 算子，Stokes 定理的流形版本
4. **李导数**：沿向量场的"流动导数"
5. **联络与协变导数**：如何"平行移动"向量，比较不同点的切空间
6. **曲率**：Riemann 曲率张量——衡量流形的弯曲程度
7. **Stokes 定理**：$\int_M d\omega = \int_{\partial M} \omega$（流形版本）

---

<div style="text-align: center; color: #666; margin-top: 40px;">

—— 微分流形：在弯曲的空间上做微积分 ——

</div>
