# 相似矩阵的性质——完整总结

> **相似的定义**：存在可逆矩阵 $P$，使得 $\boxed{B = P^{-1}AP}$，则 $A \sim B$。
>
> **本质含义**：$A$ 和 $B$ 是**同一个线性变换在不同基下的矩阵表示**。

\newpage

# 一、相似不变量（相似矩阵的共有性质）

以下所有量在相似变换下**保持不变**：

## 1.1 特征值

$$\boxed{\lambda_A = \lambda_B \quad\text{（包括代数重数）}}$$

**证明**：$|\lambda I - B| = |\lambda I - P^{-1}AP| = |P^{-1}(\lambda I - A)P| = |P^{-1}||\lambda I - A||P| = |\lambda I - A|$

特征多项式相同 → 特征值完全相同。

> ⚠️ **特征向量一般不同！** 若 $A\boldsymbol{x} = \lambda\boldsymbol{x}$，则 $B(P^{-1}\boldsymbol{x}) = \lambda(P^{-1}\boldsymbol{x})$。$B$ 的特征向量是 $A$ 的特征向量左乘 $P^{-1}$。

## 1.2 行列式

$$\boxed{|A| = |B|}$$

**证明**：$|B| = |P^{-1}AP| = |P^{-1}||A||P| = |A|$（$|P^{-1}| = 1/|P|$）

**推论**：$|A| = \prod \lambda_i$，既然特征值相同，行列式自然相同。

## 1.3 迹

$$\boxed{\text{tr}(A) = \text{tr}(B)}$$

**证明**：$\text{tr}(P^{-1}AP) = \text{tr}(APP^{-1}) = \text{tr}(A)$（迹的循环性）

**推论**：$\text{tr}(A) = \sum \lambda_i$，既然特征值相同，迹自然相同。

## 1.4 秩

$$\boxed{\text{rank}(A) = \text{rank}(B)}$$

可逆矩阵乘法不改变秩。

## 1.5 特征多项式

$$\boxed{f_A(\lambda) = f_B(\lambda)}$$

即 $|\lambda I - A| = |\lambda I - B|$。特征多项式完全一致。

## 1.6 最小多项式

$$\boxed{m_A(\lambda) = m_B(\lambda)}$$

最小多项式（使 $m(A)=0$ 的次数最低的首一多项式）在相似下不变。

## 1.7 若尔当标准形

$$\boxed{J_A = J_B}$$

相似矩阵有**相同的若尔当标准形**——这是所有相似不变量的"总根源"。实际上：

$$A \sim B \;\Longleftrightarrow\; A \text{ 和 } B \text{ 有相同的若尔当标准形}$$

\newpage

# 二、性质速查表

| 性质 | 是否相同 | 说明 |
|------|---------|------|
| 特征值 + 重数 | ✅ 相同 | 核心不变量 |
| 特征向量 | ❌ 不同 | $B$ 的特征向量 = $P^{-1}\times$（$A$ 的特征向量） |
| 行列式 | ✅ 相同 | $=\prod\lambda_i$ |
| 迹 | ✅ 相同 | $=\sum\lambda_i$ |
| 秩 | ✅ 相同 | 可逆变换不改变秩 |
| 特征多项式 | ✅ 相同 | $|\lambda I-A|=|\lambda I-B|$ |
| 最小多项式 | ✅ 相同 | 决定可对角化条件 |
| 若尔当标准形 | ✅ 相同 | 相似的最高判据 |
| 可逆性 | ✅ 相同 | $A$ 可逆 $\Leftrightarrow$ $B$ 可逆 |
| 幂零性 | ✅ 相同 | $A^k=0 \Leftrightarrow B^k=0$ |
| 转置 | ❌ 不一定 | $A\sim B$ 不保证 $A^T\sim B^T$（实际上 $A$ 总相似于 $A^T$，但不是通过同一个 $P$） |

\newpage

# 三、相似 ⇔ 相同的若尔当标准形

这是判断两个矩阵是否相似的**充要条件**：

$$\boxed{A \sim B \;\Longleftrightarrow\; J_A = J_B}$$

两条路径殊途同归：

```
         A              B
         │              │
    P⁻¹AP│         Q⁻¹BQ│
         ▼              ▼
         J_A  ======  J_B
```

若尔当标准形由**特征值**和**每个特征值的若尔当块结构**唯一确定。所以：

> 相似 = 特征值相同 + 每个特征值的几何重数（即对应若尔当块个数）相同。

## 例题：判断两个矩阵是否相似

$$A = \begin{pmatrix} 2 & 1 & 0 \\ 0 & 2 & 1 \\ 0 & 0 & 2 \end{pmatrix},\quad B = \begin{pmatrix} 2 & 1 & 0 \\ 0 & 2 & 0 \\ 0 & 0 & 2 \end{pmatrix}$$

- 特征值：都是 $\lambda=2$（三重）→ 特征值相同 ✓
- $A$ 的若尔当标准形：一个 $3\times 3$ 若尔当块（$\text{rank}(A-2I)=2$）
- $B$ 的若尔当标准形：一个 $2\times 2$ 块 + 一个 $1\times 1$ 块（$\text{rank}(B-2I)=1$）

若尔当结构不同 → **$A$ 和 $B$ 不相似！**

\newpage

# 四、相似与对角化

## 4.1 可对角化的充要条件

$A$ 可对角化（相似于对角矩阵）⇔ $A$ 有 $n$ 个线性无关的特征向量 ⇔ 每个特征值的几何重数 = 代数重数。

此时相似变换矩阵 $P$ 由 $A$ 的特征向量组成，且：

$$\boxed{P^{-1}AP = \Lambda = \begin{pmatrix} \lambda_1 & & \\ & \ddots & \\ & & \lambda_n \end{pmatrix}}$$

## 4.2 对角化的推论

若 $A \sim \Lambda$（对角矩阵），则：

- $A^k = P\Lambda^k P^{-1}$（计算 $A^k$ 极其简单）
- $e^A = P e^\Lambda P^{-1}$
- 行列式 $|A| = \prod\lambda_i$
- 迹 $\text{tr}(A) = \sum\lambda_i$

\newpage

# 五、考试中最常见的考法

## 考法1：用相似不变量求参数

已知 $A \sim B$，利用 $\text{tr}(A)=\text{tr}(B)$ 和 $|A|=|B|$ 列方程求未知参数。

## 考法2：判断是否相似

① 算特征多项式 → 相同？② 对每个特征值算几何重数 → 相同？③ 都相同 → 相似。

## 考法3：利用相似简化计算

$A = PBP^{-1} \Rightarrow A^k = PB^kP^{-1}$，先算简单的 $B^k$ 再乘回来。

## 考法4：证明题

证 $AB \sim BA$（$A,B$ 至少一个可逆时），或证 $A$ 相似于 $A^T$。

\newpage

# 六、记忆口诀

> **特征值、行列式、迹、秩——四条全等，才可能相似。但要确认相似，还需看若尔当结构（特征向量的个数）。**

## 一句话总结

> 相似矩阵是**同一线性变换在不同基下**的矩阵。它们共享特征值、行列式、迹、秩、特征多项式、最小多项式和若尔当标准形。判断相似的终极标准：**若尔当标准形是否相同**。
