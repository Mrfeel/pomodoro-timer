# 数据结构 · 第4章补充：广义表

> 《数据结构及其算法》复习资料 | 杨思辰 · 未来技术学院 · 物理学专业 · PB25992094
>
> 本补充属于第4章「串和数组」的扩展内容，严蔚敏教材将广义表与数组合为第5章。

---

# §1 广义表的定义

## 1.1 什么是广义表

**广义表（Generalized List）** 是 $n$（$n \ge 0$）个数据元素的有限序列，记为：

$$LS = (a_1, a_2, \dots, a_n)$$

其中 $a_i$ 可以是**原子**（不可再分的单个数据元素），也可以是**另一个广义表**（子表）。

> 💡 广义表 = 线性表的推广——线性表元素只能是原子，广义表元素可以是子表（递归定义）。

## 1.2 术语

| 术语 | 含义 | 示例 |
|:--|:--|:--|
| **原子** | 不可再分的数据元素 | 数字、字符 |
| **子表** | 广义表中的另一个广义表 | `(a,(b,c))` 中 `(b,c)` 是子表 |
| **表头（Head）** | 广义表的第一个元素 | `Head((a,b,c)) = a` |
| **表尾（Tail）** | 除去第一个元素外剩余元素组成的**广义表** | `Tail((a,b,c)) = (b,c)` |
| **长度** | 最外层元素的个数 | `Len((a,(b,c),d)) = 3` |
| **深度** | 括号的最大嵌套层数 | `Depth((a,(b,(c))))=3` |
| **空表** | 长度为 0 的广义表，记为 `()` | `Len(()) = 0` |

> ⚠️ **表尾一定是广义表**——就算只剩一个元素 `(b)`，表尾也是 `(b)` 而不是原子 `b`。

## 1.3 示例

| 广义表 | 长度 | 深度 | Head | Tail |
|:--|:--:|:--:|:--|:--|
| `()` | 0 | 1 | 无 | 无 |
| `(a, b)` | 2 | 1 | `a` | `(b)` |
| `(a, (b, c))` | 2 | 2 | `a` | `((b, c))` |
| `((a))` | 1 | 2 | `(a)` | `()` |
| `(a, (b, (c)))` | 2 | 3 | `a` | `((b, (c)))` |

---

# §2 广义表的存储结构

## 2.1 头尾链表表示法

每个结点要么是**原子结点**（存数据），要么是**子表结点**（指向子表）。

```c
typedef enum { ATOM, LIST } ElemTag;    // 标记：原子 or 子表

typedef struct GLNode {
    ElemTag tag;                         // 标记位
    union {
        int data;                        // 原子：数据值
        struct {                         // 子表：两个指针
            struct GLNode *hp;           // ★ 表头指针
            struct GLNode *tp;           // ★ 表尾指针
        } ptr;
    };
} GLNode, *GList;
```

**示例**：$L = (a, (b, c))$

```
L → [LIST|hp→] → [ATOM|a]  (tp→)
           tp→ ↓
              [LIST|hp→] → [ATOM|b]  (tp→)
                     tp→ ↓
                        [ATOM|c]  (tp→NULL)
```

> 💡 记忆：`hp` = 表头（Head Pointer），`tp` = 表尾（Tail Pointer）。每个结点都通过 `hp`/`tp` 指针表达"首元素/剩余元素"的结构。

## 2.2 扩展线性链表表示法

每个结点有 `next` 指针直接指向同一层的下一个兄弟，子表用 `sublist` 指针指向。

```c
typedef struct GLNode2 {
    ElemTag tag;
    union {
        int data;                        // 原子值
        struct GLNode2 *sublist;         // 子表指针
    };
    struct GLNode2 *next;                // ★ 同层下一个兄弟
} GLNode2;
```

**示例**：$L = (a, (b, c), d)$

```
L → [LIST|sublist→] → [ATOM|a] → [LIST|sublist→] → [ATOM|d|NULL]
                                    ↓
                              [ATOM|b] → [ATOM|c|NULL]
```

---

# §3 广义表的递归算法

## 3.1 求深度

```c
int GListDepth(GList L) {
    if (L == NULL) return 1;              // 空表深度为 1
    if (L->tag == ATOM) return 0;          // 原子深度为 0
    int maxD = 0;
    for (GList p = L; p; p = p->ptr.tp) {  // 遍历同层
        int d = GListDepth(p->ptr.hp);      // 递归求子表深度
        if (d > maxD) maxD = d;
    }
    return maxD + 1;
}
```

## 3.2 求长度

长度 = 最外层元素个数。遍历 `tp` 指针链计数即可。

## 3.3 等价的二叉树表示

广义表可以看作一种"树"——每个 LIST 结点是内部结点，原子是叶结点。广义表本质是树的**括号表示法**。

---

# §4 易错点

| 易错点 | 正确理解 |
|:--|:--|
| 表头 ≠ 第一个原子 | 表头 = 第一个**元素**（可能是一个子表）。如 `((a,b),c)` 的表头是 `(a,b)`，不是 `a` |
| 表尾一定是广义表 | `Tail((a)) = ()`，不是 `a`；`Tail((a,b)) = (b)`，不是 `b` |
| 空表 `()` 的长度 | 长度为 **0**，深度为 **1** |
| 原子有深度吗 | 原子的深度为 **0** |
| 深度怎么数 | 数括号嵌套的最大层数，不是数括号对数 |

---

<div style="text-align: center; margin-top: 2em;">

—— 广义表补充完 ——

> 📎 本补充附于第4章「串和数组」知识点总结之后。

</div>
