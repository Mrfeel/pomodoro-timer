# 数据结构 · 栈（Stack）知识点与考点总结

> 《数据结构及其算法》复习资料 | 杨思辰 · 未来技术学院 · 物理学专业
>
> 🔥 考试重点：**出栈序列合法性（卡特兰数）、顺序栈判空判满、表达式求值（算符优先）、中缀转后缀、栈与递归、共享栈**

## 目录

1. [§1 栈的基本概念](#sec1)
2. [§2 栈的存储结构](#sec2)
3. [§3 栈的基本操作](#sec3)
4. [§4 栈的典型应用](#sec4)
5. [§5 栈与递归](#sec5)
6. [§6 出栈序列问题（重点考点）](#sec6)
7. [§7 易错点与速查卡片](#sec7)

---

# §1 栈的基本概念 {#sec1}

## 1.1 定义

**栈（Stack）** 是一种**只允许在一端进行插入和删除**的线性表。允许操作的一端称为**栈顶（top）**，另一端称为**栈底（bottom）**。

> ⭐ 核心特性：**后进先出（LIFO，Last In First Out）**。最后入栈的元素最先出栈。

> 💡 直观类比：一摞盘子——只能从最上面放（入栈）和取（出栈）；子弹匣——最后压入的子弹最先射出。

## 1.2 基本术语

| 术语 | 含义 |
|------|------|
| 入栈 / 压栈（push） | 在栈顶插入一个元素 |
| 出栈 / 弹栈（pop） | 删除栈顶元素并返回 |
| 栈顶（top） | 允许操作的一端 |
| 栈底（bottom） | 不允许操作的一端 |
| 空栈 | 不含任何元素的栈 |
| 上溢（overflow） | 栈满时再入栈 |
| 下溢（underflow） | 栈空时再出栈 |

> ⭐ 栈是**操作受限的线性表**：逻辑结构仍是线性表，只是插入/删除被限制在栈顶。

---

# §2 栈的存储结构 {#sec2}

栈有两种实现：**顺序栈**（数组）和**链栈**（链表）。

## 2.1 顺序栈

用一维数组 `data[MAXSIZE]` 加一个栈顶指针 `top` 实现。

**常见约定**：`top` 指向**栈顶元素**（另一种约定是指向栈顶的下一个空位，两者判空判满条件不同，务必看清题目约定）。

| 状态 | 条件（top 指向栈顶元素） |
|------|--------------------------|
| 栈空 | `top == -1` |
| 栈满 | `top == MAXSIZE - 1` |
| 入栈 | `data[++top] = x;` |
| 出栈 | `x = data[top--];` |

> ⚠️ 若约定 `top` 指向栈顶**上方空位**（初值 0），则栈空为 `top == 0`、栈满为 `top == MAXSIZE`。**判空判满条件与约定强相关，是常见考点。**

## 2.2 共享栈（两栈共享空间）

两个栈**共用一个数组**，两个栈底分别设在数组两端，栈顶向中间生长，以提高空间利用率。

- 栈 1 栈顶 `top1` 初值 $-1$，向右增长；栈 2 栈顶 `top2` 初值 `MAXSIZE`，向左增长。

> 🔥 **栈满条件：`top1 + 1 == top2`**（两栈顶相邻）。这是共享栈的必考点。

## 2.3 链栈

用单链表实现，**栈顶设在链表头部**（头插法）。通常**不带头结点**，栈顶指针即链头指针。

- 入栈 = 头插，出栈 = 删头，均为 $O(1)$。
- **不存在"栈满"**（除非内存耗尽），因此无需判满。

## 2.4 顺序栈 vs 链栈 对比

| 对比项 | 顺序栈 | 链栈 |
|--------|--------|------|
| 存储 | 数组，需预先定容 | 链表，动态分配 |
| 栈满 | 可能上溢，需判满 | 一般不会满 |
| 空间 | 可能浪费或不足 | 按需分配，有指针开销 |
| 多栈共享 | 可用共享栈 | 不便共享 |
| 操作复杂度 | 入栈/出栈 $O(1)$ | 入栈/出栈 $O(1)$ |

---

# §3 栈的基本操作 {#sec3}

一组标准操作（**时间复杂度均为 $O(1)$**）：

- `InitStack(S)`：初始化空栈
- `StackEmpty(S)`：判空
- `Push(S, x)`：入栈
- `Pop(S, &x)`：出栈
- `GetTop(S, &x)`：读栈顶（不删除）

**顺序栈入栈/出栈核心代码：**

```c
// 入栈：先判满，再 top 加 1、放元素
int Push(SqStack *S, int x){
    if(S->top == MAXSIZE - 1) return 0;   // 上溢
    S->data[++S->top] = x;
    return 1;
}
// 出栈：先判空，再取元素、top 减 1
int Pop(SqStack *S, int *x){
    if(S->top == -1) return 0;            // 下溢
    *x = S->data[S->top--];
    return 1;
}
```

> ⚠️ 入栈**先判满**、出栈**先判空**——顺序写反会数组越界或读到非法值。

---

# §4 栈的典型应用 {#sec4}

> 🔥 这是栈这一章**考题最密集**的部分，务必掌握每个应用的思路。

## 4.1 括号匹配

从左到右扫描：遇左括号入栈；遇右括号则与栈顶左括号匹配并出栈。**最终栈空且中途无失配**则匹配成功。

> 💡 记忆：左括号"欠账"入栈，右括号"还账"出栈；账不平（栈非空或提前空）即不匹配。

**计算方法**（以 `([{}])` 为例，逐字符跟踪栈）：

| 读入 | 动作 | 栈（底→顶） |
|:--:|:--|:--|
| `(` | 左括号入栈 | `(` |
| `[` | 左括号入栈 | `( [` |
| `{` | 左括号入栈 | `( [ {` |
| `}` | 与栈顶 `{` 配对，出栈 | `( [` |
| `]` | 与栈顶 `[` 配对，出栈 | `(` |
| `)` | 与栈顶 `(` 配对，出栈 | （空） |

扫描结束栈空 → **匹配成功**。反例 `[(])`：读到 `]` 时栈顶是 `(`，类型不符 → 立即判定**不匹配**。

**代码示例：**

```c
int bracketMatch(const char *s){
    char st[100]; int top = -1;
    for(int i = 0; s[i]; i++){
        char c = s[i];
        if(c=='('||c=='['||c=='{') st[++top] = c;          // 左括号入栈
        else if(c==')'||c==']'||c=='}'){
            if(top == -1) return 0;                         // 右括号但栈空 → 失配
            char t = st[top--];                             // 弹出栈顶左括号
            if((c==')'&&t!='(')||(c==']'&&t!='[')||(c=='}'&&t!='{'))
                return 0;                                   // 类型不匹配
        }
    }
    return top == -1;   // 扫描完必须栈空
}
```

## 4.2 表达式求值（算符优先法）

用**两个栈**：**OPTR**（算符栈）+ **OPND**（操作数栈）。依据**算符优先关系表**决策：

- 栈顶算符 `<` 当前算符 → 当前算符**入栈**
- 栈顶算符 `>` 当前算符 → **弹出栈顶算符**，从 OPND 取两数运算，结果压回
- 栈顶算符 `=` 当前算符 → **脱括号**（`(` 与 `)` 抵消）或结束

**算符间优先关系表**（θ1 = 栈顶算符，θ2 = 读入算符）：

| θ1＼θ2 | + | − | * | / | ( | ) | # |
|:---:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|
| **+** | > | > | < | < | < | > | > |
| **−** | > | > | < | < | < | > | > |
| **\*** | > | > | > | > | < | > | > |
| **/** | > | > | > | > | < | > | > |
| **(** | < | < | < | < | < | = | · |
| **)** | > | > | > | > | · | > | > |
| **#** | < | < | < | < | < | · | = |

**计算方法**（以 `3*(7-2)#` 为例，跟踪两个栈）：

| 读入 | 关系 | 动作 | OPTR | OPND |
|:--:|:--:|:--|:--|:--|
| `3` | — | 操作数入栈 | `#` | `3` |
| `*` | `#`<`*` | 算符入栈 | `# *` | `3` |
| `(` | `*`<`(` | 算符入栈 | `# * (` | `3` |
| `7` | — | 操作数入栈 | `# * (` | `3 7` |
| `-` | `(`<`-` | 算符入栈 | `# * ( -` | `3 7` |
| `2` | — | 操作数入栈 | `# * ( -` | `3 7 2` |
| `)` | `-`>`)` | 弹 `-` 算 $7-2=5$ | `# * (` | `3 5` |
| `)` | `(`=`)` | 脱括号弹 `(` | `# *` | `3 5` |
| `#` | `*`>`#` | 弹 `*` 算 $3\times5=15$ | `#` | `15` |
| `#` | `#`=`#` | 结束 | `#` | `15` |

结果 = OPND 栈顶 = **15**。

**代码示例**（核心决策循环，完整可运行版见配套 `07_expr_eval.c`）：

```c
while(c != '#' || topOptr() != '#'){
    if(isdigit(c)){ /* 解析多位数, 压入 OPND */ }
    else switch(precede(topOptr(), c)){       // 比较栈顶算符与当前算符
        case '<': pushOptr(c); c = next();     break;  // 当前算符入栈
        case '>': {                                    // 弹栈顶算符做一次运算
            char op = popOptr();
            double b = popOpnd(), a = popOpnd();
            pushOpnd(operate(a, op, b));       break;
        }
        case '=': popOptr(); c = next();       break;  // 脱括号
    }
}
// 结果 = popOpnd();
```

## 4.3 中缀表达式转后缀（逆波兰式 RPN）

后缀表达式（如 `3 4 5 - *` 表示 $3\times(4-5)$）**无需括号**，便于计算机求值。

**转换规则**（一个算符栈）：
- 操作数：直接输出
- 算符：弹出栈中**优先级 ≥ 当前算符**的算符并输出，再把当前算符入栈
- 左括号 `(`：直接入栈；右括号 `)`：弹栈输出直到遇 `(`
- 扫描完毕：弹出栈中所有剩余算符

**计算方法**（以 `a+b*c-d` 为例）：

| 读入 | 动作 | 输出 | 算符栈 |
|:--:|:--|:--|:--|
| `a` | 操作数输出 | `a` | |
| `+` | 入栈 | `a` | `+` |
| `b` | 操作数输出 | `ab` | `+` |
| `*` | `*` 优先级高于栈顶 `+`，入栈 | `ab` | `+ *` |
| `c` | 操作数输出 | `abc` | `+ *` |
| `-` | 弹出 `*`、`+`（≥`-`）再入栈 | `abc*+` | `-` |
| `d` | 操作数输出 | `abc*+d` | `-` |
| 结束 | 弹出剩余算符 | `abc*+d-` | |

结果后缀式：**`abc*+d-`**。

**代码示例：**

```c
int prec(char op){ if(op=='+'||op=='-') return 1; if(op=='*'||op=='/') return 2; return 0; }

void infixToPostfix(const char *s, char *out){
    char st[100]; int top=-1, k=0;
    for(int i=0; s[i]; i++){
        char c = s[i];
        if(isalnum(c)) out[k++] = c;                        // 操作数直接输出
        else if(c=='(') st[++top] = c;                       // 左括号入栈
        else if(c==')'){                                     // 右括号：弹到左括号
            while(top>=0 && st[top]!='(') out[k++] = st[top--];
            if(top>=0) top--;                                // 弹掉 '('
        } else {                                             // 算符
            while(top>=0 && st[top]!='(' && prec(st[top])>=prec(c))
                out[k++] = st[top--];
            st[++top] = c;
        }
    }
    while(top>=0) out[k++] = st[top--];                       // 弹出剩余算符
    out[k] = '\0';
}
```

## 4.4 后缀表达式求值

只需**一个操作数栈**：遇操作数入栈；遇算符则弹出两个操作数运算，结果压回。扫描完毕栈顶即结果。

> ⭐ 注意运算顺序：先弹出的是**右操作数**，后弹出的是**左操作数**（`a op b` 中 b 先出）。

**计算方法**（以 `345*+` 为例，即中缀 $3+4\times5$）：

| 读入 | 动作 | 操作数栈 |
|:--:|:--|:--|
| `3` | 入栈 | `3` |
| `4` | 入栈 | `3 4` |
| `5` | 入栈 | `3 4 5` |
| `*` | 弹 5、4 → $4\times5=20$，压回 | `3 20` |
| `+` | 弹 20、3 → $3+20=23$，压回 | `23` |

结果 = **23**。

**代码示例：**

```c
double evalPostfix(const char *s){
    double st[100]; int top = -1;
    for(int i = 0; s[i]; i++){
        char c = s[i];
        if(isdigit(c)) st[++top] = c - '0';                 // 操作数入栈
        else {                                              // 算符
            double b = st[top--], a = st[top--];            // 先弹右 b，后弹左 a
            double r = c=='+'?a+b : c=='-'?a-b : c=='*'?a*b : a/b;
            st[++top] = r;
        }
    }
    return st[top];
}
```

## 4.5 数制转换

十进制数 $N$ 转 $r$ 进制：反复 $N \bmod r$ 得到的余数**逆序**排列即为结果。因"先得低位、后得高位"，恰好用栈**逆序输出**。

$$N = \left\lfloor N/r \right\rfloor \times r + (N \bmod r)$$

**计算方法**（以 $1348$ 转八进制为例）：

| 步骤 | $N$ | 余数 $N \bmod 8$（入栈） | $\lfloor N/8 \rfloor$ |
|:--:|:--:|:--:|:--:|
| 1 | 1348 | **4** | 168 |
| 2 | 168 | **0** | 21 |
| 3 | 21 | **5** | 2 |
| 4 | 2 | **2** | 0 |

余数出栈（逆序）：$2\,5\,0\,4$，即 $(1348)_{10} = (2504)_8$。

**代码示例：**

```c
void convert(int N, int r){
    int st[100], top = -1;
    if(N == 0){ printf("0"); return; }
    while(N > 0){ st[++top] = N % r; N /= r; }      // 余数依次入栈（先得低位）
    while(top >= 0){                                 // 出栈即逆序 → 高位在前
        int d = st[top--];
        printf("%c", d < 10 ? '0'+d : 'A'+d-10);     // d≥10 用 A~F（支持十六进制）
    }
}
```

## 4.6 迷宫求解与行编辑

- **迷宫求解**：用栈保存路径，走不通则**回溯**（出栈）换方向——栈天然支持回溯。
- **行编辑程序**：用栈缓存输入，退格符 `#` 出栈一个字符，退行符 `@` 清空栈。

---

# §5 栈与递归 {#sec5}

> ⭐ **递归的本质就是栈**：系统用一个"运行时栈"保存每层调用的**返回地址、参数、局部变量**（称为**活动记录 / 栈帧**）。

- 每次递归调用 → **压入**一个栈帧；每次返回 → **弹出**栈帧。
- 递归深度过大会导致**栈溢出（stack overflow）**。
- **任何递归都可用栈改写为非递归**（人工模拟系统栈）。

> 💡 典型例子：Hanoi 塔、斐波那契、二叉树遍历，都可用栈把递归改成迭代。

---

# §6 出栈序列问题（重点考点） {#sec6}

> 🔥 几乎每次考试都会出：给定入栈顺序，问某序列是否为合法出栈序列 / 合法序列有多少种。

## 6.1 合法出栈序列的数量

对 $n$ 个元素按 $1,2,\dots,n$ 顺序入栈，可能的合法出栈序列总数为**卡特兰数（Catalan number）**：

$$C_n = \frac{1}{n+1}\binom{2n}{n} = \frac{(2n)!}{(n+1)!\,n!}$$

例如 $n=3$ 时：

$$C_3 = \frac{1}{4}\binom{6}{3} = \frac{1}{4}\times 20 = 5$$

即 3 个元素共有 5 种合法出栈序列，唯一**非法**的是 $312$。

## 6.2 合法性判定

**方法一（可靠）：模拟。** 维护一个栈，按目标出栈序列逐个匹配：能弹则弹，否则从入栈序列继续压栈，最终栈空即合法。

**方法二（速判技巧，"312 模式"）：** 对入栈序列 $1,2,\dots,n$，出栈序列**非法** $\iff$ 存在三个位置 $i<j<k$ 使得

$$\text{seq}[j] < \text{seq}[k] < \text{seq}[i]$$

即出现"**大、小、中**"的相对次序（模式 $312$）。不含该模式即合法。

> ⚠️ 易错：入栈过程中**允许穿插出栈**，并非全部入栈后才出栈。判断时切勿假设"先全进后全出"。

---

# §7 易错点与速查卡片 {#sec7}

## 7.1 常见易错点

| 易错点 | 正确做法 |
|--------|----------|
| 判空/判满 | 与 `top` 约定强相关，先看清约定 |
| 入栈出栈顺序 | 入栈先判满，出栈先判空 |
| 共享栈栈满 | `top1 + 1 == top2` |
| 后缀求值取数顺序 | 先弹出的是右操作数 |
| 出栈序列 | 入栈中可穿插出栈，用模拟或 312 模式判定 |

## 7.2 核心速查

- **特性**：后进先出（LIFO）
- **两种实现**：顺序栈（判满 `top==MAXSIZE-1`）、链栈（栈顶在链头，不判满）
- **所有基本操作复杂度**：$O(1)$
- **共享栈满**：$top_1 + 1 = top_2$
- **合法出栈序列数**：$C_n = \dfrac{1}{n+1}\binom{2n}{n}$
- **三大应用**：括号匹配、表达式求值（两栈 + 优先表）、中缀转后缀
- **递归 ⟺ 栈**：系统栈保存栈帧，递归可用栈改写为迭代

---

—— 祝复习顺利，考试加油！ ——
