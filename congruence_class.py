#!/usr/bin/env python3
"""生成相合等价类详解"""

content = r"""# 相合等价类——合同关系下的分类

> **相合**（合同）是二次型在基变换下的矩阵关系。相合等价类就是"在合同关系下被归为同一类"的矩阵集合。

\newpage

# 一、先搞清三个"等价"的区别

线性代数中有三个容易混淆的等价关系：

| 关系 | 定义 | 不变量 | 几何含义 |
|------|------|--------|---------|
| **相抵**（等价） | $B = PAQ$，$P,Q$ 可逆 | 秩 | 同一个线性映射在不同基下 |
| **相似** | $B = P^{-1}AP$ | 特征值、行列式、迹、秩… | 同一个线性变换在不同基下 |
| **相合**（合同） | $B = P^{T}AP$，$P$ 可逆 | **正负惯性指数** | 同一个二次型在不同基下 |

> 相合 $\neq$ 相似 $\neq$ 相抵。三者是不同的分类标准！

\newpage

# 二、相合的定义

$$\boxed{A \text{ 与 } B \text{ 相合 } \;\Longleftrightarrow\; \exists \text{ 可逆矩阵 } P,\; B = P^{T}AP}$$

$A$ 和 $B$ 都是**实对称矩阵**（二次型矩阵必须对称）。

## 2.1 合同与二次型

二次型 $f = \boldsymbol{x}^T A\boldsymbol{x}$，做基变换 $\boldsymbol{x} = P\boldsymbol{y}$：

$$f = (P\boldsymbol{y})^T A (P\boldsymbol{y}) = \boldsymbol{y}^T (P^{T}AP) \boldsymbol{y} = \boldsymbol{y}^T B \boldsymbol{y}$$

**同一个二次型，在不同基下的矩阵是合同的。**

## 2.2 相合与相似什么时候统一？

当 $P$ 是**正交矩阵**时（$P^{-1} = P^T$），$P^{T}AP = P^{-1}AP$，此时相合 = 相似。这就是正交变换化二次型的原理。

\newpage

# 三、相合等价类——按惯性指数分类

## 3.1 惯性定理（Sylvester）

任何实对称矩阵 $A$ 都可以通过合同变换化为**唯一的**对角规范形：

$$\boxed{A \;\xrightarrow{\text{合同}}\; \begin{pmatrix} I_p & 0 & 0 \\ 0 & -I_q & 0 \\ 0 & 0 & 0 \end{pmatrix}}$$

其中：
- $p$ = **正惯性指数**（正特征值的个数）
- $q$ = **负惯性指数**（负特征值的个数）
- $n-p-q$ = 零特征值的个数

$$\boxed{\text{相合等价类} \;=\; \text{由 }(p,q)\text{ 唯一确定的矩阵集合}}$$

## 3.2 两个矩阵合同 ⇔ 正负惯性指数相同

$$\boxed{A \text{ 与 } B \text{ 相合 } \;\Longleftrightarrow\; p_A = p_B \text{ 且 } q_A = q_B}$$

**不需要特征值完全相同！只要正、负、零的个数分别相同即可。**

## 3.3 实例

$$A = \begin{pmatrix} 1 & 0 \\ 0 & -1 \end{pmatrix},\quad B = \begin{pmatrix} 2 & 0 \\ 0 & -3 \end{pmatrix},\quad C = \begin{pmatrix} 5 & 0 \\ 0 & -1 \end{pmatrix}$$

- $A$：正惯性指数 $p=1$，负惯性指数 $q=1$ → $(p,q)=(1,1)$
- $B$：$p=1$，$q=1$ → $(1,1)$
- $C$：$p=1$，$q=1$ → $(1,1)$

$A,B,C$ 的**特征值完全不同**，但它们都在**同一个相合等价类**中——因为它们都可以合同于 $\text{diag}(1,-1)$。

> **相似**看特征值的具体数值；**相合**只看特征值的正负号个数。

\newpage

# 四、相合等价类的数量

对于 $n$ 阶实对称矩阵，惯性指数 $(p,q)$ 满足：

$$0 \leq p \leq n,\quad 0 \leq q \leq n,\quad p+q \leq n$$

可能的 $(p,q)$ 组合数 = 相合等价类的总数。

| $n$ | 可能的 $(p,q)$ | 等价类数量 |
|-----|---------------|----------|
| 1 | (0,0), (1,0), (0,1) | 3 |
| 2 | (0,0), (1,0), (0,1), (2,0), (1,1), (0,2) | 6 |
| 3 | … | 10 |

$$\text{一般公式：} \frac{(n+1)(n+2)}{2}$$

\newpage

# 五、正定与相合

## 5.1 正定的等价表述

以下每条都等价于"$A$ 正定"：

| 条件 | 表述 |
|------|------|
| 相合于 $I$ | $A$ 与单位矩阵 $I$ 相合（$A = P^T P$） |
| 惯性指数 | $p=n$，$q=0$（全是正的，没有负的和零） |
| 特征值 | 所有特征值 $>0$ |
| 顺序主子式 | 所有顺序主子式 $>0$ |

## 5.2 正定矩阵构成一个相合等价类

所有正定矩阵都在**同一个**相合等价类中——都以 $I_n$ 为合同规范形，惯性指数都是 $(n,0)$。

\newpage

# 六、三个"等价类"的对比总结

| | 相抵等价类 | 相似等价类 | 相合等价类 |
|--|----------|----------|----------|
| 关系 | $B = PAQ$ | $B = P^{-1}AP$ | $B = P^TAP$ |
| 适用对象 | 任意 $m\times n$ 矩阵 | 方阵 | **实对称矩阵** |
| 分类依据 | **秩 $r$** | **若尔当标准形** | **正负惯性指数 $(p,q)$** |
| 等价类数量（$n$ 阶） | $n+1$ | 无穷多 | $(n+1)(n+2)/2$ |
| 规范形 | $\begin{pmatrix} I_r & 0 \\ 0 & 0 \end{pmatrix}$ | 若尔当标准形 | $\begin{pmatrix} I_p & & \\ & -I_q & \\ & & 0 \end{pmatrix}$ |

\newpage

# 七、典型考题

## 考法1：判断两个矩阵是否合同

**方法**：求特征值 → 数正负零的个数 → 比较 $(p,q)$。

## 考法2：求合同规范形

**方法**：配方法或求特征值（注意：合同变换不保持特征值，但惯性指数不变）。

## 考法3：已知惯性指数求参数

**方法**：由正负惯性指数的条件反推参数范围。

## 考法4：判断题——相似 vs 合同

- 两个矩阵相似 → 特征值相同 → $p,q$ 相同 → **一定合同**
- 两个矩阵合同 → $p,q$ 相同 → 特征值不一定相同 → **不一定相似**

> **相似 $\Rightarrow$ 合同，但合同 $\not\Rightarrow$ 相似。**

\newpage

# 八、一句话总结

> **相合等价类 = 由正负惯性指数 $(p,q)$ 分类的实对称矩阵集合。** 同一类中的所有矩阵可以通过 $P^TAP$ 互相转化（代表同一个二次型在不同基下）。$n$ 阶矩阵共有 $(n+1)(n+2)/2$ 个相合等价类。判断是否合同只需看正负特征值的**个数**，不需看具体数值。
"""

with open(r"d:\辰辰\first CC\congruence_class.md", "w", encoding="utf-8") as f:
    f.write(content)

print("Markdown 已生成")
