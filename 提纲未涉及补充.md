# 数据结构复习补充 —— 提纲未涉及内容

> 杨思辰 · PB25992094 · 2026-07-29

---

# 一、栈的应用

## 1.1 括号匹配

### 算法思想

遇到**左括号**入栈，遇到**右括号**与栈顶匹配。匹配成功则出栈，否则报错。最后栈空才算全部匹配。

### 手算模拟

**例题**：判断 `{[()]}` 是否匹配。

| 步骤 | 当前字符 | 栈（底→顶） | 操作 |
|:--|:--|:--|:--|
| 1 | `{` | `{` | 左括号入栈 |
| 2 | `[` | `{[` | 左括号入栈 |
| 3 | `(` | `{[(]` | 左括号入栈 |
| 4 | `)` | `{[` | `)` 与栈顶 `(` 匹配，`(` 出栈 |
| 5 | `]` | `{` | `]` 与栈顶 `[` 匹配，`[` 出栈 |
| 6 | `}` | 空 | `}` 与栈顶 `{` 匹配，`{` 出栈 |
| 结果 | — | 空 | ✅ 匹配成功 |

**例题**：判断 `[(])` 是否匹配。

| 步骤 | 当前字符 | 栈 | 操作 |
|:--|:--|:--|:--|
| 1 | `[` | `[` | 入栈 |
| 2 | `(` | `[(]` | 入栈 |
| 3 | `]` | — | `]` 与栈顶 `(` 不匹配 → ❌ **失败** |

**核心结论**：括号匹配 = 左括号入栈 + 右括号与栈顶匹配。时间复杂度 O(n)。

---

## 1.2 表达式求值（中缀 → 后缀 → 求值）

### 1.2.1 中缀转后缀（"运算符栈"法）

**规则**：
- 操作数直接输出
- 左括号 `(` 直接入栈
- 右括号 `)` 弹栈直到遇到 `(`
- 运算符：栈顶优先级 **≥** 当前优先级时弹栈输出，然后将当前入栈
- 最后栈中剩余运算符全部弹栈输出

**优先级表**：`^`(幂) > `* /` > `+ -` > `( )`

**完整手算例题**：中缀 `a+b*c+(d*e+f)*g` → 后缀

| 步骤 | 当前 | 运算符栈（底→顶） | 输出（后缀） |
|:--|:--|:--|:--|
| 1 | `a` | — | `a` |
| 2 | `+` | `+` | `a` |
| 3 | `b` | `+` | `ab` |
| 4 | `*` | `+*` | `ab` |
| 5 | `c` | `+*` | `abc` |
| 6 | `+` | `+` → `+` 入栈 | `abc*+` |
| 7 | `(` | `+(` | `abc*+` |
| 8 | `d` | `+(` | `abc*+d` |
| 9 | `*` | `+(*` | `abc*+d` |
| 10 | `e` | `+(*` | `abc*+de` |
| 11 | `+` | `+(+` | `abc*+de*` |
| 12 | `f` | `+(+` | `abc*+de*f` |
| 13 | `)` | `+` | `abc*+de*f+` |
| 14 | `*` | `+*` | `abc*+de*f+` |
| 15 | `g` | `+*` | `abc*+de*f+g` |
| 结束 | — | 弹空 | **`abc*+de*f+g*+`** |

验证：中缀 `a+b*c+(d*e+f)*g` → 后缀 `abc*+de*f+g*+`

### 1.2.2 后缀表达式求值（"操作数栈"法）

**规则**：遇操作数入栈，遇运算符弹出两个操作数 → 计算 → 结果入栈。

**例题**：求 `6 2 3 + - 3 8 2 / + * 2 ^ 3 +` 的值。

| 步骤 | 当前 | 操作数栈 | 说明 |
|:--|:--|:--|:--|
| 1 | 6 | `[6]` | |
| 2 | 2 | `[6,2]` | |
| 3 | 3 | `[6,2,3]` | |
| 4 | `+` | `[6,5]` | 2+3=5 |
| 5 | `-` | `[1]` | 6-5=1 |
| 6 | 3 | `[1,3]` | |
| 7 | 8 | `[1,3,8]` | |
| 8 | 2 | `[1,3,8,2]` | |
| 9 | `/` | `[1,3,4]` | 8/2=4 |
| 10 | `+` | `[1,7]` | 3+4=7 |
| 11 | `*` | `[7]` | 1×7=7 |
| 12 | 2 | `[7,2]` | |
| 13 | `^` | `[49]` | 7²=49 |
| 14 | 3 | `[49,3]` | |
| 15 | `+` | `[52]` | 49+3=52 |

答案：**52**

---

## 1.3 递归调用机制

### 核心理解

递归函数的每次调用都会在**系统调用栈**上创建一个栈帧，包含：
- 局部变量
- 参数
- 返回地址

**递归 = 系统帮你维护了一个隐式栈**

### 递归转非递归

任何递归都可以用**显式栈**改写为非递归。例如二叉树中序遍历：

```c
/* 递归版（系统栈） */
void inorder(TreeNode *t) {
    if (t) { inorder(t->left); visit(t); inorder(t->right); }
}

/* 非递归版（显式栈） */
void inorder_nr(TreeNode *t) {
    TreeNode *stack[MAX]; int top = -1;
    while (t || top >= 0) {
        while (t) { stack[++top] = t; t = t->left; }  /* 一路左下 */
        t = stack[top--]; visit(t);                     /* 出栈访问 */
        t = t->right;                                   /* 转右子树 */
    }
}
```

### 递归的优缺点

| 优点 | 缺点 |
|:--|:--|
| 代码简洁、逻辑清晰 | 函数调用开销大 |
| 天然适合分治和树结构 | 可能导致栈溢出（递归过深） |
| | 重复计算（斐波那契典型） |

---

# 二、数组存储与矩阵压缩

## 2.1 数组的存储

按**行优先**或**列优先**存放。C 语言用行优先，Fortran 用列优先。

**行优先**：LOC(aᵢⱼ) = LOC(a₀₀) + (i×n + j)×L（n 为列数，L 为元素大小）

**例题**：5×6 矩阵，LOC(a₀₀)=100，每个元素占 4 字节，求 LOC(a₃₄)（行优先）。

LOC(a₃₄) = 100 + (3×6 + 4)×4 = 100 + 22×4 = 100 + 88 = **188**

## 2.2 特殊矩阵的压缩存储

### 对称矩阵

只存**下三角**（含对角线），共 n(n+1)/2 个元素。行优先存储：

aᵢⱼ（i≥j，下三角）在压缩数组中的下标：**k = i(i-1)/2 + j-1**（1-based）

**例题**：5阶对称矩阵，求 a₄₃ 在压缩存储中的下标（1-based）。

k = 4×3/2 + 3-1 = 6 + 2 = **8**（即第 8 个位置）

### 三角矩阵

上三角：下三角元素 + 上三角常量
下三角：上三角元素 + 下三角常量

存储空间：n(n+1)/2 + 1

### 对角矩阵（三对角）

只有三条对角线有非零元素。3n-2 个元素，按行优先压缩存储。

aᵢⱼ（|i-j|≤1）下标：**k = 2i + j - 3**（1-based，i 从 0 开始则 k = 3i + (j-i) + C）

### 稀疏矩阵

非零元素远少于零元素。常用存储方式：

| 方法 | 思路 |
|:--|:--|
| 三元组表 | `(row, col, value)` 按行优先排列 |
| 十字链表 | 每行每列各一个链表，结点含 row/col/value + 行指针 + 列指针 |

---

## 2.3 概念辨识（易混淆概念对）

| 概念 A | 概念 B | 区别 |
|:--|:--|:--|
| **逻辑结构** | **存储结构** | 逻辑=数据元素之间的关系（线性/树/图）；存储=在内存中的存放方式（顺序/链式） |
| **时间复杂度** | **空间复杂度** | 时间=执行次数随 n 的增长；空间=额外内存随 n 的增长 |
| **O(1)** | **O(n)** | O(1)=常数时间，与 n 无关；O(n)=线性增长 |
| **头指针** | **头结点** | 头指针=指向第一个结点的指针（必须）；头结点=在第一个数据结点前加的哨兵结点（可选） |
| **循环队列空** | **循环队列满** | 空:`front==rear`；满:`(rear+1)%MAX==front`（牺牲一个单元） |
| **栈** | **队列** | 栈=LIFO（后进先出）；队列=FIFO（先进先出） |
| **串** | **数组** | 串=字符的线性表，有专门的串操作；数组=同类型元素的集合 |
| **子树** | **子二叉树** | 子树可以只是一部分，二叉树的操作不要求对称 |
| **完全二叉树** | **满二叉树** | 满=每层全满（2ʰ-1个结点）；完全=只有最后一层可能不满，且结点靠左 |
| **二叉排序树** | **平衡二叉树(AVL)** | BST=只看大小关系；AVL=BST + 平衡因子∈{-1,0,1} |
| **哈夫曼树** | **哈夫曼编码** | 树=带权路径最短的二叉树；编码=基于哈夫曼树的变长编码 |
| **连通图** | **强连通图** | 连通=无向图任意两点可达；强连通=**有向图**任意两点双向可达 |
| **生成树** | **生成森林** | 生成树=连通图的极小连通子图（n-1条边）；生成森林=非连通图的每个连通分量各一棵生成树 |
| **DAG** | **AOE网** | DAG=有向无环图（拓扑排序成立）；AOE=边有工期，用于关键路径 |
| **Prim** | **Kruskal** | Prim=加点法 O(n²) 适合稠密；Kruskal=加边法 O(e log e) 适合稀疏 |
| **Dijkstra** | **Floyd** | Dijkstra=单源最短路径 O(n²)；Floyd=所有点对最短路径 O(n³) |
| **深度优先遍历** | **广度优先遍历** | DFS=栈/递归，一条路走到底；BFS=队列，一层一层扩散 |
| **顺序查找** | **二分查找** | 顺序=无需有序 O(n)；二分=必须有序 O(log n) 仅适用于顺序存储 |
| **开放定址法** | **链地址法** | 开放定址=冲突找空位；链地址=冲突挂链表 |
| **稳定排序** | **不稳定排序** | 稳定=相同元素相对位置不变（插、冒、归、基）；不稳定=可能改变（希、快、选、堆） |
| **内排序** | **外排序** | 内=全部在内存完成；外=涉及外部存储器的读/写 |

---

# 三、二叉树遍历与重建

## 3.1 三种遍历方式（手工模拟）

以二叉树为例：

```
        A
      /   \
     B     C
    / \   / \
   D   E F   G
```

| 遍历方式 | 顺序 | 结果 |
|:--|:--|:--|
| **先序**（根-左-右） | A→B→D→E→C→F→G | ABDECFG |
| **中序**（左-根-右） | D→B→E→A→F→C→G | DBEAFGC |
| **后序**（左-右-根） | D→E→B→F→G→C→A | DEBFGCA |

**手算技巧**：
- 先序：看到结点先记下来，再走左边，左边完了走右边
- 中序：一路往左走到底，退回来时记录，再走右边
- 后序：从左子树回来先不记，从右子树回来再记

## 3.2 由遍历序列重建二叉树（必考！）

### 核心定理

**先序 + 中序 → 唯一二叉树**
**后序 + 中序 → 唯一二叉树**
先序 + 后序 → **不唯一**（无法区分左右子树中只有一个结点的情况）

### 手工重建步骤（先序+中序）

**例题**：先序 `ABDGHCEIF`，中序 `GDHBAEICF`，重建二叉树。

**步骤**：先序第一个一定是根。

```
先序: [A]BDGHCEIF     根=A
中序: GDHB [A] EICF     ← A把中序分成左(GDHB)和右(EICF)

左子树: 先序 B D G H, 中序 G D H B
  根=B, 左=GDH, 右=(空)
  先序: [B]DGH  中序: GDH [B] (空)

  左子树的左子树: 先序 D G H, 中序 G D H
    根=D, 左=G, 右=H
    先序: [D]GH  中序: G [D] H
    → D 的左孩子=G, 右孩子=H

右子树: 先序 C E I F, 中序 E I C F
  根=C, 左=EI, 右=F
  先序: [C]EIF  中序: EI [C] F

  右子树的左子树: 先序 E I, 中序 E I
    根=E, 左=空, 右=I
    → E 的右孩子=I
```

重建结果：

```
         A
       /   \
      B     C
     /     / \
    D     E   F
   / \     \
  G   H     I
```

验证：先序=ABDGHCEIF ✓  中序=GDHBAEICF ✓

### 后序+中序重建

**步骤**：后序**最后一个**一定是根，其余逻辑同上。

---

# 四、树与森林

## 4.1 树的存储结构

| 方法 | 思路 |
|:--|:--|
| 双亲表示法 | 每个结点只存父结点下标（数组） |
| 孩子表示法 | 每个结点存所有孩子的链表 |
| **孩子兄弟表示法** | 每个结点存**第一个孩子**和**下一个兄弟**（= 二叉链表） |

## 4.2 树 ↔ 二叉树（左孩子右兄弟）

**树 → 二叉树**：
1. 所有兄弟之间加一条线
2. 每个结点只保留与第一个孩子的连线，删掉与其他孩子的连线
3. 顺时针旋转 45°

**二叉树 → 树**（逆过程）：
1. 每个结点的左孩子变为自己的第一个孩子
2. 每个结点的右孩子变为自己的下一个兄弟

**遍历对应关系**：

| 树的遍历 | = 对应二叉树的哪种遍历 |
|:--|:--|
| 树先根 | 二叉树**先序** |
| 树后根 | 二叉树**中序** |

## 4.3 森林 ↔ 二叉树

**森林 → 二叉树**：
1. 每棵树分别转为二叉树
2. 第一棵不动，后面每棵挂在前一棵根的右子树上

**二叉树 → 森林**（逆过程）：沿根的右链断开，每段转回树。

**遍历对应**：

| 森林遍历 | = 对应二叉树的哪种遍历 |
|:--|:--|
| 森林先序 | 二叉树**先序** |
| 森林中序 | 二叉树**中序** |

---

# 五、哈夫曼树与哈夫曼编码

## 5.1 哈夫曼树的构造

**算法**：每次取权值最小的两个结点合并，产生新的内部结点（权值 = 两者之和），重复直到只剩一棵树。

**例题**：权值集合 W = {5, 6, 7, 8, 15}，构造哈夫曼树。

```
初始: {5, 6, 7, 8, 15}   (5个独立结点)

第1步: 取最小两个 5 和 6，合并 → 新结点权值=11
       {7, 8, 11, 15}

第2步: 取最小两个 7 和 8，合并 → 新结点权值=15
       {11, 15, 15}

第3步: 取最小两个 11 和 15，合并 → 新结点权值=26
       {15, 26}

第4步: 合并 15 和 26 → 新结点权值=41
       {41}

完成！哈夫曼树:

             41
           /    \
         15      26
                /  \
              11    15
             / \   / \
            5   6 7   8
```

## 5.2 WPL（带权路径长度）

**WPL = Σ(叶结点权值 × 该叶结点到根的路径长度)**

路径长度 = 经过的边数（或父内部结点数）。

上例：
- 5: 路径长度=3, 贡献 5×3=15
- 6: 路径长度=3, 贡献 6×3=18
- 7: 路径长度=3, 贡献 7×3=21
- 8: 路径长度=3, 贡献 8×3=24
- 15: 路径长度=1, 贡献 15×1=15

**WPL = 15+18+21+24+15 = 93**

另一种算法：**WPL = 所有内部结点的权值之和** = 11+15+26+41 = 93 ✓

## 5.3 哈夫曼编码

**规则**：从哈夫曼树根出发，走**左**分支 = **0**，走**右**分支 = **1**。

上例的编码：
| 字符（权值） | 编码 |
|:--|:--|
| 15 | `0` |
| 5 | `100` |
| 6 | `101` |
| 7 | `110` |
| 8 | `111` |

**特点**：
- 哈夫曼编码是**前缀编码**（没有一个编码是另一个编码的前缀）
- 权值越大，编码越短 → 总编码长度最短

## 5.4 压缩率计算

**例题**：5 个字符频率分别为 {15,5,6,7,8}（总数 41）。如需传输 1000 个字符，比较等长编码与哈夫曼编码的总码长。

| 编码方式 | 每字符码长 | 总码长 |
|:--|:--|:--|
| 等长编码（3位） | log₂5≈3 位 | 1000×3 = 3000 位 |
| 哈夫曼编码 | WPL/总频率=93/41≈2.27 位 | 实际按频率加权：15×1000×(1/41)+... |

等长：每个字符需 ⌈log₂5⌉ = 3 位 → 总长 = 41×3 = 123（41个字符）
哈夫曼：总长 = WPL = 93
**压缩率 = (123-93)/123 ≈ 24.4%**

---

# 六、二叉排序树（BST）手工模拟

## 6.1 给定插入序列，画构建过程

**例题**：依次插入 {50, 30, 80, 20, 40, 70, 90, 35}，画 BST 构建过程。

```
插入50:     50

插入30:     50
           /
          30

插入80:     50
          /  \
         30   80

插入20:     50
          /  \
         30   80
        /
       20

插入40:     50
          /  \
         30   80
        / \
       20  40

插入70:     50
          /  \
         30   80
        / \   /
       20 40 70

插入90:     50
          /  \
         30   80
        / \   / \
       20 40 70  90

插入35:     50
          /  \
         30   80
        / \   / \
       20 40 70  90
         /
        35
```

## 6.2 BST 的查找

和二分查找类似：比根小走左边，比根大走右边。查找失败时插入该位置。

## 6.3 BST 删除的三种情况

**例题**：在上面的 BST 中删除 30（双支结点）。

| 情况 | 描述 | 操作 |
|:--|:--|:--|
| **叶结点** | 如删除 35 | 直接删，父结点指针置 NULL |
| **单支结点** | 如删除 20（删除20时） | 用唯一孩子（35）替代 |
| **双支结点** ⭐ | 如删除 30 | 取**左子树最大值**（40）或**右子树最小值**（70）替代 |

**删除 30（取左子树最大值 40 替代）**：

```
删除前:   50                    删除后:   50
         /  \                           /  \
        30   80                        40   80
       / \   / \                      / \   / \
      20 40 70 90                    20 35 70 90
        /                                  ↑
       35                             (35 挂到原40之父的右孩子)
```

**删除 30（取右子树最小值替代）**：右子树最小值是 ? 30 的右子树只有一个 40，所以 40 就是右子树的最小值。同理。

---

# 七、图的概念辨析

## 7.1 基本概念

| 概念 | 定义 | 示例 |
|:--|:--|:--|
| **顶点 (Vertex)** | 图中的结点 | V={v₀,v₁,...,vₙ₋₁} |
| **边 (Edge)** | 顶点之间的关系 | (vᵢ,vⱼ) |
| **有向图** | 边有方向，`<u,v>` ≠ `<v,u>` | 单行道 |
| **无向图** | 边无方向，`(u,v)` = `(v,u)` | 双向道路 |
| **度 (Degree)** | 无向图中，顶点的边数 | deg(v)=3 |
| **入度 (In-degree)** | 有向图中，指向 v 的边数 | indeg(v) |
| **出度 (Out-degree)** | 有向图中，从 v 出发的边数 | outdeg(v) |
| **路径** | 一序列顶点，相邻都有边 | v₀-v₁-v₂-v₃ |
| **简单路径** | 路径中顶点不重复 | — |
| **回路/环** | 起点=终点且长度>0的路径 | v₀-v₁-v₂-v₀ |
| **连通图** | 无向图，任意两点间有路径 | — |
| **连通分量** | 无向图的极大连通子图 | — |
| **强连通图** | **有向图**，任意两点间双向都有路径 | — |
| **强连通分量** | 有向图的极大强连通子图 | — |
| **生成树** | 连通图的极小连通子图，包含全部 n 个顶点、n-1 条边 | — |
| **生成森林** | 非连通图的每个连通分量各一棵生成树 | — |
| **网** | 边带权的图 | — |

## 7.2 完全图

| 类型 | 边数 |
|:--|:--|
| 无向完全图 | n(n-1)/2 |
| 有向完全图 | n(n-1) |

---

# 八、最小生成树（MST）

## 8.1 MST 性质（横切定理）

**定义**：设 G=(V,E) 是连通网，U 是 V 的非空真子集。边 (u,v) 满足 u∈U, v∈V-U，则 (u,v) 是**横切边**。

**MST 性质**：所有横切边中**权值最小**的那条，一定属于某个 MST。

**理解**：把已选顶点和未选顶点分成两个阵营，两阵营之间最便宜的边一定可以加入 MST（不会产生坏的后果）。

**贪心安全性**：基于 MST 性质的贪心选择不会导致错过全局最优解。

## 8.2 Prim 算法（加点法）—— 手工模拟步骤

**思想**：设 U={起始顶点}，每次从连接 U 与 V-U 的横切边中选权值最小的，把对面的顶点加入 U。

**例题**：给定下面邻接矩阵，从 v1 开始手工模拟 Prim。

```
        v1  v2  v3  v4  v5  v6
    v1 [ 0,  6,  1,  5, ∞, ∞ ]
    v2 [ 6,  0,  5, ∞,  3, ∞ ]
    v3 [ 1,  5,  0,  5,  6,  4 ]
    v4 [ 5, ∞,  5,  0, ∞,  2 ]
    v5 [ ∞, 3,  6, ∞,  0,  6 ]
    v6 [ ∞, ∞, 4,  2,  6,  0 ]
```

**执行过程**：

```
U={v1}

候选边（U→V-U）:
  v1-v2:6, v1-v3:1, v1-v4:5
  → 最小是 v1-v3=1  → 选 v3，U={v1,v3}

候选边:
  v1-v2:6, v1-v4:5, v3-v2:5, v3-v4:5, v3-v5:6, v3-v6:4
  → 最小是 v3-v6=4  → 选 v6，U={v1,v3,v6}

候选边:
  v1-v2:6, v1-v4:5, v3-v2:5, v3-v4:5, v3-v5:6, v6-v4:2, v6-v5:6
  → 最小是 v6-v4=2  → 选 v4，U={v1,v3,v6,v4}

候选边:
  v1-v2:6, v3-v2:5, v3-v5:6, v6-v5:6
  → 最小是 v3-v2=5  → 选 v2，U={v1,v3,v6,v4,v2}

候选边:
  v3-v5:6, v6-v5:6, v2-v5:3
  → 最小是 v2-v5=3  → 选 v5，U 包含全部 → 完成！
```

**MST 边集**：{v1-v3(1), v3-v6(4), v6-v4(2), v3-v2(5), v2-v5(3)}
**总权值**：1+4+2+5+3 = **15**

## 8.3 Kruskal 算法（加边法）—— 手工模拟步骤

**思想**：所有边按权值从小到大排序，逐条检查：如果加入这条边不构成回路，就选它。

**同例题**：所有边排序（权值+端点）：

```
(1) v1-v3=1, (2) v4-v6=2, (3) v2-v5=3, 
(4) v3-v6=4, (5) v1-v4=5, (6) v2-v3=5, (7) v3-v4=5,
(8) v1-v2=6, (9) v3-v5=6, (10) v5-v6=6
```

**执行过程**（用并查集判环）：

```
(1) v1-v3=1: v1和v3不在同一集合 → 选！ {v1,v3} {...}
(2) v4-v6=2: v4和v6不在同一集合 → 选！ {v1,v3} {v4,v6}
(3) v2-v5=3: v2和v5不在同一集合 → 选！ {v1,v3} {v4,v6} {v2,v5}
(4) v3-v6=4: v3和v6不在同一集合 → 选！ {v1,v3,v4,v6} {v2,v5}
(5) v1-v4=5: v1和v4已经同集合 → 跳过（会成环！）
(6) v2-v3=5: v2和v3不在同一集合 → 选！ 全部连通 → 完成！
```

**MST 边集**：{v1-v3(1), v4-v6(2), v2-v5(3), v3-v6(4), v2-v3(5)}
**总权值**：1+2+3+4+5 = **15**（与 Prim 结果一致 ✓）

---

# 九、线性时间排序

## 9.1 计数排序

**适用条件**：待排序元素是整数，且范围 k 不能太大（k = O(n)）。

**算法思想**：统计每个值出现了几次 → 前缀和确定每个值的最终位置 → 反向填充。

**手算例题**：对 `[2,5,3,0,2,3,0,3]` 计数排序（范围 0~5）。

```
① 统计频次 count[]:
   值:  0  1  2  3  4  5
   次数: 2  0  2  3  0  1

② 前缀和（变成"该值最后一个元素的目标下标+1"）:
   值:  0  1  2  3  4  5
   前缀和: 2  2  4  7  7  8
   含义: 值0的元素放位置0~1, 值2的元素放位置2~3, 值3的元素放位置4~6, 值5的元素放位置7

③ 反向填充（从最后一个元素开始，保证稳定性）:
   原始: [2,5,3,0,2,3,0,3]
   
   从右往左:
   a[7]=3, 前缀和[3]=7 → 放入 output[6], 前缀和[3]--
   a[6]=0, 前缀和[0]=2 → 放入 output[1], 前缀和[0]--
   a[5]=3, 前缀和[3]=6 → 放入 output[5], 前缀和[3]--
   a[4]=2, 前缀和[2]=4 → 放入 output[3], 前缀和[2]--
   a[3]=0, 前缀和[0]=1 → 放入 output[0], 前缀和[0]--
   a[2]=3, 前缀和[3]=5 → 放入 output[4], 前缀和[3]--
   a[1]=5, 前缀和[5]=8 → 放入 output[7], 前缀和[5]--
   a[0]=2, 前缀和[2]=3 → 放入 output[2], 前缀和[2]--

④ 结果: output = [0,0,2,2,3,3,3,5]  ← 排序完成，且稳定
```

**复杂度**：O(n+k)，当 k=O(n) 时是 O(n)。

**性质**：**稳定**、**非原地**（需要额外的计数数组和输出数组）。

## 9.2 基数排序（LSD）

**适用条件**：待排序元素是等长的数字或字符串。

**算法思想**：从最低位（LSD）到最高位（MSD），每趟按当前位做一次计数排序（稳定）。

**手算例题**：对 `[329, 457, 657, 839, 436, 720, 355]` LSD 基数排序。

```
① 按个位分配-收集（计数排序）:
   桶0: 720
   桶5: 355
   桶6: 436
   桶7: 457, 657
   桶9: 329, 839
   收集: [720, 355, 436, 457, 657, 329, 839]

② 按十位分配-收集:
   桶2: 720, 329
   桶3: 436, 839
   桶5: 355, 457, 657
   收集: [720, 329, 436, 839, 355, 457, 657]

③ 按百位分配-收集:
   桶3: 329, 355
   桶4: 436, 457
   桶6: 657
   桶7: 720
   桶8: 839
   收集: [329, 355, 436, 457, 657, 720, 839]  ← 排序完成
```

**复杂度**：O(d(n+r))，d=位数，r=基数（计数排序的取值范围）。

**性质**：**稳定**（每趟都是稳定的计数排序）。

## 9.3 桶排序

**适用条件**：数据均匀分布在一个区间内。

**算法思想**：把区间分成 b 个桶 → 各元素分配到桶中 → 桶内各自排序 → 收集。

**手算例题**：`[0.78, 0.17, 0.39, 0.26, 0.72, 0.94, 0.21, 0.12, 0.23, 0.68]`（[0,1) 均匀分布，10 个桶）。

```
① 分10个桶 [0,0.1), [0.1,0.2), ..., [0.9,1.0)
   桶[1]: 0.17, 0.12
   桶[2]: 0.26, 0.21, 0.23
   桶[3]: 0.39
   桶[6]: 0.68
   桶[7]: 0.78, 0.72
   桶[9]: 0.94
   其余桶为空

② 各桶内插入排序（假设用插入排序）

③ 收集: [0.12,0.17,0.21,0.23,0.26,0.39,0.68,0.72,0.78,0.94]
```

**复杂度**：
- 平均 O(n)（数据均匀分布）
- 最坏 O(n²)（所有数据落到同一个桶，退化为插入排序）

---

## 9.4 三种线性排序对比

| | 计数排序 | 基数排序 | 桶排序 |
|:--|:--|:--|:--|
| 适用 | 整数，范围小 | 整数/等长串 | 均匀分布实数 |
| 复杂度 | O(n+k) | O(d(n+r)) | 平均 O(n), 最坏 O(n²) |
| 稳定性 | 稳定 | 稳定 | 取决于桶内排序 |
| 额外空间 | O(k) | O(n+r) | O(n+b) |
| 关键操作 | 前缀和 | 多趟计数排序 | 分配+桶内排序 |

---

# 附录：快速自查清单

| 要求 | 页码 |
|:--|:--|
| 括号匹配手算 | §1.1 |
| 中缀→后缀手工转换 | §1.2.1 |
| 后缀表达式求值 | §1.2.2 |
| 递归调用机制 | §1.3 |
| 矩阵压缩下标计算 | §2.2 |
| 20 组概念辨析 | §2.3 |
| 三种遍历手工模拟 | §3.1 |
| 由遍历序列重建二叉树 | §3.2 |
| 树/森林与二叉树互转 | §4 |
| 哈夫曼树构造 + WPL 计算 | §5.1-5.2 |
| 哈夫曼编码 + 压缩率 | §5.3-5.4 |
| BST 插入构建 + 删除 | §6 |
| 图基本概念辨析 | §7 |
| MST 性质 | §8.1 |
| Prim 手工模拟 | §8.2 |
| Kruskal 手工模拟 | §8.3 |
| 计数排序手算 | §9.1 |
| 基数排序手算 | §9.2 |
| 桶排序手算 | §9.3 |

---

> **文档版本**: v1.0  
> **完成日期**: 2026-07-29
