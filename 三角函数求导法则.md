# 三角函数求导法则

## 六个基本三角函数的导数

| 函数 | 导数 | 记忆口诀 |
|------|------|----------|
| $\sin x$ | $\cos x$ | sin 导 cos，不变号 |
| $\cos x$ | $-\sin x$ | cos 导 -sin，**要变号** |
| $\tan x$ | $\sec^2 x = \frac{1}{\cos^2 x}$ | tan 导 sec² |
| $\cot x$ | $-\csc^2 x = -\frac{1}{\sin^2 x}$ | cot 导 -csc² |
| $\sec x$ | $\sec x\tan x$ | sec 导 sec·tan |
| $\csc x$ | $-\csc x\cot x$ | csc 导 -csc·cot |

---

## 反三角函数的导数

| 函数 | 导数 | 定义域 |
|------|------|--------|
| $\arcsin x$ | $\dfrac{1}{\sqrt{1-x^2}}$ | $\|x\| < 1$ |
| $\arccos x$ | $-\dfrac{1}{\sqrt{1-x^2}}$ | $\|x\| < 1$ |
| $\arctan x$ | $\dfrac{1}{1+x^2}$ | $\mathbb{R}$ |
| $\text{arccot}\,x$ | $-\dfrac{1}{1+x^2}$ | $\mathbb{R}$ |

---

## 链式法则（复合函数求导）

$$\frac{d}{dx}\sin(u(x)) = \cos(u(x)) \cdot u'(x)$$

| 例子 | 过程 | 结果 |
|------|------|------|
| $\sin(2x)$ | $\cos(2x) \cdot 2$ | $2\cos(2x)$ |
| $\cos(x^2)$ | $-\sin(x^2) \cdot 2x$ | $-2x\sin(x^2)$ |
| $\tan(e^x)$ | $\sec^2(e^x) \cdot e^x$ | $e^x\sec^2(e^x)$ |
| $\arctan(ax)$ | $\frac{1}{1+(ax)^2} \cdot a$ | $\frac{a}{1+a^2x^2}$ |

---

## 数分考试中最常用的几个

### $\arctan$ 的导数（出现频率最高！）

$$\frac{d}{dx}\arctan x = \frac{1}{1+x^2}$$

$$\frac{d}{dx}\arctan(ax) = \frac{a}{1+a^2x^2}$$

> 含参积分中**引入参数 → 对参数求导**时，$\arctan$ 求导变成有理函数是关键技巧。

### $\sin$ 和 $\cos$ 的高阶导数（Fourier 级数中频繁用到）

| 函数 | 一阶导 | 二阶导 | $n$ 阶导 |
|------|--------|--------|-----------|
| $\sin(kx)$ | $k\cos(kx)$ | $-k^2\sin(kx)$ | $k^n\sin(kx + \frac{n\pi}{2})$ |
| $\cos(kx)$ | $-k\sin(kx)$ | $-k^2\cos(kx)$ | $k^n\cos(kx + \frac{n\pi}{2})$ |

### 分部积分中常用的配对

$$\int \sin(kx)dx = -\frac{\cos(kx)}{k} + C$$

$$\int \cos(kx)dx = \frac{\sin(kx)}{k} + C$$

---

## 一句话总结

> **基本三角函数求导背熟，链式法则加 $u'(x)$，$\arctan$ 导数是 $1/(1+x^2)$（考试最高频），分部积分时 $\sin/\cos$ 来回互换。**
