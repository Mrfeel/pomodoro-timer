# 数据结构 · 第5章 树和二叉树 知识点与考点总结

> 《数据结构及其算法》复习资料 | 杨思辰 · 未来技术学院 · 物理学专业 · PB25992094
>
> 🔥 **全课最重要的章节——习题集 24 题，期末考试重中之重！**

---

## 目录

1. [§1 树的基本概念与术语](#sec1)
2. [§2 二叉树定义与性质（必背）](#sec2)
3. [§3 二叉树的存储结构](#sec3)
4. [§4 二叉树的遍历（本章核心）](#sec4)
5. [§5 遍历序列还原二叉树（考试必考）](#sec5)
6. [§6 线索二叉树](#sec6)
7. [§7 树与森林](#sec7)
8. [§8 Huffman 树与 Huffman 编码](#sec8)
9. [§9 易错点与速查卡片](#sec9)

---

# §1 树的基本概念与术语 {#sec1}

## 1.1 树的定义

**树（Tree）** 是 $n$（$n \ge 0$）个结点的**有限集合**。当 $n=0$ 时为空树；非空树满足：

1. 有且仅有一个称为**根（Root）**的结点
2. 其余结点分为 $m$（$m \ge 0$）个**互不相交**的有限集 $T_1, T_2, \dots, T_m$，每个集合本身又是一棵树，称为根的**子树（Subtree）**

> ⚠️ 树的递归定义——每棵子树又是树。**互不相交**意味着没有共享子树（结点）。

## 1.2 基本术语

| 术语 | 含义 | 示例说明 |
|:--|:--|:--|
| **结点** | 树中的基本单元，含数据域 + 指针域 | — |
| **度（Degree）** | 结点拥有的**子树数目** | 叶子的度为 0 |
| **树的度** | 树中所有结点的度的**最大值** | — |
| **叶子/终端结点** | 度为 0 的结点 | — |
| **分支/非终端结点** | 度不为 0 的结点 | — |
| **孩子（Child）** | 结点的子树的根 | — |
| **双亲（Parent）** | 结点的上层结点 | 根无双亲 |
| **兄弟（Sibling）** | 同一双亲的结点之间互称兄弟 | — |
| **堂兄弟** | 双亲在同一层的结点（双亲不同） | — |
| **祖先** | 从根到该结点路径上的所有结点 | — |
| **子孙** | 以该结点为根的子树中的所有结点 | — |
| **层次（Level）** | 根为第 1 层，孩子层数 = 双亲层数 + 1 | — |
| **深度/高度** | 树中结点的**最大层次** | — |
| **有序树** | 子树从左到右**有次序**（不可互换） | 考试默认为有序树 |
| **无序树** | 子树可互换位置 | — |
| **森林** | $m$（$m \ge 0$）棵**互不相交**的树的集合 | 删去树根即得森林 |

### 📌 关键关系

- **结点数 = 所有结点的度之和 + 1**（每个结点有且仅有一个双亲（除根），度 = 本结点的孩子数）
- 或者：**结点数 = 分支总数 + 1**

---

# §2 二叉树定义与性质（必背）{#sec2}

## 2.1 二叉树的定义

**二叉树（Binary Tree）** 是 $n$（$n \ge 0$）个结点的有限集合：
- 空二叉树（$n=0$）
- 或由一个根结点和两棵**互不相交**的、分别称为**左子树**和**右子树**的二叉树构成

> ⚠️ **二叉树不是度为 2 的树！** 二叉树的左右子树严格区分，树的孩子之间无序。二叉树的度可以为 0、1（只有左或只有右）或 2（全有）。含 3 个结点的二叉树有 5 种不同形态。

## 2.2 满二叉树与完全二叉树

### 满二叉树（Full Binary Tree）

深度为 $k$ 且含有 $2^k - 1$ 个结点的二叉树。**每层都填满。**

### 完全二叉树（Complete Binary Tree）

深度为 $k$、有 $n$ 个结点的二叉树，当且仅当其每个结点都与深度为 $k$ 的满二叉树中**编号 1~n** 的结点一一对应。

> 💡 直观判断：把二叉树从上到下、从左到右编号，n 个结点之间不空档、无缺号 → 完全二叉树。

```
完全二叉树：        非完全二叉树（缺了 5 号）：
      1                 1
    /   \             /   \
   2     3           2     3
  / \   /           / \   /
 4   5 6           4     6
```

## 2.3 二叉树的性质（5条必背）

### 性质 1：第 $i$ 层结点数

> 二叉树的第 $i$ 层上**至多**有 $2^{i-1}$ 个结点（$i \ge 1$）。

### 性质 2：深度为 $k$ 的总结点数

> 深度为 $k$ 的二叉树**至多**有 $2^k - 1$ 个结点（$k \ge 1$）。

### 性质 3：叶子与度为 2 结点的关系 🔥

$$\boxed{n_0 = n_2 + 1}$$

> 对任何二叉树，叶子结点数 $n_0$ = 度为 2 的结点数 $n_2$ + 1。

**推导**：设 $n$ 为总结点数，$n_0, n_1, n_2$ 分别为度数为 0、1、2 的结点数，则：

$$n = n_0 + n_1 + n_2$$

同时，考虑分支数 $B$（每个结点贡献一个入度，除了根）。度数为 $d$ 的结点贡献 $d$ 个出度（分支），所以：

$$B = n_1 + 2n_2$$

又因为总分支数 = 总结点数 − 1（根无入度），即 $B = n - 1$，所以：

$$n_1 + 2n_2 = n - 1 = n_0 + n_1 + n_2 - 1$$

化简得：$\boxed{n_0 = n_2 + 1}$。

> 🎯 考试最常考的性质，必须会推导。

### 性质 4：完全二叉树的深度

> 具有 $n$ 个结点的完全二叉树的深度为 $\lfloor \log_2 n \rfloor + 1$（或 $\lceil \log_2 (n+1) \rceil$）。

### 性质 5：完全二叉树的父子编号关系 🔥

若对含 $n$ 个结点的完全二叉树按层序（从上到下、从左到右）从 1 开始编号，则对编号为 $i$ 的结点：

| 关系 | 编号公式 | 条件 |
|:--|:--|:--|
| **双亲** | $\lfloor i/2 \rfloor$ | $i > 1$ |
| **左孩子** | $2i$ | $2i \le n$ |
| **右孩子** | $2i + 1$ | $2i + 1 \le n$ |

> 💡 这是完全二叉树能用**顺序存储**的理论基础。C 语言数组 0-based 时编号从 0 开始，公式相应变为：左孩 $2i+1$、右孩 $2i+2$、双亲 $\lfloor (i-1)/2 \rfloor$。

---

# §3 二叉树的存储结构 {#sec3}

## 3.1 顺序存储

用一维数组按层序存放完全二叉树的结点（$1 \sim n$ 编号）。**不存在结点用 0 或 -1 占位。**

```
     1
   /   \
  2     3
 / \   /
4   5 6

数组: [1] [2] [3] [4] [5] [6]
索引:  0   1   2   3   4   5
```

> ⚠️ 仅在**完全二叉树**时高效（无空间浪费）。对一般的二叉树，中间会大量空位——极浪费（若为**右斜树**，深度 $k$ 需 $2^k-1$ 个数组位置，但实际只有 $k$ 个结点）。

```c
#define MAX 100
int tree[MAX];    // 0 表示空结点
```

## 3.2 链式存储（二叉链表）

每个结点含：数据域 + 左孩子指针 + 右孩子指针。

```c
typedef struct BiTNode {
    int data;                    // 数据域
    struct BiTNode *lchild;      // 左孩子指针
    struct BiTNode *rchild;      // 右孩子指针
} BiTNode, *BiTree;
```

- 含 $n$ 个结点的二叉链表共有 $2n$ 个指针域
- 其中非空指针 $n-1$ 个（每个结点一个入度，根除外）
- **空指针** $= 2n - (n-1) = n + 1$ 个 → 这正是**线索二叉树**要利用的空闲空间

### 三叉链表（可选）

在二叉链表基础上增加 **parent** 指针，方便向上查找祖先。

---

# §4 二叉树的遍历（本章核心）{#sec4}

> 🔥 **遍历是树结构的灵魂**——几乎所有二叉树算法都建立在遍历的基础上。

## 4.1 四种基本遍历方式

| 遍历方式 | 访问顺序 | 记忆口诀 |
|:--|:--|:--|
| **前序**（Preorder） | 根 → 左子树 → 右子树 | **NLR** |
| **中序**（Inorder） | 左子树 → 根 → 右子树 | **LNR** |
| **后序**（Postorder） | 左子树 → 右子树 → 根 | **LRN** |
| **层序**（Level-order） | 从上到下、从左到右逐层 | **BFS** |

### 示例树

```
       A
     /   \
    B     C
   / \   / \
  D   E F   G
```

| 遍历 | 结果序列 |
|:--|:--|
| 前序（NLR） | A, B, D, E, C, F, G |
| 中序（LNR） | D, B, E, A, F, C, G |
| 后序（LRN） | D, E, B, F, G, C, A |
| 层序 | A, B, C, D, E, F, G |

## 4.2 递归实现（三行代码）

### 前序遍历

```c
void PreOrder(BiTree T) {
    if (T == NULL) return;
    visit(T);              // ① 访问根
    PreOrder(T->lchild);   // ② 遍历左子树
    PreOrder(T->rchild);   // ③ 遍历右子树
}
```

### 中序遍历

```c
void InOrder(BiTree T) {
    if (T == NULL) return;
    InOrder(T->lchild);    // ① 遍历左子树
    visit(T);              // ② 访问根
    InOrder(T->rchild);    // ③ 遍历右子树
}
```

### 后序遍历

```c
void PostOrder(BiTree T) {
    if (T == NULL) return;
    PostOrder(T->lchild);  // ① 遍历左子树
    PostOrder(T->rchild);  // ② 遍历右子树
    visit(T);              // ③ 访问根
}
```

> 💡 三种递归遍历是三行排列组合！**前/中/后仅是 visit(T) 的位置不同**。递归本质是借助系统栈——递归展开等价于非递归的显式栈。

## 4.3 非递归实现（用栈模拟递归）

> 🔥 考试可能要求手写非递归遍历，理解"手动维护栈"是关键。

### 中序非递归（最经典）

```c
void InOrder_NonRec(BiTree T) {
    BiTNode *Stack[100]; int top = -1;  // 栈
    BiTNode *p = T;
    while (p != NULL || top != -1) {
        if (p != NULL) {
            Stack[++top] = p;           // 入栈——暂存结点
            p = p->lchild;              // 一路向左走到底
        } else {
            p = Stack[top--];           // 弹出栈顶 → 访问
            visit(p);
            p = p->rchild;              // 转向右子树
        }
    }
}
```

**执行口诀**：一路向左，遇空弹栈（访问），转向右子树。

### 前序非递归

```c
void PreOrder_NonRec(BiTree T) {
    BiTNode *Stack[100]; int top = -1;
    BiTNode *p = T;
    while (p != NULL || top != -1) {
        if (p != NULL) {
            visit(p);                   // ☆ 入栈前先访问！与中序的区别
            Stack[++top] = p;
            p = p->lchild;
        } else {
            p = Stack[top--];
            p = p->rchild;
        }
    }
}
```

### 后序非递归（最复杂）

后序的非递归最难：必须记录**右子树是否已被访问过**，否则会重复入栈。

```c
void PostOrder_NonRec(BiTree T) {
    BiTNode *Stack[100]; int top = -1;
    BiTNode *p = T, *lastVisited = NULL;  // lastVisited 标记上次访问的结点
    while (p != NULL || top != -1) {
        if (p != NULL) {
            Stack[++top] = p;             // 一路向左
            p = p->lchild;
        } else {
            p = Stack[top];               // 取栈顶（不弹！）
            if (p->rchild != NULL && p->rchild != lastVisited) {
                p = p->rchild;            // 右子树尚未访问 → 转向
            } else {
                visit(p);                 // 右子树已访问或无 → 可以访问了
                lastVisited = p;
                top--;                    // 弹出
                p = NULL;                 // 置空，下一轮取栈顶
            }
        }
    }
}
```

## 4.4 层序遍历（用队列）

```c
void LevelOrder(BiTree T) {
    if (T == NULL) return;
    BiTNode *Queue[100]; int front = 0, rear = 0;
    Queue[rear++] = T;                    // 根入队
    while (front < rear) {
        BiTNode *p = Queue[front++];      // 队头出队
        visit(p);
        if (p->lchild) Queue[rear++] = p->lchild;  // 左孩子入队
        if (p->rchild) Queue[rear++] = p->rchild;  // 右孩子入队
    }
}
```

### 复杂度分析

| | 递归 | 非递归 | 层序 |
|:--|:--|:--|:--|
| **时间** | $O(n)$ | $O(n)$ | $O(n)$ |
| **空间** | $O(h)$ 递归栈 | $O(h)$ 显式栈 | $O(w)$ 队列宽度 |
| 最坏空间 | $O(n)$ 斜树 | $O(n)$ | $O(n)$ |

---

# §5 遍历序列还原二叉树（考试必考）{#sec5}

> 🔥 **中科大期末考试每年必有一道"给两个遍历序列，还原二叉树"的大题！**

## 5.1 核心原理

**唯一确定二叉树的充分条件：必须有中序序列！**

| 已知条件 | 能唯一确定二叉树？ |
|:--|:--:|
| 前序 + 中序 | ✅ 能 |
| 后序 + 中序 | ✅ 能 |
| 层序 + 中序 | ✅ 能 |
| 前序 + 后序 | ❌ **不能**（无法区分左右） |

> 💡 为什么前序 + 后序不行？因为缺少中序信息，无法知道一个孩子到底是左孩子还是右孩子（"只有一个孩子时无法判断左右"）。

## 5.2 前序 + 中序 → 还原二叉树

**算法思想**：
- 前序第一个结点 = **整棵树的根**
- 在中序中找到该根的位置 → 左侧为左子树、右侧为右子树
- **递归**对左右子树重复以上过程

### 手算示例

已知：
- 前序：`A B D E C F G`
- 中序：`D B E A F C G`

**步骤跟踪**：

```
Step 1:
  前序首 A → 根 = A
  中序: D B E | A | F C G
         左子树    ↑   右子树

Step 2: 左子树
  前序对应部分: B D E → 根 = B
  中序对应: D | B | E
            左  ↑  右
  → B 的左孩子 = D，右孩子 = E

Step 3: 右子树
  前序对应: C F G → 根 = C
  中序对应: F | C | G
            左  ↑  右
  → C 的左孩子 = F，右孩子 = G

最终还原:
       A
     /   \
    B     C
   / \   / \
  D   E F   G
```

### 完整算法（递归实现）

```c
BiTree BuildByPreIn(char pre[], char in[], int preL, int preR, int inL, int inR) {
    if (preL > preR) return NULL;
    // 前序第一个 = 根
    BiTree root = (BiTree)malloc(sizeof(BiTNode));
    root->data = pre[preL];
    // 在中序中找到根的位置
    int k;
    for (k = inL; k <= inR; k++)
        if (in[k] == pre[preL]) break;
    int leftLen = k - inL;   // 左子树的结点数
    // 递归构造左右子树
    root->lchild = BuildByPreIn(pre, in, preL+1, preL+leftLen, inL, k-1);
    root->rchild = BuildByPreIn(pre, in, preL+leftLen+1, preR, k+1, inR);
    return root;
}
```

## 5.3 后序 + 中序 → 还原二叉树

与"前序 + 中序"完全同理，只是**根在后序的最后一个位置**：

- 后序最后一个结点 = 整棵树的根
- 其余步骤一致

---

# §6 线索二叉树 {#sec6}

## 6.1 为什么要线索化

含 $n$ 个结点的二叉链表有 **$n+1$ 个空指针域**（因为 $2n$ 个指针中只有 $n-1$ 个非空指针）。线索化的思想就是**利用这些空指针**，指向其**前驱**或**后继**，使得遍历不需要栈或递归。

## 6.2 线索化标记

增设两个标志位，区分孩子指针和线索：

| ltag | lchild 含义 | rtag | rchild 含义 |
|:--:|:--|:--:|:--|
| 0 | 指向**左孩子** | 0 | 指向**右孩子** |
| 1 | 指向**前驱**（线索） | 1 | 指向**后继**（线索） |

```c
typedef struct BiThrNode {
    int data;
    struct BiThrNode *lchild, *rchild;
    int ltag, rtag;              // 0=孩子指针  1=线索
} BiThrNode, *BiThrTree;
```

## 6.3 中序线索化

中序线索二叉树中：
- 若 `ltag == 1`，则 `lchild` 指向**中序前驱**
- 若 `rtag == 1`，则 `rchild` 指向**中序后继**

**算法**：在中序遍历过程中，用一个 `pre` 指针记录**刚刚访问过的结点**。当前结点 `p` 的**前驱**就是 `pre`（即 `p->lchild = pre`），`pre` 的**后继**就是 `p`（即 `pre->rchild = p`）。

```c
BiThrNode *pre = NULL;  // 全局变量，记录中序遍历中刚访问的结点

void InThreading(BiThrTree p) {
    if (p == NULL) return;
    InThreading(p->lchild);          // 左子树线索化

    if (p->lchild == NULL) {         // 左孩子空 → 指向前驱
        p->ltag = 1;
        p->lchild = pre;
    }
    if (pre != NULL && pre->rchild == NULL) {  // 前驱的右孩子空 → 指向后继(当前p)
        pre->rtag = 1;
        pre->rchild = p;
    }
    pre = p;                         // 更新 pre

    InThreading(p->rchild);          // 右子树线索化
}
```

## 6.4 线索二叉树的遍历（无需栈！）

### 找中序第一个结点

一路向左走到底（`ltag==0` 时继续），直到 `ltag==1`。

```c
BiThrNode* FirstNode(BiThrNode *p) {
    while (p->ltag == 0) p = p->lchild;  // 最左下结点
    return p;
}
```

### 找后继

若 `rtag==1`：`rchild` 就是后继（线索已设好）
若 `rtag==0`：右子树的**第一个结点**（右子树中最左下结点）是后继

```c
BiThrNode* NextNode(BiThrNode *p) {
    if (p->rtag == 1) return p->rchild;   // 线索直通后继
    else return FirstNode(p->rchild);     // 右子树的最左下
}
```

### 中序遍历（利用了线索，无栈）

```c
void InOrder_Threaded(BiThrTree T) {
    for (BiThrNode *p = FirstNode(T); p != NULL; p = NextNode(p))
        visit(p);
}
```

---

# §7 树与森林 {#sec7}

## 7.1 树的三种存储结构

### (1) 双亲表示法

每个结点只记**双亲的位置**：

```c
typedef struct {
    int data;
    int parent;      // 双亲在数组中的下标（根为 -1）
} PTNode;
```

> 优点：找双亲 $O(1)$。缺点：找孩子需遍历全部结点 $O(n)$。

### (2) 孩子表示法

每个结点挂一个**孩子链表**（头指针数组 + 链表）：

```c
typedef struct ChildNode { int child; struct ChildNode *next; } ChildNode;
typedef struct { int data; ChildNode *firstChild; } Box;
Box tree[MAX];   // 顺序存放所有结点，每个结点链表存储孩子下标
```

> 优点：找孩子方便。缺点：找双亲需遍历所有链表。

### (3) 孩子-兄弟表示法（二叉链表表示）🔥

**最重要**——树 → 二叉树的桥梁！

```c
typedef struct CSNode {
    int data;
    struct CSNode *firstChild;   // 指向第一个孩子
    struct CSNode *nextSibling;  // 指向下一个兄弟
} CSNode, *CSTree;
```

> 💡 记忆：**左指针 = 第一个孩子，右指针 = 下一个兄弟**。这就是树 ↔ 二叉树的转换规则！

## 7.2 树与二叉树的相互转换 🔥

### 树 → 二叉树（三步）

1. **连线**：同一双亲的所有孩子之间加横线（兄弟连线）
2. **断线**：除第一个孩子外，断掉其他孩子与双亲的连线
3. **旋转**：以根为轴心，顺时针旋转 45°

> 变换后：二叉树的**左子树 = 原树的孩子**，**右子树 = 原树的兄弟**。原树的根没有右子树。

```
原树:           转换后二叉树:
    A               A
  / | \            /
 B  C  D    →     B
    |              \
    E               C
                     \
                      D
                     /
                    E
```

### 二叉树 → 树（逆变换）

逐结点应用：结点的**左孩子 = 原树的孩子**，**右孩子 = 原树的兄弟**。把二叉树的"右链"还原为兄弟关系。

> 🎯 核心公式：**左孩右兄**——这四个字决定了所有转换。

## 7.3 森林与二叉树的转换

### 森林 → 二叉树

1. 将每棵树分别转为二叉树，得到多棵二叉树
2. 从第二棵开始，依次将其作为前一棵的**右子树**（"一根接一根"挂上去）

### 二叉树 → 森林

1. 沿右链断开：从根开始，不断分离右子树——即**每次取一棵树的根 + 其左子树为一棵树，右子树为下一棵树的入口**
2. 每棵分离出的二叉树再转为树

## 7.4 树与森林的遍历

### 树的遍历

| 遍历 | 规则 | 与二叉树等价 |
|:--|:--|:--|
| **先根遍历** | 访问根 → 依次先根遍历每棵子树 | 转二叉树的**前序** |
| **后根遍历** | 依次后根遍历每棵子树 → 访问根 | 转二叉树的**中序** ⚡ |

> ⚡ 树的**后根**遍历 = 二叉树**中序**！这是考试易错点。

### 森林的遍历

| 遍历 | 规则 | 与二叉树等价 |
|:--|:--|:--|
| **先序遍历** | 先根遍历第 1 棵树 → 依次先序遍历剩余森林 | 森林转二叉树的**前序** |
| **中序遍历** | 后根遍历第 1 棵树 → 访问第 1 棵的根 → 依次中序遍历剩余森林 | 森林转二叉树的**中序** |

---

# §8 Huffman 树与 Huffman 编码 {#sec8}

> 🔥 考试重点：手工构造 Huffman 树 + 求 WPL + 给编码。

## 8.1 相关定义

### 路径与路径长度

- **路径**：从树中一个结点到另一个结点的分支序列
- **路径长度**：路径上的**分支数目**（经过的边数）
- **树的路径长度**：从根到**每个结点**的路径长度之和

### 结点的权与带权路径长度

- **权**：给结点赋予的、有某种意义的实数
- **结点的带权路径长度**：根到该结点的路径长度 × 该结点的权

### 树的带权路径长度 WPL

$$\boxed{WPL = \sum_{i=1}^{n} w_i \cdot l_i}$$

其中 $w_i$ 为第 $i$ 个叶子结点的权，$l_i$ 为根到该叶子的路径长度（边数）。

### Huffman 树（最优二叉树）

给定 $n$ 个权值，构造一棵含 $n$ 个叶子结点的二叉树，使 **WPL 最小**。这棵树称为 Huffman 树（最优二叉树）。

## 8.2 Huffman 树的性质

1. **没有度为 1 的结点**（每个内结点都有两个孩子）
2. **$n$ 个叶子结点的 Huffman 树共有 $2n-1$ 个结点**（由 $n_0 = n_2 + 1$ 且 $n_1 = 0$ 推得 $n = 2n_0 - 1$）
3. **权值越大的叶子离根越近**（这是 WPL 最小化的保证）
4. Huffman 树**不唯一**（当有多棵权值相同的子树时，任选两棵合并，WPL 相同）

## 8.3 Huffman 树的构造算法（贪心）

**构造过程**（反复"选两棵最小权值的树合并"）：

```
输入：n 个权值 {w₁, w₂, ..., wₙ}

1. 将每个权值作为一棵单结点二叉树 → 放入森林 F
2. 重复 n-1 次：
   a) 在 F 中选两棵根权值最小的树 → 作为左、右子树
   b) 新建一棵二叉树，根权 = 两棵子树根权之和
   c) 从 F 中移除这两棵，加入新树
3. F 中唯一剩下的那棵树 → Huffman 树
```

### 手算示例

给定权值 $\{5, 3, 7, 2, 1\}$，构造 Huffman 树：

```
F = {1, 2, 3, 5, 7}

步骤① 选最小两棵: 1 + 2 = 3  → F = {3, 3, 5, 7}
步骤② 选 3 + 3 = 6          → F = {5, 6, 7}
步骤③ 选 5 + 6 = 11         → F = {7, 11}
步骤④ 选 7 + 11 = 18        → F = {18}

最终 Huffman 树:
         18
       /    \
      7     11
           /    \
          5      6
                /   \
               3     3
                    /   \
                   1     2
```

**WPL 计算**：$WPL = 7 \times 1 + 5 \times 2 + (1+2) \times 4 = 7 + 10 + 12 = 29$

或者：$WPL = 7 \times 1 + 5 \times 2 + 3 \times 3 + 1 \times 4 + 2 \times 4 = 7 + 10 + 9 + 4 + 8 = 38$ ← 不对

实际上，路径长度 = 边数（或深度 − 1）：

| 叶子 | 权 | 深度 | 路径长度 | $w \times l$ |
|:--|:--:|:--:|:--:|:--:|
| 7 | 7 | 2 | 1 | 7 |
| 5 | 5 | 3 | 2 | 10 |
| 3 | 3 | 4 | 3 | 9 |
| 1 | 1 | 5 | 4 | 4 |
| 2 | 2 | 5 | 4 | 8 |

**WPL = 7 + 10 + 9 + 4 + 8 = 38**

> 💡 另法：**WPL = 所有非叶结点的权值之和**。$18 + 11 + 6 + 3 = 38$。✅ 这种算法最快！

## 8.4 Huffman 编码

### 前缀编码

> **任一字符的编码都不是另一字符编码的前缀**。这是解码时唯一性的保证（可以"切"开）。

- Huffman 编码是**最优前缀编码**（总码长最短）

### 编码方法

从 Huffman 树生成编码：

```
约定：左分支 → '0'，右分支 → '1'（反过来亦可，但要保持一致）
从根到叶子沿路径收集 0/1，即得该字符（叶子）的 Huffman 码
```

**对上例的编码**（左 0 右 1）：

| 权值(字符) | 路径 | Huffman 码 |
|:--|:--|:--|
| 7 | 左 | `0` |
| 5 | 右→左 | `10` |
| 3 | 右→右→左 | `110` |
| 1 | 右→右→右→左 | `1110` |
| 2 | 右→右→右→右 | `1111` |

> 💡 权值越大 → 编码越短（WPL 最小化的直接体现）。

### Huffman 编码的存储

```c
typedef struct {
    char ch;         // 字符
    char *code;      // 编码串（不定长）
    int len;         // 编码长度
} HuffmanCode;
```

---

# §9 易错点与速查卡片 {#sec9}

## 9.1 常见易错点

| 易错点 | 正确做法 |
|:--|:--|
| 二叉树 ≠ 度为 2 的树 | 二叉树**严格区分左右**，且度可为 0、1、2 |
| $n_0 = n_2 + 1$ 的使用条件 | 适用于**任何二叉树**（非空） |
| 非递归后序遍历 | 必须判断**右子树是否已访问过**，否则会死循环 |
| 前序+后序能否还原树 | **不能**！必须要有中序 |
| 树的"后根遍历" vs 二叉树"后序遍历" | 树的后根 = 二叉树**中序**（不是后序！）⚡ |
| Huffman 树无度为 1 的结点 | $n_1 = 0$，$n_{\text{total}} = 2n_0 - 1$ |
| WPL 快速算法 | WPL = 所有**非叶结点权值之和** |
| 森林转二叉树后的右子树 | 根的右子树 = 第二棵树 + 第三棵树 + … |
| 完全二叉树编号 | 从 1 开始！若题目说下标从 0 开始则相应调整公式 |
| 二叉链表空指针数 | $n+1$ 个空指针（$= 2n - (n-1)$） |

## 9.2 核心速查

| 知识点 | 核心公式/方法 |
|:--|:--|
| **二叉树性质 3** | $n_0 = n_2 + 1$ |
| **完全二叉树深度** | $\lfloor \log_2 n \rfloor + 1$ |
| **完全二叉树编号** | 左孩 $=2i$，右孩 $=2i+1$，双亲 $=\lfloor i/2 \rfloor$（1-based） |
| **四种遍历** | 前 NLR / 中 LNR / 后 LRN / 层 BFS |
| **递归遍历三行** | visit 的位置决定是前/中/后 |
| **中序非递归** | 一路向左，遇空弹栈访问，转向右 |
| **后序非递归** | 需 lastVisited 标记——最难 |
| **还原二叉树** | 必须有中序 + 另一种（前/后/层） |
| **线索二叉树** | 利用 $n+1$ 个空指针存前驱/后继 |
| **树↔二叉树** | 左孩右兄——口诀四个字 |
| **树的后根遍历** | = 二叉树的**中序** |
| **森林转二叉树** | 每棵树先转 → 再沿右链挂接 |
| **Huffman 树构造** | 反复选两棵最小合并，共 $n-1$ 次 |
| **Huffman 树结点数** | $2n_0 - 1$（无度为 1 的结点） |
| **WPL 快速公式** | 所有非叶结点权值之和 |
| **Huffman 编码** | 左 0 右 1（或调换），前缀编码 |
| **最优性** | Huffman 编码是总码长最短的前缀编码 |

---

<div style="text-align: center; margin-top: 2em;">

—— 第 5 章「树和二叉树」知识点总结完 ——

> 📚 下一步建议：配合习题集第 5 章 24 道题逐题攻克，重点练习「遍历序列还原树」「Huffman 构造」「线索化」

</div>
