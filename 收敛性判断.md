# 数分B2 收敛性判断方法总结

> 涵盖反常积分收敛性、含参变量反常积分的一致收敛性、Fourier 级数的收敛性。

---

# 第一部分：反常积分的收敛性（基础）

## 一、两类反常积分

| 类型 | 形式 | 奇点位置 |
|------|------|----------|
| 无穷区间 | $\int_a^{+\infty} f(x)dx$ | 上限为 $+\infty$ |
| 无界函数 | $\int_a^b f(x)dx$ | 被积函数在 $a$ 或 $b$（或中间某点）趋于无穷 |

---

## 二、比较判别法（最常用）

### 无穷区间 $\int_a^{+\infty} f(x)dx$

拿 $f(x)$ 和 $\frac{1}{x^p}$ 比较：

$$f(x) \sim \frac{C}{x^p} \; (x \to +\infty) \;\Longrightarrow\; \begin{cases} p > 1 & \text{收敛} \\ p \leq 1 & \text{发散} \end{cases}$$

### 无界函数 $\int_a^b f(x)dx$（奇点在 $a$）

拿 $f(x)$ 和 $\frac{1}{(x-a)^p}$ 比较：

$$f(x) \sim \frac{C}{(x-a)^p} \; (x \to a^+) \;\Longrightarrow\; \begin{cases} p < 1 & \text{收敛} \\ p \geq 1 & \text{发散} \end{cases}$$

> 💡 **记忆口诀**：无穷远处 $p>1$ 收敛（"远方的要够小"），奇点附近 $p<1$ 收敛（"爆炸的不能太猛"）。两个判别标准恰好相反。

### 两端都有奇点

拆成两个积分分别判断。例如 $\int_0^{+\infty}$ → 拆成 $\int_0^1 + \int_1^{+\infty}$，分别看 $x \to 0^+$ 和 $x \to +\infty$。

---

## 三、比较判别法的极限形式

若 $\lim_{x \to +\infty} \frac{f(x)}{g(x)} = L$（$0 < L < +\infty$），则 $\int f$ 和 $\int g$ 同敛散。

**标准比较对象**：
- $x \to +\infty$：$\frac{1}{x^p}$
- $x \to 0^+$：$\frac{1}{x^p}$
- 含 $\ln x$ 时也可用 $\frac{1}{x^p \ln^q x}$（$p=1, q>1$ 收敛）

---

## 四、具体例子

### 例1：$\int_0^{+\infty} \frac{x^\alpha}{1+x^2}dx$（例3-7）

- $x \to 0^+$：$\frac{x^\alpha}{1+x^2} \sim x^\alpha$，收敛需 $\alpha > -1$
- $x \to +\infty$：$\frac{x^\alpha}{1+x^2} \sim x^{\alpha-2}$，收敛需 $\alpha - 2 < -1$ 即 $\alpha < 1$
- 结论：收敛域 $-1 < \alpha < 1$

### 例2：$\int_0^1 \frac{dx}{\sqrt{x}}$

$x \to 0^+$ 时 $f(x) \sim x^{-1/2}$，$p = \frac{1}{2} < 1$ → 收敛。

### 例3：$\int_1^{+\infty} \frac{dx}{x^2}$

$p = 2 > 1$ → 收敛（值为 $1$）。

### 例4：$\int_2^{+\infty} \frac{dx}{x\ln x}$

$p = 1$（$\ln x$ 不影响 $p$ 值）→ 发散。

---

# 第二部分：含参反常积分的一致收敛性（第13章重点）

## 一、什么是"一致收敛"？

> 💡 直观理解：**所有参数的尾巴同时变得足够小**。

$$\int_a^{+\infty} f(x,u)dx \text{ 关于 } u \in I \text{ 一致收敛}$$

意思是：$\forall \varepsilon > 0$，存在一个**与 $u$ 无关**的 $X$，使得对所有 $u \in I$：

$$\left|\int_A^{+\infty} f(x,u)dx\right| < \varepsilon \quad (\forall A > X)$$

---

## 二、三大判别法

### 1. Weierstrass 判别法（M-判别法）★最常用

若 $|f(x,u)| \leq F(x)$ 对所有 $u \in I$ 成立，且 $\int_a^{+\infty} F(x)dx$ 收敛，则 $\int_a^{+\infty} f(x,u)dx$ 关于 $u \in I$ **一致收敛**。

> 💡 所谓"找一个不含参数的控制函数 $F(x)$，它收敛了，原积分就一致收敛。"

**例**：$\int_0^{+\infty} e^{-\alpha x^2}dx$，$\alpha \in [\alpha_0, +\infty)$（$\alpha_0 > 0$）。

$e^{-\alpha x^2} \leq e^{-\alpha_0 x^2} = F(x)$，$\int_0^{+\infty} e^{-\alpha_0 x^2}dx = \frac{1}{2}\sqrt{\frac{\pi}{\alpha_0}}$ 收敛 → 一致收敛 ✓

### 2. Dirichlet 判别法

若：
1. $\left|\int_a^A f(x,u)dx\right| \leq M$（对 $A, u$ 一致有界）
2. $g(x,u)$ 关于 $x$ 单调，且 $x \to +\infty$ 时关于 $u$ **一致**趋于 $0$

则 $\int_a^{+\infty} f(x,u)g(x,u)dx$ 关于 $u$ 一致收敛。

> 💡 **典型场景**：$\int \frac{\sin(ux)}{x}dx$ 型——$\sin$ 积分有界，$1/x$ 单调趋于 $0$。

**经典例子**：$\int_0^{+\infty} \frac{\sin x}{x}dx$ 收敛但不绝对收敛。$\int_0^{+\infty} \frac{\sin(ux)}{x}dx$ 关于 $u \in [u_0, +\infty)$（$u_0 > 0$）一致收敛。

### 3. Abel 判别法

若：
1. $\int_a^{+\infty} f(x,u)dx$ 关于 $u$ **一致收敛**
2. $g(x,u)$ 关于 $x$ 单调，且关于 $x, u$ **一致有界**

则 $\int_a^{+\infty} f(x,u)g(x,u)dx$ 关于 $u$ 一致收敛。

> 💡 Abel 是 Dirichlet 的"补集"——积分已经一致收敛了，再乘一个单调有界函数。

---

## 三、重要结论

### 一致收敛 → 连续性

若 $f(x,u)$ 连续且 $\int_a^{+\infty} f(x,u)dx$ 关于 $u$ 一致收敛，则 $\varphi(u) = \int_a^{+\infty} f(x,u)dx$ 在 $I$ 上**连续**。

### 一致收敛 → 可交换积分次序

$$\int_\alpha^\beta \left[\int_a^{+\infty} f(x,u)dx\right]du = \int_a^{+\infty} \left[\int_\alpha^\beta f(x,u)du\right]dx$$

### 一致收敛 → 可在积分号下求导

若 $f_u(x,u)$ 也连续且 $\int_a^{+\infty} f_u(x,u)dx$ 一致收敛，则：

$$\varphi'(u) = \int_a^{+\infty} \frac{\partial f}{\partial u}(x,u)dx$$

---

## 四、判断流程

```
反常积分 ∫_a^∞ f(x,u) dx 关于 u ∈ I
              │
              ├─ 找不含 u 的控制函数 F(x) ≥ |f(x,u)|？
              │     │
              │     ├─ 找到了且 ∫F 收敛 → 一致收敛 ✅ (Weierstrass)
              │     │
              │     └─ 找不到 → 继续
              │
              ├─ 能拆成 f(x,u)·g(x,u)？
              │     │
              │     ├─ ∫f 一致有界 + g↘0(一致) → 一致收敛 ✅ (Dirichlet)
              │     │
              │     └─ ∫f 一致收敛 + g 单调有界 → 一致收敛 ✅ (Abel)
              │
              └─ 都行不通 → 考虑区间端点行为
                    │
                    ├─ I 是闭区间 [α,β]（紧）？通常好判断
                    └─ I 是开区间 (α,β)？看端点附近是否"一致"
```

---

# 第三部分：Fourier 级数的收敛性

## Dirichlet 收敛定理

若 $f(x)$ 以 $2\pi$ 为周期且在 $[-\pi,\pi]$ 上**分段单调**（或分段可微），则其 Fourier 级数处处收敛于：

$$S(x) = \frac{f(x+0) + f(x-0)}{2}$$

| 位置 | 收敛值 |
|------|--------|
| 连续点 | $S(x) = f(x)$ |
| 跳跃间断点 | 左右极限的平均值 |
| 端点 $x = \pm\pi$ | $\frac{f(\pi-0) + f(-\pi+0)}{2}$ |

---

## 一句总结

> **反常积分收敛性看 $p$ 值（$\infty$ 处 $p>1$，$0$ 处 $p<1$）；一致收敛性首选 Weierstrass 找控制函数，其次考虑 Dirichlet/Abel；Fourier 级数满足分段单调就一定收敛于左右极限平均值。**
