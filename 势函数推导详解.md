# 势函数 $\varphi(x,y) = \frac{1}{ab}\arctan\frac{by}{ax} + C$ 的完整推导

## 问题回顾（例1-5）

向量场 $\mathbf{v} = \dfrac{-y}{a^2x^2 + b^2y^2}\,\mathbf{i} + \dfrac{x}{a^2x^2 + b^2y^2}\,\mathbf{j}$ 在 $D_1: x>0$ 上是保守场。

求势函数 $\varphi(x,y)$ 使得 $\nabla\varphi = (P, Q)$。

---

## 核心方法：沿折线积分

保守场的势函数 = 从基点 $(1,0)$ 到 $(x,y)$ 的线积分。因为积分与路径无关，选**最简单的折线**：

```
        y ↑
          │              (x,y)
          │                ↑
          │                │ 第二段：竖直走
          │                │ x 固定，从 y=0 到 y=y
          │                │
          │   (1,0)   (x,0)
  ────────┼────────────→───→ x
          │  第一段：水平走
          │  y=0，从 x=1 到 x=x
```

$$\varphi(x,y) = \underbrace{\int_{(1,0)}^{(x,0)} P\,dx + Q\,dy}_{\text{第一段}} \;+\; \underbrace{\int_{(x,0)}^{(x,y)} P\,dx + Q\,dy}_{\text{第二段}}$$

---

## 第一段：水平路径 $(1,0) \to (x,0)$

在 $x$ 轴上：$y = 0$，$dy = 0$。

$$P(x,0) = \frac{-0}{a^2x^2 + b^2 \cdot 0^2} = 0$$

$$\int_{(1,0)}^{(x,0)} P\,dx + Q\,dy = \int_1^x 0 \cdot dx + \int_1^x Q(x,0) \cdot 0 = 0$$

**第一段贡献为 0。** $\gets$ 这就是为什么选 $(1,0)$ 作为基点——它在 $x$ 轴上，分母简化。

---

## 第二段：竖直路径 $(x,0) \to (x,y)$

$x$ 固定为常数，$dx = 0$。设参数 $t$ 从 $0$ 到 $y$：

$$P\,dx = 0$$

$$\int_{(x,0)}^{(x,y)} Q\,dy = \int_0^y Q(x,t)\,dt = \int_0^y \frac{x}{a^2x^2 + b^2t^2}\,dt$$

---

## 关键积分计算

$$\int_0^y \frac{x}{a^2x^2 + b^2t^2}\,dt$$

分母提出 $b^2$：

$$= \frac{x}{b^2}\int_0^y \frac{1}{\frac{a^2x^2}{b^2} + t^2}\,dt$$

令 $c = \dfrac{ax}{b}$，则 $\dfrac{a^2x^2}{b^2} = c^2$：

$$= \frac{x}{b^2}\int_0^y \frac{1}{c^2 + t^2}\,dt$$

基本积分公式 $\displaystyle\int \frac{1}{c^2 + t^2}\,dt = \frac{1}{c}\arctan\frac{t}{c}$：

$$= \frac{x}{b^2} \cdot \frac{1}{c} \cdot \left[\arctan\frac{t}{c}\right]_{t=0}^{t=y}$$

代入 $c = \frac{ax}{b}$：

$$\frac{x}{b^2} \cdot \frac{1}{\frac{ax}{b}} = \frac{x}{b^2} \cdot \frac{b}{ax} = \frac{1}{ab}$$

$$\left[\arctan\frac{t}{\frac{ax}{b}}\right]_0^y = \left[\arctan\frac{bt}{ax}\right]_0^y = \arctan\frac{by}{ax} - \arctan(0)$$

---

## 最终结果

$$\varphi(x,y) = 0 + \frac{1}{ab}\arctan\frac{by}{ax} = \boxed{\frac{1}{ab}\arctan\frac{by}{ax} + C}$$

---

## 验证（可选但推荐）

对 $\varphi$ 求偏导，应该回到 $P, Q$：

$$\frac{\partial\varphi}{\partial x} = \frac{1}{ab} \cdot \frac{1}{1+(\frac{by}{ax})^2} \cdot \left(-\frac{by}{ax^2}\right) = \frac{1}{ab} \cdot \frac{a^2x^2}{a^2x^2+b^2y^2} \cdot \left(-\frac{by}{ax^2}\right) = \frac{-y}{a^2x^2+b^2y^2} = P \;\checkmark$$

$$\frac{\partial\varphi}{\partial y} = \frac{1}{ab} \cdot \frac{1}{1+(\frac{by}{ax})^2} \cdot \frac{b}{ax} = \frac{1}{ab} \cdot \frac{a^2x^2}{a^2x^2+b^2y^2} \cdot \frac{b}{ax} = \frac{x}{a^2x^2+b^2y^2} = Q \;\checkmark$$

---

## 技巧总结

| 步骤 | 技巧 |
|------|------|
| 选基点 | 选在使 $P$ 或 $Q$ 简化的地方（如 $x$ 轴或 $y$ 轴上） |
| 选路径 | 折线——先水平再竖直，或先竖直再水平 |
| 计算水平段 | $y$ 固定，$dy=0$，只需积 $\int P\,dx$ |
| 计算竖直段 | $x$ 固定，$dx=0$，只需积 $\int Q\,dy$ |
| 常用公式 | $\int\frac{1}{c^2+t^2}dt = \frac{1}{c}\arctan\frac{t}{c}$ |
