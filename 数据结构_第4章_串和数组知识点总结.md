# 数据结构 · 第4章 串和数组 知识点与考点总结

> 《数据结构及其算法》复习资料 | 杨思辰 · 未来技术学院 · 物理学专业
>
> 🔥 考试重点：**KMP 算法 + next 数组手算、nextval、矩阵压缩存储、特殊矩阵地址映射、稀疏矩阵三元组**

## 目录

1. [§1 串的基本概念](#sec1)
2. [§2 串的存储结构](#sec2)
3. [§3 模式匹配算法](#sec3)
4. [§4 KMP 算法详解（本章灵魂）](#sec4)
5. [§5 数组](#sec5)
6. [§6 矩阵的压缩存储](#sec6)
7. [§7 易错点与速查卡片](#sec7)

---

# §1 串的基本概念 {#sec1}

## 1.1 定义

**串（String）** 是由零个或多个字符组成的**有限序列**，记为 $S =$ `"a₁ a₂ … aₙ"`。

| 术语 | 含义 | 示例 |
|------|------|------|
| 串长 | 串中字符个数 $n$ | `"abc"` 长 3 |
| 空串 | 长度为零的串，记为 `""` | 不含任何字符 |
| 空格串 | 由一个或多个空格组成的串 | `" "`（不是空串！） |
| 子串 | 串中任意连续字符组成的子序列 | `"bc"` 是 `"abcd"` 的子串 |
| 主串 | 包含子串的串 | `"abcd"` 是 `"bc"` 的主串 |
| 位置 | 子串第一个字符在主串中的序号 | `"bc"` 在 `"abcd"` 中位置为 2 |

> ⚠️ 必区分：**空串 `""` ≠ 空格串 `" "`**（前者长度 0，后者长度 ≥ 1）。

> ⭐ 串是一种**特殊的线性表**：元素限定为字符。但串的操作对象常是**整个串或子串**（查找、替换、比较），不同于线性表"按单个元素操作"。

---

# §2 串的存储结构 {#sec2}

## 2.1 定长顺序存储（静态）

用字符数组存储，预定义最大长度。C 语言以 `'\0'` 作为串结束标志。

```c
char s[100] = "hello";   // 实际存 5 个字符 + '\0'
```

> 缺点：截断问题——若运算结果超过预定义长度，会丢失数据。

## 2.2 堆分配存储（动态）

用 `malloc` 按需申请空间，长度灵活。

## 2.3 链式存储（块链）

每个结点存**多个字符**（块），结点间用指针链接。存储密度 = 串值占内存 / 结点总内存。块越大存储密度越高、操作越不便——需在密度和灵活性间权衡。

---

# §3 模式匹配算法 {#sec3}

**模式匹配**（Pattern Matching）：在主串 $S$ 中查找模式串 $T$ 首次出现的位置。这是串最重要的操作。

## 3.1 朴素匹配（Brute Force / BF）

**思想**：从主串每个位置依次尝试匹配整个模式串。失配则主串指针回溯到下一个起始位置、模式串指针回到 0。

**计算方法**（以 $S=\text{"ababcabcacbab"}$，$T=\text{"abcac"}$ 为例）：

| 趟数 | 主串起点 i | 比较过程 | 结果 |
|:--:|:--:|:--|:--:|
| 1 | 0 | `a=a, b=b, a≠c` → 失配 | 失败 |
| 2 | 1 | `b≠a` → 失配 | 失败 |
| 3 | 2 | `a=a, b=b, c=c, a=a` → 部分匹配 | 失败 |
| … | … | … | … |
| 6 | 5 | `a=a, b=b, c=c, a=a, c=c` → 全匹配 | **成功，位置=5** |

**代码示例：**

```c
int BF(const char *S, const char *T){
    int n=strlen(S), m=strlen(T);
    for(int i=0; i<=n-m; i++){
        int j;
        for(j=0; j<m && S[i+j]==T[j]; j++);
        if(j==m) return i;         // 匹配成功，返回位置
    }
    return -1;                     // 未找到
}
```

**复杂度**：最坏情况 $O(n \times m)$（如 $S$="aaaaab"，$T$="aaab"——每趟都比到最后一个字符才发现失败）。最好情况 $O(n+m)$。

---

# §4 KMP 算法详解（本章灵魂）{#sec4}

> 🔥 考试频率最高的内容——习题集第4章 20 道题中大量涉及 KMP，next 数组手算几乎是必考题。

## 4.1 KMP 的核心思想

BF 的问题：失配时**主串指针 i 会回溯**——已经比较过的字符要重新比。KMP 的精髓：**i 绝不回溯**。失配时只让模式串指针 j 跳到一个合适的 `next[j]` 位置，主串指针 i 保持不动继续比较。

> 💡 直观类比：你是阅卷老师，逐字对答案。标准答案是"abaabc"，学生写的是"abaaba..."。你对到第 6 个字符发现`a≠c`。BF 会让你整张卷子退回到第 2 个字重新对——KMP 会说"前面"ab"已经对上了，直接从模式串的第 3 个字（a）接着往下对就行"。

## 4.2 next 数组的定义

> ⭐ 约定：**`next[0] = -1`**，`next[j]` 表示"当模式串第 $j$ 个字符（下标 $j$）失配时，模式串指针应跳到的位置"。

**本质**：`next[j]` = 模式串前 $j$ 个字符构成的子串中，**最长相等前后缀的长度**（前后缀不能等于自身）。

$$\boxed{next[j] = \max\{ k \mid 0 \le k < j,\; P_0 P_1 \dots P_{k-1} = P_{j-k} P_{j-k+1} \dots P_{j-1} \}}$$

（注：$k=0$ 时左右为空串，约定最大空匹配长度为0；$j=0$ 时 $next[0] = -1$ 为特殊标记）

## 4.3 next 数组手算方法（考试必考）

**以模式串 $P = \text{"abaabc"}$ 为例**，逐位手算：

| j | P[j] | 子串（前 j 个字符） | 最长公共前后缀 | 长度 | next[j] |
|:--:|:--:|:--|:--|:--:|:--:|
| 0 | a | （空） | 无（约定） | — | **-1** |
| 1 | b | `"a"` | 无（前后缀不能等于整个子串） | 0 | **0** |
| 2 | a | `"ab"` | 无（`"a"`≠`"b"`） | 0 | **0** |
| 3 | a | `"aba"` | `"a"` = `"a"` | 1 | **1** |
| 4 | b | `"abaa"` | `"a"` = `"a"` | 1 | **1** |
| 5 | c | `"abaab"` | `"ab"` = `"ab"` | 2 | **2** |

> 💡 手算口诀：**看前 $j$ 个字符，找"从开头取 $k$ 个"和"从末尾取 $k$ 个"相等的最大 $k$，就是 next[j]**。next[0] 恒为 -1。

**代码求 next 数组**：

```c
void getNext(const char *P, int *next){
    int m=strlen(P);
    next[0] = -1;
    int k = -1, j = 0;
    while(j < m-1){
        if(k==-1 || P[j]==P[k]){ j++; k++; next[j]=k; }
        else k = next[k];          // KMP 的自引用精髓
    }
}
```

## 4.4 nextval（优化版 next）

**为什么需要优化**：若 $P[j] = P[next[j]]$，那么失配后跳到的字符**和原字符一样**——还得继续失配！白跳了。

**修正规则**：

$$nextval[j] = \begin{cases}
nextval[next[j]] & \text{若 } P[j]=P[next[j]] \\
next[j]          & \text{否则}
\end{cases}$$

**手算示例**（继续模式串 `"abaabc"`，$\text{next} = [-1,0,0,1,1,2]$）：

| j | P[j] | next[j] | P[j] vs P[next[j]] | nextval[j] |
|:--:|:--:|:--:|:--|:--:|
| 0 | a | -1 | —（约定） | **-1** |
| 1 | b | 0 | `'b'` ≠ `'a'` | **0** |
| 2 | a | 0 | `'a'` = `'a'` → 递归 | `nextval[0]` = **-1** |
| 3 | a | 1 | `'a'` ≠ `'b'` | **1** |
| 4 | b | 1 | `'b'` = `'b'` → 递归 | `nextval[1]` = **0** |
| 5 | c | 2 | `'c'` ≠ `'a'` | **2** |

结果：**nextval = [-1, 0, -1, 1, 0, 2]**

## 4.5 KMP 匹配主循环

```c
int KMP(const char *S, const char *T){
    int n=strlen(S), m=strlen(T), next[100];
    getNext(T, next);
    int i=0, j=0;
    while(i<n && j<m){
        if(j==-1 || S[i]==T[j]){ i++; j++; }  // 匹配：双双前进
        else j = next[j];                      // 失配：j 跳转，i 不动
    }
    return (j==m) ? i-m : -1;
}
```

**复杂度**：$O(n+m)$——主串每个字符最多被访问两次（一次前进 + 至多一次因跳转而重新比较），模式串同理。**比 BF 的 $O(nm)$ 优很多。**

## 4.6 KMP 手算跟踪示例

以 $S=$ `"ababcabcacbab"`，$T=$ `"abcac"` 为例（next = [-1,0,0,0,1]）：

| 轮次 | i | j | 状态 | 动作 |
|:--:|:--:|:--:|:--|:--|
| 1 | 0→2 | 0→2 | S[2]=a≠c 失配 | j=next[2]=0 |
| 2 | 2→2 | 0→0 | S[2]=a=a 继续 | i++,j++ |
| 3 | 3→4 | 1→2 | S[5]=a≠c 失配 | j=next[2]=0 |
| … | … | … | … | … |
| 最后 | 5→9 | 0→4 | 全部匹配 | j==m → 返回 i-m=5 |

---

# §5 数组 {#sec5}

## 5.1 基本概念

数组是由 $n$ 个**相同类型**数据元素构成的有限序列。**逻辑上是线性表的推广**（一维），也可以是二维/多维。

> ⭐ 数组**一旦定义了维数和维界，不能再改变**——这是它与线性表的区别（不能插入/删除）。

## 5.2 数组的存储映射

**核心考点**：二维数组在内存中是一维排列的，有两种映射方式。给定基址 + 下标，算物理地址。

### 行优先（Row-Major）——C 语言默认

$$LOC(a[i][j]) = LOC(a[0][0]) + (i \times n + j) \times size$$

### 列优先（Column-Major）——Fortran / MATLAB 默认

$$LOC(a[i][j]) = LOC(a[0][0]) + (j \times m + i) \times size$$

其中 $m$ 为行数、$n$ 为列数、$size$ 为每个元素字节数。

**计算方法**（以 `a[3][4]`、基址=1000、size=4，求 `a[1][2]`）：

- 行优先：$1000 + (1\times4 + 2)\times4 = 1024$
- 列优先：$1000 + (2\times3 + 1)\times4 = 1028$

> 💡 记法："Which index runs faster?"——行优先 = 右边的列下标跑得快（inner loop），列优先 = 左边的行下标跑得快。

---

# §6 矩阵的压缩存储 {#sec6}

> 🔥 考试喜欢考"给一个特殊矩阵，手推某个元素在压缩数组中的下标"。

## 6.1 对称矩阵

$a_{ij} = a_{ji}$，只需存**下三角（含对角线）**共 $\frac{n(n+1)}{2}$ 个元素。

**按行优先存入一维数组 B**，$a_{ij}$（i ≥ j，下三角）在 B 中的下标：

$$k = \frac{i(i-1)}{2} + (j-1) \quad (i \ge j,\; \text{1-based 下标})$$

若 $i < j$（上三角），用对称性取 $a_{ji}$。

## 6.2 三角矩阵

下三角矩阵：上三角全为同一个常数 c。只存下三角 + 一个 c。

$$k = \begin{cases}
\dfrac{i(i-1)}{2} + (j-1) & i \ge j \\
\dfrac{n(n+1)}{2} & i < j \;\text{(即上三角——存在末尾)}
\end{cases}$$

## 6.3 稀疏矩阵

非零元素**远少于**零元素。用**三元组表**存储：每个非零元记为 `(行号, 列号, 值)`。

```c
typedef struct { int row, col; int val; } Triple;   // 三元组
Triple data[100]; int count;                        // 三元组表
```

**转置算法**：常规方法需排序，可用"**快速转置**"——预先统计每列非零元个数，计算起始位置，一趟扫描完成。

## 6.4 对角矩阵（三对角）

所有非零元素集中在主对角线及相邻两条对角线上。存储时同样映射到一维数组，下标公式为 $k = 2i + j$（0-based 下三角部分）。

---

# §7 易错点与速查卡片 {#sec7}

## 7.1 常见易错点

| 易错点 | 正确做法 |
|--------|----------|
| 空串 ≠ 空格串 | 空串长度 0，空格串长度 ≥ 1 |
| next[0] | 约定为 **-1**（不是 0！好多初学者在这里翻车） |
| next 手算 | 看**前 j 个字符**的最长相等前后缀，不是整个模式串 |
| KMP 主循环 i 回退 | **i 绝不回退**——这是 KMP 和 BF 的本质区别 |
| 地址计算下标起始 | 看清题目的下标是 0-based 还是 1-based |
| 对称矩阵存储 | 只存**下三角**（含对角线），上三角用对称取 |
| 存储密度 | 顺序表 > 链式；块链存储密度 = 字符量 / 总内存 |

## 7.2 核心速查

- **串**：特殊的线性表，元素限定为字符
- **BF 匹配**：$O(n \times m)$ 最坏
- **KMP 匹配**：$O(n+m)$
- **next[j]** = 前 j 个字符的**最长相等前后缀长度**
- **next[0] = -1**（约定）
- **nextval**：若 $P[j] = P[next[j]]$ 则递归取 $nextval[next[j]]$
- **行优先地址**：$LOC(a[i][j]) = base + (i \times n + j) \times size$
- **列优先地址**：$LOC(a[i][j]) = base + (j \times m + i) \times size$
- **对称矩阵压缩**：存下三角 $n(n+1)/2$ 个元素
- **稀疏矩阵**：三元组 `(行,列,值)` 存储

---

—— KMP 手算练 10 遍，第 4 章就稳了！ ——
