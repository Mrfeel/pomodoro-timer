# 单位圆上 $\frac{\partial f}{\partial n} = f_x\cos\theta + f_y\sin\theta$ 的推导

## 第一步：单位圆的参数化与外法向量

单位圆 $x^2+y^2=1$ 的参数表示为：

$$x = \cos\theta,\quad y = \sin\theta$$

圆心在原点，圆上任意一点 $(x,y)$ 的位置向量就是 $(\cos\theta, \sin\theta)$。

**外法向量**指向远离圆心的方向（径向向外）。因为圆心是原点，所以从圆心指向圆上一点的径向方向就是外法向：

$$\boxed{\mathbf{n} = (\cos\theta,\; \sin\theta)}$$

> 💡 直观验证：$\mathbf{n}$ 与位置向量同方向，且 $|\mathbf{n}| = \sqrt{\cos^2\theta + \sin^2\theta} = 1$（单位长度）。对于任意半径 $r$ 的圆，外法向量同样是 $(\cos\theta, \sin\theta)$，与半径无关。

---

## 第二步：方向导数的定义

函数 $f(x,y)$ 沿单位向量 $\mathbf{n} = (n_1, n_2)$ 的方向导数定义为**梯度与方向向量的点积**：

$$\frac{\partial f}{\partial n} = \nabla f \cdot \mathbf{n}$$

其中梯度 $\nabla f = \left(\frac{\partial f}{\partial x},\; \frac{\partial f}{\partial y}\right) = (f_x, f_y)$。

---

## 第三步：代入 $\mathbf{n} = (\cos\theta, \sin\theta)$

$$\begin{aligned}
\frac{\partial f}{\partial n} &= \nabla f \cdot \mathbf{n}  \\
&= (f_x,\; f_y) \cdot (\cos\theta,\; \sin\theta)  \\
&= f_x \cos\theta + f_y \sin\theta
\end{aligned}$$

$$\boxed{\frac{\partial f}{\partial n} = f_x\cos\theta + f_y\sin\theta}$$

---

## 第四步：几何理解

| 分量 | 含义 |
|------|------|
| $f_x\cos\theta$ | $x$ 方向的变化率在法向（径向）上的投影 |
| $f_y\sin\theta$ | $y$ 方向的变化率在法向（径向）上的投影 |
| 两者之和 | 函数在径向方向上的总变化率 |

> 💡 **直观理解**：$\cos\theta$ 和 $\sin\theta$ 是外法向量在 $x$ 轴和 $y$ 轴上的分量。$f_x$ 告诉你"沿 $x$ 方向走一步 $f$ 变多少"，乘上 $\cos\theta$ 后就是"沿 $x$ 方向走、但步伐大小按法向的 $x$ 分量来算"。两项加起来就是沿法向的总变化率。

---

## 附：一般曲面的外法向量

对于一般闭曲线（不一定是圆），外法向量 $\mathbf{n}$ 的定义是垂直于切线、指向区域外侧的单位向量。

在圆（或任意半径的圆周）上这个方向恰好是径向方向——因为圆的切线垂直于半径，而外法向与径向重合。

---

## 一句话总结

> **梯度 $\nabla f$ 点乘单位外法向量 $\mathbf{n}$ 就是法向导数。单位圆上 $\mathbf{n}=(\cos\theta,\sin\theta)$ 是因为圆心在原点、外法向就是径向。**
