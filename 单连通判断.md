# 怎么判断区域是不是单连通的？

## 一句话定义

> **单连通 = 区域内任何一条闭合曲线都能在区域内连续收缩成一个点，而不碰到区域外的点。**

---

## 通俗理解

把区域想象成一张橡皮膜：

- **单连通**：膜上没有洞。你在膜上画任何一个圈，都可以把圈慢慢收紧成一个点，收紧过程中圈始终在膜上。
- **非单连通**：膜上有洞。你画的圈如果套住了那个洞，就收不紧——洞挡住了。

---

## 平面区域（二维）

### 单连通 ✅

| 区域 | 为什么是单连通 |
|------|---------------|
| 整个平面 $\mathbb{R}^2$ | 没有洞 |
| 单位圆盘 $x^2+y^2 \leq 1$ | 实心圆，没有洞 |
| 右半平面 $x > 0$ | 无限延伸但没有洞 |
| 矩形区域 | 实心，没有洞 |

### 非单连通 ❌

| 区域 | 为什么不是单连通 |
|------|-----------------|
| $x^2+y^2 > 0$（去掉原点） | 原点被挖掉了，有个"针孔" |
| 圆环 $1 < x^2+y^2 < 4$ | 中间有个洞 |
| $x > 0, y > 0$ | 还是单连通… |

等等——$x>0, y>0$（第一象限）是单连通的！虽然它"缺了"其他三个象限，但它没有**洞**——你画任何圈都能收紧。

**判断标准**：看区域是否有"被完全包围的外部点"。原点被圆环包围 → 非单连通。无穷远处的点不算"包围"。

---

## 空间区域（三维）

三维中"洞"的概念更微妙：

### 单连通 ✅
- 整个 $\mathbb{R}^3$
- 实心球 $x^2+y^2+z^2 \leq 1$
- 半空间 $x > 0$

### 非单连通 ❌
- **挖去一点**：$\mathbb{R}^3 \setminus \{(0,0,0)\}$ — 在三维中去掉一个点仍然是单连通的！因为你可以让圈绕过那个点。
- **挖去一条线**：$\mathbb{R}^3 \setminus \{z\text{轴}\}$ — **这才是非单连通**。绕 $z$ 轴画一个圈，它套住了 $z$ 轴，收不紧。

> ⚠️ 二维 vs 三维的区别：**在二维中挖点 ≠ 单连通；在三维中挖点 = 单连通，挖线才 ≠ 单连通。**

---

## 考试中的判断法

数分B2考试中，判断单连通主要看一个东西——**分母的零点（奇点）**：

### 方法：看奇点是否在区域内

1. 找出被积函数（或向量场）无定义的点（通常是分母为零的点）
2. 在区域内画圈，看能不能不经过奇点而把圈收紧

### 例1-5 回顾

$$\mathbf{v} = \frac{-y\mathbf{i} + x\mathbf{j}}{a^2x^2 + b^2y^2}$$

奇点：$(0,0)$。

| 区域 | 奇点在区域内？ | 单连通？ | 保守场？ |
|------|:---:|:---:|:---:|
| $D_1: x > 0$（右半平面） | ❌ 不在（原点在边界上但不属于 $x>0$） | ✅ 是 | ✅ 是 |
| $D_2: x^2+y^2 > 0$（去掉原点） | ✅ 原点被挖掉但形成了"洞" | ❌ 不是 | ❌ 不是 |

---

## 对保守场判定的影响

回忆判断保守场的两个条件：

| 条件 | 说明 |
|------|------|
| $\frac{\partial Q}{\partial x} = \frac{\partial P}{\partial y}$ | **必要条件** |
| 区域是**单连通**的 | **充分性条件** |

两者**同时满足**才是保守场。例1-5中 $D_2$ 满足第一个条件但不满足第二个 → 不是保守场。

---

## 解题流程

```
向量场 v = (P, Q) 在区域 D 中
           │
           ├─ 算 ∂Q/∂x 和 ∂P/∂y
           │     │
           │     ├─ 不相等 → 不是保守场（结束）
           │     │
           │     └─ 相等 → 继续判断
           │              │
           │              ├─ D 是单连通？ → 是 → 保守场 ✅
           │              │
           │              └─ D 不是单连通？ → 找绕奇点的闭路
           │                       │
           │                       ├─ 闭路积分为 0 → 保守场
           │                       └─ 闭路积分 ≠ 0 → 不是保守场 ❌
```

---

## 一句总结

> **直观判断：在区域内任意画一个圈，能收紧成一个点的就是单连通；圈被"洞"（奇点）卡住收不紧的就是非单连通。**
