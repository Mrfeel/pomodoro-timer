# 曲面积分中 $dydz, dzdx, dxdy$ 为何有的为零？

## 题目背景（复习讲义 0.12(2)）

$S$ 为抛物面 $z=x^2+y^2$（$0 \leq z \leq 2$），不闭合，需补顶面 $D: z=2, x^2+y^2 \leq 2$（取**上侧**）。

在 $D$ 上计算 $\iint_D \mathbf{F} \cdot d\mathbf{S}$，其中 $\mathbf{F} = (4xz, -2yz, 1-z^2)$。

结论：$D$ 上只有 $dxdy$ 项，$dydz$ 和 $dzdx$ 项为零。

---

## 第一步：理解 $\mathbf{F} \cdot d\mathbf{S}$ 的三种写法

第二型曲面积分有两种等价表示：

### 写法1：向量点积形式
$$\iint_S \mathbf{F} \cdot d\mathbf{S} = \iint_S \mathbf{F} \cdot \mathbf{n}\,dS$$

其中 $\mathbf{n}$ 是单位法向量，$dS$ 是面积微元（标量）。

### 写法2：分量投影形式
$$\iint_S \mathbf{F} \cdot d\mathbf{S} = \iint_S P\,dydz + Q\,dzdx + R\,dxdy$$

两个写法的对应关系：

$$\boxed{\mathbf{n}\,dS = (dydz,\; dzdx,\; dxdy)}$$

即：**$dydz$ 是向量面积元 $\mathbf{n}dS$ 的 $x$ 分量，$dzdx$ 是 $y$ 分量，$dxdy$ 是 $z$ 分量。**

> 💡 直观：$\mathbf{n}dS$ 是一个"有方向的面积微元"——它的大小是面积，方向是法向。$dydz$ 就是这个向量投影到 $yz$ 平面上的面积。

---

## 第二步：投影公式（关键！）

当曲面表示为 $z = z(x,y)$（取上侧）时，法向量 $\mathbf{n}dS$ 的分量为：

$$\boxed{\mathbf{n}\,dS = \left(-\frac{\partial z}{\partial x},\; -\frac{\partial z}{\partial y},\; 1\right)dxdy}$$

即：
$$dydz = -\frac{\partial z}{\partial x}dxdy$$
$$dzdx = -\frac{\partial z}{\partial y}dxdy$$
$$dxdy = dxdy$$

（取上侧时的公式；取下侧时三个分量全反号。）

> 💡 **记忆方法**：$z=z(x,y)$ 时以 $dxdy$ 为基准。$x$ 分量 $-z_x$ 和 $y$ 分量 $-z_y$ 是曲面倾斜带来的"侧向投影"。

---

## 第三步：$D$ 是 $z=2$ 的平面圆盘

**关键**：$D$ 上 $z$ 是常数（$z=2$），所以：
$$\frac{\partial z}{\partial x} = 0,\quad \frac{\partial z}{\partial y} = 0$$

代入投影公式：
$$dydz = -0 \cdot dxdy = 0$$
$$dzdx = -0 \cdot dxdy = 0$$
$$dxdy = dxdy$$

> 💡 **几何直观**：$z=2$ 是水平面。法向量 $\mathbf{n} = (0,0,1)$ 完全指向正上方，没有任何 $x$ 或 $y$ 分量。所以向量面积元 $\mathbf{n}dS$ 在 $yz$ 和 $zx$ 平面上的投影面积为零——就像一张水平放置的纸，从侧面看是一条线，面积为零。

---

## 第四步：代入原积分

$$\begin{aligned}
\iint_D \mathbf{F} \cdot d\mathbf{S} &= \iint_D P\,\underbrace{dydz}_{=0} + Q\,\underbrace{dzdx}_{=0} + R\,dxdy \\[4pt]
&= \iint_D R\,dxdy \\[4pt]
&= \iint_D (1-z^2)\big|_{z=2}\,dxdy \\[4pt]
&= \iint_{x^2+y^2 \leq 2} (1-4)\,dxdy \\[4pt]
&= -3\iint_{x^2+y^2 \leq 2} dxdy
\end{aligned}$$

---

## 第五步：计算面积

圆盘 $x^2+y^2 \leq 2$ 的面积 $= \pi \cdot (\sqrt{2})^2 = 2\pi$。

$$\iint_D \mathbf{F} \cdot d\mathbf{S} = -3 \cdot 2\pi = -6\pi$$

---

## 总结：什么情况下某些分量为零？

| 曲面类型 | $dydz$ | $dzdx$ | $dxdy$ | 原因 |
|----------|--------|--------|--------|------|
| $z =$ 常数（水平面） | $0$ | $0$ | $\neq 0$ | $z_x = z_y = 0$ |
| $y =$ 常数（平行于 $xz$ 面） | $0$ | $\neq 0$ | $0$ | 法向在 $y$ 方向 |
| $x =$ 常数（平行于 $yz$ 面） | $\neq 0$ | $0$ | $0$ | 法向在 $x$ 方向 |
| 一般曲面 $z=z(x,y)$ | $\neq 0$ | $\neq 0$ | $\neq 0$ | 三个分量都有 |

> 💡 一句话：**水平面上法向量是 $(0,0,\pm 1)$，只有 $dxdy$ 分量（$z$ 分量）非零。$dydz$ 和 $dzdx$ 是"侧向投影"，水平面没有侧向倾斜，投影面积为零。**
