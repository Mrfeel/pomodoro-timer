#!/usr/bin/env python3
"""生成主元与自由变量识别详解"""

content = r"""# 如何识别主元与自由变量？

> 这是求解线性方程组、基础解系、矩阵秩等问题的**第一步**。一旦分清了主元和自由变量，后续操作都是机械流程。

\newpage

# 一、一句话结论

> **行最简形（RREF）中，包含"阶梯拐角处 1"的列是主元列 → 对应主变量；其余列是自由列 → 对应自由变量。**

\newpage

# 二、什么是"行最简形"（RREF）？

行最简形（Reduced Row Echelon Form）必须同时满足：

| 条件 | 说明 |
|------|------|
| ① 阶梯形 | 每行第一个非零元素（主元）下方的元素全为零 |
| ② 主元全为 1 | 每行第一个非零元素必须是 1 |
| ③ 主元列其余全零 | 主元 1 **所在列**的其他元素必须全部为零 |

**反例对比**：

```
阶梯形（不够！）：        行最简形（正确！）：
┌           ┐            ┌           ┐
│ 1  2  3  4│            │ 1  2  0  4│
│ 0  1  5  6│            │ 0  0  1  6│
│ 0  0  0  0│            │ 0  0  0  0│
└           ┘            └           ┘
第1行主元1，但第2列      第1列和第3列的主元1
主元1上方有非零元素      所在列其余全为零
（第1行第2列=2≠0）       ✓ 这才是RREF
```

**必须化到行最简形才能正确识别！** 很多人图省事停在阶梯形，然后就搞混了。

\newpage

# 三、识别规则——看行最简形

## 规则

$$\boxed{\text{主元列：包含"主元 1"的列} \quad\longleftrightarrow\quad \text{主变量}}$$

$$\boxed{\text{自由列：不包含"主元 1"的列} \quad\longleftrightarrow\quad \text{自由变量}}$$

## 手把手识别

行最简形：

$$\begin{pmatrix} \boxed{1} & 2 & 0 & 4/3 & -1/3 \\ 0 & 0 & \boxed{1} & -5/3 & -4/3 \end{pmatrix}$$

- 第 1 列有主元 1 → **主元列** → $x_1$ 是主变量
- 第 2 列无主元 1 → **自由列** → $x_2$ 是自由变量
- 第 3 列有主元 1 → **主元列** → $x_3$ 是主变量
- 第 4 列无主元 1 → **自由列** → $x_4$ 是自由变量
- 第 5 列无主元 1 → **自由列** → $x_5$ 是自由变量

主变量：$x_1,\,x_3$（2 个） | 自由变量：$x_2,\,x_4,\,x_5$（3 个）

$n=5$，主变量数 = 秩 $r = 2$，自由变量数 $= n-r = 3$。

\newpage

# 四、四个手把手例子

## 例1：标准情况

$$\begin{pmatrix} \boxed{1} & 0 & 2 \\ 0 & \boxed{1} & -1 \\ 0 & 0 & 0 \end{pmatrix}$$

- 主元列：第 1 列 ✓、第 2 列 ✓ → $x_1,x_2$ 主变量
- 自由列：第 3 列 → $x_3$ 自由变量

$r=2$，$n=3$，自由变量 $=1$ 个。

## 例2：只有一个主元

$$\begin{pmatrix} \boxed{1} & 3 & 0 & -2 \\ 0 & 0 & 0 & 0 \\ 0 & 0 & 0 & 0 \end{pmatrix}$$

- 主元列：第 1 列 → $x_1$ 主变量
- 自由列：第 2、3、4 列 → $x_2,x_3,x_4$ 自由变量

$r=1$，$n=4$，自由变量 $=3$ 个。

## 例3：满秩方阵（无自由变量）

$$\begin{pmatrix} \boxed{1} & 0 & 0 \\ 0 & \boxed{1} & 0 \\ 0 & 0 & \boxed{1} \end{pmatrix}$$

- 每列都有主元 → 全是主变量，**无自由变量** → 只有零解 $\boldsymbol{x}=\boldsymbol{0}$。

$r=n=3$，自由变量 $=0$ 个。

## 例4：含全零行

$$\begin{pmatrix} \boxed{1} & 0 & 0 & 2 \\ 0 & \boxed{1} & 0 & 3 \\ 0 & 0 & \boxed{1} & -1 \\ 0 & 0 & 0 & 0 \end{pmatrix}$$

- 主元列：第 1、2、3 列 → $x_1,x_2,x_3$ 主变量
- 自由列：第 4 列 → $x_4$ 自由变量

$r=3$，$n=4$，自由变量 $=1$ 个。第 4 行虽全零，但它**不影响主元列数**——秩只看非零行个数。

\newpage

# 五、常见易错点

## 错法1：停在阶梯形就开始判断

```
阶梯形：        正确应该是行最简形：
┌         ┐     ┌           ┐
│ 1  2  3 │     │ 1  2  0  5│
│ 0  1  4 │     │ 0  0  1  4│
└         ┘     └           ┘
```

阶梯形第 2 列有主元，但如果继续消元（$R_1-2R_2$），第 2 列上方变成 0，第 3 列才出现主元。**不化到底就判断，会错**。

## 错法2：把非零元素最多的列当主元列

主元是由**消元过程**决定的，不是看哪列非零元素多。一个全是非零元素的列也可能是自由列。

## 错法3：混淆"主元"和"矩阵中的非零元素"

主元特指**每行最左边的 1**，不是随便哪个 1。

\newpage

# 六、逆序判断法（不写出 RREF 也能判断）

已知矩阵的秩 $r$ 时（比如通过行列式或秩的定理），可以这样判断：

- **主变量 $= r$ 个**：在线性无关的 $r$ 个方程中"地位独立"的变量
- **自由变量 $= n-r$ 个**：其余变量，其取值可以任意指定

但实际操作中，**化行最简形是最可靠的方法**——直观、不会错。

\newpage

# 七、速查流程图

```
给定矩阵 A (m×n)
    │
    ▼
初等行变换 → 行最简形 (RREF)
    │
    ▼
找到每行的"主元 1"（该行最左边的 1）
    │
    ▼
主元 1 所在的列 → 主元列 → 对应的变量 = 主变量
    │
    ▼
其余列 → 自由列 → 对应的变量 = 自由变量
    │
    ▼
验证: r = 主变量个数, n-r = 自由变量个数
```

## 一句话总结

> 化到**行最简形**，数每行最左边那个 1 在哪一列——那些列就是主元列，对应主变量。剩下的列全是对应自由变量。$r =$ 主元 1 的个数，$n-r =$ 自由变量个数。
"""

with open(r"d:\辰辰\first CC\pivot_free.md", "w", encoding="utf-8") as f:
    f.write(content)

print("Markdown 已生成")
