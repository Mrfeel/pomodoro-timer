# Dirichlet 定理（Fourier 级数收敛定理）

## 定理内容

设 $f(x)$ 以 $2\pi$ 为周期，在 $[-\pi, \pi]$ 上**分段单调**（或分段可微），则其 Fourier 级数**处处收敛**，且收敛于：

$$\boxed{S(x) = \frac{f(x+0) + f(x-0)}{2}}$$

即：Fourier 级数在每一点收敛到该点**左右极限的平均值**。

---

## 分三种情况

| 点的类型 | $S(x)$ 收敛到 | 示意 |
|----------|---------------|------|
| **连续点** | $S(x) = f(x)$ | Fourier 级数还原函数值 |
| **跳跃间断点** | $\frac{f(x^+) + f(x^-)}{2}$ | 取左右极限的**中间值** |
| **区间端点** $x = \pm\pi$ | $\frac{f(\pi^-) + f(-\pi^+)}{2}$ | 两端极限的平均 |

---

## 直观理解

> 💡 **通俗解释**：Fourier 级数就像一个"老实人"——在函数光滑的地方老老实实还原函数值；在函数跳崖（间断）的地方，它不偏不倚地站在悬崖正中间。

### 方波的例子

$$f(x) = \begin{cases} 1, & 0 < x < \pi \\ -1, & -\pi < x < 0 \end{cases}$$

在 $x=0$ 处：$f(0^+) = 1$，$f(0^-) = -1$。Dirichlet 定理说 Fourier 级数在 $x=0$ 收敛于：
$$S(0) = \frac{1 + (-1)}{2} = 0$$

实际画出 Fourier 级数的部分和——确实在 $x=0$ 处穿过 $0$（Gibbs 现象是超调量不消失，但收敛值确实是 $0$）。

---

## 定理的条件

| 条件 | 含义 |
|------|------|
| **周期 $2\pi$** | $f(x+2\pi) = f(x)$ |
| **分段单调** | 区间可分成有限段，每段上 $f$ 单调（从而最多有有限个跳跃间断点） |

更常用的充分条件是**分段可微**（分段单调 $\subset$ 分段可微），考试中遇到的函数几乎都满足。

---

## 考试中的典型考法

### 考法1：求 $S(x)$ 在某点的值

已知 $f(x)$ 在某区间上的表达式，延拓为周期函数。求 $S(-3)$、$S(12)$ 等。

**做法**：不看 Fourier 级数本身，直接用 Dirichlet 定理——

1. 把 $-3$（或 $12$）通过周期性映射到 $[-\pi, \pi]$（或 $[0, 2\pi]$）内
2. 判断该点是连续点还是间断点
3. 连续点 → 直接代入 $f$
4. 间断点 → 取左右极限平均

### 考法2：利用特定点的收敛值求级数和

展开 Fourier 级数后，取 $x = x_0$，Dirichlet 定理给出 $S(x_0) = \frac{f(x_0^+)+f(x_0^-)}{2}$，而 $S(x_0)$ 是级数的和，从而导出级数和。

例如 0.21 题：$f(x)=\pi-|x|$ 在 $x=0$ 连续，$S(0)=f(0)=\pi$。把 $x=0$ 代入 Fourier 展开式得到：
$$\pi = \frac{\pi}{2} + \frac{4}{\pi}\sum_{k=1}^{\infty}\frac{1}{(2k-1)^2}$$

从而解出 $\sum \frac{1}{(2n-1)^2} = \frac{\pi^2}{8}$。

---

## 与 Dirichlet 判别法的区别

| | Dirichlet 定理 | Dirichlet 判别法 |
|------|------|------|
| 用于 | Fourier 级数的**逐点收敛性** | 反常积分的**收敛性**判断 |
| 结论 | 级数收敛于 $(f(x^+)+f(x^-))/2$ | 积分收敛 |
| 章节 | 第12章 | 第13章 |

> ⚠️ 名字相同，内容不同！别搞混。

---

## 一句话总结

> **Dirichlet 定理告诉你 Fourier 级数收敛到哪里——连续点收敛到函数值，间断点收敛到左右极限的中间值。有了它，不用真的把无穷级数加起来，就能知道级数在任意点的和。**
