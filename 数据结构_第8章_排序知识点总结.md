# 数据结构 · 第8章 排序 知识点与考点总结

> 《数据结构及其算法》复习资料 | 杨思辰 · 未来技术学院 · 物理学专业 · PB25992094
>
> 🔥 考试重点：**快排每趟结果、堆排序建堆过程、归并排序合并过程、各算法稳定性与复杂度对比**

---

## 目录

1. [§1 排序的基本概念](#sec1)
2. [§2 插入排序](#sec2)
3. [§3 交换排序](#sec3)
4. [§4 选择排序](#sec4)
5. [§5 归并排序](#sec5)
6. [§6 基数排序](#sec6)
7. [§7 八大排序对比（核心表格）](#sec7)
8. [§8 易错点与速查卡片](#sec8)

---

# §1 排序的基本概念 {#sec1}

| 术语 | 含义 |
|:--|:--|
| **稳定排序** | 关键字相同的元素排序后**相对次序不变** |
| **不稳定排序** | 相同关键字可能改变相对次序 |
| **内部排序** | 全部记录可一次装入内存 |
| **外部排序** | 数据量大，需内外存交换（第 10 章） |
| **基于比较的排序** | 通过比较关键字决定次序——**下界 $O(n \log n)$** |
| **非比较排序** | 基数排序等，不比较关键字——可达 $O(n)$ |

---

# §2 插入排序 {#sec2}

## 2.1 直接插入排序

**思想**：将未排序元素依次插入**已排序区**的合适位置。类似打牌时整理手牌。

```c
void InsertSort(int a[], int n) {
    for (int i = 1; i < n; i++) {
        int key = a[i], j = i - 1;
        while (j >= 0 && a[j] > key)
            { a[j + 1] = a[j]; j--; }   // 后移
        a[j + 1] = key;                   // 插入
    }
}
```

| | 值 |
|:--|:--|
| 时间（最好） | $O(n)$（已有序） |
| 时间（最坏/平均） | $O(n^2)$ |
| 空间 | $O(1)$ |
| **稳定性** | **✅ 稳定** |

## 2.2 希尔排序（缩小增量排序）

**思想**：先以大步长（间隔）分组做插入排序，逐步缩小步长到 1。最后一次步长=1 等价于完整插入排序但数据已基本有序。

```
d = 5:  [a0,a5,a10] [a1,a6] [a2,a7] [a3,a8] [a4,a9]  各组独立排序
d = 3:  [a0,a3,a6,a9] [a1,a4,a7,a10] [a2,a5,a8]        各组独立排序
d = 1:  整体插入排序（此时数组已近有序→ 接近 O(n)）
```

| | 值 |
|:--|:--|
| 时间（平均） | $O(n^{1.3})$ 实际 |
| 空间 | $O(1)$ |
| **稳定性** | **❌ 不稳定**（跨组跳跃） |

---

# §3 交换排序 {#sec3}

## 3.1 冒泡排序

**思想**：相邻两两比较，逆序则交换；每趟将最大元素"浮"到最后。

```c
void BubbleSort(int a[], int n) {
    for (int i = 0; i < n - 1; i++) {
        int flag = 0;
        for (int j = 0; j < n - 1 - i; j++)
            if (a[j] > a[j + 1])
                { swap(a[j], a[j+1]); flag = 1; }
        if (!flag) break;    // 本趟无交换 → 已有序 → 提前结束
    }
}
```

| | 值 |
|:--|:--|
| 时间（最好） | $O(n)$（有序+flag 优化） |
| 时间（最坏/平均） | $O(n^2)$ |
| 空间 | $O(1)$ |
| **稳定性** | **✅ 稳定**（仅交换相邻逆序对） |

## 3.2 快速排序（Quick Sort）🔥

> 🔥 最重要的一种排序！必须能手写划分过程。

**思想**：选**枢轴（pivot）**，将表分为"左边 < pivot < 右边"两部分，再递归排序左右子表。

```c
int Partition(int a[], int low, int high) {
    int pivot = a[low];                      // 选第一个元素为枢轴
    while (low < high) {
        while (low < high && a[high] >= pivot) high--;  // 从右找比pivot小的
        a[low] = a[high];                                // 移到左边
        while (low < high && a[low] <= pivot) low++;     // 从左找比pivot大的
        a[high] = a[low];                                // 移到右边
    }
    a[low] = pivot;
    return low;                              // 枢轴最终位置
}

void QuickSort(int a[], int low, int high) {
    if (low < high) {
        int p = Partition(a, low, high);
        QuickSort(a, low, p - 1);
        QuickSort(a, p + 1, high);
    }
}
```

### 手算过程示例

数组 `{49, 38, 65, 97, 76, 13, 27}`，pivot=49：

```
初始: [49]  38  65  97  76  13  27        pivot=49
    → 右扫: 27<49 → a[0]=27
      左扫: 38<49, 65>49 → a[6]=65
    → [27  38]  49  [65  97  76  13]      第1趟结束
    → 递归左右子表...
```

| | 值 |
|:--|:--|
| 时间（最好/平均） | $O(n \log n)$ |
| 时间（最坏） | $O(n^2)$（正序/逆序+首元素为枢轴） |
| 空间 | $O(\log n)$ 递归栈（最好），$O(n)$（最坏） |
| **稳定性** | **❌ 不稳定**（跨距离交换） |

> 💡 优化：三数取中选枢轴、随机选枢轴、小数据量切换到插入排序。

---

# §4 选择排序 {#sec4}

## 4.1 简单选择排序

**思想**：每趟从未排序区选出**最小元素**，放到已排序区末尾。

```c
void SelectSort(int a[], int n) {
    for (int i = 0; i < n - 1; i++) {
        int min = i;
        for (int j = i + 1; j < n; j++)
            if (a[j] < a[min]) min = j;
        swap(a[i], a[min]);
    }
}
```

| | 值 |
|:--|:--|
| 时间 | $O(n^2)$（无论什么情况都 $n(n-1)/2$ 次比较） |
| 空间 | $O(1)$ |
| **稳定性** | **❌ 不稳定**（交换破坏相对次序） |

## 4.2 堆排序（Heap Sort）🔥

> 🔥 必考：给定数组 → 手工建堆（大根堆/小根堆）+ 每趟输出堆顶 + 调整。

### 堆的定义

- **大根堆**：每个结点 ≥ 所有后代（堆顶 = 最大值）
- **小根堆**：每个结点 ≤ 所有后代（堆顶 = 最小值）
- 堆是完全二叉树 → 可用数组存储，父子下标关系同完全二叉树

### 核心操作

**① 建堆**（自底向上调整）：从最后一个非叶结点 $\lfloor n/2 \rfloor - 1$ 开始，逐结点**向下筛选**。

```c
void HeapAdjust(int a[], int k, int n) {
    // 将以 k 为根的子树调整为大根堆
    int key = a[k];
    for (int i = 2 * k + 1; i < n; i = 2 * i + 1) {
        if (i + 1 < n && a[i] < a[i + 1]) i++;  // 选较大的孩子
        if (key >= a[i]) break;                   // 已满足堆性质
        a[k] = a[i]; k = i;                       // 孩子上移
    }
    a[k] = key;
}

void BuildHeap(int a[], int n) {
    for (int i = n / 2 - 1; i >= 0; i--)
        HeapAdjust(a, i, n);        // 自底向上逐结点调整
}
```

**② 堆排序**：反复输出堆顶（交换到末尾）→ 剩余元素重新调整。

```c
void HeapSort(int a[], int n) {
    BuildHeap(a, n);                         // ① 建初堆
    for (int i = n - 1; i > 0; i--) {
        swap(a[0], a[i]);                     // ② 堆顶（最大）与末尾交换
        HeapAdjust(a, 0, i);                  // ③ 缩小范围，重新调整
    }
}
```

### 手算示例

数组 `{4, 10, 3, 5, 1}` → 建大根堆：

```
原始完全二叉树:    调整后大根堆:
      4                 10
    /   \             /    \
  10     3     →     5      3
 /  \               / \
5    1             4   1
                  (堆顶=10最大)
```

排序过程：输出 10 → 末尾 1 移顶 → 调整 → 输出 9...

| | 值 |
|:--|:--|
| 时间（建堆） | $O(n)$ |
| 时间（排序） | $O(n \log n)$ |
| 总时间 | $O(n \log n)$ |
| 空间 | $O(1)$ |
| **稳定性** | **❌ 不稳定** |

---

# §5 归并排序（Merge Sort）{#sec5}

## 5.1 思想（分治法）

1. **分**：将数组递归**二分成子数组**，直到每段只有 1 个元素
2. **治**：将有序子数组**两两合并**成更大的有序数组

```c
void Merge(int a[], int left, int mid, int right) {
    int i = left, j = mid + 1, k = 0;
    int *tmp = (int*)malloc((right - left + 1) * sizeof(int));

    while (i <= mid && j <= right)          // 两路归并
        tmp[k++] = (a[i] <= a[j]) ? a[i++] : a[j++];
    while (i <= mid)  tmp[k++] = a[i++];     // 剩余追加
    while (j <= right) tmp[k++] = a[j++];
    for (i = 0; i < k; i++) a[left + i] = tmp[i];  // 写回

    free(tmp);
}

void MergeSort(int a[], int left, int right) {
    if (left < right) {
        int mid = (left + right) / 2;
        MergeSort(a, left, mid);             // 左半递归
        MergeSort(a, mid + 1, right);        // 右半递归
        Merge(a, left, mid, right);          // 合并
    }
}
```

### 手算过程

```
[38] [27] [43] [3] [9] [82] [10]
  \   /    \   /    \   /    \   /
  [27,38]  [3,43]  [9,82]  [10]
     \       /        \       /
    [3,27,38,43]      [9,10,82]
           \             /
         [3,9,10,27,38,43,82]
```

| | 值 |
|:--|:--|
| 时间 | $O(n \log n)$ 始终如一（最好 = 最坏 = 平均） |
| 空间 | $O(n)$（需要临时数组） |
| **稳定性** | **✅ 稳定**（合并时相等取左边） |

---

# §6 基数排序 {#sec6}

## 6.1 思想

**不比较关键字**，按关键字的各位值**分配+收集**。

以 LSD（最低位优先）为例：先按个位分配→收集；再按十位分配→收集；…直到最高位。

```
原始: [329, 457, 839, 436, 720, 355]

个位分配:              收集:
  0: [720]             [720, 839, 355, 436, 457, 329]
  5: [355]
  6: [436]
  7: [457]
  9: [329, 839]

十位分配:              收集:
  2: [329, 720]        [720, 329, 436, 839, 355, 457]
  3: [436, 839]
  5: [355, 457]

百位分配:              收集:
  3: [329, 355]        [329, 355, 436, 457, 720, 839] ✅ 有序
  4: [436, 457]
  7: [720]
  8: [839]
```

| | 值 |
|:--|:--|
| 时间 | $O(d \cdot (n + r))$，d=位数，r=基数 |
| 空间 | $O(n + r)$ |
| **稳定性** | **✅ 稳定** |

---

# §7 八大排序终极对比 {#sec7}

> 🔥 必背表格——期末考试选择题直接拿来查。

| 排序算法 | 最好时间 | 平均时间 | 最坏时间 | 空间 | 稳定？ |
|:--|:--|:--|:--|:--|:--:|
| **直接插入** | $O(n)$ | $O(n^2)$ | $O(n^2)$ | $O(1)$ | ✅ |
| **希尔排序** | — | $O(n^{1.3})$ | $O(n^2)$ | $O(1)$ | ❌ |
| **冒泡排序** | $O(n)$ | $O(n^2)$ | $O(n^2)$ | $O(1)$ | ✅ |
| **快速排序** | $O(n \log n)$ | $O(n \log n)$ | $O(n^2)$ | $O(\log n)$ | ❌ |
| **简单选择** | $O(n^2)$ | $O(n^2)$ | $O(n^2)$ | $O(1)$ | ❌ |
| **堆排序** | $O(n \log n)$ | $O(n \log n)$ | $O(n \log n)$ | $O(1)$ | ❌ |
| **归并排序** | $O(n \log n)$ | $O(n \log n)$ | $O(n \log n)$ | $O(n)$ | ✅ |
| **基数排序** | $O(d(n+r))$ | $O(d(n+r))$ | $O(d(n+r))$ | $O(r)$ | ✅ |

## 选择口诀

```
数据少且基本有序 → 直接插入
数据少且随机     → 简单选择
数据大且内存够   → 快速排序（平均最快）
数据大且怕最坏   → 堆排序（空间O(1)，永不过载）
需要稳定且大     → 归并排序（空间换稳定）
多关键字整数     → 基数排序
```

---

# §8 易错点与速查卡片 {#sec8}

## 8.1 常见易错点

| 易错点 | 正确做法 |
|:--|:--|
| 快排枢轴选择 | 首元素当枢轴在正序/逆序时退化 $O(n^2)$ → 三数取中 |
| 堆排序建堆起点 | 从 $\lfloor n/2 \rfloor - 1$ 开始，不是 $0$ |
| 堆排序只需 $O(1)$ 空间 | 堆排序是**原地排序**，不需要额外数组 |
| 归并排序空间 | **$O(n)$**，需要临时数组——不是 $O(1)$ |
| 稳定 vs 不稳定 | "**插冒归基**稳，**希快选堆**不稳" |
| 希尔排序 ≠ 稳定 | 虽然基于插入，但跳跃分组破坏了稳定性 |
| 基数排序适用条件 | 关键字每位可分离，且取值范围不大 |

## 8.2 核心速查

$$\boxed{\text{基于比较的排序下界: } \Omega(n \log n)}$$

快速排序、堆排序、归并排序均达到此下界。

$$\boxed{\text{稳定口诀: 插冒归基 —— 插入、冒泡、归并、基数}}$$

---

<div style="text-align: center; margin-top: 2em;">

—— 第 8 章「排序」知识点总结完 ——

> 🏁 全书 8 章知识点总结全部完成！配合习题集逐个击破，考试无忧。

</div>
