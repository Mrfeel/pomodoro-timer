# 数据结构 实验报告

## 实验名称：实验二——停车场问题模拟

**班级：** ________　**姓名：** 杨思辰　**学号：** PB25992094　**实验日期：** 2026-07-18

---

## 一、实验项目与实验要求

### 1. 实验项目

模拟一个停车场管理系统。停车场是一个可以停放 $n$ 辆车的狭长通道，只有一个门可供车辆进出，车辆按到达时间的先后顺序依次停放。当停车场停满后，新到达的车辆在停车场外的便道上排队等候。一旦有车离开停车场，便道上等候的第一辆车立即进入停车场。

当停车场内有车辆需要离开时，由于停车场单门通道的限制，排在该车后面的所有车辆必须暂时先开出停车场为其让路。待目标车辆驶离停车场后，让路的车辆再按原来的次序依次返回停车场。

### 2. 核心功能要求

- 停车场采用 **顺序栈** 模拟（单门,只能从一端进出,后进先出）；
- 便道采用 **链队列** 模拟（先进先出的排队规则）；
- 车辆让路时使用 **临时顺序栈** 暂存让路车辆；
- 每辆车到达和离开时，系统自动输出操作信息；
- 离开车辆根据停留时间计算停车费用（单价 $p$ 元 / 分钟）；
- 程序以交互命令行方式接收指令（A 到达、D 离开、P 显示、E 退出）；
- 若车辆既不在停车场也不在便道中，离场操作应输出相应提示。

### 3. 准备测试数据与预期结果

**配置参数**：

- 停车场容量 $n = 3$
- 便道容量 $m = 4$
- 停车单价 $p = 2$ 元 / 分钟

**命令序列**（A 车牌 到达时间 / D 车牌 离开时间 / E 0 0）：

| 序号 | 命令 | 时间 | 操作说明 | 预期结果 |
|:----:|------|:----:|----------|----------|
| 1 | `A 101 5` | 5 | 车辆 101 到达 | 停车场有空位,直接停入停车场 (车位: 1/3) |
| 2 | `A 102 10` | 10 | 车辆 102 到达 | 停车场有空位,直接停入停车场 (车位: 2/3) |
| 3 | `D 101 15` | 15 | 车辆 101 离开 | 停留 10 分钟,缴费 20 元。102 在 101 之后,需让路再返回 |
| 4 | `A 103 20` | 20 | 车辆 103 到达 | 停车场有空位,直接停入停车场 |
| 5 | `A 104 25` | 25 | 车辆 104 到达 | 停车场有空位,直接停入停车场 (车位: 3/3,已满) |
| 6 | `A 105 30` | 30 | 车辆 105 到达 | 停车场已满,进入便道等候 |
| 7 | `D 102 35` | 35 | 车辆 102 离开 | 停留 25 分钟,缴费 50 元。103、104 让路后返回。便道 105 进入停车场 |
| 8 | `D 104 40` | 40 | 车辆 104 离开 | 停留 15 分钟,缴费 30 元。105 在 104 之后,需让路再返回 |
| 9 | `E 0 0` | — | 退出程序 | 程序正常结束 |

**预期停车场状态变化汇总**：

| 时刻 | 操作 | 停车场 (栈底→栈顶) | 便道 |
|:----:|------|-------------------|------|
| 5 | A 101 | [101] | 空 |
| 10 | A 102 | [101, 102] | 空 |
| 15 | D 101 | [102] | 空 |
| 20 | A 103 | [102, 103] | 空 |
| 25 | A 104 | [102, 103, 104] | 空 |
| 30 | A 105 | [102, 103, 104] | [105] |
| 35 | D 102 | [103, 104, 105] | 空 |
| 40 | D 104 | [103, 105] | 空 |

**预期缴费输出**：

- 车辆 101：停留 $15 - 5 = 10$ 分钟,缴费 $10 \times 2 = 20$ 元
- 车辆 102：停留 $35 - 10 = 25$ 分钟,缴费 $25 \times 2 = 50$ 元
- 车辆 104：停留 $40 - 25 = 15$ 分钟,缴费 $15 \times 2 = 30$ 元

---

## 二、实验原理与详细设计

### 1. 选择数据结构的原因

本实验涉及三种不同角色的数据存储,分别选用最适合其行为特征的数据结构：

**(1) 停车场 —— 顺序栈**

选用顺序栈（数组实现）模拟停车场,理由如下：
- 停车场是单门狭长通道,车辆只能从同一端进出,这与栈的"后进先出"（LIFO）特性完全吻合——最后停入的车辆最先被挡住（也最先需要让路）；
- 让路操作本质上是反复的 pop/push,顺序栈的数组随机访问能力（$O(1)$）使得查找车辆位置（`parkPos` 函数）非常高效；
- 容量固定（$n=3$）,顺序栈不会动态扩容,数组实现简洁明了。

**(2) 便道 —— 链队列**

选用链队列（带头结点的单链表）模拟便道,理由如下：
- 便道上的车辆按"先来先服务"规则进入停车场,这正是队列的"先进先出"（FIFO）特性；
- 链式队列无需预设数组容量,虽本题设定了上限（$m=4$）,但链式结构支持灵活扩展；
- 车辆还可以从便道中间直接离开（不进入停车场）,链队列虽然在中间删除时需要遍历查找（$O(m)$）,但便道容量小（$m=4$）,遍历代价可忽略；
- 带头结点简化了入队/出队时对空队列边界条件的处理。

**(3) 让车道 —— 临时顺序栈**

选用与停车场相同的顺序栈结构：
- 让路时,需要将挡住目标车的车辆先"倒出来",再按原序"倒回去"；
- 利用栈的 LIFO 特性,两次 push/pop 逆转恰好恢复原始顺序：`出栈(逆序)` → `入临时栈` → `从临时栈出栈(再次逆序)` → `回停车场`,两次逆序=原序不变；
- 与停车场栈共用同一套栈操作函数,代码复用性高。

### 2. 结构体 / 类型定义

```c
/* ===== 车辆信息 ===== */
typedef struct {
    int plate;          // 车牌号（唯一标识）
    int time;           // 到达 / 进场时间
} Car;

/* ===== 停车场：顺序栈 ===== */
typedef struct {
    Car data[MAX_PARK]; // 栈数组,容量 = n
    int top;            // 栈顶指针（-1 表示空栈）
} ParkStack;

/* ===== 便道：链队列结点 ===== */
typedef struct QNode {
    Car car;            // 车辆信息
    struct QNode *next; // 指向下一结点
} QNode;

/* ===== 便道：链队列控制结构 ===== */
typedef struct {
    QNode *front;       // 队头指针（指向头结点）
    QNode *rear;        // 队尾指针（指向最后入队的结点）
    int count;          // 当前等候车辆数
} WaitQueue;
```

### 3. 核心函数说明

| 分类 | 函数 | 功能 | 关键点 / 复杂度 |
|:----:|------|------|:---------------|
| 停车场栈 | `initPark(ParkStack *s)` | 初始化空栈 | `top = -1` |
| 停车场栈 | `parkEmpty(ParkStack *s)` | 判空 | `top == -1` |
| 停车场栈 | `parkFull(ParkStack *s)` | 判满 | `top == MAX_PARK - 1` |
| 停车场栈 | `pushPark(ParkStack *s, Car c)` | 入栈（停车） | `++top`, $O(1)$ |
| 停车场栈 | `popPark(ParkStack *s)` | 出栈（取车） | `top--`, $O(1)$ |
| 停车场栈 | `topPark(ParkStack *s)` | 查看栈顶车辆 | $O(1)$ |
| 停车场栈 | `parkPos(ParkStack *s, int plate)` | 按车牌查找在栈中的位置（下标） | 线性遍历 $O(n)$ |
| 便道队列 | `initWait(WaitQueue *q)` | 初始化链队列,建立头结点 | 头结点 `front==rear` |
| 便道队列 | `waitEmpty(WaitQueue *q)` | 判空 | `front == rear` |
| 便道队列 | `waitFull(WaitQueue *q)` | 判满 | `count >= MAX_WAIT` |
| 便道队列 | `enWait(WaitQueue *q, Car c)` | 入队（尾插） | 更新 rear, $O(1)$ |
| 便道队列 | `deWait(WaitQueue *q)` | 出队（头删,进停车场） | 删除首元结点, $O(1)$ |
| 显示 | `showPark(ParkStack *s)` | 输出停车场当前状态 | 遍历栈数组, $O(n)$ |
| 显示 | `showWait(WaitQueue *q)` | 输出便道当前等候车辆 | 遍历链队列, $O(m)$ |
| **核心逻辑** | **`carArrive(park, wait, plate, now)`** | **车辆到达处理** | 优先停入停车场 → 停车场满则入便道 → 便道满则拒绝 |
| **核心逻辑** | **`carDepart(park, wait, plate, now)`** | **车辆离开处理** | 含让车道逻辑（详见下文） |

**`carDepart` 函数的三分支逻辑**：

```
carDepart(park, wait, plate, now)
  │
  ├── 情况①: 车在停车场内（parkPos ≠ -1）
  │    ├── 让路阶段：将目标车之后的所有车依次 pop → push 到临时栈 temp
  │    ├── 目标车 pop 出停车场，计算停留时间和费用，输出缴费信息
  │    ├── 回库阶段：将 temp 中的车依次 pop → push 回停车场（顺序自动恢复）
  │    └── 补位阶段：若便道非空，deWait 取第一辆车进入停车场（时间更新为 now）
  │
  ├── 情况②: 车在便道中（遍历链队列查找）
  │    └── pre/p 双指针遍历链队列，找到后删除结点并释放，输出"不收费"
  │
  └── 情况③: 车不在停车场也不在便道
       └── 输出"不在停车场也不在便道"
```

**让路与回库逻辑详解**（情况①的核心）：

假设停车场栈当前为 `[A, B, C, D]`（栈底到栈顶）,目标车 B（位置=1）要离开：

| 步骤 | 操作 | 停车场 | 让车道 temp |
|:----:|------|--------|:----------:|
| 初始 | — | [A, B, C, D] | 空 |
| 1 | pop D → push temp | [A, B, C] | [D] |
| 2 | pop C → push temp | [A, B] | [D, C] |
| 3 | pop B（目标车离开） | [A] | [D, C] |
| 4 | pop C → push park | [A, C] | [D] |
| 5 | pop D → push park | [A, C, D] | 空 |

结果：原序为 A, B, C, D,去除 B 后剩余 A, C, D,顺序保持不变。双栈 LIFO 操作天然保证了原序恢复。

### 4. 程序执行逻辑与函数调用关系

```
main()
  ├─ SetConsoleOutputCP(CP_UTF8)         设置控制台为 UTF-8 编码（中文输出支持）
  ├─ initPark(&park)                      初始化停车场栈
  ├─ initWait(&wait)                      初始化便道链队列
  ├─ 输出配置信息（容量、单价）
  │
  ├─ while(1)                              命令交互主循环
  │    ├─ scanf(" %c", &cmd)              读取命令字符
  │    │
  │    ├─ cmd == 'E'/'e'                  【退出】break 跳出循环
  │    │
  │    ├─ cmd == 'P'/'p'                  【显示】showPark + showWait
  │    │
  │    ├─ cmd == 'W'/'w'                  【显示便道】showWait
  │    │
  │    ├─ cmd == 'A'/'a'                  【到达】
  │    │    ├─ scanf plate, time           读取参数
  │    │    ├── carArrive(&park, &wait, plate, time)
  │    │    │    ├── parkFull? 否 → pushPark (停入停车场)
  │    │    │    ├── waitFull? 否 → enWait (进入便道等候)
  │    │    │    └── 满则输出拒绝信息
  │    │    └── showPark + showWait         显示当前状态
  │    │
  │    └── cmd == 'D'/'d'                 【离开】
  │         ├─ scanf plate, time           读取参数
  │         ├── carDepart(&park, &wait, plate, time)
  │         │    ├── parkPos ≠ -1? → 让路+离场+回库+便道补位
  │         │    ├── 遍历 wait 找到? → 从便道删除
  │         │    └── 都不是 → 输出"不存在"
  │         └── showPark + showWait         显示当前状态
  │
  └─ printf("程序结束。\n")
```

---

## 三、算法时空复杂度分析

### 1. 各核心操作时间复杂度

| 操作 | 时间复杂度 | 分析说明 |
|------|:---------:|----------|
| `pushPark` / `popPark` | $O(1)$ | 数组随机访问,仅修改 top 指针 |
| `parkPos` (查找) | $O(n)$ | 线性遍历栈数组,n 为停车场容量 |
| `enWait` / `deWait` | $O(1)$ | 链队列头插/尾删,仅修改指针 |
| 在便道中查找并删除某车 | $O(m)$ | 遍历单链表查找,m 为便道容量 |
| `carArrive` | $O(1)$ | 仅做一次判满/入栈或入队操作 |
| **`carDepart`（停车场）** | **$O(n)$** | 让路时需将目标车之后的所有车（最多 $n-1$ 辆）pop→push→pop→push |
| **`carDepart`（便道）** | **$O(m)$** | 在便道链表中遍历查找目标车并删除 |

### 2. 整体时间复杂度

- 每条 A 命令：$O(1)$
- 每条 D 命令：$O(n + m)$（n 和 m 均为常量,本题 n=3, m=4）
- k 条命令的整体时间复杂度：$O(k \cdot (n+m))$

由于 $n$ 和 $m$ 在本实验场景中都是小常数（$\le 4$）,实际运行极快。

### 3. 辅助空间复杂度

- **停车场栈**：$O(n)$ —— 同时最多存放 n 辆车的 `Car` 结构体
- **便道链队列**：$O(m)$ —— 同时最多存放 m 辆车的结点
- **临时让车道栈**：$O(n)$ —— 最坏情况需暂存 $n-1$ 辆车
- **总空间**：$O(n + m)$

与问题规模（n + m 辆车）完全匹配,无额外浪费。

### 4. 数据结构选择对复杂度的影响分析

| 存储结构 | 入停车场 | 出停车场（含让路） | 便道插入 | 便道离开 | 综合评价 |
|----------|:-------:|:-----------------:|:-------:|:-------:|:-------:|
| 顺序栈（停车场）| $O(1)$ ✅ | $O(n)$（让路需要）| — | — | 适合固定容量、单端进出 |
| 链队列（便道） | — | — | $O(1)$ ✅ | $O(1)$（队头）/ $O(m)$（中间） | FIFO 规则天然匹配 |
| 若用数组队列（便道） | — | — | $O(1)$ | $O(1)$（循环队列）/ $O(m)$（出队需搬移） | 存在假溢出问题,链式更灵活 |

本实验的三栈一队列组合是最优选择：栈模拟单门通道、队列模拟排队等候、临时栈实现让路顺序恢复,三种基础数据结构的特性各自得到充分利用。

---

## 四、程序使用说明与测试运行结果

### 1. 使用说明

- **运行环境**：Windows 11,gcc 16.1.0（MSYS2 ucrt64）,命令行交互
- **编译命令**：`gcc lab2_parking.c -o lab2_parking.exe`
- **运行命令**：`.\lab2_parking.exe`
- **交互命令格式**：

| 命令 | 格式 | 说明 |
|:----:|------|------|
| A | `A 车牌号 到达时间` | 车辆到达,自动判定进入停车场或便道 |
| D | `D 车牌号 离开时间` | 车辆离开,自动结算费用,触发让路/补位 |
| P | `P` | 显示停车场和便道的当前状态 |
| W | `W` | 仅显示便道等候车辆 |
| E | `E 0 0` | 退出程序 |

- **输入约定**：车牌号为整数,时间为整数（分钟）,以空格或回车分隔。

### 2. 测试运行结果

以下为按预设命令序列运行的程序完整输出：

```
=== 停车场模拟 ===
容量: 停车场=3 便道=4 单价=2元/分钟
命令: A 车牌 到达时间 | D 车牌 离去时间 | P 显示 | W 显示便道 | E 退出

> A 101 5
→ 车辆 101 停入停车场（时间 5）
停车场: [101] （栈顶→栈底方向）
便道: (空)

> A 102 10
→ 车辆 102 停入停车场（时间 10）
停车场: [101] [102] （栈顶→栈底方向）
便道: (空)

> D 101 15
→ 车辆 101 离场，停留 10 分钟，应缴费 20 元
停车场: [102] （栈顶→栈底方向）
便道: (空)

> A 103 20
→ 车辆 103 停入停车场（时间 20）
停车场: [102] [103] （栈顶→栈底方向）
便道: (空)

> A 104 25
→ 车辆 104 停入停车场（时间 25）
停车场: [102] [103] [104] （栈顶→栈底方向）
便道: (空)

> A 105 30
→ 车辆 105 进入便道等候（时间 30，车位已满）
停车场: [102] [103] [104] （栈顶→栈底方向）
便道: 105 （等候 1 辆）

> D 102 35
→ 车辆 102 离场，停留 25 分钟，应缴费 50 元
→ 车辆 105 从便道进入停车场
停车场: [103] [104] [105] （栈顶→栈底方向）
便道: (空)

> D 104 40
→ 车辆 104 离场，停留 15 分钟，应缴费 30 元
停车场: [103] [105] （栈顶→栈底方向）
便道: (空)

> E 0 0
程序结束。
```

### 3. 测试结果与预期对照

| 对比项 | 预期结果 | 实际输出 | 状态 |
|--------|----------|----------|:----:|
| 101 缴费 | $10 \times 2 = 20$ 元 | 20 元 | ✅ |
| 102 缴费 | $25 \times 2 = 50$ 元 | 50 元 | ✅ |
| 104 缴费 | $15 \times 2 = 30$ 元 | 30 元 | ✅ |
| 最终停车场 | [103, 105] | [103, 105] | ✅ |
| 最终便道 | 空 | 空 | ✅ |
| 停车场容量限制 | 最多 3 辆 | 105 被挡在便道 | ✅ |
| 便道补位 | 102 离开后 105 进入 | 105 从便道进入 | ✅ |
| 让路逻辑 | 104 离开时 105 让路后返回 | 105 正确返回 | ✅ |

全部 8 项对照均与预期一致 ✅

---

## 五、测试分析与实验总结

### 1. 测试过程中遇到的问题（Bug）及解决

| 问题 | 原因 | 解决方法 |
|------|------|---------|
| 中文输出乱码 | Windows 控制台默认使用 GBK 编码,而源码以 UTF-8 保存 | 在 `main()` 开头调用 `SetConsoleOutputCP(CP_UTF8)`（需 `#include <windows.h>`） |
| 便道出队后 `count` 未递减 | `deWait` 函数体中 `free(p)` 后忘记 `q->count--` | 在 `free(p)` 后添加 `q->count--` |
| `carDepart` 中 temp 栈弹出顺序错误导致车辆顺序反转 | 错误地在从 temp 回库时使用了 `for` 循环正序遍历而非 `while` 循环 pop | 改为 `while(!parkEmpty(&temp)) pushPark(park, popPark(&temp))`,利用栈的 LIFO 特性自动恢复原序 |
| 停车场离开后便道车辆进场时,停留时间计算有误 | 便道车辆进场时未更新 `w.time = now` | 在 `pushPark` 前添加 `w.time = now`,确保后续停留时间计算基于实际进场时刻 |
| D 命令查不到车时程序无反馈 | `carDepart` 末尾缺少"车辆不存在"分支 | 添加第三条分支：若 parkPos=-1 且便道中也未找到,则输出"不在停车场也不在便道" |
| 便道中删除中间结点（车辆从便道直接离开）时, `rear` 指针可能悬空 | 删除尾结点后未更新 `q->rear` | 在删除结点前判断 `if(wait->rear == p) wait->rear = pre` |
| `scanf` 读取命令字符时跳过空白符导致误读 | `scanf("%c", &cmd)` 会将前一次输入的回车符作为命令 | 改为 `scanf(" %c", &cmd)`（格式串中加空格,自动跳过空白字符） |

### 2. 实验收获

- 深入理解了**顺序栈与链队列的协作**：三种同构数据结构的组合使用是本题的核心——停车场栈模拟单门通道、链队列模拟 FIFO 等候、临时栈实现让路顺序恢复,体会到了"选择合适的数据结构"对问题建模的决定性作用；
- 掌握了 **"双栈逆序 = 原序"** 这一栈应用的经典技巧：在让路场景中,从停车场 pop 到 temp（一次逆序）再 pop 回停车场（二次逆序）,恰好保证车辆原排列顺序不变——这是栈 LIFO 特性的精妙运用；
- 加深了对**链队列带头结点设计**的理解：头结点 `front` 始终不存数据,使得 `enWait` 和 `deWait` 在处理空队列时无需额外的判空分支,代码简洁且不易出错；
- 认识到了**链队列"中间删除"**的边界处理要点：从便道中移除一辆车（不进入停车场）时,若恰好删除的是尾结点,必须同步更新 `rear` 指针,否则后续入队操作会出错；
- 学习了 **Windows 控制台中文编码处理**：`SetConsoleOutputCP(CP_UTF8)` 是 MSYS2 gcc 编译的程序在 Windows 终端输出中文的关键一步。

### 3. 可选扩展与优化思路

- **动态容量配置**：将 `MAX_PARK`、`MAX_WAIT`、`PRICE` 从宏定义改为运行时由用户输入,增加程序的通用性；
- **添加更多命令**：
  - `S 车牌号`：查询指定车辆当前位置（停车场 / 便道 / 不在）和已停留时长；
  - `L`：输出完整的停车记录日志（车牌、到达时间、离开时间、缴费金额）；
  - `R`：统计当前总收入和停车场使用率；
- **便道容量动态扩展**：将链队列的容量限制 `MAX_WAIT` 移除,使便道支持无限等候（仅在内存耗尽时拒绝）；
- **便道车辆按等待时间计费**：扩展功能,车辆在便道中等待超过一定时间也收取等待费；
- **图形化界面**：使用 Win32 API 或 Qt 实现可视化停车场模拟,动态展示车辆进出和让路过程。

---

## 六、附录：完整 C 语言源代码

```c
#include <stdio.h>
#include <stdlib.h>
#include <windows.h>

/* ============ 实验二：停车场问题 ============
 * 停车场：顺序栈（单门，只能从一端进出）
 * 便　道：链队列（先进先出排队）
 * 让车道：临时顺序栈（车辆让路时暂存）
 *
 * 测试数据：n=3 m=4 p=2
 * A 101 5 / A 102 10 / D 101 15 / A 103 20
 * A 104 25 / A 105 30 / D 102 35 / D 104 40 / E 0 0
 * ======================================== */

#define MAX_PARK 3      // 停车场容量 n
#define MAX_WAIT 4      // 便道容量 m
#define PRICE 2         // 单价 p（每分钟）

// ---------- 车辆信息 ----------
typedef struct {
    int plate;          // 车牌号
    int time;           // 到达时间
} Car;

// ===== 停车场：顺序栈 =====
typedef struct {
    Car data[MAX_PARK];
    int top;
} ParkStack;

void initPark(ParkStack *s)  { s->top = -1; }
int  parkEmpty(ParkStack *s) { return s->top == -1; }
int  parkFull(ParkStack *s)  { return s->top == MAX_PARK - 1; }
void pushPark(ParkStack *s, Car c) { if(!parkFull(s)) s->data[++(s->top)] = c; }
Car  popPark(ParkStack *s)         { return s->data[(s->top)--]; }
Car  topPark(ParkStack *s)         { return s->data[s->top]; }
int  parkPos(ParkStack *s, int plate) {         // 查找车牌在停车场中的位置（-1=不在）
    for(int i=0; i<=s->top; i++) if(s->data[i].plate==plate) return i;
    return -1;
}

// ===== 便道：链队列 =====
typedef struct QNode {
    Car car;
    struct QNode *next;
} QNode;
typedef struct {
    QNode *front, *rear;
    int count;                      // 当前等候车辆数
} WaitQueue;

void initWait(WaitQueue *q) {
    q->front = q->rear = (QNode*)malloc(sizeof(QNode)); // 头结点
    q->front->next = NULL;
    q->count = 0;
}
int  waitEmpty(WaitQueue *q) { return q->front == q->rear; }
int  waitFull(WaitQueue *q)  { return q->count >= MAX_WAIT; }
void enWait(WaitQueue *q, Car c) {                 // 入队（尾插）
    if(waitFull(q)) return;
    QNode *p = (QNode*)malloc(sizeof(QNode));
    p->car = c; p->next = NULL;
    q->rear->next = p; q->rear = p; q->count++;
}
Car deWait(WaitQueue *q) {                          // 出队（头删）
    Car c = {0,0};
    if(waitEmpty(q)) return c;
    QNode *p = q->front->next;
    c = p->car;
    q->front->next = p->next;
    if(q->rear == p) q->rear = q->front;
    free(p); q->count--;
    return c;
}

// ===== 输出函数 =====
void showPark(ParkStack *s) {
    if(parkEmpty(s)) { printf("停车场: (空)\n"); return; }
    printf("停车场: ");
    for(int i=0; i<=s->top; i++) printf("[%d] ", s->data[i].plate);
    printf("（栈顶→栈底方向）\n");
}
void showWait(WaitQueue *q) {
    if(waitEmpty(q)) { printf("便道: (空)\n"); return; }
    printf("便道: ");
    QNode *p = q->front->next;
    while(p){ printf("%d ", p->car.plate); p=p->next; }
    printf("（等候 %d 辆）\n", q->count);
}

// ===== 核心：车辆到达 =====
void carArrive(ParkStack *park, WaitQueue *wait, int plate, int now){
    Car c = {plate, now};
    if(!parkFull(park)){
        pushPark(park, c);
        printf("→ 车辆 %d 停入停车场（时间 %d）\n", plate, now);
    } else if(!waitFull(wait)){
        enWait(wait, c);
        printf("→ 车辆 %d 进入便道等候（时间 %d，车位已满）\n", plate, now);
    } else {
        printf("→ 车辆 %d 无法进入（停车场和便道均已满）\n", plate);
    }
}

// ===== 核心：车辆离去（含让车道逻辑）=====
void carDepart(ParkStack *park, WaitQueue *wait, int plate, int now){
    ParkStack temp; initPark(&temp);         // 临时让车道
    int found = 0, position = parkPos(park, plate);

    if(position != -1){  // 车在停车场内
        // ★ 让路：弹出后面的车，暂存到 temp
        while(park->top > position){
            pushPark(&temp, popPark(park));
        }
        // 目标车出库
        Car leaving = popPark(park);
        int stay = now - leaving.time;
        printf("→ 车辆 %d 离场，停留 %d 分钟，应缴费 %d 元\n", plate, stay, stay*PRICE);
        // 让路的车回库
        while(!parkEmpty(&temp)){
            pushPark(park, popPark(&temp));
        }
        // 便道第一辆车进入停车场（如果有）
        if(!waitEmpty(wait)){
            Car w = deWait(wait);
            w.time = now;                        // 进场时间更新为当前时刻
            pushPark(park, w);
            printf("→ 车辆 %d 从便道进入停车场\n", w.plate);
        }
    } else {              // 车在便道中
        // 遍历链队列查找并移除
        QNode *pre = wait->front, *p = wait->front->next;
        while(p && p->car.plate != plate){ pre = p; p = p->next; }
        if(p){ pre->next = p->next; if(wait->rear==p) wait->rear=pre;
                free(p); wait->count--;
                printf("→ 车辆 %d 从便道离去，不收费\n", plate); }
        else   printf("→ 车辆 %d 不在停车场也不在便道\n", plate);
    }
}

int main(void){
    SetConsoleOutputCP(CP_UTF8);

    ParkStack park; initPark(&park);
    WaitQueue wait; initWait(&wait);

    printf("=== 停车场模拟 ===\n");
    printf("容量: 停车场=%d 便道=%d 单价=%d元/分钟\n", MAX_PARK, MAX_WAIT, PRICE);
    printf("命令: A 车牌 到达时间 | D 车牌 离去时间 | P 显示 | W 显示便道 | E 退出\n\n");

    char cmd;
    int plate, time;
    while(1){
        printf("> ");
        scanf(" %c", &cmd);
        if(cmd=='E'||cmd=='e') break;
        if(cmd=='P'||cmd=='p') { showPark(&park); showWait(&wait); continue; }
        if(cmd=='W'||cmd=='w') { showWait(&wait); continue; }
        scanf("%d %d", &plate, &time);
        if(cmd=='A'||cmd=='a') carArrive(&park, &wait, plate, time);
        else if(cmd=='D'||cmd=='d') carDepart(&park, &wait, plate, time);
        showPark(&park); showWait(&wait); printf("\n");
    }
    printf("程序结束。\n");
    return 0;
}
```

---

<div style="text-align: center; margin-top: 2em;">

—— 实验报告完 ——

</div>
