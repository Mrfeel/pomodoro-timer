# 三维向量叉乘的计算方法

## 定义

两个三维向量 $\mathbf{a} = (a_1, a_2, a_3)$，$\mathbf{b} = (b_1, b_2, b_3)$ 的叉乘：

$$\mathbf{a} \times \mathbf{b} = (a_2b_3 - a_3b_2,\; a_3b_1 - a_1b_3,\; a_1b_2 - a_2b_1)$$

结果是一个**向量**（不同于点乘得标量），方向垂直于 $\mathbf{a}$ 和 $\mathbf{b}$ 所在平面，由右手定则确定。

---

## 方法一：行列式法（最推荐）

写成三阶行列式的形式，沿第一行展开：

$$\mathbf{a} \times \mathbf{b} = \begin{vmatrix} \mathbf{i} & \mathbf{j} & \mathbf{k} \\ a_1 & a_2 & a_3 \\ b_1 & b_2 & b_3 \end{vmatrix}$$

按第一行展开：

$$= \mathbf{i} \cdot \begin{vmatrix} a_2 & a_3 \\ b_2 & b_3 \end{vmatrix} - \mathbf{j} \cdot \begin{vmatrix} a_1 & a_3 \\ b_1 & b_3 \end{vmatrix} + \mathbf{k} \cdot \begin{vmatrix} a_1 & a_2 \\ b_1 & b_2 \end{vmatrix}$$

$$= \mathbf{i}(a_2b_3 - a_3b_2) - \mathbf{j}(a_1b_3 - a_3b_1) + \mathbf{k}(a_1b_2 - a_2b_1)$$

$$= (a_2b_3 - a_3b_2,\; a_3b_1 - a_1b_3,\; a_1b_2 - a_2b_1)$$

> ⚠️ 注意 $\mathbf{j}$ 分量前有一个**负号**！这是最常见的出错点。

---

## 方法二：口诀法（快速记忆）

把两个向量的分量写两行，掐头去尾：

```
a₁  a₂  a₃  a₁  a₂
│   │   ╲  ╱   │   │
b₁  b₂  b₃  b₁  b₂
```

- **$x$ 分量**：$a_2b_3 - a_3b_2$（中间两个交叉）
- **$y$ 分量**：$a_3b_1 - a_1b_3$（右边两个交叉）
- **$z$ 分量**：$a_1b_2 - a_2b_1$（左边两个交叉）

记忆规律：每个分量 = **另两个下标交叉相乘的差**。下标顺序是 $(1,2,3)$ 的轮换。

---

## 方法三：轮换记忆法

记住第一个分量：$a_2b_3 - a_3b_2$

然后**下标轮换**得到另外两个：$1 \to 2 \to 3 \to 1$

| 分量 | 下标替换 |
|------|----------|
| $x$（第1分量） | $a_2b_3 - a_3b_2$ |
| $y$（第2分量） | $a_3b_1 - a_1b_3$（每个下标 $+1$：$2\to3,3\to1$） |
| $z$（第3分量） | $a_1b_2 - a_2b_1$（每个下标再 $+1$：$3\to1,1\to2$） |

---

## 复习提纲中出现的三处叉乘

### 1. 曲面法向量 $\mathbf{r}_u \times \mathbf{r}_v$（例1-6）

$$\mathbf{r}_u = (\cos v, \sin v, 0), \quad \mathbf{r}_v = (-u\sin v, u\cos v, 1)$$

$$\mathbf{r}_u \times \mathbf{r}_v = \begin{vmatrix} \mathbf{i} & \mathbf{j} & \mathbf{k} \\ \cos v & \sin v & 0 \\ -u\sin v & u\cos v & 1 \end{vmatrix}$$

$$= \mathbf{i}(\sin v \cdot 1 - 0 \cdot u\cos v) - \mathbf{j}(\cos v \cdot 1 - 0 \cdot (-u\sin v)) + \mathbf{k}(\cos v \cdot u\cos v - \sin v \cdot (-u\sin v))$$

$$= (\sin v, -\cos v, u\cos^2 v + u\sin^2 v) = (\sin v, -\cos v, u)$$

### 2. 旋度 $\nabla \times \mathbf{F}$（Stokes公式）

$$\nabla \times \mathbf{F} = \begin{vmatrix} \mathbf{i} & \mathbf{j} & \mathbf{k} \\ \frac{\partial}{\partial x} & \frac{\partial}{\partial y} & \frac{\partial}{\partial z} \\ P & Q & R \end{vmatrix}$$

$$= \left(\frac{\partial R}{\partial y} - \frac{\partial Q}{\partial z},\; \frac{\partial P}{\partial z} - \frac{\partial R}{\partial x},\; \frac{\partial Q}{\partial x} - \frac{\partial P}{\partial y}\right)$$

### 3. 空间曲线法向量

两曲面交线在某点的切向量 $=$ 两曲面梯度在该点的叉乘。

---

## 练习

计算 $(1, 2, 3) \times (4, 5, 6)$：

$$\begin{vmatrix} \mathbf{i} & \mathbf{j} & \mathbf{k} \\ 1 & 2 & 3 \\ 4 & 5 & 6 \end{vmatrix} = \mathbf{i}(2\cdot6 - 3\cdot5) - \mathbf{j}(1\cdot6 - 3\cdot4) + \mathbf{k}(1\cdot5 - 2\cdot4)$$

$$= \mathbf{i}(12-15) - \mathbf{j}(6-12) + \mathbf{k}(5-8) = (-3, 6, -3)$$

---

## 一句总结

> **行列式法最可靠：$\mathbf{i}, \mathbf{j}, \mathbf{k}$ 放第一行，两个向量的分量放第二、三行，按第一行展开，$\mathbf{j}$ 项前面记得加负号。**
