
# 线性代数 第七章：内积空间与正交变换 详尽讲解

> 基于课堂 PPT (B1, 2026.03) | 杨思辰 · 未来技术学院 · 物理学专业
>
> 🔥 小测重点：**Schmidt 正交化、正交变换、正交相似对角化**

---

# 目录

1. [§1 欧几里得空间（内积空间）](#sec1)
2. [§2 标准正交基与 Schmidt 正交化](#sec2)
3. [§3 欧几里得空间的同构](#sec3)
4. [§4.1 正交变换](#sec4.1)
5. [§4.2 对称变换与正交相似对角化](#sec4.2)
6. [§5 酉空间](#sec5)
7. [附录1 正交投影](#app1)
8. [SO(n) 特殊正交群](#son)
9. [镜面反射](#mirror)
10. [反对称变换](#skew)
11. [小测速查卡片](#cheatsheet)

---

# §1 欧几里得空间（内积空间） {#sec1}

## 1.1 从高中数学出发的直观理解

在高中，我们学过向量的**点乘（内积）**：

$$\boldsymbol{\alpha} \cdot \boldsymbol{\beta} = a_1 b_1 + a_2 b_2 + a_3 b_3$$

有了点乘，我们就能定义向量的**长度**和**夹角**：

$$\|\boldsymbol{\alpha}\| = \sqrt{\boldsymbol{\alpha} \cdot \boldsymbol{\alpha}}$$

$$\cos\langle\boldsymbol{\alpha},\boldsymbol{\beta}\rangle = \frac{\boldsymbol{\alpha} \cdot \boldsymbol{\beta}}{\|\boldsymbol{\alpha}\| \cdot \|\boldsymbol{\beta}\|}$$

**本章的核心思想**就是把"点乘"这个操作推广到抽象的线性空间中去。只要给线性空间配备一个满足特定条件的"内积"运算，这个空间就有了"几何"——可以谈长度、角度、正交（垂直）。

> 💡 **通俗类比**：线性空间 = 给了你一堆向量；内积 = 给了你一把"尺子"和一个"量角器"。有了尺子和量角器，你才能测量长度和角度。**不同的内积定义 = 不同的测量方式**。

---

## 1.2 内积的严格定义 (Definition 1.1)

设 $V$ 是实数域 $\mathbb{R}$ 上的线性空间。如果有一个二元函数 $(\cdot, \cdot): V \times V \to \mathbb{R}$，满足以下四个条件，就称它为一个**内积**，称 $(V, (\cdot, \cdot))$ 为一个**欧几里得空间**（简称欧氏空间）：

**条件 (1) — 对称性：**
$$(\alpha, \beta) = (\beta, \alpha)$$

**条件 (2) — 第一变元的线性性（加法）：**
$$(\alpha_1 + \alpha_2, \beta) = (\alpha_1, \beta) + (\alpha_2, \beta)$$

**条件 (3) — 第一变元的齐次性：**
$$(k\alpha, \beta) = k(\alpha, \beta)$$

**条件 (4) — 正定性：**
$$(\alpha, \alpha) \geq 0, \quad \text{且} \quad (\alpha, \alpha) = 0 \Leftrightarrow \alpha = 0$$

> 💡 由 (1)+(2) 可得第二变元也是线性的：$(\alpha, \beta_1+\beta_2) = (\alpha, \beta_1) + (\alpha, \beta_2)$；由 (1)+(3) 可得 $(\alpha, k\beta) = k(\alpha, \beta)$。所以内积对两个变元都是线性的！

**简单性质：**

- $(\alpha, 0) = (0, \alpha) = 0$
- 若 $(\alpha, \gamma) = (\beta, \gamma)$ 对所有 $\gamma$ 成立，则 $\alpha = \beta$
- 若对某 $\alpha$ 有 $(\alpha, \beta) = 0$ 对所有 $\beta$ 成立，则 $\alpha = 0$

---

## 1.3 长度与夹角 (Definition 1.2)

给定欧氏空间 $(V, (\cdot, \cdot))$，对任意 $\alpha, \beta \in V$：

**长度（模）：**
$$\|\alpha\| = \sqrt{(\alpha, \alpha)}$$

**夹角：**
$$\langle\alpha, \beta\rangle = \arccos\frac{(\alpha, \beta)}{\|\alpha\| \cdot \|\beta\|}$$

**正交（垂直）：**
$$(\alpha, \beta) = 0 \quad \Leftrightarrow \quad \alpha \perp \beta$$

长度为 1 的向量称为**单位向量**。任何非零向量 $\alpha$ 都可以通过除以自己的长度得到同方向的单位向量：$\frac{\alpha}{\|\alpha\|}$。

**两个核心不等式：**

> ⭐ **Cauchy-Schwarz 不等式**（最重要！）：
> $$|(\alpha, \beta)| \leq \|\alpha\| \cdot \|\beta\|$$
> 当且仅当 $\alpha$ 与 $\beta$ 线性相关时取等号。

> **三角不等式**：
> $$\|\alpha + \beta\| \leq \|\alpha\| + \|\beta\|$$

> ⭐ Cauchy-Schwarz 不等式是本课程最重要的不等式之一。它是证明三角不等式和其他许多定理的基础。

---

## 1.4 标准内积的几个例子

### 📝 例 1：$\mathbb{R}^n$ 上的标准内积

这是最常用的内积，就是我们熟悉的高中向量的推广。对 $\alpha=(a_1,\ldots,a_n)^T$, $\beta=(b_1,\ldots,b_n)^T$：

$$(\alpha, \beta) = a_1 b_1 + a_2 b_2 + \cdots + a_n b_n = \alpha^T \beta$$

此时 $\|\alpha\| = \sqrt{a_1^2 + \cdots + a_n^2}$，夹角公式就是高中所学。

### 📝 例 2：加权内积

设 $A$ 是可逆实矩阵，定义：

$$(X, Y) = X^T A^T A Y = (AX)^T (AY)$$

这也是合法的内积。相当于先对向量做变换 $AX$，再做标准内积。

### 📝 例 3：矩阵空间的 Frobenius 内积

在 $n \times n$ 实矩阵空间 $\mathbb{R}^{n \times n}$ 中，定义：

$$(A, B) = \operatorname{tr}(A B^T)$$

这本质上就是把矩阵"拉直"成向量后做标准内积。

### 📝 例 4：多项式空间的内积

在次数不超过 $n$ 的多项式空间 $\mathbb{R}_n[x]$ 中，可以定义两种不同的内积：

**方式一（离散求和）：**
$$(f, g)_1 = \sum_{i=1}^{n+1} f(c_i) g(c_i) \quad \text{（在 $n+1$ 个点上取值再求和）}$$

**方式二（连续积分）：**
$$(f, g)_2 = \int_a^b f(x) g(x) \, dx \quad \text{（在区间 $[a,b]$ 上积分）}$$

> 💡 同一个线性空间可以定义不同的内积，从而得到不同的欧氏空间。例如取 $f(x)=x$，在 $(\cdot,\cdot)_1$ 下 $\|x\|^2 = \sum c_i^2$，而在 $(\cdot,\cdot)_2$ 下 $\|x\|^2 = \int x^2 dx = \frac{b^3-a^3}{3}$。

---

## 1.5 Gram 矩阵 (Definition 1.3-1.4)

设 $\alpha_1, \alpha_2, \ldots, \alpha_s$ 是欧氏空间 $V$ 中的 $s$ 个向量，定义 $s \times s$ 矩阵：

$$G(\alpha_1, \ldots, \alpha_s) = \begin{pmatrix}
(\alpha_1, \alpha_1) & (\alpha_1, \alpha_2) & \cdots & (\alpha_1, \alpha_s) \\
(\alpha_2, \alpha_1) & (\alpha_2, \alpha_2) & \cdots & (\alpha_2, \alpha_s) \\
\vdots & \vdots & \ddots & \vdots \\
(\alpha_s, \alpha_1) & (\alpha_s, \alpha_2) & \cdots & (\alpha_s, \alpha_s)
\end{pmatrix}$$

这个矩阵称为 **Gram 矩阵**。它的第 $i$ 行第 $j$ 列就是 $\alpha_i$ 和 $\alpha_j$ 的内积。

**Gram 矩阵的核心作用：** 给定基 $\varepsilon_1, \ldots, \varepsilon_n$，Gram 矩阵 $G$ 完全确定了整个空间的内积结构。对任意两个向量：

$$u = (\varepsilon_1, \ldots, \varepsilon_n) X, \quad v = (\varepsilon_1, \ldots, \varepsilon_n) Y \quad \text{（$X,Y$ 是坐标列向量）}$$

$$(u, v) = X^T G Y$$

这样，在抽象空间中计算内积，就转化为坐标向量的矩阵乘法。

**基变换下的 Gram 矩阵：** 如果换一组基，新基下的 Gram 矩阵 $\tilde{G}$ 与旧基下的 $G$ 满足：

$$\tilde{G} = T^T G T$$

其中 $T$ 是从旧基到新基的过渡矩阵。两个矩阵 $A, B$ 称为**合同**，如果存在可逆矩阵 $T$ 使得 $B = T^T A T$。

### 📝 例 5：用 Gram 矩阵定义内积

在 $\mathbb{R}^2$ 上，定义内积如下：

$$(\begin{pmatrix}x_1\\x_2\end{pmatrix}, \begin{pmatrix}y_1\\y_2\end{pmatrix}) = x_1 y_1 - x_2 y_1 - x_1 y_2 + 4 x_2 y_2$$

这意味着在标准基 $e_1, e_2$ 下的 Gram 矩阵为：

$$G = \begin{pmatrix} 1 & -1 \\ -1 & 4 \end{pmatrix}$$

---

## 1.6 子空间的正交 (Definition 1.6)

两个子集 $S_1, S_2$ 称为**正交**（记为 $S_1 \perp S_2$），如果对任意 $\alpha \in S_1, \beta \in S_2$ 都有 $(\alpha, \beta) = 0$。

给定子集 $S$，定义其**正交补**：

$$S^\perp = \{ \alpha \in V \mid \alpha \perp \text{ 每个 } S \text{ 中的向量} \}$$

> 💡 如果 $W = \operatorname{span}\{\alpha_1, \ldots, \alpha_s\}$，那么 $v \in W^\perp$ 当且仅当 $v$ 与每个生成元 $\alpha_i$ 都正交。这大大简化了正交补的计算——只需检查生成元即可。

---

# §2 标准正交基与 Schmidt 正交化 {#sec2}

## 2.1 标准正交基的定义

在 $n$ 维欧氏空间中，一组基 $\varepsilon_1, \ldots, \varepsilon_n$ 称为**标准正交基**，如果满足：

$$(\varepsilon_i, \varepsilon_j) = \delta_{ij} = \begin{cases} 0 & i \neq j \\ 1 & i = j \end{cases}$$

即：**(1) 两两正交**，**(2) 每个都是单位向量**。

**标准正交基的好处：**

- 在标准正交基下，Gram 矩阵就是单位矩阵 $I_n$
- 任意向量的坐标 = 该向量与基向量的内积：若 $\alpha = \sum x_i \varepsilon_i$，则 $x_i = (\alpha, \varepsilon_i)$
- 两个向量的内积 = 坐标向量的标准内积：$(u, v) = X^T Y = \sum x_i y_i$
- **计算变得极其简单——就像在 $\mathbb{R}^n$ 中一样！**

> 💡 标准正交基让抽象的欧氏空间"变回"我们熟悉的 $\mathbb{R}^n$ 标准内积空间。这是本章一切操作的目标。

---

## 2.2 🔥 Gram-Schmidt 正交化 (Theorem 2.2)

> 🔥 **这是小测必考内容，务必熟练掌握计算步骤！**

给定任意一组线性无关的向量 $\alpha_1, \ldots, \alpha_n$，Gram-Schmidt 正交化过程可以构造出一组标准正交基。

### 核心思想（几何直觉）

想象你在搭积木：
- $\alpha_2$ 里面有一部分"投影"在 $\beta_1$ 方向上 → 把这部分**减掉** → 剩下的 $\beta_2$ 就与 $\beta_1$ 垂直了
- $\alpha_3$ 里面有一部分在 $\beta_1$ 方向上，一部分在 $\beta_2$ 方向上 → **都减掉** → $\beta_3$ 与三者都垂直
- 以此类推……

### 正交化公式

**第一步（正交化）—— 先得到正交向量组：**

$$\boxed{\beta_1 = \alpha_1}$$

$$\boxed{\beta_2 = \alpha_2 - \frac{(\alpha_2, \beta_1)}{(\beta_1, \beta_1)} \beta_1}$$

$$\boxed{\beta_3 = \alpha_3 - \frac{(\alpha_3, \beta_1)}{(\beta_1, \beta_1)} \beta_1 - \frac{(\alpha_3, \beta_2)}{(\beta_2, \beta_2)} \beta_2}$$

一般形式：

$$\boxed{\beta_k = \alpha_k - \sum_{i=1}^{k-1} \frac{(\alpha_k, \beta_i)}{(\beta_i, \beta_i)} \beta_i}$$

**第二步（单位化）—— 得到标准正交基：**

$$\boxed{\eta_i = \frac{\beta_i}{\|\beta_i\|}}$$

> 💡 **记忆口诀**：$\beta_k = \alpha_k$ 减去它在之前所有 $\beta_i$ 上的投影。投影系数 = $\frac{(\alpha_k, \beta_i)}{(\beta_i, \beta_i)}$。

**定理的另一个重要结论：** Gram 矩阵 $G$ 与过渡矩阵 $T$（上三角）满足 $G = T^T T$。这意味着从 Gram 矩阵可以"反解"出标准正交基。

---

## 2.3 🔥 Example 6（基础 Schmidt 正交化）

> 给定 $\mathbb{R}^3$ 中的三个向量（标准内积）：
> $$\alpha_1 = (0, 0, 1)^T, \quad \alpha_2 = (0, 1, 1)^T, \quad \alpha_3 = (1, 1, 1)^T$$

**解：**

**第1步：** $\beta_1 = \alpha_1 = (0, 0, 1)^T$，检查 $\|\beta_1\| = 1$ ✓（已经单位化）

**第2步：** 计算 $\alpha_2$ 在 $\beta_1$ 上的投影并减去：
$$(\alpha_2, \beta_1) = 0 \cdot 0 + 1 \cdot 0 + 1 \cdot 1 = 1$$
$$(\beta_1, \beta_1) = 1$$
$$\beta_2 = \alpha_2 - \frac{1}{1}\beta_1 = (0,1,1)^T - (0,0,1)^T = (0, 1, 0)^T, \quad \|\beta_2\| = 1 \ \checkmark$$

**第3步：** 减去 $\alpha_3$ 在 $\beta_1$ 和 $\beta_2$ 上的投影：
$$(\alpha_3, \beta_1) = 1, \quad (\alpha_3, \beta_2) = 1$$
$$\beta_3 = (1,1,1)^T - 1 \cdot (0,0,1)^T - 1 \cdot (0,1,0)^T = (1, 0, 0)^T, \quad \|\beta_3\| = 1 \ \checkmark$$

**结果：** 标准正交基恰好是 $\{(1,0,0), (0,1,0), (0,0,1)\}$，即 $\mathbb{R}^3$ 的标准基。

---

## 2.4 Example 7（已知 Gram 矩阵求标准正交基）

> 设三维欧氏空间 $V$ 在某基 $\alpha_1, \alpha_2, \alpha_3$ 下的 Gram 矩阵为：
> $$G = \begin{pmatrix} 10 & 0 & 1 \\ 0 & 10 & -2 \\ 1 & -2 & 2 \end{pmatrix}$$
> 求 $V$ 的一组标准正交基。

**解（两种等价方法）：**

**方法一（直接对原向量做 Schmidt）：**

- $\eta_1 = \frac{\alpha_1}{\|\alpha_1\|} = \frac{\alpha_1}{\sqrt{10}}$（因为 $(\alpha_1, \alpha_1) = G_{11} = 10$）
- $\beta_2 = \alpha_2 - (\alpha_2, \eta_1)\eta_1$。由 $G_{21} = 0$，$\beta_2 = \alpha_2$，$\|\beta_2\| = \sqrt{10}$，$\eta_2 = \frac{\alpha_2}{\sqrt{10}}$
- $\beta_3 = \alpha_3 - (\alpha_3, \eta_1)\eta_1 - (\alpha_3, \eta_2)\eta_2$，代入各内积值继续计算

**方法二（先不做单位化，最后一起单位化）—— 推荐考试时使用！**

$$\beta_1 = \alpha_1$$

$$\beta_2 = \alpha_2 - \frac{(\alpha_2, \alpha_1)}{(\alpha_1, \alpha_1)}\alpha_1 = \alpha_2 - \frac{0}{10}\alpha_1 = \alpha_2$$

$$\beta_3 = \alpha_3 - \frac{(\alpha_3, \alpha_1)}{(\alpha_1, \alpha_1)}\alpha_1 - \frac{(\alpha_3, \alpha_2)}{(\alpha_2, \alpha_2)}\alpha_2 = \alpha_3 - \frac{1}{10}\alpha_1 - \frac{-2}{10}\alpha_2 = -\frac{1}{10}\alpha_1 + \frac{1}{5}\alpha_2 + \alpha_3$$

然后分别单位化：

$$\eta_1 = \frac{\alpha_1}{\sqrt{10}}, \quad \eta_2 = \frac{\alpha_2}{\sqrt{10}}, \quad \eta_3 = \frac{-\frac{1}{10}\alpha_1 + \frac{1}{5}\alpha_2 + \alpha_3}{\|\cdots\|}$$

> 💡 **考试技巧**：方法二在实际计算中更方便，因为避免了早期处理根号。推荐先正交化（不管长度），最后一起单位化。

---

## 2.5 Corollary 2.3（重要推论）

**(1)** 任何 $n$ 维欧氏空间都存在标准正交基。$n$ 个两两正交的非零向量一定线性无关，从而是基。

**(2)** 对任何子空间 $W$，有 $V = W \oplus W^\perp$。即整个空间可以分解为一个子空间和它的正交补的直和。这是正交投影的理论基础。

**(3)** $(W^\perp)^\perp = W$。正交补的正交补等于原空间。

---

## 2.6 正交矩阵 (Definition 2.4)

一个 $n$ 阶实方阵 $Q$ 称为**正交矩阵**，如果满足：

$$\boxed{Q^T Q = I_n \quad \text{即} \quad Q^{-1} = Q^T}$$

**等价说法：**

- $Q$ 的列向量是 $\mathbb{R}^n$ 的一组标准正交基（两两正交且均为单位向量）
- $Q$ 的行向量也是一组标准正交基
- $Q$ 保持内积：$(QX, QY) = X^T Q^T Q Y = X^T Y = (X, Y)$
- $Q$ 保长度：$\|QX\| = \|X\|$

**重要性质：**

- $I_n$ 是正交矩阵
- 若 $Q_1, Q_2$ 是正交矩阵，则 $Q_1 Q_2$ 也是
- 若 $Q$ 是正交矩阵，则 $Q^{-1} = Q^T$ 也是
- **正交矩阵的行列式** $\det(Q) = \pm 1$

> 💡 **与标准正交基的联系**：若 $\varepsilon_1,\ldots,\varepsilon_n$ 是标准正交基，$\eta_1,\ldots,\eta_n$ 是新基，且 $(\eta_1\ldots\eta_n)=(\varepsilon_1\ldots\varepsilon_n)T$，则 $\eta$ 是标准正交基 $\Leftrightarrow$ $T$ 是正交矩阵。

---

## 2.7 QR 分解 (Theorem 2.5)

若 $A$ 是可逆实方阵，则存在唯一的分解：

$$\boxed{A = Q R}$$

其中 $Q$ 是正交矩阵，$R$ 是对角线元素为正的上三角矩阵。

QR 分解本质上就是对 $A$ 的列向量做 Gram-Schmidt 正交化：$Q$ 的列是正交化后得到的标准正交向量组，$R$ 记录了正交化过程中的系数。

---

# §3 欧几里得空间的同构 {#sec3}

两个欧氏空间 $(V_1, (\cdot,\cdot)_1)$ 和 $(V_2, (\cdot,\cdot)_2)$ 称为**同构**，如果存在一个线性映射 $\sigma: V_1 \to V_2$ 满足：

1. $\sigma$ 是线性映射（保持加法与数乘）
2. $\sigma$ 是双射（一一对应）
3. $(\sigma(\alpha), \sigma(\beta))_2 = (\alpha, \beta)_1$ —— **保持内积！**

**判断同构的关键：** 两个欧氏空间同构 $\Leftrightarrow$ 它们维数相同。

**核心结论：任何 $n$ 维欧氏空间都同构于 $\mathbb{R}^n$（配备标准内积）。** 这意味着你可以在纸上用 $\mathbb{R}^n$ 的坐标来研究任何有限维欧氏空间，前提是选标准正交基。

> 💡 这就是为什么选标准正交基如此重要——在标准正交基下，一切计算都退化成了 $\mathbb{R}^n$ 中的普通向量运算。

---

# §4.1 🔥 正交变换 {#sec4.1}

> 🔥 **正交变换是小测重点考查内容！**

## 4.1.1 定义与等价条件

设 $\mathcal{A}$ 是欧氏空间 $V$ 上的线性变换。若对任意 $\alpha, \beta \in V$，都有：

$$\boxed{(\mathcal{A}(\alpha), \mathcal{A}(\beta)) = (\alpha, \beta)}$$

则称 $\mathcal{A}$ 为**正交变换**。

### ⭐ 正交变换的三个等价刻画（非常重要，考试常考判断）

| 条件 | 含义 | 判定方法 |
|------|------|----------|
| ① 保内积 | $(\mathcal{A}\alpha, \mathcal{A}\beta) = (\alpha, \beta)$ | 直接验证内积 |
| ② 保长度 | $\|\mathcal{A}\alpha\| = \|\alpha\|$ | 验证模长不变 |
| ③ 保标准正交基 | $\{\varepsilon_i\}$ 是标准正交基 ⇒ $\{\mathcal{A}\varepsilon_i\}$ 也是 | 看基的像 |

> ⭐ **这三个条件是等价的！** 即只要满足其中一个，就自动满足另外两个。考试中根据题目给的条件灵活使用。

### 📝 Example 13（PPT 中的四种典型情况）

**(1)** 恒等变换 $E$ 一定是正交变换（显然保内积）

**(2)** 如果定义 $\mathcal{A}$ 满足 $(\mathcal{A}\alpha, \mathcal{A}\beta) = (\alpha, \beta)$，则 $\mathcal{A}$ 是正交变换（直接由定义）

**(3)** 如果定义 $\mathcal{A}$ 只满足 $\|\mathcal{A}\alpha\| = \|\alpha\|$，则 $\mathcal{A}$ 也是正交变换（条件② → 条件①）

**(4)** 如果能验证 $\mathcal{A}$ 把某个标准正交基映成标准正交基，则 $\mathcal{A}$ 是正交变换（条件③）

---

## 4.1.2 正交变换与正交矩阵的关系

取 $V$ 的一组标准正交基，线性变换 $\mathcal{A}$ 在该基下的矩阵记为 $A$。则：

$$\boxed{\mathcal{A} \text{ 是正交变换 } \quad \Leftrightarrow \quad A \text{ 是正交矩阵（} A^T A = I_n \text{）}}$$

**证明思路：** 在标准正交基下，$(\mathcal{A}\varepsilon_i, \mathcal{A}\varepsilon_j)$ 对应矩阵 $A$ 的第 $j$ 列与第 $i$ 列的内积。如果 $\mathcal{A}$ 是正交变换，则 $(\mathcal{A}\varepsilon_i, \mathcal{A}\varepsilon_j) = (\varepsilon_i, \varepsilon_j) = \delta_{ij}$，所以 $A^T A = I$。

---

## 4.1.3 正交变换的性质 (Theorem 4.3)

- 恒等变换 $E$ 是正交变换
- 若 $\mathcal{A}, \mathcal{B}$ 是正交变换，则复合 $\mathcal{A}\mathcal{B}$ 也是正交变换
- 若 $\mathcal{A}$ 是正交变换，则逆变换 $\mathcal{A}^{-1}$ 也是正交变换

> 这意味着所有正交变换构成一个群（在复合运算下封闭）。

---

## 4.1.4 旋转与镜像 (Definition 4.4, Proposition 4.5)

根据行列式的值，正交变换分为两类：

$$\boxed{\text{第一类（旋转）：} \det(A) = +1 \quad \text{—— 保持定向}}$$

$$\boxed{\text{第二类（镜像）：} \det(A) = -1 \quad \text{—— 翻转定向}}$$

**特征值的性质：**

- 正交矩阵在复数域中的特征值模长均为 $1$（即 $|\lambda| = 1$）
- 实特征值只能是 $1$ 或 $-1$
- 若 $\det = +1$ 且 $n$ 为奇数，则必有特征值 $1$
- 若 $\det = -1$ 且 $n$ 为奇数，则必有特征值 $-1$

> 💡 **直观理解**：在三维空间中，第一类正交变换就是绕某个轴的旋转（刚体运动），第二类是旋转后再做一次镜像反射。你可以想象成：第一类是你的手在空间中转来转去（保持"手性"），第二类是你照镜子看到的手（左右颠倒，手性翻转）。

---

# §4.2 🔥 对称变换与正交相似对角化 {#sec4.2}

> 🔥 **这是小测最重要的考点！实对称矩阵的正交相似对角化必考！**

## 4.2.1 对称变换的定义 (Definition 4.6)

设 $\mathcal{A}$ 是欧氏空间 $V$ 上的线性变换。若对任意 $\alpha, \beta \in V$，都有：

$$\boxed{(\mathcal{A}(\alpha), \beta) = (\alpha, \mathcal{A}(\beta))}$$

则称 $\mathcal{A}$ 为**对称变换**。

**对称变换 ↔ 对称矩阵：** 在标准正交基下，对称变换的矩阵就是实对称矩阵（$A^T = A$），反之亦然。

**证明思路：** 在标准正交基下，$(\mathcal{A}\varepsilon_i, \varepsilon_j) = a_{ji}$（第 $j$ 行第 $i$ 列），$(\varepsilon_i, \mathcal{A}\varepsilon_j) = a_{ij}$（第 $i$ 行第 $j$ 列）。对称变换要求这俩相等，即 $a_{ji} = a_{ij}$，这正是对称矩阵的定义。

---

## 4.2.2 对称变换的例子

**📝 例 14（正交投影）：** 设 $W$ 是 $V$ 的子空间，$P_W$ 是向 $W$ 的**正交投影**。对任意向量，$P_W(\alpha)$ 是 $\alpha$ 在 $W$ 中的"最佳逼近"。正交投影一定是对称变换。

**📝 例 15（镜面反射）：** 关于某向量的镜像反射也是对称变换。

---

## 4.2.3 对称变换的核心性质

> ⭐ **性质 1（实特征值）：** 实对称矩阵的特征值都是实数。
>
> *(这是极其重要的性质！一般的实矩阵特征值可能是复数，但对称矩阵保证了特征值全为实数。)*

> ⭐ **性质 2（特征向量正交）：** 实对称矩阵属于**不同特征值**的特征向量自动正交。

> ⭐ **性质 3（可对角化）：** 实对称矩阵一定可以对角化，而且可以用**正交矩阵**来对角化。

这三个性质递进地构成了正交相似对角化的理论基础：
1. 特征值实 →
2. 不同特征值的特征向量自动正交 →
3. 同一特征值内部做 Schmidt 正交化 →
4. 拼起来就是正交矩阵

---

## 4.2.4 🔥 正交相似对角化的完整流程 (Theorem 4.11)

**目标：** 对实对称矩阵 $A$，找一个正交矩阵 $T$，使得：

$$\boxed{T^{-1} A T = T^T A T = \operatorname{diag}(\lambda_1, \lambda_2, \ldots, \lambda_n)}$$

**完整操作步骤：**

**第1步：** 求 $A$ 的所有特征值 $\lambda_1, \lambda_2, \ldots, \lambda_n$（通过 $|\lambda I - A| = 0$）

**第2步：** 对每个特征值 $\lambda_i$，求解齐次线性方程组 $(\lambda_i I - A)X = 0$，得到特征子空间 $V_{\lambda_i}$ 的一组基

**第3步：** 在每个特征子空间 $V_{\lambda_i}$ 内部，对基向量做 Gram-Schmidt 正交化和单位化

> ⚠️ **注意：不同特征子空间的向量已自动正交（性质 2），不需要再做处理！**

**第4步：** 将所有正交化后的向量按特征值顺序拼成正交矩阵 $T$

**第5步：** 验证：$T^T A T = \operatorname{diag}(\lambda_1, \ldots, \lambda_n)$。对角线上特征值的排列顺序与 $T$ 中列向量的顺序一致。

> 💡 **记忆口诀**：求特征值 → 求特征向量 → 同一特征值内部做 Schmidt（不同特征值之间不用！） → 拼起来。

---

## 4.2.5 🔥 Example 16（PPT 核心例题，必须掌握）

> 给定实对称矩阵：
> $$A = \begin{pmatrix} 1 & 2 & 4 \\ 2 & -2 & 2 \\ 4 & 2 & 1 \end{pmatrix}$$
> 求正交矩阵 $T$ 使 $T^T A T$ 为对角矩阵。

### 详细解答：

**第1步：求特征多项式**

$$\begin{aligned}
|\lambda I - A| &= \begin{vmatrix} \lambda-1 & -2 & -4 \\ -2 & \lambda+2 & -2 \\ -4 & -2 & \lambda-1 \end{vmatrix} \\
&= (\lambda - 6)(\lambda + 3)^2
\end{aligned}$$

所以特征值为：$\lambda_1 = 6$（单根），$\lambda_2 = \lambda_3 = -3$（二重根）。

**第2步：对 $\lambda_1 = 6$，解 $(6I - A)X = 0$**

得到 $Y_1 = (2, 2, 1)^T$。单位化：

$$X_1 = \frac{Y_1}{\|Y_1\|} = \left(\frac{2}{3}, \frac{2}{3}, \frac{1}{3}\right)^T$$

> （$\|Y_1\| = \sqrt{4+4+1} = 3$）

**第3步：对 $\lambda_2 = \lambda_3 = -3$，解 $(-3I - A)X = 0$**

得到两个线性无关的特征向量：

$$Y_2 = (1, -2, 0)^T, \quad Y_3 = (0, -2, 1)^T$$

但 $Y_2$ 和 $Y_3$ **不正交**！计算 $(Y_2, Y_3) = 1 \cdot 0 + (-2) \cdot (-2) + 0 \cdot 1 = 4 \neq 0$。

所以在特征子空间内做 Schmidt 正交化：

$$\begin{aligned}
Z_2 &= Y_2 = (1, -2, 0)^T \\
Z_3 &= Y_3 - \frac{(Y_3, Z_2)}{(Z_2, Z_2)} Z_2 = (0,-2,1)^T - \frac{4}{5}(1,-2,0)^T = \left(-\frac{4}{5}, -\frac{2}{5}, 1\right)^T
\end{aligned}$$

然后分别单位化：

$$X_2 = \frac{Z_2}{\|Z_2\|} = \left(\frac{1}{\sqrt{5}}, -\frac{2}{\sqrt{5}}, 0\right)^T$$

$$X_3 = \frac{Z_3}{\|Z_3\|} = \left(-\frac{4}{3\sqrt{5}}, -\frac{2}{3\sqrt{5}}, \frac{5}{3\sqrt{5}}\right)^T$$

**第4步：拼成正交矩阵 $T$**

$$T = (X_1, X_2, X_3) = \begin{pmatrix} \frac{2}{3} & \frac{1}{\sqrt{5}} & -\frac{4}{3\sqrt{5}} \\[4pt] \frac{2}{3} & -\frac{2}{\sqrt{5}} & -\frac{2}{3\sqrt{5}} \\[4pt] \frac{1}{3} & 0 & \frac{5}{3\sqrt{5}} \end{pmatrix}$$

**验证：**

$$T^T A T = \begin{pmatrix} 6 & & \\ & -3 & \\ & & -3 \end{pmatrix} \quad \checkmark$$

---

## 4.2.6 从抽象角度看正交相似对角化

PPT 还给出了从抽象线性变换角度看的三个步骤：

1. 选 $V$ 的任意一组标准正交基 $\varepsilon_1, \ldots, \varepsilon_n$，写出 $\mathcal{A}$ 在这组基下的矩阵 $A$（对称矩阵）
2. 用上面的方法找到正交矩阵 $T$，使得 $T^{-1} A T = \operatorname{diag}(\lambda_1, \ldots, \lambda_n)$
3. 令 $(\eta_1, \ldots, \eta_n) = (\varepsilon_1, \ldots, \varepsilon_n) T$，则 $\eta_1, \ldots, \eta_n$ 也是标准正交基，且 $\mathcal{A}$ 在新基下是对角矩阵

> ⭐ **结论：对称变换在适当的（标准正交）基下一定是对角矩阵。**

---

# §5 酉空间（复内积空间） {#sec5}

把欧几里得空间的实数域换成复数域，内积定义做适当修改，就得到**酉空间**（又称复内积空间）。

## 5.1 复内积的定义 (Definition 5.1)

设 $V$ 是复数域 $\mathbb{C}$ 上的线性空间。一个二元函数 $(\cdot,\cdot): V \times V \to \mathbb{C}$ 称为内积，如果满足：

**(1) 共轭对称性：** $(\alpha, \beta) = \overline{(\beta, \alpha)}$ （注意不是普通的对称！）

**(2) 第二变元线性：** $(\alpha, \beta_1 + \beta_2) = (\alpha, \beta_1) + (\alpha, \beta_2)$

**(3) 第二变元齐次：** $(\alpha, k\beta) = k(\alpha, \beta)$

**(4) 正定性：** $(\alpha, \alpha) \geq 0$，且 $(\alpha, \alpha) = 0 \Leftrightarrow \alpha = 0$

**与实内积的关键区别：**

- 对称性变为共轭对称性
- 第一变元不是线性的！$(k\alpha, \beta) = \bar{k}(\alpha, \beta)$ —— 常数提出来要取共轭
- 第二变元是线性的：$(\alpha, k\beta) = k(\alpha, \beta)$

> 💡 **记忆**：复内积对第一变元是"共轭线性"的，对第二变元是线性的。PPT 上的约定是第二变元线性。

## 5.2 标准复内积 (Example 17)

在 $\mathbb{C}^n$ 上，标准的复内积定义为：

$$(\alpha, \beta) = \bar{a}_1 b_1 + \bar{a}_2 b_2 + \cdots + \bar{a}_n b_n = \bar{\alpha}^T \beta$$

此时 $\|\alpha\| = \sqrt{|a_1|^2 + \cdots + |a_n|^2}$。

## 5.3 酉矩阵

在酉空间中，标准正交基之间的过渡矩阵称为**酉矩阵**，满足 $\bar{U}^T U = I$（即 $U$ 的共轭转置等于逆）。

> 💡 酉空间的大部分结论与欧氏空间平行，只需把"转置"换为"共轭转置"即可。小测重点在实空间部分。

---

# 附录1 正交投影 {#app1}

## A1.1 一维子空间上的投影

设 $\eta$ 是欧氏空间 $V$ 中一个非零向量（方向）。对任意 $\alpha \in V$，$\alpha$ 在 $\eta$ 方向上的**正交投影**为：

$$\boxed{\hat{\alpha} = \frac{(\alpha, \eta)}{(\eta, \eta)} \cdot \eta}$$

剩余部分 $\alpha - \hat{\alpha}$ 与 $\eta$ 正交。

**推导**：设 $\hat{\alpha} = \hat{x} \cdot \eta$。由正交条件 $(\alpha - \hat{\alpha}, \eta) = 0$，即 $(\alpha, \eta) - \hat{x}(\eta, \eta) = 0$，解得 $\hat{x} = \frac{(\alpha, \eta)}{(\eta, \eta)}$。

投影算子 $P(\alpha) = \hat{\alpha}$ 是线性变换，且满足 $P^2 = P$（幂等性）。

## A1.2 一般子空间上的投影

设 $W$ 是 $V$ 的子空间，$\eta_1, \ldots, \eta_s$ 是 $W$ 的一组基。对任意 $\alpha \in V$，求其在 $W$ 上的正交投影 $\hat{\alpha} \in W$，满足 $(\alpha - \hat{\alpha}) \perp W$。

**解法：** 设 $\hat{\alpha} = \sum_{i=1}^{s} \hat{x}_i \eta_i$。由 $(\alpha - \hat{\alpha}, \eta_j) = 0$ 对所有 $j=1,\ldots,s$ 成立，得到关于 $\hat{x}_i$ 的线性方程组：

$$G(\eta_1, \ldots, \eta_s) \cdot (\hat{x}_1, \ldots, \hat{x}_s)^T = ((\alpha, \eta_1), \ldots, (\alpha, \eta_s))^T$$

其中 $G$ 是基向量的 Gram 矩阵。如果 $\eta_1, \ldots, \eta_s$ 线性无关，Gram 矩阵可逆，方程组有唯一解。

## A1.3 最佳逼近定理

> ⭐ **定理：正交投影 $\hat{\alpha}$ 是 $W$ 中离 $\alpha$ 最近的向量：**
> $$\|\alpha - \hat{\alpha}\| < \|\alpha - w\| \quad \text{对所有 } w \in W, w \neq \hat{\alpha}$$

这一定理是概率论（条件期望）、数值分析（最小二乘法）和机器学习（线性回归）的理论基础。

> 💡 **直观理解**：如果 $W$ 是一个平面，$\alpha$ 是平面外的一个点，那么 $\alpha$ 到平面的最短距离就是垂直距离，脚点就是正交投影。

## A1.4 Example 18（函数空间的正交投影）

在 $\mathbb{R}_3[x]$ 上定义内积：

$$(f, g) = \frac{1}{2} \int_{-1}^{1} f(x) g(x) \, dx$$

(1) 验证这是合法的内积。

(2) 对 $\{1, x, x^2, x^3\}$ 做 Schmidt 正交化，得到 Legendre 多项式的雏形。

(3) 设 $W = \operatorname{span}\{x, x^2\}$，$h(x) = 1 + x^3$。求 $k(x) \in W$ 使得 $\|h(x) - k(x)\|$ 最小。解法就是求 $h$ 在 $W$ 上的正交投影。

---

# SO(n) 特殊正交群 {#son}

## SO(n) 的定义

**O(n)（正交群）：** 所有 $n$ 阶正交矩阵的集合。
$$\boxed{O(n) = \{ A \in M_n(\mathbb{R}) \mid A^T A = I_n \}}$$

**SO(n)（特殊正交群）：** 行列式为 $1$ 的正交矩阵（即旋转）的集合。
$$\boxed{SO(n) = \{ A \in O(n) \mid \det(A) = 1 \}}$$

对于行列式为 $-1$ 的正交矩阵 $A$，可以写成 $A = D \cdot B$，其中 $D = \operatorname{diag}(-1, 1, \ldots, 1)$，$B \in SO(n)$。即所有第二类正交变换 = 一次镜像反射 + 一次旋转。

---

## SO(2) 的完全分类

**2 阶特殊正交矩阵（旋转矩阵）的标准形式：**

$$\boxed{A = \begin{pmatrix} \cos\theta & -\sin\theta \\ \sin\theta & \cos\theta \end{pmatrix}}$$

这是绕原点逆时针旋转 $\theta$ 角的矩阵。

**2 阶行列式为 $-1$ 的正交矩阵（镜像）：**

$$A = \begin{pmatrix} -\cos\theta & \sin\theta \\ \sin\theta & \cos\theta \end{pmatrix}$$

特征值为 $1$ 和 $-1$，两个特征子空间正交。等价于关于某条直线的镜像反射。

---

## SO(3) 的分类

**3 阶特殊正交矩阵（三维旋转）的标准型：**

$$\boxed{A \sim \begin{pmatrix} \cos\theta & -\sin\theta & 0 \\ \sin\theta & \cos\theta & 0 \\ 0 & 0 & 1 \end{pmatrix} \quad \text{或} \quad I_3}$$

即绕某个固定轴旋转 $\theta$ 角。轴的朝向就是特征值 $1$ 对应的特征向量方向。

**3 阶行列式为 $-1$ 的正交矩阵**（旋转+镜像），标准型为：

$$\operatorname{diag}(1, 1, -1) \quad \text{或} \quad \begin{pmatrix} \cos\theta & -\sin\theta & 0 \\ \sin\theta & \cos\theta & 0 \\ 0 & 0 & -1 \end{pmatrix}$$

---

## 正交变换的一般标准型 (Theorem 7.3)

任意正交变换 $\mathcal{A}$ 在适当的（标准正交）基下可以表示为块对角矩阵：

$$\boxed{\operatorname{diag}\big( R(\theta_1), R(\theta_2), \ldots, R(\theta_s), I_t, -I_{n-2s-t} \big)}$$

其中 $R(\theta_k) = \begin{pmatrix} \cos\theta_k & -\sin\theta_k \\ \sin\theta_k & \cos\theta_k \end{pmatrix}$ 是 $2 \times 2$ 旋转块。

> 💡 **直观理解**：在合适的坐标系下，任何正交变换都可以分解为：几个二维平面上的独立旋转 + 一些方向上的恒等（保持不动）+ 一些方向上的翻转。

**两个重要的引理：**

- **引理 7.1：** 若 $\mathcal{A}$ 是正交变换，$W$ 是 $\mathcal{A}$-不变子空间，则 $W^\perp$ 也是 $\mathcal{A}$-不变子空间。
- **引理 7.2：** $n$ 维欧氏空间 $V$ 上的正交变换 $\mathcal{A}$ 必有 $1$ 维或 $2$ 维不变子空间。

> 这两个引理配合使用：找到 $1$ 维或 $2$ 维不变子空间 → 取正交补 → 在正交补上继续找，最终把空间分解为不变子空间的直和。

---

### 📝 Example 19（三维空间中正交变换的判定）

> 给定矩阵：
> $$A = \frac{1}{3}\begin{pmatrix} 2 & 2 & -1 \\ 2 & -1 & 2 \\ -1 & 2 & 2 \end{pmatrix}$$
>
> (1) 验证这是 $\mathbb{R}^3$ 上的正交变换（检查 $A^T A = I$，且 $\det A = 1$，属于 $SO(3)$）
>
> (2) 求旋转轴 $l$：即特征值 $1$ 对应的特征向量
>
> (3) 求旋转角 $\theta$：由 $\operatorname{tr}(A) = 1 + 2\cos\theta$，这里 $\operatorname{tr}(A)=1$，所以 $\cos\theta=0$，$\theta = \pi/2$（90°）

---

# 镜面反射（Householder 变换） {#mirror}

镜面反射是正交变换中非常重要的一类，它关于某个超平面做镜像反射。

## 定义 (Definition 8.1)

设 $\eta$ 是欧氏空间 $V$ 中的一个**单位向量**。定义线性变换 $S: V \to V$：

$$\boxed{S(\alpha) = \alpha - 2(\alpha, \eta) \cdot \eta}$$

$S$ 称为关于超平面 $\langle\eta\rangle^\perp$ 的**镜面反射**。

**几何理解：**

- $\langle\eta\rangle$ 是由 $\eta$ 张成的一维子空间（**法线方向**）
- $\langle\eta\rangle^\perp$ 是与 $\eta$ 正交的 $n-1$ 维超平面（**镜面**）
- $S$ 的作用：把 $\alpha$ 在 $\eta$ 方向上的分量反转，平行于镜面的分量保持不变
- 就像镜子反射光线——垂直于镜面的分量反号，平行于镜面的分量不变

> 💡 在 $\mathbb{R}^n$ 的标准基下，镜面反射矩阵 = $I_n - 2\eta\eta^T$。注意 $\eta$ 是列向量，$\eta\eta^T$ 是一个矩阵。

## 镜面反射的性质

- $S$ 是正交变换（保内积）
- $S^2 = I$（反射两次回到原向量，即 $S^{-1} = S$）
- $S$ 有 $n-1$ 个特征值 $1$（镜面上的向量全不动），$1$ 个特征值 $-1$（法线方向被翻转）
- $\det(S) = -1$（第二类正交变换）
- 任何正交变换都可以表示为至多 $n$ 个镜面反射的复合（Cartan-Dieudonné 定理）

## 镜面反射的应用

- 给定两个长度相同的向量 $\alpha$ 和 $\beta$，存在镜面反射 $S$ 使得 $S(\alpha) = \beta$（取 $\eta = \frac{\alpha-\beta}{\|\alpha-\beta\|}$）
- QR 分解的 Householder 算法就是用镜面反射实现的（比 Gram-Schmidt 数值更稳定）
- 任何正交矩阵都可以分解为镜面反射的乘积

---

# 反对称变换 {#skew}

## 定义 (Definition 9.1)

设 $\mathcal{A}$ 是欧氏空间 $V$ 上的线性变换。若对任意 $\alpha, \beta \in V$，都有：

$$\boxed{(\mathcal{A}(\alpha), \beta) = -(\alpha, \mathcal{A}(\beta))}$$

则称 $\mathcal{A}$ 为**反对称变换**。

在标准正交基下，反对称变换对应的矩阵满足 $A^T = -A$（即**反对称矩阵**）。

## 反对称变换的性质 (Theorem 9.2)

- 反对称变换在标准正交基下的矩阵是反对称矩阵（$A^T = -A$）
- 反对称变换的特征值必须是 $0$ 或纯虚数（形如 $\pm b_\ell i$）
- 反对称变换在适当的（标准正交）基下可以化为块对角标准型

## 反对称变换的标准型 (Theorem 9.4)

任意反对称变换在适当的（标准正交）基下可表示为：

$$\boxed{\operatorname{diag}\left( \begin{pmatrix} 0 & b_1 \\ -b_1 & 0 \end{pmatrix}, \begin{pmatrix} 0 & b_2 \\ -b_2 & 0 \end{pmatrix}, \ldots, \begin{pmatrix} 0 & b_s \\ -b_s & 0 \end{pmatrix}, O_{n-2s} \right)}$$

其中每个 $2 \times 2$ 块 $\begin{pmatrix} 0 & b_k \\ -b_k & 0 \end{pmatrix}$ 对应一对共轭纯虚数特征值 $\pm b_k i$。最后的 $O$ 块对应特征值 $0$。

**不变子空间的性质 (Lemma 9.3)：**

- 若 $\mathcal{A}$ 是对称变换，$W$ 是 $\mathcal{A}$-不变子空间，则 $W^\perp$ 也是 $\mathcal{A}$-不变子空间
- 若 $\mathcal{A}$ 是反对称变换，$W$ 是 $\mathcal{A}$-不变子空间，则 $W^\perp$ 也是 $\mathcal{A}$-不变子空间

> 这个性质对对称和反对称变换都成立——正交补保持不变性！

> 💡 对称变换与反对称变换的关系：任意线性变换 $\mathcal{A}$ 可以唯一分解为 $\mathcal{A} = \mathcal{A}_1 + \mathcal{A}_2$，其中 $\mathcal{A}_1$ 是对称变换，$\mathcal{A}_2$ 是反对称变换：$\mathcal{A}_1 = \frac{\mathcal{A} + \mathcal{A}^*}{2}, \quad \mathcal{A}_2 = \frac{\mathcal{A} - \mathcal{A}^*}{2}$。这是"矩阵的Cartesian分解"。

---

# 综合习题

### 📝 Example 20（内积空间综合题）

设 $V$ 是 $n$ 维欧氏空间。证明：

(1) 若对 $\alpha \in V$ 有 $(\alpha, \beta) = 0$ 对所有 $\beta \in V$ 成立，则 $\alpha = 0$。

(2) 若 $\alpha_1, \ldots, \alpha_n$ 是标准正交基，且 $(\xi, \alpha_i) = (\eta, \alpha_i)$ 对所有 $i$ 成立，则 $\xi = \eta$。

(3) 若 $(\cdot,\cdot)_1$ 和 $(\cdot,\cdot)_2$ 是 $V$ 上的两个内积，则 $(\alpha, \beta) = (\alpha, \beta)_1 + (\alpha, \beta)_2$ 也是 $V$ 上的内积。

(4) 若 $\varepsilon_1, \ldots, \varepsilon_n$ 是标准正交基，且 $\sum (\xi, \varepsilon_i)^2 = 1$，则 $\|\xi\| = 1$。（Parseval 恒等式的特例）

### 📝 Example 21/22（$\mathbb{R}^2$ 上的特殊线性变换）

分析 $\mathcal{A}: (x, y)^T \mapsto (2x+y, x+2y)^T$ 和 $\mathcal{A}: (x, y)^T \mapsto \frac{1}{\sqrt{2}}(3x+2y, -2x+2y)^T$ 的性质。

---

# 📋 小测重点速查卡片 {#cheatsheet}

## 🔥 考点一：Schmidt 正交化

$$\boxed{\beta_k = \alpha_k - \sum_{i=1}^{k-1} \frac{(\alpha_k, \beta_i)}{(\beta_i, \beta_i)} \beta_i}, \qquad \boxed{\eta_i = \frac{\beta_i}{\|\beta_i\|}}$$

**核心**：减去前面所有方向上的投影分量，再单位化。

---

## 🔥 考点二：正交变换

**三个等价条件（只需满足一个即可）：**

1. $(\mathcal{A}\alpha, \mathcal{A}\beta) = (\alpha, \beta)$ —— 保内积
2. $\|\mathcal{A}\alpha\| = \|\alpha\|$ —— 保长度
3. 标准正交基 → 标准正交基

**矩阵判定**：$A^T A = I \Leftrightarrow A$ 是正交矩阵

**分类**：$\det = +1$（旋转/第一类）vs $\det = -1$（镜像/第二类）

---

## 🔥 考点三：正交相似对角化（最重要！）

**前提**：$A$ 是实对称矩阵

**流程**：

1. 求特征值：$|\lambda I - A| = 0$
2. 对每个 $\lambda_i$，解 $(\lambda_i I - A)X = 0$ 求特征向量
3. **同一特征值内部**的向量做 Schmidt 正交化（**不同特征值之间自动正交！**）
4. 所有向量单位化
5. 拼成正交矩阵 $T$，则 $T^T A T = \operatorname{diag}(\lambda_1, \ldots, \lambda_n)$

---

## 核心定理速记

| 定理 | 要点 |
|------|------|
| 实对称矩阵 ⇔ 可正交相似对角化 | 不同特征值的特征向量自动正交 |
| Gram-Schmidt | 任意线性无关组 → 标准正交基 |
| 正交变换 ↔ 正交矩阵 | 在标准正交基下 $A^T A = I$ |
| 对称变换 ↔ 实对称矩阵 | 在标准正交基下 $A^T = A$ |
| 反对称变换 ↔ 反对称矩阵 | 在标准正交基下 $A^T = -A$ |
| Cauchy-Schwarz 不等式 | $\|(\alpha, \beta)\| \leq \|\alpha\| \cdot \|\beta\|$ |
| 正交补分解 | $V = W \oplus W^\perp$，最佳逼近 = 正交投影 |
| 镜面反射 | $S(\alpha) = \alpha - 2(\alpha, \eta)\eta$，$S^2 = I$，$\det(S) = -1$ |

---

<div style="text-align: center; font-size: 18pt; font-weight: bold; color: #1A3C6E; margin-top: 2cm;">

—— 祝小测顺利！——

</div>
