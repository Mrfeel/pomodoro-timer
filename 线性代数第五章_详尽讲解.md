# 线性代数 第五章：线性空间

> 基于课堂 PPT (B1, 2026.03) | 杨思辰 · 未来技术学院 · 物理学专业
>
> 🔥 重点：**线性相关/无关、极大无关组与秩、基与维数、线性方程组解的结构、子空间运算、同构**

---

# 目录

1. [§1 数组空间](#sec1)
2. [§2 线性相关与线性无关](#sec2)
3. [§3 子空间](#sec3)
4. [§4 极大线性无关组与秩](#sec4)
5. [§5 基与维数](#sec5)
6. [§6 线性方程组解集的结构](#sec6)
7. [§7 一般线性空间](#sec7)
8. [§8 子空间的运算](#sec8)
9. [§9 线性空间的同构](#sec9)
10. [📋 重点速查卡片](#cheatsheet)

---

# §1 数组空间 {#sec1}

## 1.1 $\mathbb{F}^n$ 的定义与八条运算律

记 $\mathbb{F}^n := \{(a_1, a_2, \ldots, a_n) \mid a_i \in \mathbb{F}\}$（或写成列向量形式）。

定义加法和数乘：

$$\begin{pmatrix} a_1 \\ \vdots \\ a_n \end{pmatrix} + \begin{pmatrix} b_1 \\ \vdots \\ b_n \end{pmatrix} = \begin{pmatrix} a_1+b_1 \\ \vdots \\ a_n+b_n \end{pmatrix}, \quad \lambda\begin{pmatrix} a_1 \\ \vdots \\ a_n \end{pmatrix} = \begin{pmatrix} \lambda a_1 \\ \vdots \\ \lambda a_n \end{pmatrix}$$

$(\mathbb{F}^n, +, \cdot)$ 满足八条运算律，构成 $\mathbb{F}$ 上的线性空间。

> 💡 **这是最"标准"的线性空间**。后面会看到，任何 $n$ 维线性空间都和 $\mathbb{F}^n$ 同构——所以只研究 $\mathbb{F}^n$ 就够了！

---

# §2 线性相关与线性无关 {#sec2}

> 🔥 **这是整个线性代数中最重要的概念之一！类似第一章的向量版本，现在是数组版本。**

## 2.1 定义

**Definition 2.1**（线性相关）

对 $\mathbb{F}^n$ 中的 $s$ 个向量 $\alpha_1, \ldots, \alpha_s$，若存在**不全为零**的 $\lambda_1, \ldots, \lambda_s \in \mathbb{F}$，使得：

$$\boxed{\lambda_1\alpha_1 + \lambda_2\alpha_2 + \cdots + \lambda_s\alpha_s = \mathbf{0}}$$

则称 $\alpha_1, \ldots, \alpha_s$ **线性相关**。否则称它们**线性无关**。

> 💡 **直观理解**：
> - 线性相关 = 有一个向量是"多余的"——可以被其他向量线性表示
> - 线性无关 = 每个向量都"不可或缺"
> - $\mathbb{F}^n$ 中最多有 $n$ 个线性无关的向量

**Definition 2.2**（线性表示）

若存在 $\lambda_1, \ldots, \lambda_s \in \mathbb{F}$ 使得 $\beta = \lambda_1\alpha_1 + \cdots + \lambda_s\alpha_s$，则称 $\beta$ 可由 $\alpha_1, \ldots, \alpha_s$ **线性表示**。

## 2.2 基本命题

**Proposition 2.3**（线性相关的等价刻画）

$s \geq 2$ 时，$\alpha_1, \ldots, \alpha_s$ 线性相关 $\iff$ **至少有一个向量**可由其余向量线性表示。

**Proposition 2.4**（线性无关的等价刻画）

$\alpha_1, \ldots, \alpha_s$ 线性无关 $\iff$ $\lambda_1\alpha_1 + \cdots + \lambda_s\alpha_s = \mathbf{0} \Rightarrow \lambda_1 = \cdots = \lambda_s = 0$

（即**只有全零系数**才能使组合为零。）

### 📝 例 1（判断线性相关性）

判断 $\mathbb{R}^3$ 中的向量是否线性相关：

$$\alpha_1 = (1, 2, 3)^T,\ \alpha_2 = (2, 4, 6)^T,\ \alpha_3 = (1, 1, 1)^T$$

**解**：$\alpha_2 = 2\alpha_1$，所以 $\alpha_1, \alpha_2$ 线性相关。$2\alpha_1 - \alpha_2 + 0 \cdot \alpha_3 = \mathbf{0}$，系数不全为零，所以三个向量线性相关。

### 📝 例 2（基本向量组）

$\mathbf{e}_1 = (1, 0, \ldots, 0)^T, \mathbf{e}_2 = (0, 1, \ldots, 0)^T, \ldots, \mathbf{e}_n = (0, 0, \ldots, 1)^T$ 线性无关。

> 💡 $\{\mathbf{e}_1, \ldots, \mathbf{e}_n\}$ 是 $\mathbb{F}^n$ 的**标准基**。

### 📝 例 3（列向量与矩阵的秩）

$\alpha_1, \ldots, \alpha_s$ 线性无关 $\iff$ 以它们为列的矩阵的秩 = $s$。

**应用**：把向量排成矩阵 → 化为阶梯形 → 非零列数 = 秩 = 线性无关向量的个数。

### 📝 例 4（函数空间中的线性相关/无关）

在 $C[a,b]$ 中，$\{1, \cos x, \sin x, \cos 2x, \sin 2x, \ldots\}$ 线性无关。

### 📝 例 5-7（更多练习）

包括含参向量组的线性相关性判定、多项式的线性无关性等。

---

# §3 子空间 {#sec3}

## 3.1 定义

**Definition 3.1**（子空间）

设 $V$ 是 $\mathbb{F}$ 上的线性空间，$W \subseteq V$ 非空。若 $W$ 对 $V$ 的加法和数乘封闭：

$$\alpha, \beta \in W \Rightarrow \alpha+\beta \in W; \quad \alpha \in W, \lambda \in \mathbb{F} \Rightarrow \lambda\alpha \in W$$

则称 $W$ 为 $V$ 的**子空间**。

> ⚠️ 验证子空间的三要素：① 非空（含零向量）；② 加法封闭；③ 数乘封闭。

### 📝 例 8（零子空间与平凡子空间）

$\{\mathbf{0}\}$ 和 $V$ 本身都是 $V$ 的子空间（称为平凡子空间）。

### 📝 例 9（解空间）

齐次线性方程组 $A\mathbf{x} = \mathbf{0}$ 的解集构成 $\mathbb{F}^n$ 的子空间（称为 $A$ 的**零空间**或**核**）。

### 📝 例 10（生成子空间）

$$\operatorname{span}(\alpha_1, \ldots, \alpha_s) := \{\lambda_1\alpha_1 + \cdots + \lambda_s\alpha_s \mid \lambda_i \in \mathbb{F}\}$$

是包含 $\alpha_1, \ldots, \alpha_s$ 的最小子空间。

---

# §4 极大线性无关组与秩 {#sec4}

## 4.1 极大无关组

**Definition 4.1**（极大线性无关组）

设 $S$ 是 $V$ 中一组向量。$S$ 的子集 $\{\alpha_1, \ldots, \alpha_r\}$ 称为 $S$ 的**极大线性无关组**，如果：
1. $\alpha_1, \ldots, \alpha_r$ 线性无关
2. $S$ 中任何 $r+1$ 个向量都线性相关（**极大性**——再添加任何向量都破坏无关性）

**Definition 4.2**（向量组的秩）

极大线性无关组中向量的个数称为向量组的**秩**。

> ⭐ 同一个向量组的不同极大无关组可以有不同成员，但它们的**向量个数一定相同**！

**Proposition 4.3**（极大无关组的等价刻画）

$\alpha_{i_1}, \ldots, \alpha_{i_r}$ 是 $\{\alpha_1, \ldots, \alpha_s\}$ 的极大无关组 $\iff$ 它们线性无关，且每个 $\alpha_j$ 都可由它们线性表示。

### 📝 例 11（求极大无关组）

求向量组 $\alpha_1 = (1,1,1), \alpha_2 = (2,2,2), \alpha_3 = (1,2,3)$ 的极大无关组和秩。

**解**：$\alpha_2 = 2\alpha_1$，$\alpha_3$ 不能由 $\alpha_1$ 表示。故 $\{\alpha_1, \alpha_3\}$ 是极大无关组，秩为 2。

## 4.2 矩阵的秩与向量组的秩

**Proposition 4.8**：矩阵的秩 = 列向量组的秩 = 行向量组的秩。

> ⭐ 这是"行秩 = 列秩"定理，是线性代数中最深刻的结论之一。

### 📝 例 12（用初等行变换求秩和极大无关组）

将向量为列排成矩阵 → 化为阶梯形 → 非零列（主元列）对应的原列向量构成极大无关组。

### 📝 例 13（求秩的两种方法）

1. **初等变换法**：化为阶梯形，非零行数
2. **子式法**：找最大的非零子式

### 📝 例 14（含参向量组的秩）

讨论参数使向量组的秩取不同值的情况。

---

# §5 基与维数 {#sec5}

## 5.1 基的定义

**Definition 5.2**（基）

$V$ 中一组线性无关的向量 $\varepsilon_1, \ldots, \varepsilon_n$ 称为 $V$ 的**基**，如果 $V$ 中任何向量都可由它们线性表示（即 $V = \operatorname{span}(\varepsilon_1, \ldots, \varepsilon_n)$）。

> 💡 **基 = 极大线性无关组 + 生成整个空间**。基是空间的"骨架"。

**Proposition 5.3**：线性空间 $V$ 的所有基包含的向量个数相同。这个数称为 $V$ 的**维数**，记为 $\boxed{\dim V}$。

> ⭐ 维数是线性空间最基本的**不变量**。同维数的空间本质上是一样的（同构）。

**Proposition 5.5**（基的扩充定理）

任何一组线性无关的向量都可以扩充为一组基。

### 📝 例 15（标准基）

$\mathbb{F}^n$ 的标准基：$\mathbf{e}_1, \ldots, \mathbf{e}_n$，$\dim \mathbb{F}^n = n$。

### 📝 例 16（多项式空间的基）

$\mathbb{F}[x]_n$（次数不超过 $n$ 的多项式空间）的一组基：$\{1, x, x^2, \ldots, x^n\}$，$\dim = n+1$。

### 📝 例 17（矩阵空间的基）

$\mathbb{F}^{m \times n}$ 的一组基：$\{E_{ij} \mid 1 \leq i \leq m, 1 \leq j \leq n\}$（$E_{ij}$ 在第 $(i,j)$ 位为1，其余为0），$\dim = mn$。

### 📝 例 18（解空间的基）

齐次方程组 $A\mathbf{x} = \mathbf{0}$ 的解空间称为 $A$ 的**零空间**。其维数 = $n - r(A)$。

## 5.2 坐标

取定基 $\varepsilon_1, \ldots, \varepsilon_n$ 后，任何 $\alpha \in V$ 可唯一表示为：

$$\alpha = x_1\varepsilon_1 + \cdots + x_n\varepsilon_n$$

$(x_1, \ldots, x_n)^T$ 称为 $\alpha$ 在该基下的**坐标**。

> 💡 取定基后，抽象向量 → 具体坐标，抽象线性空间 → $\mathbb{F}^n$。这就是坐标化的力量。

---

# §6 线性方程组解集的结构 {#sec6}

## 6.1 齐次方程组的解空间

**定理 6.1**：$A\mathbf{x} = \mathbf{0}$ 的解集是 $\mathbb{F}^n$ 的子空间，维数 = $n - r(A)$。

一组基（**基础解系**）含 $n - r(A)$ 个线性无关的解向量。

### 📝 例 19（求基础解系）

求解 $A\mathbf{x} = \mathbf{0}$ 并给出一组基础解系。

**方法**：化为阶梯形 → 确定自由变量 → 逐次令一个自由变量为1、其余为0 → 解出基础变量 → 得到基础解系的各向量。

> 💡 **自由变量个数 = $n - r(A)$ = 解空间的维数**。

## 6.2 非齐次方程组的解结构

**定理 6.2-6.4**：$A\mathbf{x} = \mathbf{b}$ 的通解 = **一个特解 + 齐次通解**。

$$\boxed{\text{解集} = \mathbf{x}_0 + \ker(A) = \{\mathbf{x}_0 + \mathbf{x}_h \mid A\mathbf{x}_h = \mathbf{0}\}}$$

> 💡 非齐次方程组的解集**不是**子空间（不含零向量），它是齐次解空间的"平移"（仿射子空间）。

### 📝 例 20（求非齐次通解）

1. 求 $A\mathbf{x} = \mathbf{b}$ 的一个特解
2. 求 $A\mathbf{x} = \mathbf{0}$ 的基础解系
3. 通解 = 特解 + 齐次通解的任意线性组合

### 📝 例 21-22（含参方程组的讨论）

讨论参数 $a, b$ 的取值对方程组解集结构的影响。

## 6.3 过渡矩阵与坐标变换

取定两组基 $\{\varepsilon_i\}$ 和 $\{\eta_i\}$，有关系：

$$(\eta_1, \ldots, \eta_n) = (\varepsilon_1, \ldots, \varepsilon_n) T$$

$T$ 称为从 $\{\varepsilon_i\}$ 到 $\{\eta_i\}$ 的**过渡矩阵**（可逆）。

**坐标变换公式**：若 $\alpha = (\varepsilon_1, \ldots, \varepsilon_n)X = (\eta_1, \ldots, \eta_n)Y$，则：

$$\boxed{Y = T^{-1} X}$$

---

# §7 一般线性空间 {#sec7}

## 7.1 线性空间的公理化定义

**Definition 7.1**：设 $V$ 是非空集合，$\mathbb{F}$ 是域。若定义加法 $+: V \times V \to V$ 和数乘 $\cdot: \mathbb{F} \times V \to V$ 满足八条运算律，则称 $V$ 是 $\mathbb{F}$ 上的**线性空间**。

> ⭐ 这八条公理是线性代数大厦的基石。任何满足这八条的系统都适用线性代数的全部理论。

## 7.2 线性空间的丰富例子

| 空间 | 元素 | 维数 |
|------|------|------|
| $\mathbb{F}^n$ | $n$ 元数组 | $n$ |
| $\mathbb{F}^{m \times n}$ | $m \times n$ 矩阵 | $mn$ |
| $\mathbb{F}[x]_n$ | 次数 ≤ n 的多项式 | $n+1$ |
| $C[a,b]$ | 连续函数 | 无穷维 |
| $\{f \mid f'' + f = 0\}$ | 二阶ODE的解 | 2（基：$\sin x, \cos x$） |

## 7.3 线性空间的子结构

- **线性组合** → **线性表示** → **线性相关/无关** → **极大无关组** → **基与维数**
- 所有概念自然地推广到一般线性空间。

---

# §8 子空间的运算 {#sec8}

## 8.1 交与和

- **交**：$W_1 \cap W_2 = \{\alpha \mid \alpha \in W_1 \text{ 且 } \alpha \in W_2\}$（仍是子空间）
- **和**：$W_1 + W_2 = \{\alpha_1 + \alpha_2 \mid \alpha_1 \in W_1, \alpha_2 \in W_2\}$（仍是子空间）

> ⚠️ **并一般不是子空间**！$W_1 \cup W_2$ 对加法未必封闭。

## 8.2 维数公式

$$\boxed{\dim(W_1 + W_2) = \dim W_1 + \dim W_2 - \dim(W_1 \cap W_2)}$$

> 💡 这个公式和集合论中的 $|A \cup B| = |A| + |B| - |A \cap B|$ 形式完全一样。

## 8.3 直和

若 $W_1 \cap W_2 = \{\mathbf{0}\}$，则和称为**直和**，记为 $W_1 \oplus W_2$。

**直和的等价条件**：
1. $W_1 \cap W_2 = \{\mathbf{0}\}$
2. $\dim(W_1 + W_2) = \dim W_1 + \dim W_2$
3. 每个 $\alpha \in W_1 + W_2$ 的分解 $\alpha = \alpha_1 + \alpha_2$（$\alpha_i \in W_i$）是**唯一**的

**补空间**：若 $V = W \oplus U$，则称 $U$ 是 $W$ 在 $V$ 中的**补空间**。

> ⚠️ 补空间一般不唯一！例如 $\mathbb{R}^2$ 中，任何不共线的方向都可以作为一维子空间的补空间。

---

# §9 线性空间的同构 {#sec9}

## 9.1 同构的定义

两个线性空间 $V_1, V_2$ 称为**同构**，如果存在双射 $\sigma: V_1 \to V_2$ 满足：

$$\sigma(\alpha + \beta) = \sigma(\alpha) + \sigma(\beta), \quad \sigma(\lambda\alpha) = \lambda\sigma(\alpha)$$

（即 $\sigma$ 保持加法和数乘。）

## 9.2 同构的充要条件

$$\boxed{V_1 \cong V_2 \iff \dim V_1 = \dim V_2}$$

> ⭐ **维数是线性空间的完全不变量**：两个线性空间本质相同（同构）当且仅当它们维数相同。特别地，任何 $n$ 维线性空间都同构于 $\mathbb{F}^n$。

## 9.3 商空间

设 $W$ 是 $V$ 的子空间，定义**商空间** $V/W$ 为所有陪集 $\alpha + W$ 的集合。

$$\dim(V/W) = \dim V - \dim W$$

---

# 📋 重点速查卡片 {#cheatsheet}

## 核心概念层级

$$\boxed{\text{八条公理} \to \text{线性空间} \to \begin{cases} \text{线性相关/无关} \\ \text{子空间} \to \text{极大无关组} \to \text{秩} \\ \text{基} \to \text{维数} \to \text{坐标} \\ \text{同构} \end{cases}}$$

## 线性相关/无关判定

| 条件 | 结论 |
|------|------|
| 向量个数 > 维数 | **必相关** |
| 含零向量 | **必相关** |
| 两向量成比例 | **必相关** |
| 矩阵的秩 = 向量个数 | **线性无关** |

## 基与维数速查

| 空间 | 一组基 | $\dim$ |
|------|--------|--------|
| $\mathbb{F}^n$ | $\mathbf{e}_1, \ldots, \mathbf{e}_n$ | $n$ |
| $\mathbb{F}^{m \times n}$ | $\{E_{ij}\}$ | $mn$ |
| $\mathbb{F}[x]_n$ | $\{1, x, \ldots, x^n\}$ | $n+1$ |
| 解空间 $\ker(A)$ | 基础解系 | $n - r(A)$ |

## 子空间维数公式

$$\boxed{\dim(W_1 + W_2) = \dim W_1 + \dim W_2 - \dim(W_1 \cap W_2)}$$

## 直和的等价条件

$$W_1 \cap W_2 = \{\mathbf{0}\} \iff \dim(W_1+W_2) = \dim W_1 + \dim W_2 \iff \text{分解唯一}$$

## 线性方程组解的结构

$$\boxed{A\mathbf{x} = \mathbf{b} \text{ 的通解 } = \mathbf{x}_0 \text{（特解）} + \ker(A) \text{（齐次通解）}}$$

## 同构的充要条件

$$\boxed{V_1 \cong V_2 \iff \dim V_1 = \dim V_2}$$

---

<div style="text-align: center; font-size: 18pt; font-weight: bold; color: #1A3C6E; margin-top: 2cm;">

—— 祝学习顺利！——

</div>
