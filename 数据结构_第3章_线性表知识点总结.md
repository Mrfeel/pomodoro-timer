# 数据结构 · 第3章 线性表 知识点与考点总结

> 《数据结构及其算法》复习资料 | 杨思辰 · 未来技术学院 · 物理学专业 · PB25992094
>
> 🔥 习题集 13 题，重点：**顺序表插入/删除、单链表插入/删除/逆置、双指针技巧、循环/双向链表**

---

## 目录

1. [§1 线性表的定义与 ADT](#sec1)
2. [§2 顺序表（顺序存储）](#sec2)
3. [§3 单链表（链式存储）](#sec3)
4. [§4 循环链表与双向链表](#sec4)
5. [§5 顺序表 vs 链表对比](#sec5)
6. [§6 高频算法模板](#sec6)
7. [§7 易错点与速查卡片](#sec7)

---

# §1 线性表的定义与 ADT {#sec1}

## 1.1 定义

**线性表** 是 $n$（$n \ge 0$）个数据元素的**有限序列**，记为 $(a_1, a_2, \dots, a_n)$。

- 元素之间存在**一对一**的前驱后继关系
- $a_1$ 无前驱（表头），$a_n$ 无后继（表尾）
- 除首尾外，每个元素有且仅有**一个直接前驱**和**一个直接后继**

## 1.2 基本操作

| 操作 | 含义 |
|:--|:--|
| `InitList(&L)` | 构造空线性表 |
| `DestroyList(&L)` | 销毁表 |
| `ListInsert(&L, i, e)` | 在第 $i$ 位（$1 \le i \le n+1$）插入元素 $e$ |
| `ListDelete(&L, i, &e)` | 删除第 $i$ 位元素，用 $e$ 返回 |
| `LocateElem(L, e)` | 定位元素 $e$ 首次出现的位置 |
| `GetElem(L, i)` | 取第 $i$ 个元素 |

---

# §2 顺序表（顺序存储）{#sec2}

## 2.1 存储方式

用一段**连续**内存（数组）存放，逻辑上相邻的元素物理上也相邻。

```c
#define MAXSIZE 100
typedef struct {
    int data[MAXSIZE];   // 存储空间
    int length;          // 当前表长
} SqList;
```

**特点**：随机存取——访问任意位置元素 $O(1)$。

## 2.2 插入操作

在第 $i$ 位插入元素 $e$（$i = 1 \sim n+1$）：

```c
int ListInsert(SqList *L, int i, int e) {
    if (i < 1 || i > L->length + 1) return 0;   // 位置非法
    if (L->length >= MAXSIZE) return 0;          // 表满
    for (int j = L->length; j >= i; j--)         // ★ 从后往前移动
        L->data[j] = L->data[j - 1];
    L->data[i - 1] = e;                          // 插入新元素
    L->length++;
    return 1;
}
```

**移动次数**：平均 $n/2$，时间复杂度 $O(n)$。

```
插入前: [a] [b] [c] [d] [ ]
         i=2 ↑          ↑ j=3 开始
插入后: [a] [e] [b] [c] [d]
             新  后移 后移 后移
```

## 2.3 删除操作

```c
int ListDelete(SqList *L, int i, int *e) {
    if (i < 1 || i > L->length) return 0;        // 位置非法
    *e = L->data[i - 1];
    for (int j = i; j < L->length; j++)           // ★ 从前往后移动
        L->data[j - 1] = L->data[j];
    L->length--;
    return 1;
}
```

**移动次数**：平均 $(n-1)/2$，时间复杂度 $O(n)$。

## 2.4 顺序表习题高频操作

### 有序去重（双指针一趟）

```c
int delDupOrdered(int a[], int n) {
    int j = 0;                         // 快慢指针
    for (int i = 1; i < n; i++)
        if (a[i] != a[j]) a[++j] = a[i];
    return j + 1;
}
// 时间 O(n), 空间 O(1)
```

### 逆置

```c
void Reverse(int a[], int n) {
    for (int i = 0; i < n / 2; i++)
        swap(a[i], a[n - 1 - i]);
}
```

---

# §3 单链表（链式存储）{#sec3}

## 3.1 存储方式

结点 = 数据域 + **指针域**（指向直接后继）。

```c
typedef struct LNode {
    int data;                    // 数据域
    struct LNode *next;          // 指针域（指向后继）
} LNode, *LinkList;
```

> ⚠️ **头结点 vs 头指针**：头指针指向链表中**第一个结点**。头结点是额外附加在第一个元素前的**哑结点**（不存数据），其存在简化了插入/删除（统一了对首元结点的操作）。

```
带头结点的单链表:
  L → [头|→] → [a₁|→] → [a₂|→] → ... → [aₙ|NULL]
      头结点     首元结点
```

## 3.2 插入（先接后断！）🔥

在第 $i-1$ 个结点（前驱 `pre`）之后插入新结点：

```c
// ★ 核心——顺序绝对不能反！
p->next = pre->next;      // ① 新结点先接后继
pre->next = p;            // ② 前驱再接新结点
```

```
① 先接后继:  pre --→ [后继]
              p --↗

② 再接前驱:  pre --→ p --→ [后继]
```

> ⚠️ **如果反过来**（先改 pre->next = p），pre 的后继信息就丢了，p->next 不知道该接哪。

## 3.3 删除（需要前驱指针）

```c
// pre = 待删结点的前驱, p = 待删结点
pre->next = p->next;       // 绕过 p
free(p);                   // 释放内存
```

### 删除所有值为 x 的结点（经典题）

```c
void deleteAllX(LinkList L, int x) {
    LNode *pre = L, *p = L->next;    // pre从头结点开始
    while (p) {
        if (p->data == x) {
            pre->next = p->next;     // 绕过 p
            free(p);
            p = pre->next;           // p 前进
        } else {
            pre = p;                 // 双双前进
            p = p->next;
        }
    }
}
```

## 3.4 头插法 vs 尾插法

### 头插法（逆序建表）

每次新结点插在头结点后面——**建出来的链表与输入顺序相反**。

```c
void CreateListHead(LinkList L, int a[], int n) {
    L = (LinkList)malloc(sizeof(LNode)); L->next = NULL;
    for (int i = 0; i < n; i++) {
        LNode *p = (LNode*)malloc(sizeof(LNode));
        p->data = a[i];
        p->next = L->next;       // 新结点的next指向原来的首元
        L->next = p;             // 头结点指向新结点
    }
}
// 输入: {1,2,3} → 链表: 3→2→1
```

### 尾插法（正序建表）

每次新结点接到表尾——**建出来的链表与输入顺序相同**。

```c
void CreateListTail(LinkList L, int a[], int n) {
    L = (LinkList)malloc(sizeof(LNode));
    LNode *tail = L;                        // tail 始终指向表尾
    for (int i = 0; i < n; i++) {
        LNode *p = (LNode*)malloc(sizeof(LNode));
        p->data = a[i]; p->next = NULL;
        tail->next = p;                     // 接到表尾
        tail = p;                           // 更新 tail
    }
}
// 输入: {1,2,3} → 链表: 1→2→3
```

## 3.5 单链表逆置（原地）🔥

```c
void ReverseList(LinkList L) {
    LNode *p = L->next;          // p = 第一个元素
    L->next = NULL;              // 断开旧链
    while (p) {
        LNode *q = p->next;      // ★ 先保存后继（否则断链）
        p->next = L->next;       // 头插到新链
        L->next = p;
        p = q;                   // p 前进
    }
}
// 时间 O(n), 空间 O(1)
```

---

# §4 循环链表与双向链表 {#sec4}

## 4.1 循环链表

表尾结点的 `next` 指向**头结点**（或首元结点），形成闭环。

```c
// 判空（带头结点）：
int isEmptyC(LinkList L) { return L->next == L; }

// 判尾：
int isTail(LinkList L, LNode *p) { return p->next == L; }
```

**应用**：约瑟夫问题（实验一）——用循环链表模拟围坐一圈的人。

## 4.2 双向链表

每个结点有两个指针：`prior`（前驱）和 `next`（后继）。

```c
typedef struct DuLNode {
    int data;
    struct DuLNode *prior;       // 前驱
    struct DuLNode *next;        // 后继
} DuLNode, *DuLinkList;
```

### 插入（在 p 之后插入 s）——四条语句

```c
s->next = p->next;              // ① s 接后继
s->prior = p;                   // ② s 接 p
if (p->next) p->next->prior = s;// ③ 后继接回 s
p->next = s;                    // ④ p 接 s
```

### 删除（删除 p）

```c
p->prior->next = p->next;       // p的前驱 → p的后继
if (p->next) p->next->prior = p->prior;  // p的后继 → p的前驱
free(p);
```

> 💡 双向链表的优势：已知某结点指针即可直接删除（单向链表必须要有前驱）。

---

# §5 顺序表 vs 链表对比 {#sec5}

| 对比维度 | 顺序表 | 链表 |
|:--|:--|:--|
| 存储方式 | 连续内存（数组） | 离散结点 + 指针 |
| 存取方式 | **随机存取** $O(1)$ | **顺序存取** $O(n)$ |
| 查找（按位置） | $O(1)$ ✅ | $O(n)$ |
| 查找（按值） | $O(n)$ | $O(n)$ |
| 插入/删除 | $O(n)$（需移动） | $O(1)$（只需改指针） ✅ |
| 空间 | 预分配，可能浪费 | 按需分配，有指针开销 |
| 缓存友好 | ✅ 连续内存 | ❌ 跳跃访问 |
| 适用场景 | 查多改少、表长稳定 | 频繁插入/删除、表长动态变化 |

---

# §6 高频算法模板 {#sec6}

> 📝 伪代码答题必备——以下模板可直接用于考试作答。

### 模板 1：顺序表有序去重

```
输入：有序数组 a[0..n-1]
j ← 0
for i ← 1 to n-1 do
    if a[i] ≠ a[j] then
        j ← j+1;  a[j] ← a[i]
return j+1
```
复杂度：时间 $O(n)$，空间 $O(1)$。

### 模板 2：单链表删除所有值为 x 的结点

```
pre ← L（头结点）,  p ← L->next
while p ≠ NULL do
    if p->data = x then
        pre->next ← p->next;  free(p);  p ← pre->next
    else
        pre ← p;  p ← p->next
```
复杂度：时间 $O(n)$，空间 $O(1)$。

### 模板 3：单链表原地逆置

```
p ← L->next;  L->next ← NULL
while p ≠ NULL do
    q ← p->next           // 保存后继
    p->next ← L->next     // 头插
    L->next ← p
    p ← q
```

### 模板 4：归并两个有序顺序表

```
i ← 0;  j ← 0;  k ← 0
while i < n and j < m do
    if A[i] ≤ B[j] then C[k++] ← A[i++]
    else C[k++] ← B[j++]
将剩余元素追加到 C 末尾
```

---

# §7 易错点与速查卡片 {#sec7}

## 7.1 常见易错点

| 易错点 | 正确做法 |
|:--|:--|
| 链表插入顺序 | **先接后继，再接前驱**——反了就断链 |
| 删除前找不到前驱 | 维护 `pre` 指针或者用**双指针**（pre + cur） |
| 头插法 vs 尾插法 | 头插 = 逆序，尾插 = 正序 |
| 带头结点 vs 不带头 | 带头结点的插入/删除**统一处理**（首元与中间元操作一致） |
| 循环链表遍历终止 | 不是 `p==NULL`，而是 `p==L`（回到头） |
| 顺序表插入位置 | $i$ 从 1 开始（逻辑位序），数组下标从 0 开始 |
| 逆置时忘记保存后继 | `q = p->next` 必须写在 `p->next = ...` **之前** |

## 7.2 核心速查

| 操作 | 顺序表 | 单链表 |
|:--|:--|:--|
| 按位置访问 | $O(1)$ | $O(n)$ |
| 按值查找 | $O(n)$ | $O(n)$ |
| 插入 | $O(n)$ | $O(1)$（已知前驱） |
| 删除 | $O(n)$ | $O(1)$（已知前驱） |
| 逆置 | $O(n)$（双指针 swap） | $O(n)$（头插法） |
| 归并 | $O(n+m)$ | $O(n+m)$ |

---

<div style="text-align: center; margin-top: 2em;">

—— 第 3 章「线性表」知识点总结完 ——

</div>
