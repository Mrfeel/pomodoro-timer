# 线性代数 第六章：特征值与 Jordan 标准形

> 基于课堂 PPT (B1, 2026.03) | 杨思辰 · 未来技术学院 · 物理学专业
>
> 🔥 重点：**特征值与特征向量、相似对角化、Jordan 标准形、Cayley-Hamilton 定理**

---

# 目录

1. [§1 $\mathbb{F}^n$ 上的线性变换](#sec1)
2. [§2 特征值与特征向量](#sec2)
3. [§3 矩阵的相似](#sec3)
4. [§4 一般线性空间上的线性变换](#sec4)
5. [§5 Jordan 标准形](#sec5)
6. [*附录：Cayley-Hamilton 定理](#app1)
7. [📋 重点速查卡片](#cheatsheet)

---

# §1 $\mathbb{F}^n$ 上的线性变换 {#sec1}

## 1.1 线性变换的定义

**Definition**：映射 $\mathcal{A}: \mathbb{F}^n \to \mathbb{F}^n$ 称为**线性变换**（或线性算子），若：

$$\mathcal{A}(\alpha + \beta) = \mathcal{A}(\alpha) + \mathcal{A}(\beta), \quad \mathcal{A}(\lambda\alpha) = \lambda\mathcal{A}(\alpha)$$

> 💡 **线性变换 = 保持线性结构的映射**。"变换"强调的是定义域和值域相同（都是 $\mathbb{F}^n$）。

**定理**：$\mathbb{F}^n$ 上的任何线性变换 $\mathcal{A}$ 都可表示为矩阵乘法：

$$\boxed{\mathcal{A}(\mathbf{x}) = A\mathbf{x}}$$

其中 $A$ 是 $n$ 阶方阵（$\mathcal{A}$ 在标准基下的矩阵）。

## 1.2 核与像

$$\ker(\mathcal{A}) = \{\mathbf{x} \in \mathbb{F}^n \mid A\mathbf{x} = \mathbf{0}\} = \text{$A$ 的零空间}$$

$$\operatorname{Im}(\mathcal{A}) = \{A\mathbf{x} \mid \mathbf{x} \in \mathbb{F}^n\} = \text{$A$ 的列空间}$$

**维数公式**：

$$\boxed{\dim\ker(\mathcal{A}) + \dim\operatorname{Im}(\mathcal{A}) = n}$$

> 💡 也称为**秩-零化度定理**：$\operatorname{nullity}(A) + \operatorname{rank}(A) = n$。

---

# §2 特征值与特征向量 {#sec2}

> 🔥 **这是应用最广泛的线性代数概念——从量子力学到 Google PageRank 都要用它。**

## 2.1 定义

**Definition**：设 $A \in \mathbb{F}^{n \times n}$。若存在 $\lambda \in \mathbb{F}$ 和**非零**向量 $\mathbf{x} \in \mathbb{F}^n$，使得：

$$\boxed{A\mathbf{x} = \lambda \mathbf{x}}$$

则称 $\lambda$ 为 $A$ 的一个**特征值**，$\mathbf{x}$ 为对应的**特征向量**。

> 💡 **通俗理解**：特征向量是在线性变换下**方向保持不变**的向量（可能被拉伸或压缩）。特征值是拉伸/压缩的倍数，若为负数则表示方向反转。

## 2.2 求特征值

$A\mathbf{x} = \lambda \mathbf{x} \iff (\lambda I - A)\mathbf{x} = \mathbf{0}$，要求存在非零解，即：

$$\boxed{\det(\lambda I - A) = 0}$$

$\det(\lambda I - A)$ 是 $\lambda$ 的 $n$ 次多项式，称为 $A$ 的**特征多项式** $f_A(\lambda)$。

### 📝 例 1（求特征值和特征向量）

求 $A = \begin{pmatrix} 2 & 1 \\ 1 & 2 \end{pmatrix}$ 的特征值和特征向量。

**解**：

**第1步**：求特征多项式

$$f_A(\lambda) = \det\begin{pmatrix} \lambda-2 & -1 \\ -1 & \lambda-2 \end{pmatrix} = (\lambda-2)^2 - 1 = \lambda^2 - 4\lambda + 3$$

**第2步**：解 $f_A(\lambda) = 0$

$$\lambda^2 - 4\lambda + 3 = 0 \Rightarrow (\lambda-1)(\lambda-3) = 0 \Rightarrow \lambda_1 = 1, \lambda_2 = 3$$

**第3步**：对每个特征值求特征向量

对 $\lambda_1 = 1$，解 $(I - A)\mathbf{x} = \mathbf{0}$：

$$\begin{pmatrix} -1 & -1 \\ -1 & -1 \end{pmatrix}\begin{pmatrix} x_1 \\ x_2 \end{pmatrix} = \mathbf{0} \Rightarrow x_1 + x_2 = 0 \Rightarrow \mathbf{x} = t\begin{pmatrix} 1 \\ -1 \end{pmatrix}$$

对 $\lambda_2 = 3$，解 $(3I - A)\mathbf{x} = \mathbf{0}$：

$$\begin{pmatrix} 1 & -1 \\ -1 & 1 \end{pmatrix}\begin{pmatrix} x_1 \\ x_2 \end{pmatrix} = \mathbf{0} \Rightarrow x_1 - x_2 = 0 \Rightarrow \mathbf{x} = t\begin{pmatrix} 1 \\ 1 \end{pmatrix}$$

$$\boxed{\lambda_1 = 1: \ \mathbf{x} = t\begin{pmatrix} 1 \\ -1 \end{pmatrix}; \quad \lambda_2 = 3: \ \mathbf{x} = t\begin{pmatrix} 1 \\ 1 \end{pmatrix}}$$

> 💡 注意：属于不同特征值的特征向量自动正交（这是对称矩阵的性质，一般矩阵不保证）。

## 2.3 特征多项式的性质

$$\boxed{f_A(\lambda) = \lambda^n - (\operatorname{tr} A)\lambda^{n-1} + \cdots + (-1)^n \det(A)}$$

- **特征值之和** = $\operatorname{tr}(A)$（迹）
- **特征值之积** = $\det(A)$（行列式）

在复数域中，$n$ 阶方阵恰有 $n$ 个特征值（计重数）。

> ⭐ 这两个关系是极其重要的不变量——相似变换下迹和行列式不变。

### 📝 例 8（上三角阵的特征值）

上三角阵的特征值就是主对角线元素。因为 $\det(\lambda I - A)$ 是对角线乘积。

### 📝 例 9（利用特征值求行列式）

若已知 $A$ 有特征值 $\{1, 2, 3\}$，则 $\det(A) = 1 \times 2 \times 3 = 6$，$\operatorname{tr}(A) = 1+2+3 = 6$。

---

# §3 矩阵的相似 {#sec3}

## 3.1 相似的定义与不变量

**Definition**：$A, B \in \mathbb{F}^{n \times n}$ 称为**相似**，若存在可逆矩阵 $P$，使 $B = P^{-1}AP$。记为 $A \sim B$。

> 💡 **直观**：$A$ 和 $B$ 是**同一个线性变换在不同基下的矩阵**。相似矩阵表示"本质上相同的"线性变换。

**相似不变量**（相似矩阵的公共特征）：
- 特征多项式、特征值（含重数）
- 迹 $\operatorname{tr}$
- 行列式 $\det$
- 秩 $r$

> ⚠️ 反过来不成立：迹和行列式相同**不能**推出矩阵相似！

## 3.2 🔥 对角化

**Definition**：若 $A$ 相似于一个对角矩阵，则称 $A$ **可对角化**：

$$P^{-1}AP = \operatorname{diag}(\lambda_1, \ldots, \lambda_n)$$

**对角化的三大充要条件**：

| 条件 | 说明 |
|------|------|
| ① $A$ 有 $n$ 个线性无关的特征向量 | $P$ 的列就是这 $n$ 个特征向量 |
| ② 每个特征值的**几何重数 = 代数重数** | 几何重数 = $\dim V_\lambda$，代数重数 = 特征值重数 |
| ③ $A$ 的**最小多项式**无重根 | 等价于每个 Jordan 块都是 $1 \times 1$ |

> 🔥 **判定流程**：求特征值 → 对每个特征值，检查几何重数是否等于代数重数 → 若全相等则可对角化。

### 📝 例 10（对角化实例）

将 $A = \begin{pmatrix} 2 & 1 \\ 1 & 2 \end{pmatrix}$ 对角化。

**解**：由前例，特征值 $1, 3$，特征向量 $\begin{pmatrix}1\\-1\end{pmatrix}, \begin{pmatrix}1\\1\end{pmatrix}$。取

$$P = \begin{pmatrix} 1 & 1 \\ -1 & 1 \end{pmatrix}$$

则：

$$P^{-1}AP = \begin{pmatrix} 1 & 0 \\ 0 & 3 \end{pmatrix}$$

### 📝 例 11（不可对角化的例子）

$A = \begin{pmatrix} 1 & 1 \\ 0 & 1 \end{pmatrix}$ 的特征值是 $1$（二重），但 $(I - A)\mathbf{x} = \begin{pmatrix}0 & -1 \\ 0 & 0\end{pmatrix}\mathbf{x} = \mathbf{0}$ 的解空间维数只有 1（几何重数 = 1，代数重数 = 2）。所以 **$A$ 不可对角化**。

> ⚠️ 这就是 **Jordan 块** $\begin{pmatrix} 1 & 1 \\ 0 & 1 \end{pmatrix}$——特征值相同但只有一个线性无关的特征向量。

## 3.3 代数重数与几何重数

- **代数重数** $m_a(\lambda)$：特征值 $\lambda$ 在特征多项式中的重数
- **几何重数** $m_g(\lambda)$：特征子空间 $V_\lambda = \ker(\lambda I - A)$ 的维数

$$\boxed{1 \leq m_g(\lambda) \leq m_a(\lambda)}$$

> ⚠️ 几何重数永远 ≤ 代数重数。可对角化 $\iff$ 对每个特征值 $m_g = m_a$。

### 📝 例 12-22（对角化综合练习）

包括不同场景下的对角化问题：实对称矩阵（必可正交对角化）、含参矩阵的对角化条件、Jordan块、上三角化等。

## 3.4 上三角化定理

**定理**：任何复方阵都相似于一个上三角矩阵（Schur 三角化）。

> 💡 这意味着虽然不能保证对角化，但总可以"几乎对角化"——把非零元素挤到对角线以上。

---

# §4 一般线性空间上的线性变换 {#sec4}

## 4.1 基下的矩阵

设 $\mathcal{A}: V \to V$ 是线性变换，$\varepsilon_1, \ldots, \varepsilon_n$ 是 $V$ 的一组基。则：

$$\mathcal{A}(\varepsilon_1, \ldots, \varepsilon_n) = (\varepsilon_1, \ldots, \varepsilon_n) A$$

$A$ 称为 $\mathcal{A}$ 在基 $\{\varepsilon_i\}$ 下的矩阵。

**基变换对矩阵的影响**：若换基 $(\eta_1, \ldots, \eta_n) = (\varepsilon_1, \ldots, \varepsilon_n)T$，则：

$$\boxed{\mathcal{A} \text{ 在新基下的矩阵 } B = T^{-1}AT}$$

> ⭐ 同一线性变换在不同基下的矩阵彼此**相似**。

## 4.2 不变子空间

**Definition**：$W$ 是 $\mathcal{A}$-**不变子空间**，如果 $\mathcal{A}(W) \subseteq W$。

> 💡 不变子空间的意义：把 $\mathcal{A}$ 限制在 $W$ 上仍是线性变换。这让我们可以"分块"研究：在一组合适的基下，$\mathcal{A}$ 的矩阵是分块对角阵。

**重要结论**：
- 特征子空间 $V_\lambda$ 是不变子空间
- 一维不变子空间 $\iff$ 该方向的向量是特征向量
- $\ker(\mathcal{A})$ 和 $\operatorname{Im}(\mathcal{A})$ 都是不变子空间

### 📝 例 23-34（线性变换综合题）

包括：求线性变换在给定基下的矩阵、判断子空间是否不变、坐标变换等。

### 📝 例（相抵与相似的区别）

- **相抵**：$B = QAP^{-1}$（$P,Q$ 可逆）——同一个线性映射在不同基下的表示（定义域和值域可以各自换基）
- **相似**：$B = P^{-1}AP$——同一个线性变换（定义域=值域）在不同基下的矩阵

---

# §5 Jordan 标准形 {#sec5}

> 🔥 **这是线性代数 B1 的最高潮——当矩阵不能对角化时怎么办？**

## 5.1 为什么需要 Jordan 标准形

并非所有矩阵都可对角化。但任何复矩阵都相似于一个**Jordan 标准形**——分块对角矩阵，每个对角块是 Jordan 块。

## 5.2 Jordan 块

$$\boxed{J_k(\lambda) = \begin{pmatrix}
\lambda & 1 & 0 & \cdots & 0 \\
0 & \lambda & 1 & \cdots & 0 \\
\vdots & \ddots & \ddots & \ddots & \vdots \\
0 & \cdots & 0 & \lambda & 1 \\
0 & \cdots & 0 & 0 & \lambda
\end{pmatrix}_{k \times k}}$$

**性质**：
- 只有一个特征值 $\lambda$（代数重数 $k$）
- 只有一个线性无关的特征向量（几何重数 1）
- $(J_k(\lambda) - \lambda I)^k = 0$，但 $(J_k(\lambda) - \lambda I)^{k-1} \neq 0$（**幂零指数为 $k$**）

> 💡 Jordan 块 = "几乎对角但就差一点"——主对角线全是 $\lambda$，超对角线全是 1。

## 5.3 Jordan 标准形定理

**定理**：任何复方阵 $A$ 都相似于一个 Jordan 标准形：

$$\boxed{A \sim J = \begin{pmatrix}
J_{k_1}(\lambda_1) & & & \\
& J_{k_2}(\lambda_2) & & \\
& & \ddots & \\
& & & J_{k_s}(\lambda_s)
\end{pmatrix}}$$

其中各 $\lambda_i$ 可以相同。各 Jordan 块的阶数和排列方式由 $A$ 唯一确定（不计排列顺序）。

## 5.4 求 Jordan 标准形的四步法

**第1步**：求特征多项式，得到所有特征值及其代数重数。

**第2步**：对每个特征值 $\lambda$，求**根子空间**的维数：

$$\dim\ker((A - \lambda I)^k), \quad k = 1, 2, \ldots$$

直到维数不再增长（达到代数重数）。

**第3步**：由维数差确定各阶 Jordan 块的个数：

$k$ 阶 Jordan 块的个数 = $r_{k-1} - 2r_k + r_{k+1}$，其中 $r_k = \dim\ker((A - \lambda I)^k)$。

**第4步**：求**广义特征向量**（根向量），拼成过渡矩阵 $P$。

### 📝 例 35（求 Jordan 标准形）

求 $A = \begin{pmatrix} 2 & 1 & 0 \\ 0 & 2 & 1 \\ 0 & 0 & 2 \end{pmatrix}$ 的 Jordan 标准形。

**解**：特征值 $\lambda = 2$（三重），$A$ 本身已经是 Jordan 块 $J_3(2)$。

### 📝 （更复杂的例子）

$A = \begin{pmatrix} 2 & 1 & 0 \\ 0 & 2 & 0 \\ 0 & 0 & 2 \end{pmatrix}$

特征值 $\lambda = 2$（三重）。$A - 2I = \begin{pmatrix} 0 & 1 & 0 \\ 0 & 0 & 0 \\ 0 & 0 & 0 \end{pmatrix}$，$r_1 = \dim\ker(A-2I) = 2$（几何重数 = 2），$r_2 = \dim\ker((A-2I)^2) = 3$。

$3-2=1$ 个 2 阶块，$2-1-1=0$ 个 1 阶块...通过计算可得 Jordan 标准形为 $J_2(2) \oplus J_1(2)$。

> 💡 几何重数 = Jordan 块的个数。代数重数 = 所有 Jordan 块的阶数之和。

---

# *附录：Cayley-Hamilton 定理 {#app1}

## A.1 定理陈述

**Cayley-Hamilton 定理**：任何方阵都满足自己的特征方程。

$$\boxed{f_A(A) = A^n - (\operatorname{tr}A)A^{n-1} + \cdots + (-1)^n\det(A) I = \mathbf{0}}$$

> 💡 $f_A(\lambda) = \det(\lambda I - A)$，将 $\lambda$ 替换为矩阵 $A$ 得到零矩阵。

### 📝 例 36（C-H 定理的验证）

对 $A = \begin{pmatrix} 2 & 1 \\ 1 & 2 \end{pmatrix}$，$f_A(\lambda) = \lambda^2 - 4\lambda + 3$。

验证：$A^2 - 4A + 3I = \begin{pmatrix} 5 & 4 \\ 4 & 5 \end{pmatrix} - \begin{pmatrix} 8 & 4 \\ 4 & 8 \end{pmatrix} + \begin{pmatrix} 3 & 0 \\ 0 & 3 \end{pmatrix} = \mathbf{0}$ ✓

## A.2 应用：求 $A^{-1}$ 和 $A^n$

**求逆**：由 $f_A(A) = 0$ 得 $A \cdot (\cdots) = \det(A)I$，从而求出 $A^{-1}$（若可逆）。

**求幂**：$A^n$ 可以用 $A^{n-1}, \ldots, A, I$ 的线性组合表示，从而递推计算任意次幂。

## A.3 最小多项式

$A$ 的**最小多项式** $m_A(\lambda)$ 是满足 $m_A(A) = 0$ 的**次数最低**的首一多项式。

- $m_A(\lambda) \mid f_A(\lambda)$（最小多项式整除特征多项式）
- $m_A(\lambda)$ 和 $f_A(\lambda)$ 有相同的根（不计重数）
- $A$ 可对角化 $\iff$ $m_A(\lambda)$ 无重根

> ⭐ 最小多项式是 $A$ 的最"简约"的零化多项式——它精确刻画了 $A$ 的 Jordan 结构。

### 📝 例 37-38（最小多项式的计算）

包括利用最小多项式判断对角化、求 $A^n$ 等应用。

---

# 📋 重点速查卡片 {#cheatsheet}

## 特征值与特征向量

$$\boxed{A\mathbf{x} = \lambda \mathbf{x}, \quad \mathbf{x} \neq \mathbf{0}}$$

$$\boxed{\det(\lambda I - A) = 0} \quad \text{（特征方程）}$$

$$\boxed{\sum \lambda_i = \operatorname{tr}(A), \quad \prod \lambda_i = \det(A)}$$

## 对角化条件

| 条件 | 说明 |
|------|------|
| 充要条件 1 | $A$ 有 $n$ 个线性无关的特征向量 |
| 充要条件 2 | 每个特征值：几何重数 = 代数重数 |
| 充要条件 3 | 最小多项式无重根 |
| 充分条件 | $A$ 是实对称矩阵 → 可**正交**对角化 |

## Jordan 块速查

$$J_k(\lambda) = \begin{pmatrix} \lambda & 1 & & \\ & \ddots & \ddots & \\ & & \lambda & 1 \\ & & & \lambda \end{pmatrix}_{k \times k}$$

- 特征值 $\lambda$（$k$ 重）
- 几何重数 = 1
- $(J - \lambda I)^k = 0$（幂零指数 $k$）

## C-H 定理

$$\boxed{f_A(A) = 0}$$

即 $A$ 满足自己的特征方程。

## 重要关系

$$\boxed{1 \leq m_g(\lambda) \leq m_a(\lambda)}$$

$$\boxed{\dim\ker(\mathcal{A}) + \dim\operatorname{Im}(\mathcal{A}) = n}$$

## 相似 vs 相抵

| 关系 | 条件 | 含义 |
|------|------|------|
| **相抵** | $B = QAP^{-1}$ | 同一个线性映射，任选基 |
| **相似** | $B = P^{-1}AP$ | 同一个线性变换，任选基 |

---

<div style="text-align: center; font-size: 18pt; font-weight: bold; color: #1A3C6E; margin-top: 2cm;">

—— 祝学习顺利！——

</div>
