# 数据结构 · 第2章 导论 知识点与考点总结

> 《数据结构及其算法》复习资料 | 杨思辰 · 未来技术学院 · 物理学专业 · PB25992094
>
> 🔥 重点：**时间复杂度分析、语句频度计算、大 O 记号、ADT 定义**

---

## 目录

1. [§1 数据结构的基本概念](#sec1)
2. [§2 算法与算法分析](#sec2)
3. [§3 时间复杂度（大 O 记号）](#sec3)
4. [§4 语句频度手算（考试必考）](#sec4)
5. [§5 空间复杂度](#sec5)
6. [§6 抽象数据类型 ADT](#sec6)
7. [§7 易错点与速查卡片](#sec7)

---

# §1 数据结构的基本概念 {#sec1}

## 1.1 数据结构的定义

**数据结构** = 相互之间存在一种或多种**特定关系**的数据元素的集合。

数据结构包含三个层面：

| 层面 | 含义 | 示例（线性表） |
|:--|:--|:--|
| **逻辑结构** | 数据元素之间的**逻辑关系**（与计算机无关） | 一对一：前驱→后继 |
| **存储结构**（物理结构） | 数据在计算机中的**存储方式** | 顺序存储 / 链式存储 |
| **运算** | 对数据施加的操作 | 插入、删除、查找、排序 |

## 1.2 四种基本逻辑结构

| 结构 | 关系 | 示例 |
|:--|:--|:--|
| **集合** | 数据之间无关系（仅同属一个集合） | 查找表 |
| **线性结构** | **一对一** | 线性表、栈、队列、串 |
| **树形结构** | **一对多**（层次关系） | 树、二叉树 |
| **图/网状结构** | **多对多** | 图 |

## 1.3 两种基本存储结构

| | 顺序存储 | 链式存储 |
|:--|:--|:--|
| 实现方式 | 连续内存（数组） | 结点 + 指针 |
| 存取方式 | **随机存取** $O(1)$ | **顺序存取** $O(n)$ |
| 空间 | 预分配，可能浪费 | 按需分配，有指针开销 |
| 插入/删除 | 需移动元素 $O(n)$ | 只需改指针 $O(1)$ |

---

# §2 算法与算法分析 {#sec2}

## 2.1 算法的五个特性

| 特性 | 含义 |
|:--|:--|
| **有穷性** | 执行有限步后结束 |
| **确定性** | 每条指令含义明确、无歧义 |
| **可行性** | 每条操作都是可执行的 |
| **输入** | 可有零个或多个输入 |
| **输出** | 至少有一个输出 |

## 2.2 算法设计的要求

正确性 → 可读性 → 健壮性 → **效率**（时间 + 空间）

## 2.3 衡量效率的指标

- **时间复杂度 $T(n)$**：算法执行所需时间随问题规模 $n$ 的增长趋势
- **空间复杂度 $S(n)$**：算法执行所需辅助存储空间随 $n$ 的增长趋势

> 💡 分析效率时关注的是**增长率**（增长趋势），不是精确的执行时间。

---

# §3 时间复杂度（大 O 记号）{#sec3}

## 3.1 大 O 记号的定义

$$T(n) = O(f(n)) \quad \text{当且仅当存在正常数 } C, n_0 \text{ 使得 } T(n) \le C \cdot f(n) \text{ 对 } n \ge n_0 \text{ 成立}$$

**直观含义**：$T(n)$ 的增长速度**不超过** $f(n)$，$f(n)$ 是 $T(n)$ 的**上界**。

## 3.2 常见时间复杂度排序（从快到慢）

$$\boxed{O(1) < O(\log n) < O(n) < O(n \log n) < O(n^2) < O(n^3) < O(2^n) < O(n!)}$$

| 复杂度 | 名称 | 典型算法 | $n=10^6$ 的体感 |
|:--|:--|:--|:--|
| $O(1)$ | 常数阶 | 数组按下标访问 | 瞬间 |
| $O(\log n)$ | 对数阶 | 二分查找 | 瞬间（约 20 次） |
| $O(n)$ | 线性阶 | 顺序查找 | 约 0.001 秒 |
| $O(n \log n)$ | 线性对数阶 | 快排/归并/堆排序 | 约 0.02 秒 |
| $O(n^2)$ | 平方阶 | 冒泡/插入/选择排序 | **约 11 天！** |
| $O(2^n)$ | 指数阶 | 汉诺塔 | 不可完成 |

## 3.3 大 O 简化规则

1. **只保留最高阶项**：$3n^2 + 5n + 100 \to O(n^2)$
2. **去掉常数系数**：$5n \log n \to O(n \log n)$
3. 加法和乘法法则：
   - $O(f) + O(g) = O(\max(f, g))$
   - $O(f) \times O(g) = O(f \cdot g)$

---

# §4 语句频度手算（考试必考）{#sec4}

> 🔥 考试常见题型：给一段代码，求每条语句的频度和时间复杂度。

## 4.1 单层循环

```c
for (i = 0; i < n; i++)     // 频度: n+1 (判 n+1 次，体执行 n 次)
    x++;                     // 频度: n
```

总时间 $T(n) = O(n)$。

## 4.2 双层循环

```c
for (i = 0; i < n; i++)              // 频度: n+1
    for (j = 0; j < n; j++)          // 频度: n(n+1)
        x++;                          // 频度: n²
```

总时间 $T(n) = O(n^2)$。

## 4.3 依赖外层变量的内层循环

```c
for (i = 0; i < n; i++)              // n+1 次
    for (j = 0; j < i; j++)          // 第 i 轮执行 i 次
        x++;                          // 总计: 0+1+2+...+(n-1) = n(n-1)/2 次
```

$$\sum_{i=0}^{n-1} i = \frac{n(n-1)}{2} = O(n^2)$$

## 4.4 指数缩减循环

```c
i = 1;
while (i < n)
    i = i * 2;                       // i: 1,2,4,8,...,2^k ≥ n → 执行次数 k ≈ log₂n
```

总时间 $T(n) = O(\log n)$。

## 4.5 手算套路

| 循环类型 | 频度公式 |
|:--|:--|
| `for(i=0;i<n;i++)` | $n$ 次（体） + $n+1$ 次（判断） |
| 双层独立循环 | $n^2$ |
| 内层依赖外层 (`j<i`) | $n(n-1)/2$ |
| 每次 `i=i*2` | $\log_2 n$ |
| 三层嵌套 | $n^3$ |

---

# §5 空间复杂度 {#sec5}

**空间复杂度** = 算法**除输入数据外**额外占用的存储空间。

| 情况 | 空间复杂度 | 示例 |
|:--|:--|:--|
| 只使用几个变量 | $O(1)$（**原地**） | 冒泡排序 |
| 需要一个一维辅助数组 | $O(n)$ | 归并排序的临时数组 |
| 递归深度为 $n$ | $O(n)$ | 递归求阶乘 |
| 递归深度为 $\log n$ | $O(\log n)$ | 快速排序（最好情况） |

---

# §6 抽象数据类型 ADT {#sec6}

## 6.1 定义

**抽象数据类型（ADT）** = 一个**数学模型** + 定义在该模型上的一组**操作**。它**仅关注"做什么"（逻辑特性），不关注"怎么做"（实现细节）**。

## 6.2 ADT 三要素

$$\boxed{\text{ADT} = (D, S, P)}$$

| 要素 | 含义 |
|:--|:--|
| $D$（数据对象） | 元素的集合 |
| $S$（关系集合） | 数据元素之间的关系 |
| $P$（操作集合） | 对数据元素的基本操作 |

## 6.3 示例：线性表的 ADT

```
ADT List {
    数据对象: D = {a₁, a₂, ..., aₙ | aᵢ ∈ ElemType}
    数据关系: R = {<aᵢ₋₁, aᵢ> | aᵢ₋₁, aᵢ ∈ D, i=2,...,n}
    基本操作:
        InitList(&L)        // 初始化空表
        DestroyList(&L)     // 销毁表
        ListInsert(&L, i, e)  // 在第 i 位插入 e
        ListDelete(&L, i, &e) // 删除第 i 位元素，用 e 返回
        LocateElem(L, e)    // 查找 e 的位置
        ...
}
```

---

# §7 易错点与速查卡片 {#sec7}

## 7.1 常见易错点

| 易错点 | 正确做法 |
|:--|:--|
| 逻辑结构 ≠ 存储结构 | 逻辑是"一对多关系"，存储是"数组还是链表" |
| 频度计算双重循环 | 内层依赖外层时是 $\sum i$，不是 $n^2$ |
| $O(n^2)$ 不一定比 $O(n\log n)$ 差 | 当 $n$ 很小时常数因子可能更大 → 综合看 |
| 算法时间 ≠ 程序运行时间 | 算法分析看**增长趋势**，实际时间还受硬件影响 |
| ADT 是抽象定义 | ADT 定义"是什么操作"，不规定"怎么实现" |

## 7.2 核心速查

| 要点 | 内容 |
|:--|:--|
| 逻辑结构四类 | 集合、线性、树、图 |
| 存储结构两类 | 顺序（数组）、链式（指针） |
| 大 O 简化 | 保留最高阶、去系数 |
| $O(1) < O(\log n) < O(n) < O(n\log n) < O(n^2) < O(n^3) < O(2^n)$ |
| 单循环 $n$ 次 | $O(n)$ |
| 双层（独立） | $O(n^2)$ |
| 双层（$j<i$） | $O(n^2)$：$\frac{n(n-1)}{2}$ |
| `i=i*2` 型 | $O(\log n)$ |
| ADT 三要素 | 数据对象 D + 关系 S + 操作 P |

---

<div style="text-align: center; margin-top: 2em;">

—— 第 2 章「导论」知识点总结完 ——

</div>
