# 数据结构：第1-3章复习测验 · 详细解答

> 基于课堂复习测验 PDF | 杨思辰 · 未来技术学院 · 物理学专业
>
> 🔥 考察章节：**第1章导论 · 第2章线性表 · 第3章栈和队列**
>
> 题型：单项选择题（20×1'=20'）+ 应用题（3×10'=30'），满分 50'

## 目录

1. [一、单项选择题（1-20）](#mcq)
2. [二、应用题第1题——时间复杂度分析（递归）](#app1)
3. [三、应用题第2题——删除链表所有值为x的结点](#app2)
4. [四、应用题第3题——括号匹配](#app3)

---

# 一、单项选择题 {#mcq}

### 第1题

> 研究数据结构时，主要研究的是数据的（ ）
> A. 逻辑结构  B. 存储结构  C. 逻辑结构和存储结构  D. 逻辑结构、存储结构及其基本操作

**答案：D**

**解析**：数据结构 = **逻辑结构 + 存储结构 + 操作**三位一体。仅仅"逻辑结构"（如线性表、树）不够，还要研究"如何存"（顺序/链式）和"怎么操作"（增删改查）。三者缺一不可，这才构成完整的数据结构理论。

---

### 第2题

> 算法分析的两个主要方面是（ ）
> A. 正确性和简单性  B. 空间复杂度和时间复杂度  C. 可读性和文档性  D. 数据复杂性和程序复杂性

**答案：B**

**解析**：算法分析的核心就是**时间**和**空间**两把尺子——一个量"有多快"（大O），一个量"要占用多少内存"。A/C 是代码质量指标，但不是"算法分析"的内容。

---

### 第3题

> 下列说法正确的是（ ）
> A. 数据的逻辑结构依赖于其存储结构  B. 存储结构独立于逻辑结构
> C. 逻辑结构描述数据元素之间的相互关系，与存储无关  D. 存储结构仅包含顺序存储一种形式

**答案：C**

**解析**：逻辑结构（如"这是一个线性表"）是**抽象的数学描述**，**不依赖于**怎么存、用什么语言。先定逻辑、再选存储。A 恰好说反了；B 错在"独立"——存储结构是逻辑结构的具体实现，有依赖关系；D 错在"仅有"——还有链式、索引、散列等存储方式。

---

### 第4题：时间复杂度与（ ）无关

> A. 问题的规模  B. 计算机的运算速度  C. 算法的输入数据  D. 算法中基本操作的执行次数

**答案：B**

**解析**：时间复杂度是**算法自身**的属性（量级分析），和"用哪台电脑跑"无关。你把同一个 $O(n^2)$ 算法放 286 上跑和放超算上跑，时间差千倍，但**复杂度还是 $O(n^2)$**。大 O 比较的是"增长率"，不是"绝对秒数"。

> ⚠️ C 项需要小心的说法：时间复杂度**可能**与输入数据有关（如"最好情况 O(n)、最坏情况 O(n^2)"），但大 O 一般指**最坏情况**的增长率。此处考点明确是"与计算机性能无关"。

---

### 第5题

```c
for(i=1; i<=n; i++)
    for(j=1; j<=n; j++)
        x++;
```

**答案：B — $O(n^2)$**

**解析**：外层跑 $n$ 次、内层每轮也跑 $n$ 次，总共 $n \times n = n^2$ 次基本操作。

---

### 第6题

```c
i=1;
while(i<=n) { x++; i=i*2; }
```

**答案：C — $O(\log_2 n)$**

**解析**：`i` 每轮翻倍：$1,2,4,8,\dots$，问"翻多少次到 $n$？"——$2^k = n \Rightarrow k = \log_2 n$。所以循环体只执行约 $\log_2 n$ 次。

> 💡 这类题要会一眼识别：**循环变量指数增长 → 对数复杂度**。

---

### 第7题

> 在长度为n的顺序表中，在第i个位置（1 ≤ i ≤ n+1）插入一个新元素，需要移动的元素个数为（ ）

**答案：C — $n-i+1$**

**解析**：顺序表插入需**把第 i 到第 n 的元素整体后移一个空位**，共 $n-i+1$ 个。

> 验证：`i=n+1`（插在末尾）→ 移动 `n-(n+1)+1 = 0` 个 ✓；`i=1`（插在开头）→ 移动 `n-1+1 = n` 个 ✓。

---

### 第8题

> 在单链表中，删除p指针所指结点的后继结点，其时间复杂度为（ ）

**答案：A — $O(1)$**

**解析**：已知 `p` 指针，它的后继就是 `p->next`，删除只需改两行指针：`q=p->next; p->next=q->next; free(q);`——**不依赖链表长度**，全是常数步。这就是链表比顺序表的优势：**给定位置时，插入/删除只需 $O(1)$。**

---

### 第9题

> 若线性表最常用的操作是取第i个元素和查找第i个元素的前驱，则采用（ ）存储方式最节省时间

**答案：A — 顺序表**

**解析**：**随机存取**（直接 `data[i-1]` 拿到第 i 个元素，$O(1)$）是顺序表的杀手锏。链表要访问第 i 个，得从头走到尾，$O(n)$。

---

### 第10题

> 关于顺序表和链表的比较，下列说法错误的是（ ）

**答案：C — "顺序表的存储密度低于链表"**

**解析**：**存储密度 = 数据本身空间 / 总占用空间**。顺序表**几乎全用来装数据**（密度 ≈ 1），链表每个结点除了数据还要多放一个 `next` 指针（密度 < 1）。C 恰好说反。

> 💡 记法："顺序表→纯数据→密度高；链表→加指针→密度低。"

---

### 第11题

> 栈和队列的共同特点是（ ）

**答案：C — 只允许在端点处插入和删除元素**

**解析**：栈在一端（栈顶）、队列在两端（队头出、队尾入），但都属于**操作受限的线性表**——不允许在任意位置随便插入/删除。A 是队列特性（FIFO）、B 是栈特性（LIFO），C 是共同点。

---

### 第12题

> 一个栈的入栈序列为1,2,3,4,5，则以下不可能的出栈序列是（ ）

**答案：D — 3,4,1,2,5**

**解析**（模拟法最稳妥）：3 先出 → 说明 1,2,3 已入栈，弹 3，此时栈中为 [1,2]，栈顶 = 2。接着输出 4 → 4 入→弹，栈仍 [1,2]。此时栈顶 = **2**，不可能输出 **1**（2 压在 1 上面）。所以 D 非法。

> A（全顺序）、B（全逆序）、C（3,2,1,5,4）均合法。

---

### 第13题

> 循环队列存储在数组A[0..n-1]中，front 队头指针，rear 队尾指针（指向队尾下一空位），则队列满的条件是（ ）

**答案：B — $(rear+1) \bmod n == front$**

**解析**：这就是我们之前花时间讲透的那个条件。浪费一个单元，`(rear+1)%n == front` 即满。

---

### 第14题

> 判断一个循环队列Q（最多n个元素）为空的条件是（ ）

**答案：A — Q.front == Q.rear**

**解析**：front 和 rear 重合 = 空。14 题和 13 题是一对——一个考空、一个考满，成对出现。

---

### 第15题

> 栈的典型应用不包括（ ）

**答案：D — 打印任务排队**

**解析**：打印任务是"先来先服务"→ **队列**的典型应用（FIFO）。A（递归调用→函数栈帧）、B（后退键→历史栈）、C（数制转换→逆序输出）全都是**栈**的应用。

---

### 第16题

> 队列的典型应用是（ ）

**答案：C — 操作系统中进程调度**

**解析**：进程就绪队列按 **FCFS（先到先服务）调度**，严格按到达顺序分配 CPU。A/B 是栈应用，D 递归靠系统栈——也是栈。

---

### 第17题

> 与数据元素本身的形式、内容、内存相对位置无关的是数据的（ ）

**答案：B — 逻辑结构**

**解析**：**逻辑结构**是纯粹的数学关系（"what"）——如"这些元素之间是线性关系"，不管元素是数字还是学生记录、不管存硬盘还是内存。存储/物理结构才是"how"——具体的存法。

---

### 第18题

> 带头结点的循环单链表L，判断链表为空的条件是（ ）

**答案：C — L->next == L**

**解析**：带头结点循环链表空时：只有**一个头结点**，头结点的 `next` 指向它自己。所以 `L->next == L` 即空。

> 💡 想象"一条蛇咬着自己的尾巴"的空环。

---

### 第19题

> 在非循环的顺序表中，逻辑上相邻的两个元素在物理位置上（ ）

**答案：A — 一定相邻**

**解析**：**顺序表 = 数组**：逻辑上第 $i$ 个就放在物理的第 $i$ 个格子，逻辑相邻 $\implies$ 物理也相邻。这是顺序表"顺序"二字的本质义。

---

### 第20题

> 若某线性表最常用的操作是在表尾插入和删除元素，则最节省时间的存储方式是（ ）

**答案：C — 带尾指针的循环单链表**

**解析**：操作限定在**表尾**进行，所以关键是**表尾能否 $O(1)$ 访问**：
- 顺序表：尾插入 $O(1)$（直接 `data[++len]=x`）、尾删除 $O(1)$（直接 `len--`），但这里有一个**坑**：顺序表容量有限，可能满。选项 C 才是"无论满不满都能 $O(1)$"的答案。
- 单链表：尾插入需要从表头走到表尾 → $O(n)$
- 带尾指针的循环单链表：`rear` 直指最后一个结点，尾插入 $O(1)$；删除尾结点需要找前驱，但 `rear` 即最后一个结点，找前驱仍需要一次遍历。这里选项 C 为最近似答案，因为可借助循环结构利用 `rear->next == head` 的性质。

> 延伸思考：若要用链表在 $O(1)$ 内完成尾插入和尾删除，需要**双向循环链表**。

---

# 二、应用题第1题 {#app1}

## 题目

分析以下递归算法的时间复杂度，写出推导过程：

```c
int func(int n) {
    if (n <= 1) return 1;
    else return func(n/2) + func(n/2);
}
```

## 解答

### （1）递归方程（递推关系式）

每次递归调用规模折半，且调用了自己**两次**。设 $T(n)$ 为输入规模 $n$ 的时间开销：

$$T(n) = 2T\left(\frac{n}{2}\right) + O(1)$$
$$T(1) = O(1)$$

其中 $O(1)$ 是 `if` 判断和 `return` 语句的常数开销。

### （2）代入法展开

$$\begin{aligned}
T(n) &= 2T\left(\frac{n}{2}\right) + 1 \\
     &= 2\left[2T\left(\frac{n}{4}\right) + 1\right] + 1 = 4T\left(\frac{n}{4}\right) + 3 \\
     &= 4\left[2T\left(\frac{n}{8}\right) + 1\right] + 3 = 8T\left(\frac{n}{8}\right) + 7 \\
     &= 2^k \cdot T\left(\frac{n}{2^k}\right) + (2^k - 1)
\end{aligned}$$

当 $\frac{n}{2^k} = 1$（即 $k = \log_2 n$）时到达递归基：

$$T(n) = 2^{\log_2 n} \cdot T(1) + (2^{\log_2 n} - 1) = n \cdot 1 + (n - 1) = 2n - 1$$

### （3）大O表示

$$\boxed{\,T(n) = O(n)\,}$$

> 💡 这道算法看起来像是"每次都把问题拆成两半"，直觉可能误以为 $O(\log n)$，实则**每层总工作量翻倍**（画递归树会发现第 $k$ 层有 $2^k$ 个结点），总工作量 $1+2+4+\dots+n = O(n)$。

---

# 三、应用题第2题 {#app2}

## 题目

已知带头结点的单链表 L，设计算法**删除链表中所有值为 x 的结点**。用类 C 语言伪代码描述，并分析时间复杂度。

## 解答

### 思路

用 `pre` 指针记录当前结点的**前驱**，`p` 指针遍历链表。当 `p->data == x` 时：`pre->next = p->next; free(p); p = pre->next;`——跳过并删除。否则继续走。

> ⚠️ 关键边界：由于是**带头结点**的单链表，真正的第一个数据结点是 `L->next`，且**头结点本身永远不会被删**，pre 初始指向头结点即可，无需特判"删除第一个元素"的情况。

### 代码（类 C 伪代码）

```c
void deleteAllX(LinkList L, int x) {
    Node *pre = L;            // pre 指向头结点（不是数据结点）
    Node *p   = L->next;      // p 指向第一个数据结点

    while (p != NULL) {
        if (p->data == x) {
            pre->next = p->next;   // 绕过 p
            free(p);               // 释放内存
            p = pre->next;         // p 前进到下一个
        } else {
            pre = p;
            p   = p->next;         // 双双前进
        }
    }
}
```

### 复杂度分析

**时间复杂度**：扫描整个链表恰好一次，每个结点被访问一次 → **$O(n)$**。

**空间复杂度**：仅用了两个指针变量（`pre` 和 `p`）→ **$O(1)$**（原地算法）。

---

# 四、应用题第3题 {#app3}

## 题目

给定一个仅包含 `()`、`[]`、`{}` 六种括号的字符串，判断括号是否匹配。要求说明数据结构选择、伪代码、复杂度分析。

## 解答

### （1）数据结构选择：栈（Stack）

**理由**：括号匹配天然是 **LIFO（后进先出）**——最近遇到的左括号，必须最先被配对。这恰好是栈的行为。

> 💡 直观理解："每次遇到右括号，它必须配对上"离它最近的尚未配对"的左括号"——就像摞盘子，最上面的先被拿走。

### （2）算法步骤

初始化空栈 `S`。从左到右扫描字符串 `str`：
- 遇**左括号** `( [ {` → 入栈
- 遇**右括号** `) ] }` → 查栈顶元素是否**同类型匹配**的左括号：
  - 是 → 弹出左括号，继续
  - 否（或栈空） → 返回"不匹配"
- 扫描完毕 → 栈必须为空才匹配

### 代码（类 C 伪代码）

```c
int bracketMatch(char *str) {
    char S[MAX];  int top = -1;        // 用数组模拟栈

    for (int i = 0; str[i] != '\0'; i++) {
        char c = str[i];
        if (c == '(' || c == '[' || c == '{') {
            S[++top] = c;              // 左括号入栈
        } else if (c == ')' || c == ']' || c == '}') {
            if (top == -1) return 0;   // 右括号但栈空 → 失配
            char left = S[top--];      // 弹栈顶左括号
            if ((c == ')' && left != '(') ||
                (c == ']' && left != '[') ||
                (c == '}' && left != '{'))
                return 0;              // 类型不匹配
        }
    }
    return top == -1;                  // 扫描完必须栈空
}
```

### （3）复杂度分析

- **时间复杂度 $O(n)$**：每个字符被访问恰好一次，入栈/出栈均 $O(1)$。
- **空间复杂度 $O(n)$**：最坏情况下全是左括号（如 `(((((`），栈深 = $n$。

---

# 📋 小测重点速查卡片

## 选择题：高频易错速记

| 重点 | 一句话 |
|------|--------|
| 数据结构 = | 逻辑结构 + 存储结构 + 操作 |
| 复杂度分析 = | 时间 + 空间（不管电脑多快）|
| 循环翻倍 → | $O(\log n)$（`i*=2`）；二分递归也可能 $O(n)$ |
| 顺序表插入移动 | $n-i+1$（第 i 个位置） |
| 链表删后继 | $O(1)$（已知前驱时） |
| 顺序表 → 随机存取 | $O(1)$ 取第 i 个 |
| 存储密度 | 顺序表 ≈ 1 > 链表 |
| 循环链表判空 | `L->next == L`（带头结点） |
| 打印 / FCFS | 队列（FIFO） |
| 递归 / 后退 / 括号 | 栈（LIFO） |

## 应用题模板

| 题型 | 答题结构 |
|------|---------|
| 递归复杂度 | ①写递推式 → ②展开到 $T(1)$ → ③大 O |
| 链表删除 | pre/p 双指针，`pre->next=p->next; free(p)`，$O(n)$ |
| 括号匹配 | 栈（LIFO），左入右弹，最终栈空，$O(n)/O(n)$ |

---

—— 巩固到位，考试不慌！ ——
