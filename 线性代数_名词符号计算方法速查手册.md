# 线性代数 B1 名词·符号·计算方法 速查手册

> 基于课堂 PPT (B1, 2026.03) | 杨思辰 · 未来技术学院 · 物理学专业
>
> 涵盖第1-7章全部核心内容：向量→线性方程组→行列式→矩阵→线性空间→特征值与Jordan→内积空间

---

# 目录

1. [第一章：向量与复数](#ch1)
2. [第二章：线性方程组](#ch2)
3. [第三章：行列式](#ch3)
4. [第四章：矩阵](#ch4)
5. [第五章：线性空间](#ch5)
6. [第六章：特征值与 Jordan 标准形](#ch6)
7. [第七章：内积空间与正交变换](#ch7)
8. [附录：全课程公式汇总](#appendix)

---

# 第一章：向量与复数 {#ch1}

## 一、名词

| 名词 | 英文 | 定义 |
|------|------|------|
| **向量** | vector | 既有大小又有方向的量 |
| **模（长度）** | norm / magnitude | 向量的大小，记为 $\|\alpha\|$ |
| **零向量** | zero vector | 模为0的向量，记为 $\mathbf{0}$，方向不定 |
| **单位向量** | unit vector | 模为1的向量，$\|\alpha\| = 1$ |
| **反向量** | opposite vector | $\alpha$ 的反向量 $-\alpha$，大小相同方向相反 |
| **共线** | collinear | 平行于同一直线的一组向量 |
| **共面** | coplanar | 平行于同一平面的一组向量 |
| **线性组合** | linear combination | $\beta = \lambda_1\alpha_1 + \cdots + \lambda_s\alpha_s$ |
| **线性相关** | linearly dependent | 存在不全为零的系数使线性组合为零向量 |
| **线性无关** | linearly independent | 只有全零系数才能使线性组合为零向量 |
| **坐标系** | coordinate system | $[O; \mathbf{e}_1, \mathbf{e}_2, \mathbf{e}_3]$，原点+基向量 |
| **仿射坐标系** | affine coordinate system | 最一般的坐标系，基只需不共面 |
| **笛卡尔坐标系** | Cartesian coordinate system | 基向量均为单位向量 |
| **直角坐标系** | rectangular coordinate system | 基向量单位且两两正交 |
| **$\mathbb{R}^3$ 数组空间** | 3-dimensional array space | $\{(a_1,a_2,a_3) \mid a_i \in \mathbb{R}\}$ 配坐标运算 |
| **集合** | set | 一些对象的全体 |
| **空集** | empty set | 不含任何元素的集合，记为 $\varnothing$ |
| **映射** | mapping | $f: A \to B$，每个$a$对应唯一$b$ |
| **单射** | injection | $a_1 \neq a_2 \Rightarrow f(a_1) \neq f(a_2)$ |
| **满射** | surjection | $\operatorname{Im}(f) = B$ |
| **双射** | bijection | 既是单射又是满射，等同于可逆 |
| **等价关系** | equivalence relation | 满足自反性、对称性、传递性的关系 |
| **等价类** | equivalence class | $[a] = \{a' \mid a' \sim a\}$ |
| **商集** | quotient set | $A/\!\sim$，所有等价类的集合 |
| **环** | ring | 有加法和乘法且满足八条公理的代数结构 |
| **域** | field | 乘法可交换且非零元可逆的环 |
| **数域** | number field | 对四则运算封闭的复数子集 |
| **域扩张** | field extension | $\mathbb{Q}(\sqrt{2})$ 等，在基域上添加新元素 |
| **线性空间** | linear space / vector space | 满足八条运算律的代数结构（公理化定义） |

## 二、符号

| 符号 | 含义 | 示例 |
|------|------|------|
| $\overrightarrow{AB}$ | 以A为起点B为终点的向量 | — |
| $\vec{a}, \alpha$ | 向量 | — |
| $\|\alpha\|$ | 向量的模 | $\|(3,4)\| = 5$ |
| $\mathbf{0}$ | 零向量 | — |
| $-\alpha$ | 反向量 | — |
| $\lambda\alpha$ | 数乘 | $2\alpha$ |
| $\varnothing$ | 空集 | — |
| $\in, \notin$ | 属于/不属于 | $a \in A$ |
| $\subseteq, \subset$ | 子集/真子集 | $A \subseteq B$ |
| $\cap, \cup$ | 交/并 | $A \cap B$ |
| $\setminus$ | 差集 | $A \setminus B$ |
| $\times$ | 笛卡尔积 | $A \times B$ |
| $f: A \to B$ | 映射 | — |
| $f^{-1}$ | 逆映射 | — |
| $\operatorname{id}_A$ | A上的恒等映射 | — |
| $\circ$ | 映射复合 | $g \circ f$ |
| $\sim$ | 等价关系 | $a \sim a'$ |
| $[a]$ | 等价类 | — |
| $A/\!\sim$ | 商集 | — |
| $\mathbb{N}, \mathbb{Z}, \mathbb{Q}, \mathbb{R}, \mathbb{C}$ | 自然数/整数/有理数/实数/复数集 | — |
| $\mathbb{F}$ | 一般数域 | — |
| $\mathbb{F}[x]$ | 多项式环 | — |
| $\mathbb{Q}(\sqrt{2})$ | 域扩张 | $\{a+b\sqrt{2}: a,b\in\mathbb{Q}\}$ |
| $\cong$ | 同构 | $\mathbb{C} \cong \mathbb{R}^2$ |

## 三、计算方法

### 1. 向量运算
- **加法**：平行四边形法则或三角形法则
- **数乘**：长度乘 $|\lambda|$，方向：$\lambda>0$同向，$\lambda<0$反向
- **坐标运算**：$(x_1,y_1,z_1)+(x_2,y_2,z_2)=(x_1+x_2,y_1+y_2,z_1+z_2)$

### 2. 线性相关性判定
- 设有$\alpha_1,\ldots,\alpha_s$，解$\lambda_1\alpha_1+\cdots+\lambda_s\alpha_s=\mathbf{0}$
- 若只有零解 → 线性无关；若有非零解 → 线性相关
- 快速判据：向量个数 > 空间维数 → 必线性相关

### 3. 共线/共面判定
- 共线：$\exists \lambda,\mu$不全为零，$\lambda\alpha+\mu\beta=0$
- 共面：$\exists \lambda,\mu,\nu$不全为零，$\lambda\alpha+\mu\beta+\nu\gamma=0$

---

# 第二章：线性方程组 {#ch2}

## 一、名词

| 名词 | 英文 | 定义 |
|------|------|------|
| **线性方程组** | system of linear equations | 一组线性方程的联立 |
| **系数矩阵** | coefficient matrix | 由方程组系数构成的矩阵 $A$ |
| **增广矩阵** | augmented matrix | 系数矩阵右边加常数项列，记为 $(A \mid \mathbf{b})$ |
| **初等变换** | elementary operations | 交换、倍乘、倍加三种方程操作 |
| **初等行变换** | elementary row operations | 对增广矩阵行的相应操作 |
| **Gauss 消元法** | Gaussian elimination | 用初等行变换化为阶梯形，再回代求解 |
| **阶梯形** | echelon form | 每行首非零元（主元）逐行右移的矩阵形式 |
| **主元** | pivot | 每行第一个非零元素 |
| **回代** | back substitution | 从最后一个方程向上逐一代入求解 |
| **通解** | general solution | 含自由参数的所有解的表达式 |
| **特解** | particular solution | 取定自由参数的具体一个解 |
| **齐次线性方程组** | homogeneous system | $A\mathbf{x} = \mathbf{0}$ |
| **非齐次线性方程组** | non-homogeneous system | $A\mathbf{x} = \mathbf{b} (\mathbf{b} \neq \mathbf{0})$ |
| **零解** | trivial solution | $\mathbf{x} = \mathbf{0}$ |
| **非零解** | nontrivial solution | 齐次方程组的非零解 |
| **自由参数/自由变量** | free variable | 可取任意值的变量 |

## 二、符号

| 符号 | 含义 |
|------|------|
| $x_i$ | 未知数 |
| $a_{ij}$ | 第$i$个方程第$j$个未知数的系数 |
| $b_i$ | 第$i$个方程的常数项 |
| $m$ | 方程个数 |
| $n$ | 未知数个数 |
| $r$ | 阶梯形非零行数（有效方程数） |
| $c_{ii}, c_{ij_i}$ | 主元 |
| $d_i$ | 阶梯形常数项 |
| $(A \mid \mathbf{b})$ | 增广矩阵 |
| $\mathbb{F}$ | 数域（系数和解的集合） |

## 三、计算方法

### Gauss 消元法（核心算法）

**步骤**：
1. 写出增广矩阵 $(A \mid \mathbf{b})$
2. 用初等行变换化为阶梯形
3. 根据阶梯形判定解的情况（无解/唯一解/无穷多解）
4. 若有解，回代求出通解

**解的判定**：
- 出现 $0 = d_{r+1} \neq 0$ → **无解**
- 无矛盾行且 $r = n$ → **唯一解**
- 无矛盾行且 $r < n$ → **无穷多解**（$n-r$ 个自由参数）

**求通解方法**：
1. 由阶梯形写出等价方程组
2. 确定主变量和自由变量
3. 令自由变量为参数 $t_1, t_2, \ldots$
4. 从下往上回代，用参数表示主变量

**含参方程组**：
- 化为含参数的阶梯形
- 令 $d_{r+1} = 0$ 解出参数条件
- 在不同参数范围内分别讨论

---

# 第三章：行列式 {#ch3}

## 一、名词

| 名词 | 英文 | 定义 |
|------|------|------|
| **行列式** | determinant | 方阵到标量的一个函数，记为 $\det(A)$ 或 $\|A\|$ |
| **余子式** | minor | $M_{ij}$：划去第$i$行第$j$列后的$n-1$阶行列式 |
| **代数余子式** | cofactor | $A_{ij} = (-1)^{i+j} M_{ij}$ |
| **展开** | expansion | 按某行/列将$n$阶行列式化为$n-1$阶的线性组合 |
| **转置** | transpose | $A^T$，行列互换 |
| **Vandermonde 行列式** | Vandermonde determinant | 形如 $\det(x_i^{j-1})$ 的特殊行列式 |
| **Cramer 法则** | Cramer's rule | 用行列式之比表出线性方程组的解 |
| **排列** | permutation | $1,2,\ldots,n$ 的一个全排列 |
| **逆序数** | inversion number | $\tau(j_1\ldots j_n)$：排列中逆序的总数 |
| **奇/偶排列** | odd/even permutation | 逆序数为奇/偶的排列 |
| **对换** | transposition | 交换排列中两个数的位置（改变奇偶性） |
| **完全展开式** | full expansion / Leibniz formula | $\sum (-1)^{\tau} a_{1j_1}\cdots a_{nj_n}$（$n!$项） |
| **公理化定义** | axiomatic definition | 行列式是唯一满足列多线性+反对称+$\det(I)=1$的函数 |
| **Laplace 展开** | Laplace expansion | 按$k$行展开为子式与代数余子式乘积之和 |
| **Schur 补** | Schur complement | $S = D - CA^{-1}B$，用于分块矩阵行列式计算 |
| **有向体积** | oriented volume | 行列式的几何意义 |

## 二、符号

| 符号 | 含义 |
|------|------|
| $\det(A)$ 或 $\|A\|$ | $A$的行列式 |
| $M_{ij}$ | 余子式（划去第$i$行第$j$列） |
| $A_{ij}$ | 代数余子式 $= (-1)^{i+j}M_{ij}$ |
| $\begin{vmatrix} a & b \\ c & d \end{vmatrix}$ | 二阶行列式 $= ad-bc$ |
| $f_A(\lambda)$ | 特征多项式 $= \det(\lambda I - A)$ |
| $\tau(j_1\ldots j_n)$ | 排列的逆序数 |
| $\operatorname{sgn}(\sigma)$ | 排列的符号 $= (-1)^{\tau(\sigma)}$ |
| $\prod$ | 连乘（Vandermonde行列式等） |

## 三、计算方法

### 四种计算行列式的方法

**方法一：定义/展开法**（适用于零元多的矩阵）
1. 选择含零元最多的行或列
2. 对该行/列展开：$\det(A) = \sum_j (-1)^{i+j}a_{ij}M_{ij}$
3. 递归计算低阶行列式

**方法二：行变换法**（最通用，最推荐）
1. 用初等行变换化为上三角矩阵
2. 注意记录变换对行列式的影响：
   - 交换两行 → 变号
   - 某行乘$\lambda$ → 行列式乘$\lambda$
   - 倍加行 → 行列式不变
3. 上三角矩阵的行列式 = 主对角线乘积

**方法三：递推法**（三对角等特殊结构）
1. 按第一行展开得到递推关系 $D_n = a D_{n-1} + b D_{n-2}$
2. 解特征方程求通项

**方法四：拆项法**（行为和时）
1. 利用行列式对行的线性性拆分为多个行列式之和

### Cramer 法则

若 $\det(A) \neq 0$，则 $A\mathbf{x} = \mathbf{b}$ 的解：
$$x_i = \frac{\det(A_i)}{\det(A)}$$
其中 $A_i$ 是将 $A$ 的第$i$列替换为 $\mathbf{b}$。

### 重要公式

- **二阶**：$\begin{vmatrix} a & b \\ c & d \end{vmatrix} = ad - bc$
- **三阶（Sarrus）**：主对角线方向乘积和 − 副对角线方向乘积和
- **分块三角**：$\det\begin{pmatrix} A & B \\ 0 & D \end{pmatrix} = \det(A)\det(D)$
- **Vandermonde**：$\det(x_i^{j-1}) = \prod_{i<j}(x_j - x_i)$
- **矩阵乘积**：$\det(AB) = \det(A)\det(B)$
- **标量乘**：$\det(\lambda A) = \lambda^n \det(A)$

---

# 第四章：矩阵 {#ch4}

## 一、名词

| 名词 | 英文 | 定义 |
|------|------|------|
| **矩阵** | matrix | $m \times n$ 个数排成的数表 $(a_{ij})_{m \times n}$ |
| **方阵** | square matrix | $m = n$ 的矩阵 |
| **行向量** | row vector | $1 \times n$ 矩阵 |
| **列向量** | column vector | $n \times 1$ 矩阵 |
| **对角阵** | diagonal matrix | 非对角线全为零的方阵 |
| **单位阵** | identity matrix | 对角线全为1的对角阵，记为 $I_n$ 或 $E_n$ |
| **对称矩阵** | symmetric matrix | $A^T = A$，即 $a_{ij} = a_{ji}$ |
| **反对称矩阵** | skew-symmetric matrix | $A^T = -A$，即 $a_{ij} = -a_{ji}$（对角线必零） |
| **上/下三角矩阵** | upper/lower triangular matrix | 对角线以下/上全为零 |
| **转置** | transpose | $A^T$：行列互换 |
| **共轭转置** | conjugate transpose / Hermitian | $A^* = \bar{A}^T$ |
| **迹** | trace | $\operatorname{tr}(A) = \sum a_{ii}$（主对角线之和） |
| **逆矩阵** | inverse matrix | $AA^{-1} = A^{-1}A = I$ |
| **可逆/非奇异** | invertible / nonsingular | $\det(A) \neq 0$ |
| **初等矩阵** | elementary matrix | 对$I$做一次初等行变换得到的矩阵 |
| **初等行/列变换** | elementary row/column operations | 左乘/右乘初等矩阵 |
| **相抵** | equivalent (by row/col operations) | $B = QAP^{-1}$，$P,Q$可逆 |
| **秩** | rank | 矩阵中线性无关的行（列）数，记为 $r(A)$ |
| **满秩** | full rank | $r(A) = \min(m,n)$ |
| **伴随矩阵** | adjugate matrix | $\operatorname{adj}(A) = (A_{ji})$（注意转置） |
| **分块矩阵** | block matrix | 将矩阵按行、列分块 |
| **LU 分解** | LU decomposition | $A = LU$，$L$为单位下三角，$U$为上三角 |
| **PLU 分解** | PLU decomposition | $PA = LU$，$P$为置换矩阵 |
| **满秩分解** | full-rank factorization | $A = BC$，$B$列满秩，$C$行满秩 |

## 二、符号

| 符号 | 含义 |
|------|------|
| $(a_{ij})_{m \times n}$ | $m \times n$矩阵 |
| $A^T$ | 转置 |
| $A^*$ 或 $A^H$ | 共轭转置 |
| $I_n, E_n$ | $n$阶单位阵 |
| $\operatorname{diag}(d_1,\ldots,d_n)$ | 对角阵 |
| $A^{-1}$ | 逆矩阵 |
| $\operatorname{tr}(A)$ | 迹 |
| $r(A)$ 或 $\operatorname{rank}(A)$ | 秩 |
| $\operatorname{adj}(A)$ 或 $A^*$ | 伴随矩阵 |
| $P_{ij}$ | 交换型初等矩阵 |
| $D_i(\lambda)$ | 倍乘型初等矩阵 |
| $E_{ij}(\lambda)$ | 倍加型初等矩阵 |
| $\begin{pmatrix} A & B \\ C & D \end{pmatrix}$ | 分块矩阵 |

## 三、计算方法

### 矩阵乘法
$$c_{ij} = \sum_{k=1}^{s} a_{ik} b_{kj}$$
- 不满足交换律：一般 $AB \neq BA$
- 满足结合律和分配律
- $(AB)^T = B^T A^T$，$(AB)^{-1} = B^{-1}A^{-1}$（顺序反转！）

### 求逆矩阵的两种方法

**方法一：伴随矩阵法**
$$A^{-1} = \frac{1}{\det(A)} \operatorname{adj}(A)$$
适用于2阶、3阶方阵和理论推导。

**方法二：初等行变换法（最实用）**
$$(A \mid I) \xrightarrow{\text{初等行变换}} (I \mid A^{-1})$$

### 求矩阵的秩

**方法一：初等变换法**（最常用）
1. 用初等行变换化为阶梯形
2. 非零行数 = 秩

**方法二：子式法**
1. 找到最大的非零子式
2. 该子式的阶数 = 秩

### 秩的核心不等式
- $r(AB) \leq \min(r(A), r(B))$
- **Sylvester**：$r(AB) \geq r(A) + r(B) - n$
- **Frobenius**：$r(AB) + r(BC) \leq r(ABC) + r(B)$

### 分块矩阵求逆
$$\begin{pmatrix} A & B \\ 0 & D \end{pmatrix}^{-1} = \begin{pmatrix} A^{-1} & -A^{-1}BD^{-1} \\ 0 & D^{-1} \end{pmatrix}$$

### LU 分解（Doolittle 算法）
1. Gauss 消元过程中的乘数构成 $L$
2. 消元后得到的上三角矩阵为 $U$
3. 若需行交换 → $PA = LU$

---

# 第五章：线性空间 {#ch5}

## 一、名词

| 名词 | 英文 | 定义 |
|------|------|------|
| **线性空间（向量空间）** | linear space / vector space | 满足八条运算律的代数结构 |
| **子空间** | subspace | 对加法和数乘封闭的非空子集 |
| **平凡子空间** | trivial subspace | $\{\mathbf{0}\}$ 和 $V$ 本身 |
| **生成子空间** | span | $\operatorname{span}(\alpha_1,\ldots,\alpha_s)$：所有线性组合的集合 |
| **极大线性无关组** | maximal linearly independent set | 线性无关且再添加任何向量都变相关 |
| **秩（向量组）** | rank | 极大无关组中向量的个数 |
| **基** | basis | 线性无关且张成整个空间的向量组 |
| **维数** | dimension | 基中向量的个数，记为 $\dim V$ |
| **标准基** | standard basis | $\mathbb{F}^n$ 的 $\mathbf{e}_1,\ldots,\mathbf{e}_n$ |
| **坐标** | coordinates | 向量在基下的表示系数 |
| **过渡矩阵** | transition matrix | 两组基之间的关系矩阵 $T$ |
| **基础解系** | fundamental system of solutions | 齐次方程组解空间的一组基 |
| **核** | kernel / null space | $\ker(A) = \{\mathbf{x} \mid A\mathbf{x} = \mathbf{0}\}$ |
| **像** | image / column space | $\operatorname{Im}(A) = \{A\mathbf{x}\}$ |
| **零化度** | nullity | $\dim\ker(A) = n - r(A)$ |
| **交空间** | intersection | $W_1 \cap W_2$（仍是子空间） |
| **和空间** | sum | $W_1 + W_2 = \{w_1+w_2\}$（仍是子空间） |
| **直和** | direct sum | $W_1 \cap W_2 = \{\mathbf{0}\}$ 时的和，记为 $W_1 \oplus W_2$ |
| **补空间** | complementary subspace | $V = W \oplus U$ 中的 $U$（不唯一！） |
| **同构** | isomorphism | 保持线性结构的双射 |
| **商空间** | quotient space | $V/W$，所有陪集 $\alpha+W$ 的集合 |

## 二、符号

| 符号 | 含义 |
|------|------|
| $V$ | 线性空间 |
| $\mathbb{F}^n$ | $n$元数组构成的线性空间 |
| $\dim V$ | $V$的维数 |
| $\operatorname{span}(S)$ | 向量组$S$张成的子空间 |
| $\ker(A)$ | 矩阵$A$的核（零空间） |
| $\operatorname{Im}(A)$ | 矩阵$A$的像（列空间） |
| $V_\lambda$ | 特征子空间 |
| $\oplus$ | 直和 |
| $\cong$ | 同构 |
| $V/W$ | 商空间 |
| $W^\perp$ | 正交补（第七章） |

## 三、计算方法

### 判断子空间（三要素检验）
1. 非空（含零向量）✓
2. 加法封闭：$u,v \in W \Rightarrow u+v \in W$
3. 数乘封闭：$u \in W, \lambda \in \mathbb{F} \Rightarrow \lambda u \in W$

### 求极大无关组和秩
1. 将向量为列排成矩阵
2. 化为阶梯形
3. 主元列对应的原列向量 = 极大无关组
4. 主元个数 = 秩

### 判断线性相关性
- 向量个数 > 维数 → **必相关**
- 含零向量 → **必相关**
- 两向量成比例 → **相关**
- 矩阵秩 = 向量个数 → **线性无关**

### 求基和维数
- $\dim \mathbb{F}^n = n$（标准基 $\mathbf{e}_1,\ldots,\mathbf{e}_n$）
- $\dim \mathbb{F}^{m \times n} = mn$（基 $\{E_{ij}\}$）
- $\dim \mathbb{F}[x]_n = n+1$（基 $\{1,x,\ldots,x^n\}$）
- $\dim \ker(A) = n - r(A)$

### 子空间维数公式
$$\dim(W_1 + W_2) = \dim W_1 + \dim W_2 - \dim(W_1 \cap W_2)$$

### 直和判定（等价条件）
1. $W_1 \cap W_2 = \{\mathbf{0}\}$
2. $\dim(W_1+W_2) = \dim W_1 + \dim W_2$
3. 零向量分解唯一

### 坐标变换
若 $(\eta_1,\ldots,\eta_n) = (\varepsilon_1,\ldots,\varepsilon_n)T$，则：
$$Y = T^{-1}X \quad \text{（$X$旧坐标，$Y$新坐标）}$$

### 同构判定
$$V_1 \cong V_2 \iff \dim V_1 = \dim V_2$$

---

# 第六章：特征值与 Jordan 标准形 {#ch6}

## 一、名词

| 名词 | 英文 | 定义 |
|------|------|------|
| **线性变换** | linear transformation | 保持加法和数乘的映射 $\mathcal{A}: V \to V$ |
| **特征值** | eigenvalue | $A\mathbf{x} = \lambda\mathbf{x}$ 中的 $\lambda$（$\mathbf{x} \neq \mathbf{0}$） |
| **特征向量** | eigenvector | 满足 $A\mathbf{x} = \lambda\mathbf{x}$ 的非零向量 |
| **特征多项式** | characteristic polynomial | $f_A(\lambda) = \det(\lambda I - A)$ |
| **特征子空间** | eigenspace | $V_\lambda = \ker(\lambda I - A)$ |
| **代数重数** | algebraic multiplicity | 特征值在特征多项式中的重数 $m_a$ |
| **几何重数** | geometric multiplicity | 特征子空间的维数 $m_g = \dim V_\lambda$ |
| **相似** | similar | $B = P^{-1}AP$（同一变换在不同基下） |
| **可对角化** | diagonalizable | 相似于对角矩阵 |
| **相似不变量** | similarity invariants | 特征多项式、特征值、迹、行列式、秩 |
| **不变子空间** | invariant subspace | $\mathcal{A}(W) \subseteq W$ |
| **根向量/广义特征向量** | generalized eigenvector | $(\mathcal{A} - \lambda\mathcal{I})^k\mathbf{x} = \mathbf{0}$ 的解 |
| **根子空间** | generalized eigenspace | 所有根向量的集合 |
| **Jordan 块** | Jordan block | $J_k(\lambda)$：对角为$\lambda$、超对角为1的矩阵 |
| **Jordan 标准形** | Jordan canonical form | 分块对角矩阵，每个块为 Jordan 块 |
| **Cayley-Hamilton 定理** | Cayley-Hamilton theorem | $f_A(A) = 0$（矩阵满足自己的特征方程） |
| **最小多项式** | minimal polynomial | 使 $m_A(A) = 0$ 的次数最低的首一多项式 |

## 二、符号

| 符号 | 含义 |
|------|------|
| $\lambda$ | 特征值 |
| $f_A(\lambda)$ | 特征多项式 |
| $m_A(\lambda)$ | 最小多项式 |
| $m_a(\lambda)$ | 代数重数 |
| $m_g(\lambda)$ | 几何重数 |
| $V_\lambda$ | 特征子空间 |
| $\sim$ | 相似关系 |
| $J_k(\lambda)$ | $k$阶 Jordan 块 |
| $J$ | Jordan 标准形 |
| $P$ | 相似变换矩阵 |
| $\mathcal{A}$ | 线性变换 |
| $\ker(\mathcal{A})$ | 核 |
| $\operatorname{Im}(\mathcal{A})$ | 像 |

## 三、计算方法

### 求特征值与特征向量

**步骤**：
1. 求特征多项式 $f_A(\lambda) = \det(\lambda I - A)$
2. 解 $f_A(\lambda) = 0$ 得所有特征值
3. 对每个 $\lambda_i$，解 $(\lambda_i I - A)\mathbf{x} = \mathbf{0}$ 得特征向量

**重要关系**：
- $\sum \lambda_i = \operatorname{tr}(A)$（特征值之和 = 迹）
- $\prod \lambda_i = \det(A)$（特征值之积 = 行列式）

### 对角化判定与实现

**可对角化的充要条件**（满足任一即可）：
1. $A$ 有 $n$ 个线性无关的特征向量
2. 每个特征值的几何重数 = 代数重数
3. 最小多项式无重根

**对角化步骤**：
1. 求所有特征值和特征向量
2. 将 $n$ 个线性无关的特征向量排成 $P$
3. 则 $P^{-1}AP = \operatorname{diag}(\lambda_1,\ldots,\lambda_n)$

**注意**：实对称矩阵必可正交对角化（更强！见第七章）

### 求 Jordan 标准形（四步法）

1. 求特征多项式，得到所有特征值及其代数重数
2. 对每个特征值 $\lambda$，计算 $\dim\ker((A-\lambda I)^k)$，$k=1,2,\ldots$，直到维数 = 代数重数
3. 由维数差确定各阶 Jordan 块的个数：
   $k$阶块的个数 = $r_{k-1} - 2r_k + r_{k+1}$，其中 $r_k = \dim\ker((A-\lambda I)^k)$
4. 求广义特征向量，拼成过渡矩阵

**关键规律**：
- 几何重数 = Jordan 块的个数（每个块贡献一个特征向量）
- 代数重数 = 所有对应 Jordan 块阶数之和
- Jordan 块的最大阶数 = $(A-\lambda I)$ 的幂零指数

### Cayley-Hamilton 定理的应用

1. **求逆**：由 $f_A(A) = 0$ 解出 $A^{-1}$
2. **求幂**：$A^n$ 可用 $I, A, \ldots, A^{n-1}$ 线性表示
3. **判断对角化**：若最小多项式无重根 → 可对角化

---

# 第七章：内积空间与正交变换 {#ch7}

## 一、名词

| 名词 | 英文 | 定义 |
|------|------|------|
| **内积** | inner product | 满足对称性、线性性、正定性的二元函数 $(\cdot,\cdot)$ |
| **欧几里得空间** | Euclidean space | 配备内积的实线性空间 |
| **长度（模）** | norm | $\|\alpha\| = \sqrt{(\alpha,\alpha)}$ |
| **夹角** | angle | $\langle\alpha,\beta\rangle = \arccos\frac{(\alpha,\beta)}{\|\alpha\|\|\beta\|}$ |
| **正交（垂直）** | orthogonal | $(\alpha,\beta) = 0$，记为 $\alpha \perp \beta$ |
| **Cauchy-Schwarz 不等式** | Cauchy-Schwarz inequality | $\|(\alpha,\beta)\| \leq \|\alpha\| \cdot \|\beta\|$ |
| **三角不等式** | triangle inequality | $\|\alpha+\beta\| \leq \|\alpha\| + \|\beta\|$ |
| **标准正交基** | orthonormal basis | 两两正交且均为单位向量的基 |
| **Gram 矩阵** | Gram matrix | $G = ((\alpha_i,\alpha_j))$，内积构成的矩阵 |
| **Schmidt 正交化** | Gram-Schmidt orthogonalization | 从任意基构造标准正交基的算法 |
| **正交补** | orthogonal complement | $W^\perp = \{\alpha \mid \alpha \perp W\}$ |
| **正交投影** | orthogonal projection | $\hat{\alpha} = \frac{(\alpha,\eta)}{(\eta,\eta)}\eta$（一维） |
| **正交矩阵** | orthogonal matrix | $Q^T Q = I_n$，即 $Q^{-1} = Q^T$ |
| **正交变换** | orthogonal transformation | 保内积的线性变换 |
| **对称变换** | symmetric transformation | $(\mathcal{A}\alpha,\beta) = (\alpha,\mathcal{A}\beta)$ |
| **反对称变换** | skew-symmetric transformation | $(\mathcal{A}\alpha,\beta) = -(\alpha,\mathcal{A}\beta)$ |
| **正交相似对角化** | orthogonal diagonalization | 用正交矩阵将实对称矩阵对角化 |
| **酉空间** | unitary space | 复内积空间（共轭对称） |
| **酉矩阵** | unitary matrix | $U^*U = I$（复版本的正交矩阵） |
| **QR 分解** | QR decomposition | $A = QR$（$Q$正交，$R$上三角） |
| **SO(n)** | special orthogonal group | 行列式为1的正交矩阵（旋转） |
| **镜面反射** | Householder reflection | $S(\alpha) = \alpha - 2(\alpha,\eta)\eta$ |
| **Parseval 恒等式** | Parseval's identity | $\|\alpha\|^2 = \sum (\alpha,\varepsilon_i)^2$ |
| **最佳逼近定理** | best approximation theorem | 正交投影是子空间中距离最近的向量 |

## 二、符号

| 符号 | 含义 |
|------|------|
| $(\alpha, \beta)$ | 内积 |
| $\|\alpha\|$ | 长度（模） |
| $\langle\alpha,\beta\rangle$ | 夹角 |
| $\perp$ | 正交 |
| $\delta_{ij}$ | Kronecker delta（$1$当$i=j$，$0$否则） |
| $G$ | Gram 矩阵 |
| $W^\perp$ | $W$的正交补 |
| $P_W$ | 到$W$的正交投影算子 |
| $O(n)$ | 正交群 |
| $SO(n)$ | 特殊正交群（旋转群） |
| $S$ | 镜面反射 |
| $\overline{z}$ | 复共轭 |
| $U^*$ | 酉矩阵的共轭转置 |

## 三、计算方法

### Gram-Schmidt 正交化（🔥 必考）

**正交化公式**（先正交，后单位化）：
$$\boxed{\beta_k = \alpha_k - \sum_{i=1}^{k-1} \frac{(\alpha_k, \beta_i)}{(\beta_i, \beta_i)} \beta_i}$$

**单位化**：
$$\boxed{\eta_i = \frac{\beta_i}{\|\beta_i\|}}$$

**口诀**：减去前面所有方向上的投影分量。

### 判断正交变换（三个等价条件，满足任一即可）

1. **保内积**：$(\mathcal{A}\alpha, \mathcal{A}\beta) = (\alpha,\beta)$
2. **保长度**：$\|\mathcal{A}\alpha\| = \|\alpha\|$
3. **保标准正交基**：标准正交基的像仍是标准正交基

在标准正交基下：$\mathcal{A}$是正交变换 $\iff$ 其矩阵 $A$ 满足 $A^T A = I$

### 正交矩阵的判定
$$A^T A = I \iff A^{-1} = A^T$$
等价于：列向量是标准正交基 / 行向量是标准正交基

### 🔥 正交相似对角化（实对称矩阵，必考）

**前提**：$A$ 是实对称矩阵

**步骤**：
1. 求特征值：$\|\lambda I - A\| = 0$（全为实数）
2. 对每个 $\lambda_i$，解 $(\lambda_i I - A)X = 0$ 求特征向量
3. 同一特征值内部的向量做 Schmidt 正交化
   （⚠️ **不同特征值的特征向量自动正交，无需处理！**）
4. 所有向量单位化
5. 拼成正交矩阵 $T$，则 $T^T A T = \operatorname{diag}(\lambda_1,\ldots,\lambda_n)$

### 正交投影
- 一维：$\hat{\alpha} = \frac{(\alpha,\eta)}{(\eta,\eta)}\eta$（$\eta$为单位向量时：$\hat{\alpha} = (\alpha,\eta)\eta$）
- 一般子空间：解 $G \cdot \hat{\mathbf{x}} = \mathbf{b}$，其中 $G$ 是基的 Gram 矩阵

### 正交变换分类
- $\det = +1$：**旋转**（第一类，保持定向）
- $\det = -1$：**镜像**（第二类，翻转定向）

### SO(2) 标准形
$$\begin{pmatrix} \cos\theta & -\sin\theta \\ \sin\theta & \cos\theta \end{pmatrix} \quad \text{（旋转}\theta\text{角）}$$

### 镜面反射（Householder 变换）
$$S(\alpha) = \alpha - 2(\alpha,\eta)\eta \quad (\|\eta\|=1)$$
- $S^2 = I$（对合性）
- $\det(S) = -1$

---

# 附录：全课程公式汇总 {#appendix}

## 核心恒等式

| 名称 | 公式 | 章节 |
|------|------|------|
| Cauchy-Schwarz | $\|(\alpha,\beta)\| \leq \|\alpha\| \cdot \|\beta\|$ | Ch7 |
| 三角不等式 | $\|\alpha+\beta\| \leq \|\alpha\| + \|\beta\|$ | Ch7 |
| 维数公式 | $\dim(W_1+W_2) = \dim W_1 + \dim W_2 - \dim(W_1 \cap W_2)$ | Ch5 |
| 秩-零化度 | $\dim\ker(A) + r(A) = n$ | Ch5/Ch6 |
| Sylvester 秩不等式 | $r(AB) \geq r(A) + r(B) - n$ | Ch4 |
| 行列式乘积 | $\det(AB) = \det(A)\det(B)$ | Ch3 |
| 迹循环不变 | $\operatorname{tr}(AB) = \operatorname{tr}(BA)$ | Ch4 |
| C-H 定理 | $f_A(A) = 0$ | Ch6 |
| 特征值-迹 | $\sum \lambda_i = \operatorname{tr}(A)$ | Ch6 |
| 特征值-行列式 | $\prod \lambda_i = \det(A)$ | Ch6 |

## 可逆等价条件

$A$ 可逆 $\iff \det(A) \neq 0 \iff r(A) = n \iff$ 行/列满秩 $\iff$ 特征值均非零

## 对角化等价条件

$A$ 可对角化 $\iff$ 有 $n$ 个线性无关的特征向量 $\iff$ 每个特征值 $m_g = m_a$ $\iff$ 最小多项式无重根

## 各维数公式

| 空间 | 维数 |
|------|------|
| $\mathbb{F}^n$ | $n$ |
| $\mathbb{F}^{m \times n}$ | $mn$ |
| $\mathbb{F}[x]_n$ | $n+1$ |
| $\ker(A)$（零空间） | $n - r(A)$ |
| $V/W$（商空间） | $\dim V - \dim W$ |

## 常见特殊矩阵的行列式

| 矩阵类型 | 行列式 |
|----------|--------|
| 对角阵 | 对角线乘积 |
| 三角阵 | 对角线乘积 |
| 正交矩阵 | $\pm 1$ |
| Vandermonde | $\prod_{i<j}(x_j-x_i)$ |

---

<div style="text-align: center; font-size: 18pt; font-weight: bold; color: #1A3C6E; margin-top: 2cm;">

—— 全文完，祝查阅愉快！——

</div>
