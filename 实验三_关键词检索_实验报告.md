# 数据结构 实验报告

## 实验名称：实验三——关键词检索（KMP 算法版）

**班级：** ________　**姓名：** 杨思辰　**学号：** PB25992094　**实验日期：** 2026-07-20

---

## 一、实验项目与实验要求

### 1. 实验项目

实现一个类似 Unix `grep` 命令的关键词检索工具。程序接收一个文件路径和一个关键词作为输入，在文件中逐行搜索关键词，将包含关键词的所有行连同行号一并输出。

本实验的核心约束是：**必须自己实现字符串匹配函数，不允许调用 `strstr()` 等任何标准库提供的字符串查找函数**。本版本采用 **KMP（Knuth-Morris-Pratt）算法**——通过预处理模式串的 next 数组，在匹配失配时主串指针不回退、仅跳转模式串指针，将最坏时间复杂度从 BF 的 $O(n \cdot m)$ 优化到 $O(n+m)$。

### 2. 核心功能要求

| 序号 | 功能 | 说明 |
|:--|:--|:--|
| 1 | 自实现 `getNext()` 函数 | 计算模式串的 next 数组（KMP 预处理），next[0]=-1 作为哨兵，`k=next[k]` 递归自引用 |
| 2 | 自实现 `strindex()` 函数 | KMP 匹配主循环：匹配时 i/j 同步前进，失配时 i 不动、j=next[j] 跳转 |
| 3 | 忽略大小写版本 `strindex_()` | 在 KMP 匹配中逐字符 `tolower` 比较，next 数组基于原始模式串计算（大小写不影响） |
| 4 | 逐行文件搜索 `grep()` | 使用 `fgets` 逐行读取，对每行调用匹配函数，命中则输出行号和内容 |
| 5 | 键盘交互输入 | 程序运行时从键盘读取文件路径和关键词（使用 `fgets` 逐行读取），无需命令行参数 |
| 6 | 匹配行计数 | 搜索结束后输出匹配总行数 |

### 3. 准备测试数据与预期结果

测试文件 `test.txt`（8 行，混合大小写）：

```
This is the first line with the word index here.
The second line does not contain that keyword.
Indexing is important for databases.
We need to create an index for the array.
Please find the INDEX of this element.
This line has nothing to do with it.
Another index appears at the end of this line.
The final line is just a normal sentence.
```

**预期结果**：区分大小写搜索 `index`，应匹配第 1、4、7 行（小写 "index"），共 3 行。第 3 行 "Indexing" 和第 5 行 "INDEX" 因大小写不匹配而不输出。

**KMP 算法验证**：对于模式串 T="index"（m=5），计算 next 数组并验证匹配过程。预期 next = [-1, 0, 0, 0, 0, 0]（各字符 'i','n','d','e','x' 均不相同，无公共前后缀）。

---

## 二、实验原理与详细设计

### 1. 选择 KMP 算法的原因

| 对比维度 | BF 算法 | KMP 算法（本实验采用） |
|:--|:--|:--|
| 核心思想 | 穷举所有起始位置，逐个验证 | 利用已匹配信息，失配时主串指针不回退 |
| 最坏时间复杂度 | $O(n \cdot m)$ | **$O(n + m)$** |
| 预处理开销 | 无 | $O(m)$ 计算 next 数组 |
| 额外空间 | $O(1)$ | $O(m)$（next 数组） |
| i 是否回退 | 是（每次失配 i 退回 i-j+1） | **否（i 单调前进）** |
| 对重复模式敏感 | 是（如 T="aaaa...b" 性能极差） | 否（保证线性时间） |
| 实现复杂度 | 极简（约 10 行） | 适中（约 30 行，含 getNext） |

选用 KMP 的理由：
- **性能保障**：KMP 对任意输入均保证 $O(n+m)$ 线性时间，不存在退化情况；
- **算法教育价值**：next 数组的「模式串自己匹配自己」体现了递归自引用的精妙设计，是数据结构课程的重要知识点；
- **工程适用性**：实际项目中文件可能极大，BF 的 $O(n \cdot m)$ 最坏情况不可接受，KMP 是成熟的工程选择。

### 2. KMP 算法原理——next 数组的物理含义

**定义**：`next[j]` = 模式串 $T[0..j-1]$（即前 $j$ 个字符构成的子串）的最长相等前后缀长度。其中 `next[0] = -1` 是哨兵值，表示「第一个字符就失配，主串指针也需前进」。

**手工计算示例**（模式串 T = "ababc"）：

| j | 前 j 个字符 T[0..j-1] | 最长公共前后缀 | 长度 | **next[j]** |
|:--|:--|:--|:--|:--|
| 0 | （空串） | — | — | **-1**（哨兵） |
| 1 | `"a"` | 无（前后缀不能是整个串） | 0 | **0** |
| 2 | `"ab"` | "a"≠"b"，无 | 0 | **0** |
| 3 | `"aba"` | "a" = "a" | 1 | **1** |
| 4 | `"abab"` | "ab" = "ab" | 2 | **2** |

**getNext() 算法——KMP 自引用的精妙之处**：

```c
void getNext(const char *T, int next[]) {
    int j = 0, k = -1;
    next[0] = -1;                     // 哨兵
    while (T[j] != '\0') {
        if (k == -1 || T[j] == T[k]) {
            j++; k++;
            next[j] = k;              // 前后缀扩展：next[j+1] = k+1
        } else {
            k = next[k];              // ★ 失配！k 沿 next 回退（自引用）
        }
    }
}
```

**关键理解**：`getNext` 本质上是「模式串 T 自己匹配自己」的过程。`j` 是主串指针（扫描 T），`k` 是模式串指针（始终在 j 之前）。当 `T[j] == T[k]` 时说明前后缀可以扩展一位；当失配时 `k = next[k]`——这和 KMP 主匹配中的 `j = next[j]` 完全对称。

**KMP 匹配主循环**：

```c
int i = 0, j = 0;
while (i < n && j < m) {                  // 用长度判断，避免 j=-1 时 T[-1] 越界
    if (j == -1 || S[i] == T[j]) {
        i++; j++;                          // 匹配或哨兵 → 双双前进
    } else {
        j = next[j];                       // ★ 失配：j 跳转，i 不动！
    }
}
return (j == m) ? (i - m) : -1;           // j==m 表示匹配成功
```

### 3. 匹配过程逐步推演

以 S = "ababcabcacbab", T = "abcac"（next = [-1,0,0,0,1]）为例：

| 步骤 | i | j | S[i] vs T[j] | 操作 |
|:--|:--|:--|:--|:--|
| 1 | 0 | 0 | 'a'=='a' | i++, j++ → i=1, j=1 |
| 2 | 1 | 1 | 'b'=='b' | i++, j++ → i=2, j=2 |
| 3 | 2 | 2 | 'a'≠'c' | **j=next[2]=0**, i 不动 |
| 4 | 2 | 0 | 'a'=='a' | i++, j++ → i=3, j=1 |
| 5 | 3 | 1 | 'b'=='b' | i++, j++ → i=4, j=2 |
| 6 | 4 | 2 | 'c'=='c' | i++, j++ → i=5, j=3 |
| 7 | 5 | 3 | 'a'=='a' | i++, j++ → i=6, j=4 |
| 8 | 6 | 4 | 'b'≠'c' | **j=next[4]=1**, i 不动 |
| 9 | 6 | 1 | 'b'=='b' | i++, j++ → i=7, j=2 |
| … | … | … | … | 最终 i=11 处 j=m=5，匹配成功 |

**关键观察**：i 始终从 0 单调递增到 n-1，从未回退——这正是 KMP 做到 $O(n)$ 的本质原因。

### 4. 结构体 / 类型定义

本程序未定义自定义结构体，所有逻辑由函数封装。核心数据类型说明：

| 类型 | 用途 | 说明 |
|:--|:--|:--|
| `int next[]`（动态分配） | 存储模式串的 next 数组 | 长度 m+1，每次匹配前 `malloc`，匹配后 `free` |
| `const char *S / *T` | 匹配函数的母串/模式串参数 | C 风格字符串，`const` 保证不修改原串 |
| `char line[MAX]` | 文件行缓冲区 | 定长 4096 字节，`fgets` 逐行填充 |
| `FILE *` | 文件句柄 | C 标准 I/O |

### 5. 核心函数说明

| 分类 | 函数 | 功能 | 关键点 |
|:--|:--|:--|:--|
| KMP 预处理 | `getNext(T, next)` | 计算模式串 next 数组 | next[0]=-1（哨兵），`k=next[k]` 递归自引用，$O(m)$ |
| KMP 匹配 | `strindex(S, T)` | 区分大小写 KMP 匹配 | 失配 i 不动、j=next[j]，返回匹配位置指针或 NULL |
| KMP 匹配 | `strindex_(S, T)` | 忽略大小写 KMP 匹配 | 逐字符 `tolower((unsigned char)c)`，其余同 strindex |
| 文件搜索 | `grep(filename, key, ign)` | 文件中逐行搜索 | 遍历文件所有行，统计匹配行数 |
| 入口 | `main()` | 键盘交互读取文件路径和关键词，调用 `grep` | 使用 `fgets` 读入字符串，`strcspn` 去除行尾换行符 |

### 6. 程序执行逻辑与函数调用关系

```
main()
  ├─ 键盘交互输入:
  │    printf("请输入文件路径: "); fgets(filename, 256, stdin)
  │    printf("请输入关键词: ");   fgets(keyword, 256, stdin)
  │    去除行尾 '\n'（strcspn）
  │    ignoreCase = 0
  │
  └─ grep(filename, keyword, ignoreCase)
       ├─ fopen(filename, "r")                 以读文本模式打开
       │   └─ 失败 → 打印错误, 返回 -1
       │
       ├─ while (fgets(line, MAX, fp) != NULL)  逐行读取
       │    ├─ lineno++                         行号递增
       │    ├─ 去除行尾 '\n'                   避免输出多余空行
       │    ├─ strindex(line, keyword)          调用 KMP 匹配
       │    │    ├─ malloc next[m+1]            分配 next 数组
       │    │    ├─ getNext(keyword, next)      KMP 预处理 O(m)
       │    │    ├─ while(i<n && j<m)           KMP 主循环 O(n)
       │    │    │    ├─ j==-1||S[i]==T[j] → i++,j++
       │    │    │    └─ else → j=next[j]       ★ i 不回退
       │    │    ├─ free(next)                  释放 next 数组
       │    │    └─ return (j==m)?(S+i-m):NULL
       │    │
       │    └─ pos!=NULL → printf行号+内容; count++
       │
       ├─ fclose(fp)                            关闭文件
       └─ return count                          返回匹配行数
```

---

## 三、算法时空复杂度分析

### 1. getNext()——预处理阶段 $O(m)$

- while 循环：`j` 从 0 到 m，共 m+1 次迭代
- `k` 每次回退至少减 1，k 增量来自 `j++` 处 +1，总增量 ≤ m
- **总操作 ≤ 2m** → $O(m)$

### 2. strindex()——匹配阶段 $O(n)$

- while 循环：i 从 0 单调递增到 n（**i 从不回退！**）
- 每次迭代要么 i++（最多 n 次），要么 j=next[j]（j 减小，总回退 ≤ 总前进）
- **总操作 ≤ 2n** → $O(n)$

### 3. grep()——文件整体

文件共有 $L$ 行，第 i 行长度为 $n_i$，关键词长度为 $m$：

$$T = \sum_{i=1}^{L} O(m + n_i) = O(L \cdot m + N)$$

其中 $N = \sum n_i$ 为文件总字符数。由于 $m$（关键词长度）通常 ≪ $N$，**总时间 ≈ $O(N)$**——与文件大小成线性关系。

### 4. 空间复杂度

| 阶段 | 空间 | 说明 |
|:--|:--|:--|
| getNext | $O(m)$ | next 数组 m+1 个 int，调用结束时释放 |
| 匹配过程 | $O(1)$ | 仅 i, j, n, m 等局部变量 |
| grep 行缓冲 | $O(1)$ | line[MAX] 固定 4096 字节 |
| **整体** | **$O(m)$** | m 通常几十字节，可忽略 |

### 5. 数据结构选择对复杂度的影响

| 匹配算法 | 预处理 | 匹配最坏 | 大文件性能 | 实现行数 |
|:--|:--|:--|:--|:--|
| BF（朴素） | 无 | $O(n \cdot m)$ | 不可接受（退化） | ~10 行 |
| **KMP（本实验）** | $O(m)$ | **$O(n+m)$ ✅** | 线性，无退化 | ~30 行 |
| Boyer-Moore | $O(m+\sigma)$ | 亚线性（最好 $O(n/m)$） | Unix grep 常用 | ~100+ 行 |

**KMP 是本实验的最优选择**：既保证了理论上的线性时间保障，又在代码复杂度上适中（30 行可完整实现），非常适合教学演示。

---

## 四、程序使用说明与测试运行结果

### 1. 使用说明

- **运行环境**：Windows 11，gcc 16.1.0（MSYS2 ucrt64），命令行交互
- **编译命令**：`gcc 关键词检索_KMP版.c -o grep_kmp.exe`
- **运行命令**：

```bash
./grep_kmp.exe
```

- **交互流程**：程序启动后依次提示「请输入文件路径」和「请输入关键词」，用户从键盘输入后按回车确认。文件必须是 UTF-8 或 ASCII 编码的纯文本，关键词区分大小写（可修改 `ignoreCase` 参数为 1 以启用忽略大小写模式）。

### 2. 测试运行结果

```
请输入文件路径: test.txt
请输入关键词: index

文件：test.txt     关键词：index

[   1] This is the first line with the word index here.
[   4] We need to create an index for the array.
[   7] Another index appears at the end of this line.

共 3 行匹配
```

### 3. 测试结果与预期对照

| 验证项 | 预期结果 | 实际输出 | 判定 |
|:--|:--|:--|:--|
| 小写 "index"（第1行）匹配 | 匹配 | 命中 ✓ | ✅ |
| 小写 "index"（第4行）匹配 | 匹配 | 命中 ✓ | ✅ |
| 小写 "index"（第7行）匹配 | 匹配 | 命中 ✓ | ✅ |
| "Indexing"（第3行）区分大小写不匹配 | 不匹配 | 未命中 ✓ | ✅ |
| "INDEX"（第5行）不匹配 | 不匹配 | 未命中 ✓ | ✅ |
| 行号标注正确 | [1],[4],[7] | [1],[4],[7] | ✅ |
| 匹配行数正确 | 3 | 3 | ✅ |
| KMP i 不回退验证 | O(N) 线性扫描 | 大文件测试通过 | ✅ |

全部验证项与预期一致 ✅

---

## 五、测试分析与实验总结

### 1. 测试过程中遇到的问题及解决

| 问题 | 原因 | 解决方法 |
|:--|:--|:--|
| KMP 匹配时 while 条件用 `T[j]!='\0'` 导致越界 | `j` 可能为 -1（哨兵），`T[-1]` 访问非法内存 | 改用 `while(i < n && j < m)`，用长度而非 `\0` 判断终止 |
| `getNext` 中 `next` 数组长度分配不足 | next 需要 next[0..m] 共 m+1 个元素，只分配了 m 个 | `malloc((m + 1) * sizeof(int))` |
| next 数组内存泄漏 | `strindex` 每次调用都 `malloc` 了 next，但未释放 | 在函数所有返回路径前统一 `free(next)`，匹配成功和失败两分支都要释放 |
| `tolower` 参数直接传 `char` 导致未定义行为 | `char` 可能为有符号类型，负值（≥128 的字节）传入 `tolower` 触发 UB | 强转为 `(unsigned char)` 后再传 |
| `fgets` 行尾 `\n` 导致输出多余空行 | `fgets` 保留换行符，`printf` 又额外添加 `\n` | 输出前检测并移除行尾 `\n`：`if(line[len-1]=='\n') line[len-1]='\0'` |
| 大文件测试时 BF 版本明显变慢 | BF 对重复模式（如 T="aaaaa...b"）退化 | KMP 版本在大文件和重复模式下性能稳定，i 从不回退 |

### 2. 实验收获

- **深入理解了 KMP 算法的核心机制**：亲手实现 `getNext` 和 `strindex` 后，真正理解了「利用已匹配信息避免主串指针回退」的含义——i 从不回退，所有失配处理都通过 j=next[j] 完成；

- **掌握了 next 数组的双重角色**：next 数组既是 KMP 匹配时模式串指针的跳转表，也是 `getNext` 函数自身的递归回退依据（`k = next[k]`），这一自引用机制是 KMP 算法最精妙的设计；

- **体会了理论复杂度与实际性能的统一**：KMP 理论上保证 $O(n+m)$，实践中也确实无论输入如何都只扫描一遍。相比之下，BF 的 $O(n \cdot m)$ 虽然平均不错，但遇到刻意构造的重复模式时会急剧退化；

- **学会了 C 语言动态内存管理配合算法的写法**：next 数组长度取决于输入模式串长度，必须用 `malloc` 动态分配，使用完立即 `free`，且要覆盖所有返回分支（不能只在一个分支 `free`）；

- **加深了对字符串匹配边界的理解**：空串处理（`*T=='\0'` 返回母串首地址）、NULL 指针防御（`S==NULL||T==NULL`）、哨兵设计（`next[0]=-1`，`j==-1` 时 i/j 双双前进）——这些边界条件的正确处理是写健壮 C 程序的必修课。

### 3. 可选扩展与优化思路

- **nextval 数组优化**：当 `T[j] == T[next[j]]` 时，`next[j]` 可以直接跳到 `nextval[j] = nextval[next[j]]`，避免连续相同字符时的多余比较；
- **Boyer-Moore 算法**：从右往左匹配 + 坏字符规则 + 好后缀规则，对长模式串实现亚线性匹配（$O(n/m)$），是 Unix grep 的常用实现；
- **正则表达式支持**：将简单子串匹配扩展为正则引擎（`.`、`*`、`[]`等），大幅增强搜索能力；
- **命令行选项**：仿照 Unix grep 实现 `-i`（忽略大小写）、`-c`（仅计数）、`-v`（反向匹配）、`-n`（显示行号）等标准选项；
- **多文件搜索**：支持一次传入多个文件路径，依次搜索并标注文件名。

---

## 六、附录：完整 C 语言源代码

```c
/* 实验三：关键词检索 —— KMP 算法版
 *
 *   核心：
 *     ① getNext() —— 计算模式串的 next 数组（KMP 预处理）
 *     ② strindex / strindex_ —— KMP 匹配（i 不回退，j 跳 next）
 *     ③ grep / main —— 文件 I/O 和命令行接口
 *
 *   编译：gcc 关键词检索_KMP版.c -o grep_kmp.exe
 *   运行：.\grep_kmp.exe [文件] [关键词]
 */

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>
#include <windows.h>

#define MAX 4096

/* ================================================================
 *   getNext(T, next)
 *   —— 计算模式串 T 的 next 数组（KMP 预处理）
 *   —— next[0] = -1（哨兵）
 *   —— next[j] = T[0..j-1] 的最长相等前后缀长度
 *
 *   复杂度 O(m)，m = strlen(T)
 * ================================================================ */
void getNext(const char *T, int next[]) {
    int j = 0, k = -1;
    next[0] = -1;
    while (T[j] != '\0') {
        if (k == -1 || T[j] == T[k]) {
            j++; k++;
            next[j] = k;           /* 扩展前后缀 */
        } else {
            k = next[k];           /* 失配回溯（自引用！）*/
        }
    }
}

/* ================================================================
 *   strindex(S, T) —— KMP 匹配（区分大小写）
 *
 *   i→主串  j→模式串
 *   失配时 j = next[j]，i 不动！
 *   复杂度 O(n+m)
 * ================================================================ */
char* strindex(const char *S, const char *T) {
    if (S == NULL || T == NULL || *T == '\0')
        return (char*)S;

    int m = strlen(T);
    int *next = (int*)malloc((m + 1) * sizeof(int));
    if (next == NULL) return NULL;
    getNext(T, next);

    int n = strlen(S);
    int i = 0, j = 0;
    while (i < n && j < m) {          /* 用长度判断，避免 j=-1 时 T[-1] 越界 */
        if (j == -1 || S[i] == T[j]) {
            i++; j++;                  /* 匹配 → 双双前进 */
        } else {
            j = next[j];               /* ★ 失配 → j 跳转，i 不动！*/
        }
    }

    free(next);
    if (j == m)
        return (char*)(S + i - m);     /* 匹配成功 */
    return NULL;                       /* 未找到 */
}

/* ================================================================
 *   strindex_(S, T) —— KMP 匹配（忽略大小写）
 *   next 数组基于 T 本身计算，匹配时逐字符 tolower
 * ================================================================ */
char* strindex_(const char *S, const char *T) {
    if (S == NULL || T == NULL || *T == '\0')
        return (char*)S;

    int m = strlen(T);
    int *next = (int*)malloc((m + 1) * sizeof(int));
    if (next == NULL) return NULL;
    getNext(T, next);

    int n = strlen(S);
    int i = 0, j = 0;
    while (i < n && j < m) {
        if (j == -1 ||
            tolower((unsigned char)S[i]) == tolower((unsigned char)T[j])) {
            i++; j++;
        } else {
            j = next[j];
        }
    }

    free(next);
    if (j == m)
        return (char*)(S + i - m);
    return NULL;
}

/* ================================================================
 *   grep —— 在文件中逐行搜索关键词
 * ================================================================ */
int grep(const char *filename, const char *keyword, int ignore) {
    FILE *fp = fopen(filename, "r");
    if (fp == NULL) {
        printf("无法打开文件 %s\n", filename);
        return -1;
    }
    char line[MAX];
    int lineno = 0, count = 0;

    while (fgets(line, MAX, fp) != NULL) {
        lineno++;
        int len = strlen(line);
        if (len > 0 && line[len - 1] == '\n') line[len - 1] = '\0';

        char *pos = ignore
                    ? strindex_(line, keyword)
                    : strindex(line, keyword);
        if (pos != NULL) {
            count++;
            printf("[%4d] %s\n", lineno, line);
        }
    }
    fclose(fp);
    return count;
}

int main() {
    SetConsoleOutputCP(CP_UTF8);

    char filename[256], keyword[256];

    printf("请输入文件路径: ");
    fgets(filename, sizeof(filename), stdin);
    filename[strcspn(filename, "\n")] = '\0';   /* 去除换行 */

    printf("请输入关键词: ");
    fgets(keyword, sizeof(keyword), stdin);
    keyword[strcspn(keyword, "\n")] = '\0';

    printf("\n文件：%s     关键词：%s\n\n", filename, keyword);
    int cnt = grep(filename, keyword, 0);
    printf("\n共 %d 行匹配\n", cnt);
    return 0;
}
```

---

<div style="text-align: center; margin-top: 2em;">

—— 实验报告完 ——

</div>
