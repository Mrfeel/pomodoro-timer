# 数据结构 实验报告

## 实验名称：实验一——约瑟夫（Joseph）问题求解

**班级：** ________　**姓名：** 杨思辰　**学号：** PB25992094　**实验日期：** 2026-07-15

---

## 一、实验项目与实验要求

### 1. 实验项目

约瑟夫（Joseph）问题求解：$n$ 个人围坐一圈，每人持有一个密码。给定报数上限 $m$ 的初值，从第 1 人开始从 1 到 $m$ 顺序报数，报到 $m$ 的人出列，并将他的密码作为新的 $m$ 值。从他的下一个人重新从 1 报数，如此反复，直到所有人出列。试求出列顺序。

### 2. 核心功能要求

- 采用 **单向循环链表** 存储结构模拟围坐一圈的人；
- 区分 **输入函数**（建立链表、读入密码与 $m$ 初值）与 **输出函数**（输出出圈编号）；
- 每删除一个结点，立即释放所占内存空间。

### 3. 准备测试数据与预期结果

**标准测试数据**：

- 人数 $n = 7$
- 7 人密码依次为：$3,\,1,\,7,\,2,\,4,\,8,\,4$
- 报数上限初值 $m = 20$

**预期输出**：出圈顺序 `6 1 4 7 2 3 5`（按编号）。

**额外测试（补充验证）**：

- $n=1$，密码 `5`，$m=3$ → 预期输出 `1`
- $n=5$，密码均为 `1`，$m=2$ → 预期输出 `2 4 1 5 3`

---

## 二、实验原理与详细设计

### 1. 选择数据结构的原因

选用 **单向循环链表**（题目硬性要求）。理由：

- 围坐一圈的人首尾相连，逻辑上是"环"，循环链表的 `last->next = first` 天然表达闭环；
- 删除出圈者在链表中即为常规的 **结点删除**操作（$O(1)$，只需前驱指针）；
- 与顺序表相比，链表在删除后无需移动其他元素，且人数动态递减直至为 0，链表更自然。

### 2. 结构体 / 类型定义

```c
struct Node {
    int num;          // 编号（1..n）
    int password;     // 密码（用于更新报数上限 m）
    struct Node *next;
};
```

### 3. 核心函数说明

| 函数 | 功能 | 关键点 |
|------|------|--------|
| `inputCircle(&n, &m)` | 读入人数、密码、$m$ 初值，**建立循环链表**，返回指向 1 号结点的指针 | 尾插法构造链表；全部结点加入后 `last->next = first` 成环 |
| `outputNum(num)` | 输出一个出圈者的编号 | 独立输出函数，格式 `"%d "` |
| `main` 中的约瑟夫循环 | `pre`/`cur` 双指针遍历环，`m-1` 步前进后删除 `cur` 指向的结点 | 删除后 `m` 更新为该人密码；`cur` 前进到下一人继续报数 |

**关键算法片段（约瑟夫删除循环）**：

```c
struct Node *pre = first;
while(pre->next != first) pre = pre->next;   // pre 初始化为 last
struct Node *cur = first;
int remaining = n;

while(remaining > 0){
    for(int i=1; i<m; i++){ pre=cur; cur=cur->next; }  // 前进 m-1 步
    outputNum(cur->num);                    // cur 出圈
    m = cur->password;                      // 更新报数上限
    pre->next = cur->next;                  // 从环中摘除 cur
    struct Node *temp = cur;
    cur = cur->next;                        // 下一轮从下一个人开始
    free(temp);                             // 释放内存
    remaining--;
}
```

### 4. 程序执行逻辑与函数调用关系

```
main()
  ├─ inputCircle(&n, &m)        建立循环链表并读入数据
  ├─ [初始化 pre/cur]           pre 指向 last，cur 指向 first
  ├─ while(remaining > 0)       约瑟夫循环
  │    ├─ for(i=1; i<m; i++)    双指针同步前进
  │    ├─ outputNum(cur->num)   输出出圈者
  │    ├─ m = cur->password     更新报数上限
  │    ├─ pre->next = cur->next 摘除结点
  │    ├─ free(temp)            释放内存
  │    └─ cur = cur->next       前进到下一人
  └─ [结束，全部出圈]
```

---

## 三、算法时空复杂度分析

### 1. 核心操作时间复杂度

- **建立循环链表**：$O(n)$——共 $n$ 个结点，每次尾插 $O(1)$。
- **约瑟夫循环**：共执行 $n$ 次删除，每次删除前前进 $m-1$ 步（每步 $O(1)$），每次删除 $O(1)$。
  总前进步数 $= \sum_{k=1}^{n} (m_k - 1)$，其中 $m_k$ 为第 $k$ 轮出圈者的密码。
- **最坏情况**：若所有密码均为极大值，总步数可达到 $O(n \cdot \max(m))$。在密码为 $O(1)$ 常量时，整体为 $O(n)$。

### 2. 辅助空间复杂度

- $O(1)$——仅使用 `pre`、`cur`、`temp` 三个指针和 `remaining` 等几个整型变量。全部空间开销来自链表本身（$O(n)$，属于问题规模所需，非辅助空间）。

### 3. 数据结构选择对复杂度的影响

| 存储结构 | 删除某结点 | 前进 m 步 | 总复杂度 |
|----------|-----------|----------|---------|
| 循环链表 | $O(1)$ | $O(m)$ | $O(\sum m_k)$ |
| 顺序表（数组） | $O(k)$（需移动后继元素） | $O(1)$ | 最坏 $O(n^2)$ |
| 循环链表 + 单向 | $O(1)$ | $O(m)$ | 最优 ✅ |

单向循环链表是此题的最优选择。

---

## 四、程序使用说明与测试运行结果

### 1. 使用说明

- **运行环境**：Windows 11，gcc 16.1.0（MSYS2 ucrt64），命令行交互
- **编译**：`gcc 约瑟夫问题.c -o joseph.exe`
- **运行**：`.\joseph.exe`
- **交互流程**：输入人数 $n$ → 依次输入每人密码 → 输入初值 $m$ → 程序输出出圈顺序
- **输入约定**：所有输入均为整数，以空格或回车分隔；$n \ge 1$，密码为任意正整数

### 2. 测试结果

**测试 1：标准数据**

```
请输入人数：7
请输入第1个人的密码：3
请输入第2个人的密码：1
请输入第3个人的密码：7
请输入第4个人的密码：2
请输入第5个人的密码：4
请输入第6个人的密码：8
请输入第7个人的密码：4
请输入报数上限m的初值: 20

出圈顺序: 6 1 4 7 2 3 5
```

结果与预期完全一致 ✅

**测试 2：边界——1 人**

```
n=1, 密码=5, m=3
出圈顺序: 1
```

正确 ✅

**测试 3：$m=2$，密码恒为 1（类似"隔一个删一个"）**

```
n=5, 密码均为1, m=2
出圈顺序: 2 4 1 5 3
```

验证正确 ✅（手工数：1→报1, 2→报2出圈, 3→报1, 4→报2出圈, 5→报1, 1→报2出圈, 3→报1, 5→报2出圈, 3→最后）

---

## 五、测试分析与实验总结

### 1. 测试过程中遇到的问题（Bug）及解决

| 问题 | 原因 | 解决方法 |
|------|------|---------|
| 编译通过但程序崩溃 | `if(first = NULL)` 把赋值 `=` 当成了判断 `==` | 改为 `if(first == NULL)` |
| scanf 读入密码失败 | `scanf("%d", p->password)` 漏写了取地址符 `&` | 改为 `scanf("%d", &p->password)` |
| 循环链表只建了第 1 个结点就停止 | `return first` 误写在 for 循环体内部 | 将 return 移到 for 循环结束后 |
| 链表被反复封环导致结构混乱 | `last->next = first` 误写在 for 循环体内部 | 移至所有结点加入完毕后执行一次 |
| 编号从 0 开始 | for 循环初值为 `i=0` | 改为 `i=1`，编号与人数一致 |
| VS Code F5 调试无输出 | `externalConsole` 配置与 scanf 交互不兼容 | 改用 Ctrl+Shift+B 编译 + 终端直接运行 |
| GDB 启动崩溃 | ucrt64 版 GDB 17.2 有 bug | 改配为 mingw64 版 GDB |

### 2. 实验收获

- 深入理解了**循环链表**的构造（尾插法 → `last->next = first` 封环）与遍历（`do-while(cur != first)`）；
- 掌握了链表中 **pre/cur 双指针**配合删除的模式——pre 始终为 cur 前驱，删除时 `pre->next = cur->next` 即可绕过目标结点；
- 认识到 **C 语言中 `=` 与 `==` 的区别**是极其关键的——"=" 是赋值、"==" 是判断，混淆可能导致程序崩溃；
- 学会了调试 `scanf` 交互程序时，VS Code 的 F5 调试与命令行编译运行各自的适用场景。

### 3. 可选扩展与优化思路

- **双向循环链表**：改单向为双向，删除时无需维护 pre 指针，代码更简洁，但每个结点多一个指针域；
- **批量测试模式**：增加命令行参数模式（`./joseph.exe --test`），自动运行预设测试用例，无需手动输入；
- **密码为 0 的处理**：若某人的密码为 0，则下一轮报数上限为 0，意味着下一个人立即出圈——可增加边界处理提示。

---

## 六、附录：完整 C 语言源代码

```c
#include<stdio.h>
#include<stdlib.h>
#include<windows.h>

struct Node{
    int number;        // 编号
    int password;      // 密码
    struct Node* next;
};

/* 输入函数：建立循环链表，读入密码和 m 初值 */
struct Node *inputcircle(int *pn, int *pm){
    int n, m;
    printf("请输入人数：");
    scanf("%d", &n);

    struct Node *first = NULL, *last = NULL;
    for(int i = 1; i <= n; i++){
        struct Node *p = (struct Node*)malloc(sizeof(struct Node));
        p->number = i;
        printf("请输入第%d个人的密码：", i);
        scanf("%d", &p->password);
        p->next = NULL;
        if(first == NULL){
            first = last = p;
        } else {
            last->next = p;
            last = p;
        }
    }
    last->next = first;          // 首尾相连成环

    printf("请输入报数上限m的初值: ");
    scanf("%d", &m);
    *pn = n; *pm = m;
    return first;
}

/* 输出函数：打印一个出圈者的编号 */
void output(int num){
    printf("%d ", num);
}

int main(void){
    SetConsoleOutputCP(CP_UTF8);
    int n, m;
    struct Node* first = inputcircle(&n, &m);

    struct Node* pre = first;
    while(pre->next != first) pre = pre->next;
    struct Node* cur = first;

    printf("\n出圈顺序: ");
    int remaining = n;
    while(remaining > 0){
        for(int i = 1; i < m; i++){
            pre = cur;
            cur = cur->next;
        }
        output(cur->number);
        m = cur->password;
        pre->next = cur->next;
        struct Node* temp = cur;
        cur = cur->next;
        free(temp);
        remaining--;
    }
    printf("\n");
    return 0;
}
```

---

<div style="text-align: center; margin-top: 2em;">

—— 实验报告完 ——

</div>
