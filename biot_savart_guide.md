# 毕奥-萨伐尔定律求解方法详解

> 毕奥-萨伐尔定律是计算任意电流分布产生磁场的**基本公式**。当对称性不足以使用安培环路定理时，必须回到毕奥-萨伐尔定律做直接积分。

\newpage

# 一、定律表述与物理图像

## 1.1 基本形式

电流元 $I d\boldsymbol{l}$ 在空间某点 $P$ 产生的磁感应强度微元：

$$\boxed{d\boldsymbol{B} = \frac{\mu_0}{4\pi}\frac{I d\boldsymbol{l} \times \hat{\boldsymbol{r}}}{r^2}}$$

其中：
- $I d\boldsymbol{l}$：电流元矢量（方向沿电流方向）
- $\boldsymbol{r}$：从**电流元指向场点** $P$ 的位置矢量
- $\hat{\boldsymbol{r}} = \boldsymbol{r}/r$：单位方向矢量
- $\mu_0 = 4\pi \times 10^{-7}\,\text{H/m}$

## 1.2 物理图像

```
         电流元 I·dl
             ●────→
            /|
           / | r
          /  |
         /θ  |
        /    ↓
       ●────────→ B (垂直于纸面向外)
      场点P
```

**右手定则判断方向**：
- 右手四指从 $d\boldsymbol{l}$ 弯向 $\hat{\boldsymbol{r}}$
- 拇指方向即 $d\boldsymbol{B}$ 方向

**大小**：$dB = \dfrac{\mu_0}{4\pi}\dfrac{I\,dl\sin\theta}{r^2}$

- $\theta = 0^\circ$（平行）：$dB = 0$——电流元在**其延长线上不产生磁场**
- $\theta = 90^\circ$（垂直）：$dB$ 最大

## 1.3 距离平方反比律

$$dB \propto \frac{1}{r^2}$$

这个 $1/r^2$ 是毕奥-萨伐尔定律与库仑定律共同的数学结构——两者都源于三维空间的几何性质。

\newpage

# 二、解题五步法

| 步骤 | 操作 | 关键注意 |
|------|------|---------|
| **①画图** | 画出电流元和场点，标出 $\boldsymbol{r}$、$\theta$ | 确定积分变量和积分限 |
| **②写 $dB$ 大小** | $dB = \frac{\mu_0 I}{4\pi}\frac{dl\sin\theta}{r^2}$ | 用统一积分变量表示 $dl$、$r$、$\theta$ |
| **③定 $d\boldsymbol{B}$ 方向** | 对每个电流元用右手定则 | 特别注意：不同电流元的方向可能**不同**！必须分解为分量 |
| **④分解分量** | 将 $d\boldsymbol{B}$ 分解到坐标轴方向 | 利用对称性——某些方向的分量会因对称性抵消 |
| **⑤积分** | 对各分量分别积分 | 无穷长→积分限 $-\infty$ 到 $+\infty$；有限长→由几何定限 |

## 最重要的原则

> **永远先分析对称性！** 对称性告诉你**哪些分量会抵消**——这可以省去大量不必要的积分计算。

\newpage

# 三、三大经典电流分布的推导（必须掌握）

## 3.1 无限长直载流导线

```
        I ↑
        │
        │
    ────●──── 场点P，距离为a
        │
        │
```

**①画图定变量**：取导线上距垂足为 $l$ 的电流元 $I\,dl$。$\boldsymbol{r}$ 从电流元指向 $P$。$dl = dl$。

$$r = \sqrt{a^2+l^2}, \quad \sin\theta = \frac{a}{r} = \frac{a}{\sqrt{a^2+l^2}}$$

**②$dB$ 大小**：$dB = \dfrac{\mu_0 I}{4\pi}\dfrac{dl\cdot a}{(a^2+l^2)^{3/2}}$

**③方向**：所有电流元的 $d\boldsymbol{B}$ 都沿同一方向——垂直纸面向外（右手定则）。**方向一致，无需分解。**

**④积分**：

$$B = \int_{-\infty}^{+\infty} \frac{\mu_0 I a}{4\pi(a^2+l^2)^{3/2}}\,dl$$

换元 $l = a\tan\alpha$，$dl = a\,d\alpha/\cos^2\alpha$，$a^2+l^2 = a^2/\cos^2\alpha$：

$$B = \frac{\mu_0 I}{4\pi a}\int_{-\pi/2}^{\pi/2}\cos\alpha\,d\alpha = \frac{\mu_0 I}{4\pi a}[\sin\alpha]_{-\pi/2}^{\pi/2}$$

$$\boxed{B = \frac{\mu_0 I}{2\pi a}}$$

---

## 3.2 有限长直导线

积分限由 $-\infty,+\infty$ 改为从 $\theta_1$ 到 $\theta_2$（导线两端到场点的视角）：

$$\boxed{B = \frac{\mu_0 I}{4\pi a}(\cos\theta_1 - \cos\theta_2)}$$

其中 $\theta_1$、$\theta_2$ 是导线两端与垂线方向的夹角。

**验证**：无限长时 $\theta_1\to 0,\theta_2\to\pi$ → $B = \frac{\mu_0 I}{4\pi a}(1-(-1)) = \frac{\mu_0 I}{2\pi a}$ ✓

---

## 3.3 圆形载流线圈轴线上

```
              z轴
               ↑
               ● 场点P，距圆心z
              /|
             / |
         R  /  | r
           /   |
    ╭────●────╮
    │   圆心   │
    ╰─────────╯
     载流圆环I
```

**①变量**：$dl = R\,d\varphi$（环上弧元）。$r = \sqrt{R^2+z^2}$。$d\boldsymbol{l}\perp\hat{\boldsymbol{r}}$ → $\sin\theta = 1$。

$$dB = \frac{\mu_0 I}{4\pi}\frac{R\,d\varphi}{R^2+z^2}$$

**②方向分解**：$d\boldsymbol{B}$ 与 $z$ 轴夹角 $\alpha$。$dB_z = dB\cos\alpha = dB\cdot\frac{R}{\sqrt{R^2+z^2}}$。

水平分量（$dB_\perp$）：绕一圈后对称抵消为**零**。**这就是对称性分析的价值——水平分量不用算！**

**③积分**：

$$B_z = \int_0^{2\pi} \frac{\mu_0 I}{4\pi}\frac{R}{R^2+z^2}\cdot\frac{R}{\sqrt{R^2+z^2}}\,d\varphi$$

$$= \frac{\mu_0 I R^2}{4\pi(R^2+z^2)^{3/2}}\int_0^{2\pi}d\varphi$$

$$\boxed{B(z) = \frac{\mu_0 I R^2}{2(R^2+z^2)^{3/2}}}$$

**特例**：
- 圆心 $z=0$：$B = \dfrac{\mu_0 I}{2R}$
- 远处 $z\gg R$：$B \approx \dfrac{\mu_0 I R^2}{2z^3} = \dfrac{\mu_0}{2\pi}\dfrac{\pi R^2 I}{z^3}$（磁偶极子场，$m=\pi R^2 I$）

\newpage

# 四、何时用毕奥-萨伐尔？何时用安培环路定理？

| 条件 | 用什么 | 原因 |
|------|--------|------|
| 电流分布高度对称（直导线、螺线管、螺绕环） | **安培环路定理** | 对称性使积分退化为乘法 |
| 任意形状的电流回路 | **毕奥-萨伐尔** | 对称性不够，必须逐元积分 |
| 有限长的直导线 | **毕奥-萨伐尔** | 有限长破坏了平移对称性，安培环路不闭合 |
| 圆环轴线上任意点 | **毕奥-萨伐尔** | 轴向上无对称环路可用 |

**判断口诀**：**"对称够→安培环，对称不够→毕奥萨。"**

\newpage

# 五、手把手例题

## 例题1：正方形线圈中心的磁场

边长为 $a$ 的正方形线圈，电流 $I$。求中心 $O$ 处的 $\boldsymbol{B}$。

```
    A─────B
    │     │
    │  O  │  a
    │     │
    D─────C
```

**解**：

**①对称性**：四边在中心产生的 $B$ 大小相等、方向相同（均垂直纸面向外/内）。

**②单边计算**（以 $AB$ 边为例）：
- 中心到 $AB$ 的垂直距离 $= a/2$
- $AB$ 两端对中心的视角：$\theta_1=\theta_2=45^\circ$
- 用有限长导线公式：$B_1 = \frac{\mu_0 I}{4\pi(a/2)}(\cos 45^\circ-\cos 135^\circ)$
- $\cos 45^\circ=\sqrt{2}/2$，$\cos 135^\circ=-\sqrt{2}/2$
- $B_1 = \frac{\mu_0 I}{2\pi a}\cdot\sqrt{2}$

**③叠加**：$B = 4B_1 = \dfrac{2\sqrt{2}\mu_0 I}{\pi a}$

---

## 例题2：半无限长直导线的磁场

导线从 $O$ 点延伸到 $+\infty$，场点 $P$ 在 $O$ 点正侧方距离 $a$ 处。

**解**：有限长公式中 $\theta_1=90^\circ$（$O$ 端），$\theta_2\to 0$（远端）。

$$B = \frac{\mu_0 I}{4\pi a}(\cos 90^\circ - \cos 0^\circ) = \frac{\mu_0 I}{4\pi a}$$

恰为无限长导线结果的一半！这是合理的——无限长导线可看作两段半无限长导线的叠加。

\newpage

# 六、常见错误与应试技巧

## 常见错误

| 错误 | 纠正 |
|------|------|
| 忘记 $d\boldsymbol{l}\times\hat{\boldsymbol{r}}$ 中出现 $\sin\theta$ | $dB$ 中**必须**有 $\sin\theta$！平行时不产生磁场 |
| $\boldsymbol{r}$ 的方向搞反 | $\boldsymbol{r}$ 从**电流元指向场点**，不是从场点指向电流元 |
| 所有电流元的 $d\boldsymbol{B}$ 都当同方向 | 弯曲导线不同部分的 $d\boldsymbol{B}$ 方向不同，必须分解 |
| 积分限写错（如无限长写成 $-\infty$ 到 $+\infty$ 用错变量） | 先确定积分变量和对应的几何关系 |

## 考试技巧

1. **先画图标注**：标注电流方向、场点位置、$\boldsymbol{r}$、$\theta$——5分靠公式，5分靠几何关系
2. **利用对称性**：圆形→水平分量抵消；正方形→四边贡献相同；一对平行导线→两侧对称
3. **积分技巧**：$\int dl/(a^2+l^2)^{3/2}$ 型积分→三角换元 $l=a\tan\alpha$
4. **检查极限**：无限长→有限长→半无限长，三者有确定关系（2倍、1倍），可互相验证
5. **复杂形状分解**：任何回路可分解为直线段+圆弧段的叠加

\newpage

# 七、快速参考卡

| 电流分布 | $B$ 的大小 | 方向 |
|---------|-----------|------|
| 无限长直导线 | $\dfrac{\mu_0 I}{2\pi r}$ | 右手定则（环向）|
| 有限长直导线 | $\dfrac{\mu_0 I}{4\pi r}(\cos\theta_1-\cos\theta_2)$ | 右手定则（环向）|
| 半无限长直导线 | $\dfrac{\mu_0 I}{4\pi r}$ | 右手定则（环向）|
| 圆环中心 | $\dfrac{\mu_0 I}{2R}$ | 轴向（右手定则）|
| 圆环轴线上 | $\dfrac{\mu_0 I R^2}{2(R^2+z^2)^{3/2}}$ | 轴向 |
| 圆弧（圆心角 $\alpha$）| $\dfrac{\mu_0 I\alpha}{4\pi R}$ | 轴向 |
| 正方形线圈中心 | $\dfrac{2\sqrt{2}\mu_0 I}{\pi a}$ | 垂直纸面 |
| 正 $n$ 边形中心 | $\dfrac{\mu_0 nI}{2\pi R}\sin\dfrac{\pi}{n}$ | 垂直纸面 |

## 一句话总结

> **毕奥-萨伐尔定律** = 每个电流元产生 $dB\propto I dl\sin\theta/r^2$ 的磁场。**对称性决定哪些分量抵消**，剩下的分量积分。**无限长直导线 $B=\mu_0 I/(2\pi r)$ 和圆环轴线 $B=\mu_0 I R^2/[2(R^2+z^2)^{3/2}]$ 是必须记住的两个基本结果。**
