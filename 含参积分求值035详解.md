# 0.35 含参积分求值 —— 详细解答

## 题目

$$\varphi(t) = \int_0^{+\infty} \frac{\ln(1+tx)}{x(x+1)}dx \quad (t \geq 0)$$

(1) 证明 $\varphi(t)$ 在 $(0,+\infty)$ 上连续。

(2) 求 $\varphi(1)$。

---

# (1) 证明 $\varphi(t)$ 在 $(0,+\infty)$ 上连续

## 策略

证明含参积分**内闭一致收敛**——对任意 $0 < \delta < T < +\infty$，积分在 $[\delta, T]$ 上一致收敛。一致收敛保持连续性，从而 $\varphi(t)$ 在所有 $t > 0$ 处连续。

---

## 步骤1：分析被积函数在 $t \in [\delta, T]$ 上的界

被积函数：$f(x,t) = \dfrac{\ln(1+tx)}{x(x+1)}$。

需要找 $F(x)$（不含 $t$）使得 $|f(x,t)| \leq F(x)$ 且 $\int_0^\infty F(x)dx$ 收敛。

---

## 步骤2：$x \to 0^+$ 处的处理（下限奇点）

当 $x \to 0^+$：$\ln(1+tx) \sim tx$（一阶 Taylor）。
$$f(x,t) \sim \frac{tx}{x(x+1)} = \frac{t}{x+1}$$

对 $t \in [\delta, T]$：$\left|\frac{t}{x+1}\right| \leq \frac{T}{x+1} \leq T$。

当 $x \to 0^+$ 时 $\frac{1}{x+1} \to 1$，被积函数趋近于 $t$（有界），**下限 $x=0$ 不是奇点**（$\ln(1+tx)$ 的零点抵消了分母的 $x$）。

验证：$\lim_{x \to 0^+} f(x,t) = \lim_{x \to 0^+} \frac{\ln(1+tx)}{x} \cdot \frac{1}{x+1}$。

由 $\lim_{x \to 0^+} \frac{\ln(1+tx)}{x} = t$（L'Hôpital 或等价无穷小）：
$$\lim_{x \to 0^+} f(x,t) = t \cdot 1 = t$$

所以 $x=0$ 处**函数连续**，不需要特别处理（只需处理 $x \to +\infty$）。

---

## 步骤3：$x \to +\infty$ 处的处理（上限奇点）

当 $x \to +\infty$：$\ln(1+tx) \sim \ln x + \ln t$。
$$f(x,t) \sim \frac{\ln x}{x^2}$$

（分母 $x(x+1) \sim x^2$。）

关键估计：对 $t \in [\delta, T]$ 且 $x$ 充分大（$x > 1/\delta$ 时 $tx > 1$）：
$$\ln(1+tx) \leq \ln(1+Tx) \leq \ln((T+1)x) = \ln x + \ln(T+1) \quad (\text{当 }x>1)$$

**构造控制函数**：取
$$F(x) = \begin{cases} \dfrac{T}{x+1}, & 0 \leq x \leq 1 \\[8pt] \dfrac{\ln x + \ln(T+1)}{x^2}, & x > 1 \end{cases}$$

则对所有 $t \in [\delta, T]$：$|f(x,t)| \leq F(x)$。

---

## 步骤4：验证控制函数的可积性

**$x \in [0,1]$**：$\frac{T}{x+1} \leq T$，积分 $\int_0^1 T\,dx = T < \infty$。

**$x \in [1, +\infty)$**：
$$\int_1^{+\infty} \frac{\ln x + \ln(T+1)}{x^2}dx = \int_1^{+\infty} \frac{\ln x}{x^2}dx + \ln(T+1)\int_1^{+\infty} \frac{dx}{x^2}$$

第一项（分部积分 $u=\ln x, dv=dx/x^2$）：
$$\int_1^{+\infty} \frac{\ln x}{x^2}dx = \left[-\frac{\ln x}{x}\right]_1^{+\infty} + \int_1^{+\infty} \frac{dx}{x^2} = 0 + 1 = 1$$

第二项：$\ln(T+1) \cdot \left[-\frac{1}{x}\right]_1^{+\infty} = \ln(T+1) \cdot 1$

$\int_1^{+\infty} F(x)dx = 1 + \ln(T+1) < \infty$。✓

---

## 步骤5：Weierstrass M-判别法

$|f(x,t)| \leq F(x)$ 对所有 $t \in [\delta, T]$ 成立，且 $\int_0^{+\infty} F(x)dx$ 收敛。

由 **Weierstrass M-判别法**，$\int_0^{+\infty} f(x,t)dx$ 关于 $t \in [\delta, T]$ **一致收敛**。

对任意 $t_0 \in (0,+\infty)$，取 $\delta = t_0/2$，$T = 2t_0$，积分在 $[\delta, T]$ 上一致收敛。又 $f(x,t) = \frac{\ln(1+tx)}{x(x+1)}$ 作为 $t$ 的函数在 $[\delta, T]$ 上连续，故一致收敛保持连续性：

$$\lim_{t \to t_0} \varphi(t) = \int_0^{+\infty} \lim_{t \to t_0} f(x,t)dx = \varphi(t_0)$$

由 $t_0$ 的任意性，$\varphi(t)$ 在 $(0,+\infty)$ 上处处连续。

$$\boxed{\text{(1) 证毕}}$$

---

# (2) 求 $\varphi(1)$

## 核心技巧：引入参数 → 求导消掉 $\ln$ → 积回去

---

## 步骤1：积分号下求导

$$\varphi'(t) = \frac{d}{dt}\int_0^{+\infty} \frac{\ln(1+tx)}{x(x+1)}dx$$

对 $t$ 求导（$\frac{\partial}{\partial t}\ln(1+tx) = \frac{x}{1+tx}$）：

$$\boxed{\varphi'(t) = \int_0^{+\infty} \frac{1}{(x+1)(1+tx)}dx}$$

（$x$ 约掉了分子的 $x$。）合法性由(1)中的一致收敛性保证。

---

## 步骤2：用部分分式裂项

对 $t \neq 1$，将 $\frac{1}{(x+1)(1+tx)}$ 拆开：

设 $\frac{1}{(x+1)(1+tx)} = \frac{A}{x+1} + \frac{B}{1+tx}$。

两边乘 $(x+1)(1+tx)$：
$$1 = A(1+tx) + B(x+1)$$
$$1 = (At + B)x + (A + B)$$

比较系数：
$$\begin{cases} At + B = 0 \\ A + B = 1 \end{cases}$$

由第一式：$B = -At$。代入第二式：$A - At = 1 \Rightarrow A(1-t) = 1 \Rightarrow \boxed{A = \frac{1}{1-t}}$。

$\boxed{B = -\frac{t}{1-t}}$。

所以：
$$\frac{1}{(x+1)(1+tx)} = \frac{1}{1-t}\left(\frac{1}{x+1} - \frac{t}{1+tx}\right)$$

验证（通分）：
$$\frac{1}{1-t}\left(\frac{1+tx - t(x+1)}{(x+1)(1+tx)}\right) = \frac{1}{1-t}\left(\frac{1+tx-tx-t}{(x+1)(1+tx)}\right) = \frac{1}{1-t}\left(\frac{1-t}{(x+1)(1+tx)}\right) = \frac{1}{(x+1)(1+tx)} \checkmark$$

---

## 步骤3：计算积分（$t \neq 1$）

$$\begin{aligned}
\varphi'(t) &= \frac{1}{1-t}\int_0^{+\infty} \left(\frac{1}{x+1} - \frac{t}{1+tx}\right)dx
\end{aligned}$$

⚠️ 注意：$\int_0^{+\infty} \frac{dx}{x+1}$ 和 $\int_0^{+\infty} \frac{t\,dx}{1+tx}$ **各自发散**！不能分开积。必须合在一起取极限。

对有限上限 $R$：
$$\begin{aligned}
\int_0^R \left(\frac{1}{x+1} - \frac{t}{1+tx}\right)dx &= \Big[\ln(x+1) - \ln(1+tx)\Big]_0^R \\[4pt]
&= \ln\frac{R+1}{1+tR} - \ln\frac{0+1}{1+0} \\[4pt]
&= \ln\frac{R+1}{1+tR}
\end{aligned}$$

令 $R \to +\infty$：
$$\lim_{R \to +\infty} \ln\frac{R+1}{1+tR} = \ln\frac{1}{t} = -\ln t$$

（$\frac{R+1}{1+tR} \to \frac{1}{t}$ 当 $R \to \infty$。）

故（$t \neq 1$）：

$$\boxed{\varphi'(t) = \frac{-\ln t}{1-t} = \frac{\ln t}{t-1}}$$

**$t=1$ 时的补充**：$\varphi'(1) = \int_0^{+\infty} \frac{dx}{(x+1)^2} = \left[-\frac{1}{x+1}\right]_0^{+\infty} = 1$。

（也与 $\lim_{t \to 1} \frac{\ln t}{t-1} = 1$ 一致，用 L'Hôpital 验证：$\lim_{t\to 1}\frac{1/t}{1}=1$。）

---

## 步骤4：从 $\varphi(0)$ 积到 $\varphi(1)$

$\varphi(0) = \int_0^{+\infty} \frac{\ln(1+0)}{x(x+1)}dx = \int_0^{+\infty} 0\,dx = 0$。

由微积分基本定理：
$$\varphi(1) = \varphi(0) + \int_0^1 \varphi'(t)dt = 0 + \int_0^1 \frac{\ln t}{t-1}dt$$

---

## 步骤5：计算 $\int_0^1 \frac{\ln t}{t-1}dt$

令 $u = 1-t$（$t: 0 \to 1$ 对应 $u: 1 \to 0$）：
$$\int_0^1 \frac{\ln t}{t-1}dt = \int_0^1 \frac{-\ln t}{1-t}dt$$

利用几何级数展开：$\frac{1}{1-t} = \sum_{n=0}^{\infty} t^n$（$|t| < 1$，边界 $t=0,1$ 不影响积分）。

$$\begin{aligned}
\int_0^1 \frac{-\ln t}{1-t}dt &= \int_0^1 (-\ln t) \sum_{n=0}^{\infty} t^n\,dt \\
&= \sum_{n=0}^{\infty} \int_0^1 (-t^n\ln t)\,dt
\end{aligned}$$

（逐项积分合法，因为 $\int_0^1 |\ln t|dt$ 收敛。）

---

## 步骤6：计算 $\int_0^1 t^n\ln t\,dt$

分部积分：$u = \ln t$，$dv = t^n dt$。
$du = \frac{dt}{t}$，$v = \frac{t^{n+1}}{n+1}$。

$$\begin{aligned}
\int_0^1 t^n\ln t\,dt &= \left[\frac{t^{n+1}\ln t}{n+1}\right]_0^1 - \int_0^1 \frac{t^{n+1}}{n+1} \cdot \frac{dt}{t} \\
&= \left(\frac{1 \cdot 0}{n+1} - \lim_{t \to 0^+} \frac{t^{n+1}\ln t}{n+1}\right) - \frac{1}{n+1}\int_0^1 t^n\,dt
\end{aligned}$$

$\lim_{t \to 0^+} t^{n+1}\ln t = 0$（$\ln t$ 比任何负幂都弱）。

$$\int_0^1 t^n\ln t\,dt = 0 - \frac{1}{n+1} \cdot \left[\frac{t^{n+1}}{n+1}\right]_0^1 = -\frac{1}{(n+1)^2}$$

---

## 步骤7：求和

$$\begin{aligned}
\varphi(1) &= \sum_{n=0}^{\infty} \left(-\left(-\frac{1}{(n+1)^2}\right)\right) \\
&= \sum_{n=0}^{\infty} \frac{1}{(n+1)^2} \\
&= \sum_{k=1}^{\infty} \frac{1}{k^2}
\end{aligned}$$

著名的 **Basel 问题**结果（可用 Fourier 级数或 Parseval 等式得到）：

$$\boxed{\sum_{k=1}^{\infty} \frac{1}{k^2} = \frac{\pi^2}{6}}$$

---

## 最终答案

$$\boxed{\varphi(1) = \frac{\pi^2}{6}}$$

---

# 回顾：解题脉络

```
φ(t) = ∫₀^∞ ln(1+tx)/(x(x+1)) dx
│
├─ 连读性 ← Weierstrass: 找控制函数 F(x)，∫F收敛
│
└─ 求 φ(1)
    │
    ├─ 对 t 求导 → φ'(t) = ∫₀^∞ 1/((x+1)(1+tx)) dx
    │     │
    │     ├─ 部分分式裂项
    │     ├─ 合并取极限 → φ'(t) = ln t/(t-1)
    │     └─ φ(0) = 0
    │
    ├─ φ(1) = ∫₀¹ ln t/(t-1) dt
    │
    ├─ 展开: 1/(1-t) = Σtⁿ
    ├─ 逐项积分: -∫tⁿln t = 1/(n+1)²
    └─ Σ 1/k² = π²/6
```

> 💡 关键技巧链：参数求导消除 $\ln$ → 部分分式裂有理函数 → 合并极限处理发散 → 级数展开计算 $\int_0^1 \ln t/(t-1)dt$。
