# 线性代数：Laplace 广义展开（按 k 行展开）

> 行列式计算的进阶技法 | 杨思辰 · 未来技术学院 · 物理学专业
>
> 🔥 考试重点：**广义展开公式、符号规则、k=1 退化、分块矩阵应用**

---

# §1 Laplace 展开定理（按 k 行展开）{#sec1}

---

## 1.1 公式表述

设 $A$ 是 $n$ 阶方阵，选定 $k$ 行：第 $i_1 < i_2 < \cdots < i_k$ 行。则：

$$\boxed{\det(A) = \sum_{1 \le j_1 < \cdots < j_k \le n} (-1)^{\,i_1+\cdots+i_k+j_1+\cdots+j_k} \cdot M \cdot M'}$$

> 💡 **直观记忆**：把行列式"切成" $k$ 行的截面和剩余 $(n-k)$ 行的补截面，所有可能的截面配对求和。

### 符号说明

| 符号 | 含义 |
|------|------|
| $k$ | 选定的行数（$1 \le k \le n-1$） |
| $i_1,\ldots,i_k$ | 选定的行号（从小到大） |
| $j_1,\ldots,j_k$ | 选定的列号（遍历所有 $C_n^k$ 种组合） |
| $M$ | 选定 $k$ 行与 $k$ 列交叉处的元素构成的 **$k$ 阶子式** |
| $M'$ | 删掉这 $k$ 行和 $k$ 列后，剩余元素构成的 **$(n-k)$ 阶余子式** |
| $(-1)^{i_1+\cdots+i_k+j_1+\cdots+j_k}$ | 符号因子，由选中行列的位置决定 |

> ⭐ **核心**：每一项 = $\text{符号} \times k\text{阶子式} \times (n-k)\text{阶余子式}$，求和遍历列标的所有 $C_n^k$ 种选法。

---

## 1.2 k=1 时退化为普通按一行展开

当 $k=1$ 时，就是最常见的按第 $i$ 行展开：

$$\det(A) = \sum_{j=1}^{n} (-1)^{i+j} \cdot a_{ij} \cdot M_{ij}$$

> 💡 在一行展开中，"$k$ 阶子式"退化成了单个元素 $a_{ij}$，"余子式"就是划去第 $i$ 行第 $j$ 列后的 $(n-1)$ 阶行列式。**广义展开是普通展开的自然推广：从"一个个元素拆"升级为"一块一块拆"。**

---

# §2 直观理解：划分与配对 {#sec2}

---

## 2.1 几何类比

把 $n$ 阶行列式想象成 $n$ 维平行多面体的**有向体积**。选 $k$ 行相当于：

```
选 k 行 → 在这 k 行上取 k 列 → 形成一个 k×k 的"截面"
其余行+列 → 自动形成 (n-k)×(n-k) 的"补截面"
行列式 = 所有可能的截面 × 其互补截面，带上正负号，全部加起来
```

这类似于把 $n$ 个维度切成"$k$ 维部分"和"$n-k$ 维部分"：

$$\text{体积} = \sum_{\text{所有截面}} \pm(\text{k 维投影体积}) \times (\text{补空间体积})$$

---

# §3 具体例子：4 阶行列式按前 2 行展开 {#sec3}

---

## 3.1 基本设定

取 $n=4$，$k=2$，选第 $i_1=1, i_2=2$ 行（前两行）。列选法共有 $C_4^2 = 6$ 种：

$$(j_1,j_2) = (1,2),\;(1,3),\;(1,4),\;(2,3),\;(2,4),\;(3,4)$$

## 3.2 完整展开式

$$\begin{aligned}
\det(A) =& \underbrace{(-1)^{1+2+1+2} \cdot M_{12} \cdot M'_{12}}_{选出(1,2)列} 
+ \underbrace{(-1)^{1+2+1+3} \cdot M_{13} \cdot M'_{13}}_{选出(1,3)列} \\
&+ \underbrace{(-1)^{1+2+1+4} \cdot M_{14} \cdot M'_{14}}_{选出(1,4)列}
+ \underbrace{(-1)^{1+2+2+3} \cdot M_{23} \cdot M'_{23}}_{选出(2,3)列} \\
&+ \underbrace{(-1)^{1+2+2+4} \cdot M_{24} \cdot M'_{24}}_{选出(2,4)列}
+ \underbrace{(-1)^{1+2+3+4} \cdot M_{34} \cdot M'_{34}}_{选出(3,4)列}
\end{aligned}$$

> ⚠️ 与普通的按一行展开（4 项）相比，按 2 行展开有 6 项——项数变多了！所以**盲目多选行并不减少计算量**，只有矩阵有大量零块时才有优势。

## 3.3 举例：(1,3) 项的含义

以 $(j_1,j_2) = (1,3)$ 为例：

- **取的行**：第 1, 2 行
- **取的列**：第 1, 3 列
- **$M$**：第 1,2 行与第 1,3 列交叉的 4 个元素构成的 $2 \times 2$ 子式

$$M = \begin{vmatrix} a_{11} & a_{13} \\ a_{21} & a_{23} \end{vmatrix}$$

- **$M'$**：删掉 1,2 行和 1,3 列后（即第 3,4 行与第 2,4 列）构成的余子式

$$M' = \begin{vmatrix} a_{32} & a_{34} \\ a_{42} & a_{44} \end{vmatrix}$$

- **符号**：$(-1)^{1+2+1+3} = (-1)^7 = -1$

---

# §4 什么时候用 $k>1$？{#sec4}

---

## 4.1 最佳场景：分块矩阵接近对角

最典型的是**分块上三角矩阵**，左上角 $k \times k$ 块的右上方全是零，或右下角 $(n-k) \times (n-k)$ 块的左下方全是零：

$$A = \begin{pmatrix}
a_{11} & a_{12} & 0 & 0 \\
a_{21} & a_{22} & 0 & 0 \\
c_{11} & c_{12} & b_{11} & b_{12} \\
c_{21} & c_{22} & b_{21} & b_{22}
\end{pmatrix}$$

按前 2 行展开时：只有选 $j_1=1, j_2=2$ 时 $M \neq 0$——选其他列组合的 $M$ 都包含零列，子式为零。**6 项中只剩 1 项！**

$$\det(A) = \det\begin{pmatrix}a_{11}&a_{12}\\a_{21}&a_{22}\end{pmatrix} \cdot \det\begin{pmatrix}b_{11}&b_{12}\\b_{21}&b_{22}\end{pmatrix}$$

> ⭐ 这就得到了分块上三角矩阵的经典结论：**行列式 = 对角块行列式之积**。广义展开一步到位，完全不需要逐行消元。

## 4.2 更一般的结论

对于分块三角矩阵（$A_{11}$ 为 $k \times k$，$A_{22}$ 为 $(n-k) \times (n-k)$）：

$$\det\begin{pmatrix} A_{11} & A_{12} \\ 0 & A_{22} \end{pmatrix} = \det(A_{11}) \cdot \det(A_{22})$$

$$\det\begin{pmatrix} A_{11} & 0 \\ A_{21} & A_{22} \end{pmatrix} = \det(A_{11}) \cdot \det(A_{22})$$

> ⚠️ 注意：$A_{12}$ 或 $A_{21}$ 中的元素**可以是任意的**——它们不出现在最终结果里！这点常常出人意料，但用广义展开恰好能优雅地证明。

---

# §5 核心对比总结 {#sec5}

---

| 项目 | $k=1$（普通展开） | $k>1$（广义展开） |
|------|-------------------|-------------------|
| 项数 | $C_n^1 = n$ 项 | $C_n^k$ 项（最多 $C_n^{\lfloor n/2 \rfloor}$） |
| 每项结构 | $a_{ij} \times (n-1)$ 阶余子式 | $k$ 阶子式 $\times (n-k)$ 阶余子式 |
| 符号 | $(-1)^{i+j}$ | $(-1)^{\sum i_r + \sum j_r}$ |
| 通用性 | 总是能用，计算量稳定 | 盲目使用项数暴增 |
| 最佳场景 | 通用计算 | 矩阵有大块零区域时极高效 |

---

> 💡 **实用建议**：日常计算中 $k=1$ 最常用。$k>1$ 主要用于**理论推导**（分块矩阵行列式公式、Cauchy-Binet 公式等）和特殊矩阵（如有一大块元素全为零）。实际数值计算不会用展开法（复杂度太高），但对于手算和证明，广义展开是极其优雅的工具。

---

# 📋 小测重点速查卡片 {#cheatsheet}

| 问题 | 答案 |
|------|------|
| 广义展开公式 | $\det(A) = \sum_{j} (-1)^{\sum i + \sum j} M \cdot M'$ |
| $M$ 是什么？ | 选中 $k$ 行 $k$ 列的 $k$ 阶子式 |
| $M'$ 是什么？ | 删掉选中行列后剩余的 $(n-k)$ 阶余子式 |
| 符号因子 | 所有选中行号 + 列号之和的 $(-1)$ 次幂 |
| $k=1$ 等价于什么？ | 普通的按一行展开 |
| $k=2$ 对 4 阶矩阵有几项？ | $C_4^2=6$ 项 |
| 何时有效？ | 矩阵有大块零区域时 |
| 分块上三角结论 | $\det = \det(A_{11}) \cdot \det(A_{22})$ |
| 零块的位置在哪儿不影响？ | 右上角或左下角为零均可，另一侧任意 |

---

<div style="text-align: center; color: #666; margin-top: 40px;">

—— Laplace 展开就是从"拆元素"到"拆块"的升级 ——

</div>
