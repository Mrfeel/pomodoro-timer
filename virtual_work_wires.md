# 虚功原理求两平行载流导线间的作用力——详解

> **原题**：两平行无限长载流导线（间距 $d$，电流 $I_1$、$I_2$），用虚功原理求单位长度作用力。
>
> **已知结论**：$f = \dfrac{\mu_0 I_1 I_2}{2\pi d}$，同向电流相吸，反向电流相斥。

\newpage

# 一、虚功原理的基本思想

## 1.1 什么是虚功原理？

虚功原理将**力的计算**转化为**能量的导数**。设想导线有一个微小的虚位移 $\delta d$，系统能量随之改变 $\delta W_m$：

$$\boxed{F\,\delta d = \mp\,\delta W_m}$$

符号取决于系统在什么条件下做虚位移。

## 1.2 两种约束条件

| 约束条件 | 力与能量关系 | 物理原因 |
|----------|------------|---------|
| **恒定电流**（$I = \text{const}$） | $F = +\dfrac{\partial W_m}{\partial d}$ | 电源做额外的功维持电流恒定，能量变化 = $+2\times$ 机械功 |
| **恒定磁通**（$\Phi = \text{const}$） | $F = -\dfrac{\partial W_m}{\partial d}$ | 无外来能量，机械功完全来自系统磁能的减少 |

对于本题（恒定电流源驱动），用 $F = +\partial W_m/\partial d$。但最终力的**大小**与符号约定无关，两者只差一个正负号（对应 $F$ 正方向的定义）。

> **实际操作建议**：先用能量的导数求力的大小，再用左手定则或楞次定律判断方向。

\newpage

# 二、磁能的分解——哪些与 $d$ 有关？

## 2.1 系统的总磁能

$$\boxed{W_m = \frac{1}{2}L_1 I_1^2 + \frac{1}{2}L_2 I_2^2 + M I_1 I_2}$$

| 项 | 含义 | 是否依赖于 $d$？ |
|----|------|----------------|
| $\frac{1}{2}L_1 I_1^2$ | 导线 1 的自感磁能 | **否**（对孤立无限长直导线） |
| $\frac{1}{2}L_2 I_2^2$ | 导线 2 的自感磁能 | **否** |
| $M I_1 I_2$ | 互感磁能（相互作用能） | **是！** $M$ 依赖于间距 $d$ |

$$\boxed{\frac{\partial W_m}{\partial d} = I_1 I_2\frac{\partial M}{\partial d}}$$

**自感项对力求导的贡献为零**——只有互感项贡献力。这是理解本题的第一个关键。

\newpage

# 三、单位长度互感 $M$ 的计算

## 3.1 几何设定

```
       y
       ↑
       │
  I₁ ⊙────●────●────  → x
       │    0    d
       │   (导线1) (导线2)
  I₂ ⊙
```

导线 1 在 $x=0$，导线 2 在 $x=d$，电流均沿 $+z$ 方向。

## 3.2 求互感

导线 1 在空间产生的磁感应强度（安培环路定理）：

$$B_1(r) = \frac{\mu_0 I_1}{2\pi r}$$

方向：右手定则，环绕导线 1（在 $x>0$ 区域沿 $+y$ 方向）。

导线 1 的磁场穿过"由导线 2 和无穷远处参考线构成的回路"的磁通量（单位长度）：

$$\Phi_{21} = \int_d^{r_0} B_1(r)\,dr = \frac{\mu_0 I_1}{2\pi}\int_d^{r_0}\frac{dr}{r} = \frac{\mu_0 I_1}{2\pi}\ln\frac{r_0}{d}$$

其中 $r_0$ 是截断半径（无穷远处的参考点，最终求导时会消掉）。

$$\boxed{M = \frac{\Phi_{21}}{I_1} = \frac{\mu_0}{2\pi}\ln\frac{r_0}{d}}$$

## 3.3 互感磁能（单位长度）

$$\boxed{W_{m,\text{int}} = M I_1 I_2 = \frac{\mu_0 I_1 I_2}{2\pi}\ln\frac{r_0}{d}}$$

（题解中 $W_m = \frac{\mu_0 I_1 I_2}{\pi}\ln\frac{d}{r_0}$ 是此式的等价形式，差一个常数和符号。）

\newpage

# 四、用虚功原理求力

## 4.1 对 $d$ 求导

只有 $W_{m,\text{int}}$ 依赖于 $d$：

$$\frac{\partial W_m}{\partial d} = \frac{\partial}{\partial d}\left(\frac{\mu_0 I_1 I_2}{2\pi}\ln\frac{r_0}{d}\right) = \frac{\mu_0 I_1 I_2}{2\pi}\cdot\left(-\frac{1}{d}\right) = -\frac{\mu_0 I_1 I_2}{2\pi d}$$

**截断半径 $r_0$ 在求导中消失了——这是虚功原理的优美之处。**

## 4.2 力的方向和大小

对于恒定电流系统：$F = +\dfrac{\partial W_m}{\partial d} = -\dfrac{\mu_0 I_1 I_2}{2\pi d}$

负号的含义：$F < 0$ 表示力沿 $-d$ 方向，即**减小间距的方向**——吸引力！

$$\boxed{f = |F| = \frac{\mu_0 I_1 I_2}{2\pi d}}$$

## 4.3 结果讨论

| 情况 | $I_1 I_2 > 0$（同向） | $I_1 I_2 < 0$（反向） |
|------|---------------------|---------------------|
| $F$ 的符号 | 负（吸引力） | 正（排斥力） |
| 力的大小 | $\dfrac{\mu_0 |I_1 I_2|}{2\pi d}$ | $\dfrac{\mu_0 |I_1 I_2|}{2\pi d}$ |
| 物理原因 | 两导线间磁场同向，磁力线"收缩"拉拢导线 | 两导线间磁场反向，磁力线"膨胀"推开导线 |

```
同向电流（吸引）：          反向电流（排斥）：
    I₁ ⊙        I₂ ⊙          I₁ ⊙        I₂ ⊗
     │    ←F    │              │    F→     │
     │    F→    │              │    ←F     │
    ─┴──────────┴─            ─┴──────────┴─
     ←── d ──→                ←── d ──→
  磁力线在两线之间         磁力线在两线之间
  方向相同，趋于收缩       方向相反，趋于膨胀
```

\newpage

# 五、力方向的物理判断法（不需计算）

## 5.1 磁力线图像

磁力线像橡皮筋——有**纵向张力**和**横向压力**：
- 沿磁力线方向：倾向于收缩（张力）
- 垂直于磁力线方向：倾向于膨胀（压力）

两同向电流导线之间磁力线**同向叠加** → 磁力线在中间区域"绷紧"收缩 → 导线被拉拢（吸引力）。

两反向电流导线之间磁力线**反向抵消** → 中间区域磁场弱，外部磁场强 → 磁力线"挤压"导线分开（排斥力）。

## 5.2 楞次定律的角度

如果导线 1 的电流在导线 2 处产生向上的磁场，导线 2 中的电流在该磁场中受安培力。用左手定则即可判断方向。

\newpage

# 六、用安培力公式直接验证

导线 1 在导线 2 处产生的磁场：

$$B_1 = \frac{\mu_0 I_1}{2\pi d}$$

导线 2 上单位长度的安培力：

$$f = I_2 B_1 = \frac{\mu_0 I_1 I_2}{2\pi d}$$

**与虚功原理结果完全一致！**

这既是验证，也说明了虚功原理虽然看起来"绕了个弯子"（先算磁能再求导），但在复杂几何中往往是**唯一可行的解析方法**——因为直接算安培力需要对导体上每点的 $I d\boldsymbol{l}\times\boldsymbol{B}$ 积分。

\newpage

# 七、虚功原理 vs. 直接安培力法——什么时候用哪个？

| 方法 | 适用场景 | 优点 | 缺点 |
|------|---------|------|------|
| **直接安培力** | 一条导线在已知均匀 $B$ 中 | 直接，物理图像清晰 | 需要先求 $B$ 的空间分布 |
| **虚功原理** | 复杂几何、铁磁介质、非线性系统 | 只需知道 $L$ 或 $M$ 对位移的依赖 | 需要先写出磁能表达式 |

**对于本题**：两条无限长平行直导线，安培力法更简单。但题目明确要求用虚功原理——这是因为虚功原理在更复杂的磁路问题（如含铁芯的电磁铁吸力计算）中是不可替代的方法。本题正好用来**学习虚功原理的标准操作流程**。

\newpage

# 八、操作总结

## 虚功原理求磁力的标准步骤

```
① 写出系统磁能 W_m
   （包含自感项 + 互感项）
        │
        ▼
② 找出 W_m 中与待求位移有关的项
   （自感项通常与位移无关，只管互感项）
        │
        ▼
③ 对位移求偏导
   （截断参数 r₀ 在求导中自然消去）
        │
        ▼
④ 确定符号
   恒定 I: F = +∂W_m/∂d
   恒定 Φ: F = -∂W_m/∂d
        │
        ▼
⑤ F > 0 → 力沿 +d（斥力）
   F < 0 → 力沿 -d（引力）
```

## 一句话总结

> 两平行载流导线单位长度的互感磁能 $W_m = \dfrac{\mu_0 I_1 I_2}{2\pi}\ln\dfrac{r_0}{d}$，对间距 $d$ 求导（恒定电流下 $F = +\partial W_m/\partial d$）得 $f = \dfrac{\mu_0 I_1 I_2}{2\pi d}$。同向相吸、反向相斥。虚功原理的实质是**把力的计算变成能量的求导**——算能量往往比直接积分安培力更容易。
