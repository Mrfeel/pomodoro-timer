# 线性代数 第一章：向量与复数

> 基于课堂 PPT (B1, 2026.03) | 杨思辰 · 未来技术学院 · 物理学专业
>
> 🔥 重点：**向量的线性运算、坐标系、线性相关与线性无关**

---

# 目录

1. [§1 向量的线性运算](#sec1)
2. [§2 坐标系](#sec2)
3. [*§3 集合与映射](#sec3)
4. [*§4 复数与代数结构](#sec4)
5. [📋 重点速查卡片](#cheatsheet)

---

# §1 向量的线性运算 {#sec1}

## 1.1 基本概念

**向量**是既有大小、又有方向的量。这是从高中数学中熟悉的概念。

- **表示方法**：$\overrightarrow{AB}$ 或 $\vec{a}$ 或 $\alpha$
- **模（长度）**：$|\overrightarrow{AB}|$, $|\vec{a}|$, $|\alpha|$
- **向量相等**：$\alpha = \beta \iff \begin{cases} |\alpha| = |\beta| \\ \alpha,\ \beta \text{ 方向相同} \end{cases}$

> 💡 **直观理解**：向量就像是空间中一根有方向的"箭"。两个向量相等，只看长度和方向是否相同，不关心起点在哪里——这就是"自由向量"的概念。

- **反向量（负向量）**：$\overrightarrow{AB}$ 的反向量是 $\overrightarrow{BA}$，记为 $-\overrightarrow{AB}$
- **单位向量**：$|\alpha| = 1$
- **零向量**：$|\alpha| = 0$，记为 $\mathbf{0}$。**零向量的方向不定！**

> ⚠️ **重要**：零向量的方向是"不定"的——这解释了为什么很多定理中零向量需要特殊处理。零向量与任何向量共线，与任何共面的向量组共面。

---

## 1.2 向量的运算

### 加法

两种等价的几何定义：
- **平行四边形法则**：以两向量为邻边作平行四边形，对角线为和
- **三角形法则**：将第二个向量的起点放在第一个向量的终点，从第一个起点到第二个终点的向量即为和

### 数乘

对 $\lambda \in \mathbb{R}$ 和向量 $\alpha$，定义 $\lambda\alpha$：

$$|\lambda\alpha| = |\lambda| \cdot |\alpha|$$

方向规则：$\lambda > 0$ 与 $\alpha$ 同向；$\lambda < 0$ 与 $\alpha$ 反向；$\lambda = 0$ 得到零向量。

### 线性组合

$$\beta = \lambda_1\alpha_1 + \lambda_2\alpha_2 + \cdots + \lambda_s\alpha_s$$

称 $\beta$ 可由 $\alpha_1, \ldots, \alpha_s$ **线性表示**，或称 $\beta$ 是 $\alpha_1, \ldots, \alpha_s$ 的**线性组合**。

> 💡 **规定**：零向量是空集的线性组合。

---

## 1.3 八条运算律

$\forall \alpha, \beta, \gamma$（向量），$\forall \lambda, \mu \in \mathbb{R}$：

$$\boxed{\begin{aligned}
\text{(A1) } \alpha + \beta &= \beta + \alpha & &\text{加法交换律} \\
\text{(A2) } (\alpha + \beta) + \gamma &= \alpha + (\beta + \gamma) & &\text{加法结合律} \\
\text{(A3) } \alpha + \mathbf{0} &= \alpha & &\text{零向量} \\
\text{(A4) } \alpha + (-\alpha) &= \mathbf{0} & &\text{反向量} \\[4pt]
\text{(D1) } \lambda(\alpha + \beta) &= \lambda\alpha + \lambda\beta & &\text{数乘对向量加法的分配律} \\
\text{(D2) } (\lambda + \mu)\alpha &= \lambda\alpha + \mu\alpha & &\text{数乘对标量加法的分配律} \\
\text{(M1) } \lambda(\mu\alpha) &= (\lambda\mu)\alpha & &\text{数乘结合律} \\
\text{(M2) } 1\alpha &= \alpha & &\text{单位元}
\end{aligned}}$$

> ⭐ **这八条是整个线性代数公理化体系的基石！** 第三章会看到，任何满足这八条的集合都称为"线性空间"。从三维几何向量到函数空间、多项式空间、矩阵空间，只要满足这八条，就可以用线性代数的方法来研究。

---

## 1.4 共线与共面

### 共线向量

平行于同一直线的一组向量称为**共线向量**。

> ⚠️ 规定：零向量与任何向量共线。

**Proposition 1.1**（共线的代数刻画）

$$\boxed{\alpha, \beta \text{ 共线} \iff \exists \lambda, \mu \in \mathbb{R}, \text{不全为零，使得 } \lambda\alpha + \mu\beta = \mathbf{0}}$$

> 💡 等价说法：两个非零向量共线 $\iff$ 一个是另一个的常数倍。但当涉及零向量时，用上面"不全为零的线性组合"更严谨。

### 共面向量

平行于同一平面的一组向量称为**共面向量**。

> ⚠️ 规定：零向量与任何共面的向量组共面。

**Proposition 1.2**（共面的代数刻画）

$$\boxed{\alpha, \beta, \gamma \text{ 共面} \iff \exists \lambda, \mu, \nu \in \mathbb{R}, \text{不全为零，使得 } \lambda\alpha + \mu\beta + \nu\gamma = \mathbf{0}}$$

---

## 1.5 🔥 线性相关与线性无关

> 🔥 **这是全课程最重要的概念之一，决定了很多后续定理的成立条件。**

**Definition 1.3**

对 $s$ 个向量 $\alpha_1, \alpha_2, \ldots, \alpha_s$，若存在**不全为零**的实数 $\lambda_1, \lambda_2, \ldots, \lambda_s$，使得

$$\boxed{\lambda_1\alpha_1 + \lambda_2\alpha_2 + \cdots + \lambda_s\alpha_s = \mathbf{0}}$$

则称 $\alpha_1, \alpha_2, \ldots, \alpha_s$ **线性相关**。否则，称它们**线性无关**。

> 💡 **直观理解**：
> - **线性相关** = 向量组中有"冗余"——至少有一个向量可以用其余向量线性表示出来
> - **线性无关** = 每个向量都"独立"——没有任何一个可以被其余向量线性表示
> - 平面上：两个不共线的向量线性无关；任意三个向量一定线性相关
> - 空间中：三个不共面的向量线性无关；任意四个向量一定线性相关

> **典型问题**：平面中最多有几个线性无关的向量？（答：2个）空间中呢？（答：3个）

**"线性无关"的等价表述**：

$$\lambda_1\alpha_1 + \cdots + \lambda_s\alpha_s = \mathbf{0} \implies \lambda_1 = \lambda_2 = \cdots = \lambda_s = 0$$

即：**只有全零系数才能使组合为零**。

---

# §2 坐标系 {#sec2}

## 2.1 平面坐标系

在平面中取定点 $O$ 和两个**不共线**的向量 $\mathbf{e}_1, \mathbf{e}_2$，有一一对应：

$$\text{点 } P \longleftrightarrow \overrightarrow{OP} = x_0\mathbf{e}_1 + y_0\mathbf{e}_2 \longleftrightarrow (x_0, y_0)$$

$[O; \mathbf{e}_1, \mathbf{e}_2]$ 称为一个**坐标系**，$(x_0, y_0)$ 是点 $P$ 的坐标。

> 💡 坐标系 = 原点 + 一组基向量。基向量可以任意选取（只要不共线），不同选取对应不同的坐标系。

## 2.2 空间坐标系

**Theorem 2.1**（空间向量唯一分解定理）

设 $\mathbf{e}_1, \mathbf{e}_2, \mathbf{e}_3$ 为空间三个**不共面**的向量，则任意向量 $\alpha$ 可**唯一**分解为：

$$\boxed{\alpha = x_0\mathbf{e}_1 + y_0\mathbf{e}_2 + z_0\mathbf{e}_3}$$

> ⭐ 这个"唯一分解"性质是基（basis）概念的核心。后续课程会推广到 $n$ 维空间。

在空间中取定点 $O$ 和三个不共面向量，同样有一一对应：

$$P \longleftrightarrow \overrightarrow{OP} = x_0\mathbf{e}_1 + y_0\mathbf{e}_2 + z_0\mathbf{e}_3 \longleftrightarrow (x_0, y_0, z_0)$$

### 坐标系的三种类型

| 条件 | 名称 |
|------|------|
| $\mathbf{e}_1, \mathbf{e}_2, \mathbf{e}_3$ 不共面 | **仿射坐标系**（最一般） |
| $+ |\mathbf{e}_1|=|\mathbf{e}_2|=|\mathbf{e}_3|=1$ | **笛卡尔坐标系** |
| $+ \mathbf{e}_i \perp \mathbf{e}_j\ (i \neq j)$ | **直角坐标系**（最常用） |

## 2.3 坐标形式的线性运算

取定坐标系 $[O; \mathbf{e}_1, \mathbf{e}_2, \mathbf{e}_3]$。设：

$$\alpha = x_1\mathbf{e}_1 + y_1\mathbf{e}_2 + z_1\mathbf{e}_3, \quad \beta = x_2\mathbf{e}_1 + y_2\mathbf{e}_2 + z_2\mathbf{e}_3$$

则：

$$\boxed{\alpha + \beta = (x_1+x_2)\mathbf{e}_1 + (y_1+y_2)\mathbf{e}_2 + (z_1+z_2)\mathbf{e}_3}$$

$$\boxed{\lambda\alpha = (\lambda x_1)\mathbf{e}_1 + (\lambda y_1)\mathbf{e}_2 + (\lambda z_1)\mathbf{e}_3}$$

> ⭐ **核心结论**：引入坐标系后，向量的线性运算**完全转化为**坐标的线性运算！这就是为什么我们可以"只在纸上算数组"。

## 2.4 $\mathbb{R}^3$ 数组空间

记 $\mathbb{R}^3 := \{\alpha = (a_1, a_2, a_3) \mid a_i \in \mathbb{R}\}$。

坐标形式的加法和数乘：

$$\begin{pmatrix} x_1 \\ y_1 \\ z_1 \end{pmatrix} + \begin{pmatrix} x_2 \\ y_2 \\ z_2 \end{pmatrix} := \begin{pmatrix} x_1+x_2 \\ y_1+y_2 \\ z_1+z_2 \end{pmatrix}, \quad \lambda\begin{pmatrix} x_1 \\ y_1 \\ z_1 \end{pmatrix} := \begin{pmatrix} \lambda x_1 \\ \lambda y_1 \\ \lambda z_1 \end{pmatrix}$$

- 零向量：$\mathbf{0} = (0, 0, 0)$
- 反向量：$-\alpha = (-a_1, -a_2, -a_3)$
- $(\mathbb{R}^3, +, \cdot)$ 满足全部八条运算律，称为 $\mathbb{R}$ 上的 **3 维数组空间**

---

# *§3 集合与映射 {#sec3}

> *本节为补充知识，旨在为后续课程提供严格的数学语言基础。

## 3.1 集合的基本概念与运算

**基本概念**：
- **集合**：一些对象的全体。记法：$A = \{a, b, c, d\}$ 或 $A = \{\text{描述性定义}\}$
- **属于/不属于**：$a \in A$ 或 $a \notin A$
- **空集** $\varnothing$：不包含任何元素
- **子集**：$A \subseteq B \iff (a \in A \Rightarrow a \in B)$
- **集合相等**：$A = B \iff A \subseteq B \text{ 且 } B \subseteq A$

**集合运算**：

| 运算 | 记号 | 定义 |
|------|------|------|
| 交 | $A \cap B$ | $\{x \mid x \in A \text{ 且 } x \in B\}$ |
| 并 | $A \cup B$ | $\{x \mid x \in A \text{ 或 } x \in B\}$ |
| 差 | $A \setminus B$ | $\{x \mid x \in A \text{ 且 } x \notin B\}$ |
| 笛卡尔积 | $A \times B$ | $\{(a,b) \mid a \in A, b \in B\}$ |

> 💡 笛卡尔积的元素是**有序对**：$(a, b) = (a', b') \iff a = a' \text{ 且 } b = b'$

## 3.2 映射

**定义**：设 $A, B$ 为非空集合。若对每个 $a \in A$，在 $B$ 中均有**唯一**元素 $b$ 与之对应，则称此对应为 $A$ 到 $B$ 的**映射**。

$$f: A \to B, \quad a \mapsto b = f(a)$$

**术语**：$A$—定义域；$f(A) = \{f(a) \mid a \in A\}$—值域（像）；$b$ 是 $a$ 的像，$a$ 是 $b$ 的原像。

### 三种类型的映射

| 类型 | 定义 | 直观 |
|------|------|------|
| **单射** | $a_1 \neq a_2 \Rightarrow f(a_1) \neq f(a_2)$ | 不同输入→不同输出 |
| **满射** | $\forall b \in B, \exists a \in A, f(a) = b$ | 每个输出都被命中 |
| **双射** | 单射 + 满射 | 完美一一对应 |

> ⭐ **结论**：设 $A$ 为有限集，$f: A \to A$。则 单射 $\iff$ 满射 $\iff$ 双射。

### 复合与逆映射

**复合**：$(g \circ f)(a) = g(f(a))$，满足结合律 $h \circ (g \circ f) = (h \circ g) \circ f$，但不满足交换律。

**恒等映射**：$\operatorname{id}_A: A \to A, a \mapsto a$（双射）。

**逆映射**：若存在 $g: B \to A$ 使 $g \circ f = \operatorname{id}_A$ 且 $f \circ g = \operatorname{id}_B$，则 $g = f^{-1}$。

> ⭐ **结论**：$f$ 可逆 $\iff$ $f$ 是双射。

## 3.3 等价关系与商集

**等价关系** $\sim$ 满足三条：自反性 $a \sim a$、对称性 $a \sim a' \Rightarrow a' \sim a$、传递性 $a \sim a' \wedge a' \sim a'' \Rightarrow a \sim a''$。

**等价类**：$[a] = \{a' \in A \mid a' \sim a\}$，$a' \in [a]$ 称为代表元。

**商集**：$A/\!\sim \; = \{[a] \mid a \in A\}$，有自然投射 $p: A \to A/\!\sim, a \mapsto [a]$（满射）。

> 💡 "$A$ 上的等价关系" $\longleftrightarrow$ "$A$ 的一个划分"。这是非常深刻的对应！

对任意映射 $f: A \to B$，可定义 $a \sim_f a' \iff f(a) = f(a')$，则 $f$ 诱导出 $\bar{f}: A/\!\sim_f \to B, [a] \mapsto f(a)$，满足 $f = \bar{f} \circ p$。

---

# *§4 复数与代数结构 {#sec4}

## 4.1 数系扩张

$$\mathbb{N} \subset \mathbb{Z} \subset \mathbb{Q} \subset \mathbb{R} \subset \mathbb{C}$$

### 环与域的定义

**环 (Ring)**：非空集合 $R$ 上定义 $+$ 和 $\times$，满足加法的四条公理 + 乘法结合律 + 两条分配律。

**域 (Field)**：环 + 乘法交换 + $1 \neq 0$ + 非零元可逆。

> 💡 **例子**：$\mathbb{Z}, \mathbb{Q}, \mathbb{R}, \mathbb{Z}_n, \mathbb{R}^{n \times n}$ 都是环；$\mathbb{Q}, \mathbb{R}$ 是域；$p$ 为素数时 $\mathbb{Z}_p$ 也是域。

### 多项式环

$$\mathbb{F}[x] := \{a_0 + a_1x + \cdots + a_nx^n \mid a_i \in \mathbb{F}, n \in \mathbb{N}\}$$

$\mathbb{F}[x]$ 是环（但不是域——$x$ 没有乘法逆元）。

### 域扩张的例子

$$\mathbb{Q}(\sqrt{2}) = \{a + b\sqrt{2} \mid a, b \in \mathbb{Q}\}$$

$$\mathbb{Q}(\sqrt{2})(\sqrt{3}) = \{a + b\sqrt{2} + c\sqrt{3} + d\sqrt{6} \mid a,b,c,d \in \mathbb{Q}\}$$

$$\mathbb{R}(\sqrt{-1}) = \{a + b\sqrt{-1} \mid a, b \in \mathbb{R}\} = \mathbb{C}$$

## 4.2 线性空间的公理化定义

**Definition 4.10**（重要！这是第三章的预告）

设 $V$ 为非空集合，$\mathbb{F}$ 为域。若可定义加法 $+: V \times V \to V$ 和数乘 $\cdot: \mathbb{F} \times V \to V$，满足：

$$\boxed{\begin{aligned}
\text{(1) } \alpha + \beta &= \beta + \alpha & \text{(5) } (\lambda + \mu)\alpha &= \lambda\alpha + \mu\alpha \\
\text{(2) } (\alpha + \beta) + \gamma &= \alpha + (\beta + \gamma) & \text{(6) } \lambda(\alpha + \beta) &= \lambda\alpha + \lambda\beta \\
\text{(3) } \exists 0, 0 + \alpha &= \alpha & \text{(7) } (\lambda\mu)\alpha &= \lambda(\mu\alpha) \\
\text{(4) } \exists -\alpha, \alpha + (-\alpha) &= 0 & \text{(8) } 1\alpha &= \alpha
\end{aligned}}$$

则称 $(V, +, \cdot)$ 是 $\mathbb{F}$ 上的**线性空间**。

> ⭐ 这八条就是 §1.3 中向量运算律的"原样照搬"！任何满足这八条的系统——函数空间、矩阵空间、多项式空间——都是线性空间，可以用统一的方法研究。

## 4.3 复数的矩阵表示

作为 $\mathbb{R}$ 上的线性空间：$\mathbb{C} \cong \mathbb{R}^2$

$$1 \mapsto \begin{pmatrix} 1 \\ 0 \end{pmatrix}, \quad \sqrt{-1} \mapsto \begin{pmatrix} 0 \\ 1 \end{pmatrix}$$

存在 $\mathbb{R}$-代数单同态，把复数嵌入 $2 \times 2$ 实矩阵：

$$\boxed{a + b\sqrt{-1} \;\longmapsto\; aI + bA = \begin{pmatrix} a & -b \\ b & a \end{pmatrix}}$$

其中 $I = \begin{pmatrix} 1 & 0 \\ 0 & 1 \end{pmatrix}$，$A = \begin{pmatrix} 0 & -1 \\ 1 & 0 \end{pmatrix}$（旋转 $90^\circ$）。

> 💡 **几何意义**：$\begin{pmatrix} a & -b \\ b & a \end{pmatrix} = \sqrt{a^2+b^2} \begin{pmatrix} \cos\theta & -\sin\theta \\ \sin\theta & \cos\theta \end{pmatrix}$，其中 $\theta = \arctan(b/a)$。这解释了复数乘法的几何意义：**模相乘，辐角相加**（旋转+缩放）。

---

# 📋 重点速查卡片 {#cheatsheet}

## 核心概念速查

| 概念 | 要点 | 关键记号 |
|------|------|----------|
| 线性相关 | 存在不全为零的系数使组合为零 | $\exists \lambda_i$ 不全为 $0$, $\sum \lambda_i \alpha_i = 0$ |
| 线性无关 | 只有全零系数才能使组合为零 | $\sum \lambda_i \alpha_i = 0 \Rightarrow$ 所有 $\lambda_i = 0$ |
| 共线 | 两向量平行于同一直线 | $\exists \lambda,\mu$ 不全为0: $\lambda\alpha + \mu\beta = 0$ |
| 共面 | 三向量平行于同一平面 | $\exists \lambda,\mu,\nu$ 不全为0: $\lambda\alpha + \mu\beta + \nu\gamma = 0$ |

## 八条运算律

| 加法四条 | 数乘两条 | 混合两条 |
|----------|----------|----------|
| A1 交换 | D1 数乘对向量加法分配 | M1 数乘结合 |
| A2 结合 | D2 数乘对标量加法分配 | M2 单位元 |
| A3 零向量 | | |
| A4 反向量 | | |

## 坐标系判定

| 维数 | 条件 | 结论 |
|------|------|------|
| 平面 | $\mathbf{e}_1, \mathbf{e}_2$ 不共线 | 任意向量唯一分解 |
| 空间 | $\mathbf{e}_1, \mathbf{e}_2, \mathbf{e}_3$ 不共面 | 任意向量唯一分解 |

## 映射速判

| $f: A \to B$ | 条件 | 等价 |
|--------------|------|------|
| 单射 | $a_1 \neq a_2 \Rightarrow f(a_1) \neq f(a_2)$ | — |
| 满射 | $\operatorname{Im}(f) = B$ | — |
| 双射 | 单射 + 满射 | 可逆 |

---

<div style="text-align: center; font-size: 18pt; font-weight: bold; color: #1A3C6E; margin-top: 2cm;">

—— 祝学习顺利！——

</div>
