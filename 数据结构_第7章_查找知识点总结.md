# 数据结构 · 第7章 查找 知识点与考点总结

> 《数据结构及其算法》复习资料 | 杨思辰 · 未来技术学院 · 物理学专业 · PB25992094
>
> 🔥 考试重点：**二分查找判定树、BST 插入删除、AVL 四种旋转、哈希表 ASL 手算、B 树插入删除**

---

## 目录

1. [§1 查找的基本概念](#sec1)
2. [§2 顺序查找](#sec2)
3. [§3 二分查找（折半查找）](#sec3)
4. [§4 分块查找（索引顺序查找）](#sec4)
5. [§5 二叉排序树 BST](#sec5)
6. [§6 平衡二叉树 AVL](#sec6)
7. [§7 B-树与 B+树](#sec7)
8. [§8 哈希表（散列表）](#sec8)
9. [§9 易错点与速查卡片](#sec9)

---

# §1 查找的基本概念 {#sec1}

## 1.1 术语

| 术语 | 含义 |
|:--|:--|
| **查找表** | 同一类型数据元素的集合 |
| **关键字（Key）** | 数据元素中唯一标识该元素的字段 |
| **查找** | 在查找表中确定关键字等于给定值的数据元素 |
| **平均查找长度 ASL** | 查找过程中关键字比较次数的**期望值** |

$$\boxed{ASL = \sum_{i=1}^{n} p_i \cdot c_i}$$

其中 $p_i$ = 查找第 $i$ 个元素的概率（通常 $p_i = 1/n$），$c_i$ = 找到第 $i$ 个元素所需比较次数。

**查找成功 vs 查找失败**：两者的 ASL 通常**不同**，考试常要求分别计算。

---

# §2 顺序查找 {#sec2}

## 2.1 算法

从头到尾逐个比较，可以用"**哨兵**"优化——在 0 号位置放待查关键字，省去越界判断。

```c
int SeqSearch(int a[], int n, int key) {
    a[0] = key;           // 哨兵
    int i = n;
    while (a[i] != key) i--;
    return i;              // i=0 表示未找到
}
```

## 2.2 复杂度

| | 查找成功 | 查找失败 |
|:--|:--|:--|
| ASL | $(n+1)/2$ | $n+1$（哨兵）或 $n$ |
| 时间 | $O(n)$ | $O(n)$ |

> 💡 顺序查找对表的**有序性无要求**，链表也通用。但 $O(n)$ 太慢。

---

# §3 二分查找（折半查找）{#sec3}

> 🔥 必考：给定有序表，手画判定树 + 算 ASL。

## 3.1 前提

查找表必须是**有序顺序表**（数组+有序），链表不支持二分查找（无法 $O(1)$ 访问中间元素）。

## 3.2 算法

```c
int BinarySearch(int a[], int n, int key) {
    int low = 0, high = n - 1;
    while (low <= high) {              // ★ 注意是 <= 不是 <
        int mid = (low + high) / 2;
        if (a[mid] == key) return mid;
        else if (a[mid] > key) high = mid - 1;
        else low = mid + 1;
    }
    return -1;
}
```

## 3.3 判定树（二叉判定树）

二分查找过程形成一棵**平衡二叉树**：中间元素为根，左侧递归为左子树，右侧为右子树。

**性质**：
- 判定树深度 $\lfloor \log_2 n \rfloor + 1$
- 查找成功比较次数 = 该结点在判定树中的**层数**
- 查找失败比较次数 = **到达外部结点**（空指针）的路径长度

### ASL 公式

$$\boxed{ASL_{成功} = \frac{1}{n}\sum_{i=1}^{n} h_i}, \quad h_i = \text{结点 } i \text{ 在判定树中的层数}$$

$$\boxed{ASL_{失败} \le \lfloor \log_2 n \rfloor + 1}$$

## 3.4 手算示例

有序表 `{5, 13, 19, 21, 37, 56, 64, 75, 80, 88, 92}`（11 个元素）：

```
判定树:
              56
          /        \
        19          80
       /  \        /  \
     13    37    75    88
    /     /  \        /  \
   5    21   ( )    92   ( )
                  /
                 ( )
```

$ASL_{成功} = (1×1 + 2×2 + 3×4 + 4×4) / 11 = (1+4+12+16)/11 = 33/11 = 3$

## 3.5 复杂度

| | 查找成功 | 查找失败 |
|:--|:--|:--|
| 时间 | $O(\log n)$ | $O(\log n)$ |

> ✅ 效率最高的静态查找方法，但**不适用于频繁插入/删除**（需移动大量元素）。

---

# §4 分块查找（索引顺序查找）{#sec4}

## 4.1 思想

将表分为若干**块**，块间有序（前一块最大 < 后一块最小），块内无序。建**索引表**记录每块最大关键字 + 首地址。

查找过程：先**二分查找索引**确定块 → 再**顺序查找块内**。

## 4.2 ASL

$$\boxed{ASL = ASL_{索引} + ASL_{块内} \approx \log_2\left(\frac{n}{s}\right) + \frac{s}{2}}$$

其中 $s$ = 块大小，最优 $s \approx \sqrt{n}$ 时 ASL 最小 $O(\sqrt{n})$。

---

# §5 二叉排序树 BST {#sec5}

> 🔥 必考：给一组关键字，画 BST 的构造过程 + 删除结点。

## 5.1 定义

对于任意结点：
- **左子树**所有关键字 < 根关键字
- **右子树**所有关键字 > 根关键字
- 左、右子树各自也是 BST

**中序遍历 BST = 递增有序序列**（这是 BST 最重要的性质！）

## 5.2 查找

从根开始：等于 → 找到；小于 → 走左子树；大于 → 走右子树。

```c
BSTNode* SearchBST(BSTree T, int key) {
    if (T == NULL || T->data == key) return T;
    if (key < T->data) return SearchBST(T->lchild, key);
    else return SearchBST(T->rchild, key);
}
```

## 5.3 插入

一定插在**叶子位置**。先查找 → 查找失败的位置即插入位置。

## 5.4 删除（三种情况）

| 情况 | 被删结点 | 操作 |
|:--|:--|:--|
| ① 叶子 | 无孩子 | 直接删除 |
| ② 单孩子 | 只有左或只有右 | 用**孩子替代** |
| ③ 双孩子 | 左右都有 | 用**中序前驱**（左子树最大值）或**中序后继**（右子树最小值）替代，再递归删除替代结点 |

## 5.5 复杂度

- 最好（完全二叉树）：$O(\log n)$
- 最坏（单链/斜树）：$O(n)$
- **平均**：$O(\log n)$（随机插入时）

> 💡 BST 的致命缺点：插入顺序不当会退化为链表。**AVL 树**解决了这个问题。

---

# §6 平衡二叉树 AVL {#sec6}

> 🔥 必考：识别不平衡类型 + 旋转操作。

## 6.1 定义

AVL 树 = **任意结点的左右子树高度差绝对值 ≤ 1** 的 BST。

**平衡因子 BF** = 左子树高度 − 右子树高度，取值 $\{-1, 0, 1\}$。

## 6.2 四种失衡与旋转

插入导致失衡时，沿插入路径向上找到**首个 ∣BF∣≥2 的结点 A**，分析 A 及其孩子/孙子：

| 类型 | 插入位置 | 旋转 | 口诀 |
|:--|:--|:--|:--|
| **LL** | A 的左孩子的**左子树** | **右单旋**（A 右旋） | "左左 → 右转" |
| **RR** | A 的右孩子的**右子树** | **左单旋**（A 左旋） | "右右 → 左转" |
| **LR** | A 的左孩子的**右子树** | **先左后右**（左孩左旋→A 右旋） | "左右 → 先左再右" |
| **RL** | A 的右孩子的**左子树** | **先右后左**（右孩右旋→A 左旋） | "右左 → 先右再左" |

### 旋转示意（LL 型）

```
插入前:      不平衡:  A(BF=+2)     右旋后:
    A             /      \            B
   /             B       AR          / \
  B      →      / \          →      BL  A
 /             BL  BR                  / \
BL                                     BR  AR
      (BL插入了新结点)
```

## 6.3 复杂度

查找/插入/删除：$O(\log n)$ —— 严格平衡保证。

---

# §7 B-树与 B+树 {#sec7}

## 7.1 B-树（B 树）

B-树是**平衡多路查找树**，用于磁盘等外部存储。一棵 $m$ 阶 B-树满足：

1. 每个结点**至多 $m$ 棵子树**（$m-1$ 个关键字）
2. 根至少 2 棵子树（非叶时），其余结点至少 $\lceil m/2 \rceil$ 棵子树
3. 所有**叶子在同一层**
4. 有 $k$ 棵子树的结点含 $k-1$ 个关键字

```
3阶B-树示例:
          [20, 40]
         /   |    \
    [5,10]  [25]  [50,60]
```

### 插入（分裂）

关键字超 $m-1$ → 将**中间关键字**提升到上层，左右各分一半。

### 删除

分情况：① 叶子直接删；② 非叶用前驱/后继替代；③ 删后不足 $\lceil m/2 \rceil - 1$ → 向兄弟**借**或**合并**。

## 7.2 B+树

B+树是 B-树的变种，区别：
- 关键字全部在**叶子**，非叶结点只做**索引**
- 叶子之间用**链表**串接（支持**范围查询**）

---

# §8 哈希表（散列表）{#sec8}

> 🔥 高频考点：给定哈希函数 + 冲突处理方法 → 手算哈希表 + ASL。

## 8.1 基本概念

**哈希表**：根据关键字**直接计算**存储地址，理想情况下查找 $O(1)$。

$$\text{地址} = H(key)$$

**冲突**：不同关键字映射到同一地址（$key_1 \neq key_2$ 但 $H(key_1) = H(key_2)$）。

## 8.2 常见哈希函数

| 方法 | 公式 | 特点 |
|:--|:--|:--|
| **除留余数法** | $H(key) = key \bmod p$ | $p$ 取不大于表长的**素数**效果最好 |
| 直接定址法 | $H(key) = key + C$ | 关键字连续时用 |
| 平方取中法 | 取 $key^2$ 中间几位 | 分布均匀 |
| 折叠法 | 分段求和 | 长关键字 |

## 8.3 冲突处理（两大流派）

### (1) 开放定址法（Open Addressing）

冲突时按某种探测序列找下一个空位。

| 探测方式 | 公式 | 问题 |
|:--|:--|:--|
| **线性探测** | $H_i = (H(key) + i) \bmod m$ | **聚集**（堆积） |
| **二次探测** | $H_i = (H(key) + i^2) \bmod m$ | 减轻聚集 |
| **双散列** | $H_i = (H_1(key) + i \cdot H_2(key)) \bmod m$ | 最优 |

### (2) 链地址法（Chaining）

每个地址挂一条**链表**，冲突元素连在后面。

## 8.4 ASL 手算

**线性探测法 ASL**：

| 关键字 | $H(key)$ | 实际地址 | 查找成功比较次数 |
|:--|:--:|:--:|:--:|
| 19 | $19\%13=6$ | 6 | 1 |
| 14 | $14\%13=1$ | 1 | 1 |
| 23 | $23\%13=10$ | 10 | 1 |
| 1 | $1\%13=1$ | 1→2 | 2 |
| 68 | $68\%13=3$ | 3 | 1 |

$$ASL_{成功} = (1+1+1+2+1)/5 = 1.2$$

```c
// 公式
ASL_成功 = (每个关键字找到自己的比较次数之和) / 关键字个数
ASL_失败 = (每个地址探测到空位的比较次数之和) / 哈希表长
```

## 8.5 装填因子

$$\boxed{\alpha = \frac{n}{m}} = \frac{\text{表中已有记录数}}{\text{哈希表长度}}$$

$\alpha$ 越大 → 越满越容易冲突 → ASL 越大。一般控制在 0.7~0.85。

---

# §9 易错点与速查卡片 {#sec9}

## 9.1 常见易错点

| 易错点 | 正确做法 |
|:--|:--|
| 二分查找条件 | `while(low <= high)` —— **必须带等号**！ |
| BST 删除"双孩子" | 用**中序前驱或后继**替代，不是盲删 |
| AVL 旋转判断 | 先找到首个失衡点 A，再看"**A-孩子-孙子**"三点走向 |
| B-树阶数含义 | $m$ 阶 → 最多 $m$ 棵子树 = $m-1$ 个关键字 |
| 哈希冲突 ≠ 错误 | 冲突是正常的，靠探测/链地址**解决** |
| 开放定址法删除 | 不能直接删（会断探测链），需**标记删除** |
| ASL 成功 vs 失败 | **分开算！**失败时探测到空位为止 |

## 9.2 核心速查

| 查找方法 | 时间（平均） | 结构要求 |
|:--|:--|:--|
| 顺序查找 | $O(n)$ | 无 |
| 二分查找 | $O(\log n)$ | **有序顺序表** |
| 分块查找 | $O(\sqrt{n})$ | 块间有序 |
| BST | $O(\log n)$→最坏 $O(n)$ | 二叉链表 |
| AVL | $O(\log n)$ 严格 | 二叉链表 |
| B-树 | $O(\log_m n)$ | 多路 |
| **哈希表** | **$O(1)$**（理想） | 数组 |

---

<div style="text-align: center; margin-top: 2em;">

—— 第 7 章「查找」知识点总结完 ——

> 📚 下一步：第 8 章「排序」——内部排序八大算法对比。

</div>
