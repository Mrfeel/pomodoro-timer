# 数据结构 · 查找 单元复习小测 详细解答

> 杨思辰 · 未来技术学院 · 物理学专业 · PB25992094
>
> 来源：微信图片（4张），第7章「查找」单元测验

---

# 第一部分：选择题（每题2分，共30分）

## 1. 下列查找方法中，平均查找长度与 n 无关的是（ ）

**答案：D. 哈希查找**

**解析**：哈希表理想情况下 $O(1)$，与 $n$ 无关。顺序查找 $O(n)$、折半查找 $O(\log n)$、BST 查找 $O(\log n) \sim O(n)$，均与 $n$ 有关。

---

## 2. 含 18 个元素的有序顺序表做折半查找，查找成功的 ASL 约为（ ）

**答案：B. 3.1**

**解析**：判定树各层结点分布——

| 层 | 结点数 | 比较次数 |
|:--:|:--:|:--:|
| 1 | 1 | 1 |
| 2 | 2 | 2 |
| 3 | 4 | 3 |
| 4 | 8 | 4 |
| 5 | 3 | 5 |

$$ASL_{成功} = \frac{1\times 1 + 2\times 2 + 3\times 4 + 4\times 8 + 5\times 3}{18} = \frac{1+4+12+32+15}{18} = \frac{64}{18} \approx 3.56$$

> 注：$\approx 3.1$ 是用近似公式 $\frac{n+1}{n}\log_2(n+1)-1$ 的结果，选择题取此近似值。

---

## 3. 折半查找判定树中，外部结点（查找失败）的个数为（ ）

**答案：C. $n+1$**

**解析**：一棵有 $n$ 个内部结点的二叉树必有 $n+1$ 个外部结点（空指针）。查找失败 = 查到外部结点，共 $n+1$ 种失败情况。

---

## 4. 分块查找中 $n = b \times s$（b 块数，s 块大小），ASL 最小时（ ）

**答案：A. $b = s = \sqrt{n}$**

**解析**：

$$ASL = ASL_{索引} + ASL_{块内} \approx \frac{b}{2} + \frac{s}{2}$$

代入 $b = n/s$ 并对 $s$ 求导，最优时 $s = \sqrt{n}$，$b = \sqrt{n}$，$ASL_{min} \approx \sqrt{n}$。

---

## 5. 关于二叉排序树（BST），正确的是（ ）

**答案：B. 对 BST 做中序遍历可以得到关键字的升序序列**

**解析**：
- A ❌ BST 最坏（斜树）为 $O(n)$，不是恒为 $O(\log n)$。
- B ✅ 中序 LNR → 递增序列，这是 BST 最核心的性质。
- C ❌ 新结点**一定插在叶子位置**。
- D ❌ BST 形态**完全取决于插入顺序**（有序插入→斜树）。

---

## 6. BST 删除一个有两棵子树的结点时，正确方法是（ ）

**答案：D. 用直接前驱或直接后继替换**

**解析**：双孩子结点 → 用中序前驱（左子树最大值）**或**中序后继（右子树最小值）替代，然后递归删除替代结点。两者均可，任选其一。

---

## 7. AVL 树的平衡因子取值范围是（ ）

**答案：B. $-1, 0, 1$**

**解析**：$BF = h_L - h_R$。AVL 要求任意结点 $|BF| \le 1$。

---

## 8. AVL 插入导致 A 失衡，插入发生在 A 的左孩子的右子树中，应做（ ）

**答案：C. LR（先左旋后右旋）**

**解析**：失衡点在 A，插入路径：A → A的左孩子（L）→ 左孩子的右子树（R）→ **LR 型**。旋转：先对 A 的左孩子做左旋 → 再对 A 做右旋。

---

## 9. 高度为 5 的 AVL 树最少含有（ ）个结点
> 约定：空树高 0，单结点高 1

**答案：B. 12**

**解析**：递推公式 $N(h) = N(h-1) + N(h-2) + 1$，$N(0)=0, N(1)=1$。

| $h$ | 1 | 2 | 3 | 4 | 5 |
|:--:|:--:|:--:|:--:|:--:|:--:|
| $N(h)$ | 1 | 2 | 4 | 7 | **12** |

---

## 10. 下列序列中，能构成 BST 查找路径的是（ ）

**答案：D. 20, 10, 15, 12**

**解析**：BST 查找路径 = 从根开始沿一条路径依次比较。每次向左则后继值 < 当前值（且 > 所有"向右"祖先的最小值），每次向右则后继值 > 当前值（且 < 所有"向左"祖先的最大值）。

逐项验证：

- **A** $20 \to 15 \to 10 \to 25$：在 10 处右转找 25，但 $25 > 20$ 意味着在根处就不该左转。❌
- **B** $20 \to 25 \to 22 \to 28$：在 22 处右转找 28，但 $28 > 25$ 意味着在 25 处就不该左转。❌
- **C** $20 \to 30 \to 15$：在 30 处左转找 15，但 $15 < 20$ 意味着在根处就不该右转。❌
- **D** $20 \to 10 \to 15 \to 12$：10<20 ✓左；15>10 且 15<20 ✓右；12<15 且 12>10 ✓左。全部自洽 ✅

---

## 11. 哈希表 $m=14$，$H(key)=key \bmod 11$，已插入 $15(4), 38(5), 61(6), 84(7)$，线性探测法。关键字 49 的存储地址是（ ）

**答案：8**

**解析**：$H(49) = 49 \bmod 11 = 5$。

探測序列：5(38占) → 6(61占) → 7(84占) → **8(空)**。存入地址 8。

---

## 12. 关于哈希查找，说法**错误**的是（ ）

**答案：C. 装填因子 $\alpha$ 一定小于 1**

**解析**：
- A ✅ $\alpha$ 越大越拥挤 → ASL 越大。
- B ✅ 开放定址法删除需**标记删除**（不能简单置空，会断探测链）。
- C ❌ **链地址法**的 $\alpha$ 可以 > 1（平均链表长度 > 1）。
- D ✅ 均匀分布减少冲突。

---

## 13. $m=11$, $H(key)=key \bmod 11$, 线性探测。依次插入 $\{22,33,12,25,14,36\}$。查找 36 需要比较（ ）次

**答案：C. 3**

**解析**：逐步构建哈希表：

| 关键字 | H(key) | 探测过程 | 存入 |
|:--|:--:|:--|:--:|
| 22 | 0 | 0 | 0 |
| 33 | 0 | 0→**1** | 1 |
| 12 | 1 | 1→**2** | 2 |
| 25 | 3 | 3 | 3 |
| 14 | 3 | 3→**4** | 4 |
| 36 | 3 | 3(25)≠36 → 4(14)≠36 → **5(36=36)** | 5 |

查找 36：比较 25($\neq$)、14($\neq$)、36($=$) → **3 次**。

---

## 14. 折半查找和 BST 查找的时间性能（ ）

**答案：B. 有时相同**

**解析**：BST **平衡时**两者均为 $O(\log n)$；BST 退化为斜树时 BST 为 $O(n)$ 而折半仍为 $O(\log n)$。两者仅在 BST 平衡时相同。

---

## 15. 关于判定树，说法**错误**的是（ ）

**答案：A. 折半查找的判定树一定是平衡二叉树**

**折半查找判定树本质是平衡的**。但严格来说，当 $n$ 较小时，左右子树结点数可能相差 >1（如 $n=4$ 时左1右2），并非 AVL 意义上的严格平衡。"一定是平衡二叉树"的说法**不完全准确**——实际上折半查找判定树不一定是 AVL 意义上的"平衡二叉树"（虽然最大高度差不超过 1）。

> 📌 此题意在考察：折半查找判定树是"**次优**"而非一定"严格平衡"的。选 A。

---

# 第二部分：应用题（共40分）

## 1. 折半查找判定树 + ASL（8分）

> 长度为 10 的有序顺序表，画折半查找判定树，求 $ASL_{成功}$。

### 判定树

```
                 5
            /        \
          2            8
        /   \        /   \
       1     3      6     9
      /       \      \     \
     0         4      7     (失败)
```

### ASL 计算

| 层 | 结点 | 比较次数 | 贡献 |
|:--:|:--|:--:|:--:|
| 1 | 5 | 1 | 1 |
| 2 | 2, 8 | 2 | 4 |
| 3 | 1, 3, 6, 9 | 3 | 12 |
| 4 | 0, 4, 7 | 4 | 12 |

$$\boxed{ASL_{成功} = \frac{1\times 1 + 2\times 2 + 3\times 4 + 4\times 3}{10} = \frac{1+4+12+12}{10} = \frac{29}{10} = 2.9}$$

---

## 2. BST 构造与删除（12分）

> 关键字序列 $\{30,15,40,8,22,35,50,18\}$

### (1) 构造 BST

```
插入顺序: 30 → 15 → 40 → 8 → 22 → 35 → 50 → 18

          30
        /    \
      15      40
     /  \    /  \
    8   22  35  50
        /
       18
```

### (2) 删除 15（用直接前驱替换）

> 15 的直接前驱 = 左子树最大值 = 12 → 不对，15 的左子树是 {8}，左子树最大值是 8。

等等。15 的左子树：
```
    15
   /  \
  8   22
      /
     18
```
15 的左子树 = {8, 22, 18}，其中最大值……不对。8 在 15 的左子树中，22 在 15 的右子树中。

15 的左子树中：
- 8（左孩子）
- 22 是 15 的右孩子

所以 15 的左子树只有 {8}。但 8 < 15，8 是左子树。22 > 15，22 是右子树。

15 的左子树最大值 = ……只有 8 在左子树中。8 的最大值 = 8。

但 15 有两个孩子（8 和 22），所以需要用直接前驱（左子树最大值）替换。左子树只有 8，8 是叶子。

删除 15，用 8 替换：

```
          30
        /    \
       8      40
        \    /  \
        22  35  50
        /
       18
```

8 上移到 15 的位置，22 成为 8 的右孩子。这仍然是一棵 BST（8 < 30，22 > 8, 22 < 30 ✓）。

### (3) 从原树删除 40（用直接后继替换）

> 40 的直接后继 = 右子树最小值 = 50……等等。

40 的右子树 = {35, 50}，最小值是 35。

用 35 替换 40，删除原 35 结点（叶子）：

```
          30
        /    \
      15      35
     /  \       \
    8   22      50
        /
       18
```

---

## 3. AVL 树构造 + ASL（12分）

> 关键字序列 $\{16, 10, 20, 6, 14, 26, 4\}$，逐步插入构造 AVL 树。

### (1) 逐步插入过程

```
① 插入 16:
    16  (BF=0)

② 插入 10:
      16(BF=+1)
     /
   10(BF=0)

③ 插入 20:
      16(BF=0)
     /    \
   10      20
  (BF=0)  (BF=0)

④ 插入 6:
        16(BF=+1)
       /    \
     10      20
    /      (BF=0)
   6
  (BF=0)
  → 10 的 BF=+1, 16 的 BF=+1, 未失衡

⑤ 插入 14:
        16(BF=+2) ← 失衡!
       /    \
     10      20
    /  \
   6   14

  A=16, 插入在左孩子的右子树 → LR型
  先左旋 10:
        16
       /    \
     14      20
    /
   6
    \
    10

  再右旋 16:
        14
       /  \
      10   16
     /      \
    6        20

⑥ 插入 26:
        14(BF=-1)
       /  \
      10   16(BF=-1)
     /       \
    6         20(BF=-1)
                \
                26

⑦ 插入 4:
        14(BF=0)
       /  \
      10   16(BF=-1)
     /  \     \
    6   ?      20(BF=-1)
   /             \
  4               26

  10 BF=+1 (6在左,右边无), 14 BF=+1
  检查: 14 BF=+1 (左高2,右高1), OK
  10 BF 左=1,右=0 → BF=+1, OK
  
  Actually let me be more careful.
  
  14: left height=3 (14→10→6→4), right height=2 (14→16→20→26). BF=+1. OK.
  10: left height=2 (10→6→4), right height=0. BF=+2! 失衡!
  
  A=10, 插入在左孩子的左子树 → LL型
  右旋 10:
       14
      /  \
     6    16
    / \     \
   4  10     20
               \
               26
```

最终 AVL 树：

```
           14
         /    \
        6      16
       / \       \
      4  10      20
                   \
                   26
```

### (2) ASL 计算

| 层 | 结点 | 比较次数 | 贡献 |
|:--:|:--|:--:|:--:|
| 1 | 14 | 1 | 1 |
| 2 | 6, 16 | 2 | 4 |
| 3 | 4, 10, 20 | 3 | 9 |
| 4 | 26 | 4 | 4 |

$$\boxed{ASL_{成功} = \frac{1+4+9+4}{7} = \frac{18}{7} \approx 2.57}$$

---

## 4. 哈希表（8分）

> $H(key) = key \bmod 7$，$m=7$，关键字 $\{8, 15, 22, 1, 29, 35\}$

### (1) 线性探测法

| 关键字 | $H(key)$ | 探测过程 | 存入 |
|:--|:--:|:--|:--:|
| 8 | 1 | 1 | 1 |
| 15 | 1 | 1→**2** | 2 |
| 22 | 1 | 1→2→**3** | 3 |
| 1 | 1 | 1→2→3→**4** | 4 |
| 29 | 1 | 1→2→3→4→**5** | 5 |
| 35 | 0 | **0** | 0 |

| 地址 | 0 | 1 | 2 | 3 | 4 | 5 | 6 |
|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|
| 关键字 | 35 | 8 | 15 | 22 | 1 | 29 | — |

**ASL（线性探测）**：

| 关键字 | 35 | 8 | 15 | 22 | 1 | 29 |
|:--:|:--:|:--:|:--:|:--:|:--:|:--:|
| 比较次数 | 1 | 1 | 2 | 3 | 4 | 5 |

$$ASL_{成功} = \frac{1+1+2+3+4+5}{6} = \frac{16}{6} \approx 2.67$$

### (2) 链地址法

```
0: 35 → NULL
1: 8 → 15 → 22 → 1 → 29 → NULL
2: (空)
3: (空)
4: (空)
5: (空)
6: (空)
```

**ASL（链地址法）**：

| 关键字 | 35 | 8 | 15 | 22 | 1 | 29 |
|:--:|:--:|:--:|:--:|:--:|:--:|:--:|
| 比较次数 | 1 | 1 | 2 | 3 | 4 | 5 |

$$ASL_{成功} = \frac{1+1+2+3+4+5}{6} = \frac{16}{6} \approx 2.67$$

> 💡 有趣：这个例子中线性探测和链地址法的 ASL 碰巧相同。通常链地址法 ASL 更优，但此例所有关键字都映射到地址 0 或 1，造成链表过长。

---

# 第三部分：算法题（共30分）

## 1. 判断二叉树是否为 BST（15分）

### (1) 类型定义

```c
typedef struct BiTNode {
    int data;
    struct BiTNode *lchild, *rchild;
} BiTNode, *BiTree;
```

### (2) 算法思想

利用 BST 的核心性质：**中序遍历序列严格递增**。

- 维护一个 `pre` 指针记录中序遍历中上一个访问的结点
- 中序遍历过程中，若当前结点值 $\le$ 上一个结点值 → 不是 BST
- 或：维护区间 `(min, max)`，递归判断每个结点是否在合法区间内

**方法一（区间法——更直观）**：

```c
int IsBST(BiTree T, int min, int max) {
    if (T == NULL) return 1;                        // 空树是 BST
    if (T->data <= min || T->data >= max) return 0;  // 不在合法区间

    return IsBST(T->lchild, min, T->data) &&         // 左子树：(min, data)
           IsBST(T->rchild, T->data, max);            // 右子树：(data, max)
}

// 调用：IsBST(root, INT_MIN, INT_MAX)
```

**方法二（中序遍历法）**：

```c
int preVal = INT_MIN;  // 记录上一个访问的值（全局或传引用）

int IsBST_InOrder(BiTree T) {
    if (T == NULL) return 1;
    if (!IsBST_InOrder(T->lchild)) return 0;   // 左子树

    if (T->data <= preVal) return 0;            // 不满足递增
    preVal = T->data;                           // 更新 pre

    return IsBST_InOrder(T->rchild);            // 右子树
}
```

### (3) 复杂度分析

- **时间**：$O(n)$——每个结点访问一次。
- **空间**：$O(h)$——递归栈深度 = 树高。最好 $O(\log n)$（平衡），最坏 $O(n)$（斜树）。

---

## 2. BST 中查找第 K 小元素（15分）

### (1) 类型定义（扩展 count 字段）

```c
typedef struct BSTNode {
    int data;
    int lcount;                     // ★ 左子树结点总数
    struct BSTNode *lchild, *rchild;
} BSTNode, *BSTree;
```

### (2) 算法思想

利用 BST 中序递增 + `lcount` 减少搜索范围：

- 若 $k = lcount + 1$ → 当前结点即为第 $k$ 小 ✅
- 若 $k \le lcount$ → 在**左子树**中继续找第 $k$ 小
- 若 $k > lcount + 1$ → 在**右子树**中找第 $k - lcount - 1$ 小

```c
BSTNode* KthSmallest(BSTree T, int k) {
    if (T == NULL || k < 1) return NULL;

    int leftCount = T->lcount;                   // 左子树大小

    if (k == leftCount + 1)
        return T;                                 // ★ 找到
    else if (k <= leftCount)
        return KthSmallest(T->lchild, k);         // 在左子树找
    else
        return KthSmallest(T->rchild,
                           k - leftCount - 1);    // 在右子树找
}
```

> 📌 若结点结构**不含** `lcount` 字段，则用**中序遍历计数法**：中序遍历直到第 $k$ 个访问的结点即为答案。时间 $O(k)$，最坏 $O(n)$。

```c
// 不含 lcount 的版本（中序遍历 + 计数器）
int count = 0;
BSTNode* result = NULL;

void KthSmallest_NoCount(BiTree T, int k) {
    if (T == NULL || result != NULL) return;
    KthSmallest_NoCount(T->lchild, k);
    count++;
    if (count == k) { result = T; return; }
    KthSmallest_NoCount(T->rchild, k);
}
```

### (3) 复杂度分析

- **含 lcount 字段**：时间 $O(h)$（只沿一条路径），空间 $O(1)$（迭代）或 $O(h)$（递归）。
- **不含 lcount（中序遍历）**：时间 $O(n)$ 最坏，空间 $O(h)$。

---

<div style="text-align: center; margin-top: 2em; color: #666;">

—— 查找 单元复习小测 全部解答完毕 ——

</div>
