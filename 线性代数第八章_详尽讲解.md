# 线性代数 第八章：实二次型 详尽讲解

> 基于课堂 PPT (B1, 2026.03) | 杨思辰 · 未来技术学院 · 物理学专业
>
> 🔥 本章重点：**配平方法、惯性定理、正定二次型的判定、二次曲面分类**

---

# 目录

1. [§1 双线性函数](#sec1)
2. [§2 二次型的标准形](#sec2)
3. [§3 唯一性——惯性定理](#sec3)
4. [§4 二次曲线与二次曲面的分类](#sec4)
5. [§5 正定二次型](#sec5)
6. [附录1：矩阵的奇异值分解 (SVD)](#svd)
7. [更多例题](#more)
8. [小测速查卡片](#cheatsheet)

---

# §1 双线性函数 {#sec1}

## 1.1 从欧氏空间出发的直观理解

在第七章我们学习了**内积** $(\alpha, \beta)$，它满足：
- 对称性：$(\alpha, \beta) = (\beta, \alpha)$
- 对两个变元的线性性
- 正定性：$(\alpha, \alpha) \geq 0$

**本章的核心思想**：把内积的"正定性"要求拿掉，保留"双线性 + 对称性"，就得到了**对称双线性函数**。再由对称双线性函数自然引出**二次型**的概念。

> 💡 **关系链**：对称双线性函数 $\xrightarrow{Q_\varphi(\alpha)=\varphi(\alpha,\alpha)}$ 二次型函数 $\xrightarrow{\text{取定基}}$ $n$ 元二次型 $X^T A X$ $\longleftrightarrow$ 实对称矩阵 $A$

---

## 1.2 线性函数 (Definition 1.1)

设 $V$ 是数域 $F$ 上的 $n$ 维线性空间。

**定义**：映射 $\varphi: V \to F$ 称为 $V$ 上的**线性函数**，若满足：
$$\varphi(\alpha + \beta) = \varphi(\alpha) + \varphi(\beta), \quad \varphi(\lambda\alpha) = \lambda\varphi(\alpha), \quad (\forall \alpha, \beta \in V, \lambda \in F)$$

记 $L(V, F)$ 为 $V$ 上所有线性函数的集合。

> 📌 注：$\varphi$ 就是线性映射（从 $V$ 到 $F$）。取定 $V$ 的一组基 $\alpha_1, \ldots, \alpha_n$，有双射：
> $$L(V, F) \xrightarrow{\cong} F^{1 \times n}, \quad \varphi \mapsto (\varphi(\alpha_1), \ldots, \varphi(\alpha_n))$$

**例1**：迹函数 $\text{tr}: M_n(F) \to F$，$A = (a_{ij}) \mapsto \sum_{i=1}^n a_{ii}$，是 $M_n(F)$ 上的线性函数。

**例2**：$n \geq 2$ 时，行列式 $\det: M_n(F) \to F$ **不是**线性函数。

---

## 1.3 双线性函数 (Definition 1.2)

**定义**：映射 $\varphi: V \times V \to F$ 称为 $V$ 上的**双线性函数**，若对两个变元分别满足线性性：
$$\begin{aligned}
\varphi(\lambda_1\alpha_1 + \lambda_2\alpha_2, \beta) &= \lambda_1\varphi(\alpha_1, \beta) + \lambda_2\varphi(\alpha_2, \beta) \\
\varphi(\alpha, \mu_1\beta_1 + \mu_2\beta_2) &= \mu_1\varphi(\alpha, \beta_1) + \mu_2\varphi(\alpha, \beta_2)
\end{aligned}$$

记 $L_2(V, F)$ 为所有双线性函数的集合。

> 📌 **矩阵表示**：取定 $V$ 的基 $\alpha_1, \ldots, \alpha_n$，有双射：
> $$L_2(V, F) \xrightarrow{\cong} M_n(F), \quad \varphi \mapsto A = (\varphi(\alpha_i, \alpha_j))_{n \times n}$$
> 称 $A$ 为 $\varphi$ 在基 $\alpha_1, \ldots, \alpha_n$ 下的**矩阵**。

**例3**：欧氏空间 $V$ 上的内积 $(\cdot, \cdot): V \times V \to \mathbb{R}$ 就是一个双线性函数，其对应的矩阵就是**度量矩阵**。

> ⭐ **Proposition 1.3**：$A, B \in M_n(F)$ 相合 $\Longleftrightarrow$ 存在 $F$ 上 $n$ 维线性空间 $V$ 及其上双线性函数 $\varphi$，使 $A, B$ 是 $\varphi$ 在 $V$ 的两组基下的矩阵。

---

## 1.4 对称双线性函数与二次型 (Definition 1.4-1.6)

**对称双线性函数**：$\varphi$ 是双线性函数，且 $\varphi(\alpha, \beta) = \varphi(\beta, \alpha)$。

记 $L_2^+(V, F)$ 为所有对称双线性函数的集合。取定基后，它与 $F$ 上 $n$ 阶**对称矩阵**一一对应。

**二次型函数** (Definition 1.5)：对 $V$ 上对称双线性函数 $\varphi$，定义：
$$Q_\varphi: V \to F, \quad \alpha \mapsto \varphi(\alpha, \alpha)$$
称 $Q_\varphi$ 为由 $\varphi$ 决定的**二次型函数**。

> 📌 $\varphi$ 与 $Q_\varphi$ 互相确定：
> $$\varphi(\alpha, \beta) = \frac{1}{2}\big(Q_\varphi(\alpha + \beta) - Q_\varphi(\alpha) - Q_\varphi(\beta)\big)$$

**$n$ 元二次型** (Definition 1.6)：取定基后，设 $A = (a_{ij})_{n \times n}$（$a_{ij} = a_{ji}$），$X = (x_1, \ldots, x_n)^T$：
$$Q(x_1, \ldots, x_n) = X^T A X = \sum_{i=1}^n \sum_{j=1}^n a_{ij} x_i x_j$$
称 $A$ 为**二次型的矩阵**，记为 $A_Q$。

> ⭐ **四大对象的统一**：以下四种数学对象本质上是一回事：
> 1. $V$ 上的对称双线性函数 $\varphi$
> 2. $V$ 上的二次型函数 $Q_\varphi$
> 3. $F$ 上 $n$ 元二次型 $Q(x_1, \ldots, x_n)$
> 4. $F$ 上 $n$ 阶对称矩阵 $A$

---

## 1.5 对称双线性函数基本定理

> ⭐ **Theorem 1.7（对称双线性函数基本定理）**：
> $\varphi \in L_2^+(V, F) \Longrightarrow$ 存在 $V$ 的一组基，使 $\varphi$ 在该基下的矩阵为**对角阵**。

> **Corollary 1.8**：$F$ 上的对称矩阵总是**相合于对角阵**。

---

# §2 二次型的标准形 {#sec2}

本节取定数域 $\mathbb{R}$。

## 2.1 基本概念

**实二次型**：$Q(x_1, \ldots, x_n) = \sum_{i=1}^n \sum_{j=1}^n a_{ij}x_i x_j$，其中 $a_{ij} = a_{ji} \in \mathbb{R}$。

**线性替换**：设 $X = PY$，其中 $P \in M_n(\mathbb{R})$。当 $P$ 可逆时，称为**非退化线性替换**。

> ⭐ **Theorem 2.3**：任一实二次型都可通过非退化线性替换变成**平方和**的形式：
> $$b_1 y_1^2 + b_2 y_2^2 + \cdots + b_n y_n^2$$
> 这称为 $Q(x_1, \ldots, x_n)$ 的**标准形**。

---

## 2.2 化标准形的方法

### 方法一：配平方法

**核心思想**：逐次配方，每次消去含某个变元的交叉项。

**例5（习题八 3(1)）**：$Q(x_1, x_2, x_3) = x_1^2 + x_2^2 + x_3^2 + x_1x_2 + x_1x_3 + x_2x_3$

**解**：
1. 先对 $x_1$ 配方：
$$Q = (x_1^2 + x_1x_2 + x_1x_3) + x_2^2 + x_3^2 + x_2x_3$$
$$= \left(x_1 + \frac{1}{2}x_2 + \frac{1}{2}x_3\right)^2 - \frac{1}{4}x_2^2 - \frac{1}{4}x_3^2 - \frac{1}{2}x_2x_3 + x_2^2 + x_3^2 + x_2x_3$$
$$= \left(x_1 + \frac{1}{2}x_2 + \frac{1}{2}x_3\right)^2 + \frac{3}{4}x_2^2 + \frac{3}{4}x_3^2 + \frac{1}{2}x_2x_3$$

2. 再对 $x_2$ 配方：
$$= y_1^2 + \frac{3}{4}\left(y_2^2 + \frac{2}{3}y_2y_3\right) + \frac{3}{4}y_3^2 \quad \left(\text{令 } y_1 = x_1 + \frac{1}{2}x_2 + \frac{1}{2}x_3, y_2 = x_2, y_3 = x_3\right)$$
$$= y_1^2 + \frac{3}{4}\left(y_2 + \frac{1}{3}y_3\right)^2 - \frac{1}{12}y_3^2 + \frac{3}{4}y_3^2$$
$$= y_1^2 + \frac{3}{4}\left(y_2 + \frac{1}{3}y_3\right)^2 + \frac{2}{3}y_3^2$$

3. 令 $z_1 = y_1, z_2 = y_2 + \frac{1}{3}y_3, z_3 = y_3$，得标准形：
$$\boxed{Q = z_1^2 + \frac{3}{4}z_2^2 + \frac{2}{3}z_3^2}$$

非退化线性替换矩阵：
$$P = \begin{pmatrix} 1 & -\frac{1}{2} & -\frac{1}{3} \\ 0 & 1 & -\frac{1}{3} \\ 0 & 0 & 1 \end{pmatrix}, \quad X = PZ$$

**例6（习题八 3(3)）**：$Q(x_1, x_2, x_3, x_4) = x_1x_2 + x_2x_3 + x_3x_4 + x_4x_1$

**关键技巧**：当没有平方项时，先做变换产生平方项（令 $x_1 = y_1 + y_2, x_2 = y_1 - y_2$），再配方。

最终结果：$\boxed{Q = u_1^2 - u_2^2}$（秩为 2，正惯性指数为 1）

---

### 方法二：初等变换法

对矩阵 $A$ 做成对的初等行、列变换：

$$\begin{pmatrix} A \\ I_n \end{pmatrix} \longrightarrow \cdots \longrightarrow \begin{pmatrix} P^T A P \\ I_n P \end{pmatrix} = \begin{pmatrix} \text{对角阵} \\ P \end{pmatrix}$$

或：
$$(A \mid I_n) \longrightarrow \cdots \longrightarrow (P^T A P \mid P^T)$$

> 📌 关键操作：每次对 $A$ 做一次初等**行**变换，就必须紧接着做一次**相同类型**的初等**列**变换（保持对称性）。

> ⭐ **Corollary 2.4**：设 $A$ 为实对称矩阵，则存在初等矩阵 $P_1, P_2, \ldots, P_s$，使得：
> $$P_s^T \cdots P_2^T P_1^T A P_1 P_2 \cdots P_s = \text{diag}(b_1, b_2, \ldots, b_n)$$

---

# §3 唯一性——惯性定理 {#sec3}

## 3.1 惯性定理

同一个二次型可以通过不同的非退化线性替换化成不同的标准形，但有一个量是**唯一不变**的。

> ⭐ **Theorem 3.1（惯性定理）**：
> 任意实二次型都可通过非退化线性替换变成**规范形**：
> $$y_1^2 + \cdots + y_p^2 - y_{p+1}^2 - \cdots - y_r^2$$
> 规范形是**唯一**的。

**定义 (Definition 3.2)**：
- $p$ —— **正惯性指数**
- $r - p$ —— **负惯性指数**
- $2p - r$ —— **符号差**
- $r$ —— 二次型的**秩**（= 矩阵 $A$ 的秩）

---

## 3.2 化标准形的三种方法小结

| 方法 | 要点 | 适用场景 |
|------|------|----------|
| **(1) 配平方法** | 逐次配方，先处理有平方项的变元 | 手工计算，有平方项 |
| **(2) 初等变换法** | 行列成对变换，保持对称性 | 编程计算，规范统一 |
| **(3) 正交变换法** | 等同于"求实对称矩阵相合于对角阵" | 需要保持几何性质时 |

---

## 3.3 关键例题

**例7**：$n$ 阶实对称矩阵在相合下有多少个等价类？

**答**：由惯性定理，规范形由正惯性指数 $p$（$0 \leq p \leq n$）唯一确定，共 $\boxed{\frac{(n+1)(n+2)}{2}}$ 个等价类。

**例9（习题七 29 / 习题八 10）**：设 $A$ 是 $n$ 阶实对称矩阵，且 $A^2 = A$（幂等矩阵），求 $A$ 的相合规范形。

**解**：$A^2 = A$ 说明 $A$ 的特征值只能是 $0$ 或 $1$。设 $\text{rank}(A) = r$，则有 $r$ 个特征值为 $1$，$n-r$ 个为 $0$。规范形为：
$$\boxed{\begin{pmatrix} I_r & 0 \\ 0 & 0 \end{pmatrix}}$$

**例12（习题八 8）**：
(1) $A$ 是反对称矩阵 $\Longleftrightarrow$ 对任意 $n$ 维向量 $X$，有 $X^T A X = 0$。
(2) 如果 $A$ 为对称矩阵，且对任意 $n$ 维向量 $X$ 都有 $X^T A X = 0$，则 $A = O$。

> 📌 **关键**：(2) 说明二次型 $X^T A X$ 唯一确定了对称矩阵 $A$。如果两个对称矩阵对应的二次型恒等，则两矩阵相等。

**例13（参考习题七 30）—— Rayleigh 商**：
设 $A$ 为 $n$ 阶实对称矩阵，特征值 $\lambda_1 \geq \lambda_2 \geq \cdots \geq \lambda_n$。则对 $\mathbb{R}^n$ 中任一非零列向量 $X$：
$$\lambda_n \leq \frac{X^T A X}{|X|^2} \leq \lambda_1$$
且 $a_{ii} \in [\lambda_n, \lambda_1]$（取 $X = e_i$ 即可）。

---

# §4 二次曲线与二次曲面的分类 {#sec4}

## 4.1 基本概念

- $\mathbb{R}^2$ 中：$ax^2 + bxy + cy^2 + dx + ey + f = 0$ → **二次曲线**
- $\mathbb{R}^3$ 中：$\sum a_{ij}x_i x_j + 2\sum b_i x_i + c = 0$ → **二次曲面**
- $\mathbb{R}^n$ 中：同上形式 → **二次超曲面**

**目标**：通过等距变换（正交变换 + 平移）将二次超曲面方程化为最简形式，实现分类。

---

## 4.2 等距变换 (Definition 4.1)

**定义**：映射 $\mathcal{A}: \mathbb{R}^n \to \mathbb{R}^n$ 称为**等距变换**，若满足：
$$|\mathcal{A}(X) - \mathcal{A}(Y)| = |X - Y|, \quad (\forall X, Y \in \mathbb{R}^n)$$

> ⭐ **Lemma 4.2**：
> 1. 等距变换的复合仍是等距变换
> 2. 正交变换 $\Longleftrightarrow$ 等距变换 + 保持原点（$\mathcal{A}(0) = 0$）
> 3. 任一等距变换可**唯一**分解为一个正交变换与一个平移变换的合成
> 4. 等距变换可逆

**平移变换**：$P_{X_0}(Y) = Y + X_0$，是等距变换但**不是**线性变换（因为不保持原点）。

---

## 4.3 技术性处理——齐次坐标

将 $\mathbb{R}^n$ 嵌入 $\mathbb{R}^{n+1}$：
$$Y \mapsto \begin{pmatrix} Y \\ 1 \end{pmatrix}$$

- 平移 $P_\delta$ 可表示为：$\begin{pmatrix} I_n & \delta \\ 0 & 1 \end{pmatrix} \begin{pmatrix} Y \\ 1 \end{pmatrix} = \begin{pmatrix} Y + \delta \\ 1 \end{pmatrix}$
- 正交变换 $T$ 可表示为：$\begin{pmatrix} T & 0 \\ 0 & 1 \end{pmatrix} \begin{pmatrix} Y \\ 1 \end{pmatrix} = \begin{pmatrix} TY \\ 1 \end{pmatrix}$

任意等距变换 $H = P_\delta \circ T$ 可表示为矩阵 $\begin{pmatrix} T & \delta \\ 0 & 1 \end{pmatrix}$。

---

## 4.4 二次超曲面的度量分类定理

将二次超曲面方程写为：
$$f(X) = (X^T \; 1) \begin{pmatrix} A & \beta \\ \beta^T & c \end{pmatrix} \begin{pmatrix} X \\ 1 \end{pmatrix} = 0$$

其中 $A$ 为 $n$ 阶实对称矩阵，$\beta$ 为 $n$ 维列向量。

> ⭐ **Theorem 4.4-4.5（度量分类定理）**：根据 $\text{rank}\begin{pmatrix} A & \beta \\ \beta^T & c \end{pmatrix}$ 与 $\text{rank}(A)$ 的关系分三种情形：

| 情形 | 条件 | 最简形式 |
|------|------|----------|
| (1) | $\text{rank}\begin{pmatrix} A & \beta \\ \beta^T & c \end{pmatrix} = \text{rank}(A)$ | $\lambda_1 y_1^2 + \cdots + \lambda_n y_n^2 = 0$ |
| (2) | $\text{rank}\begin{pmatrix} A & \beta \\ \beta^T & c \end{pmatrix} = \text{rank}(A) + 1$ | $\lambda_1 y_1^2 + \cdots + \lambda_n y_n^2 + c_1 = 0$（$c_1 \neq 0$） |
| (3) | $\text{rank}\begin{pmatrix} A & \beta \\ \beta^T & c \end{pmatrix} = \text{rank}(A) + 2$ | 前 $s$ 项对角化 + 最后一项为 $2c_1 y_n$（$c_1 \neq 0$）+ 常数项为 $0$ |

> 📌 **Lemma 4.3**：给出 rank 关系的判据。

---

## 4.5 二次曲面分类总表

| 类型 | 标准方程 | 名称 |
|------|----------|------|
| **椭球面型** | $\frac{x^2}{a^2} + \frac{y^2}{b^2} + \frac{z^2}{c^2} = 1$ | 椭球面 |
| **双曲面型** | $\frac{x^2}{a^2} + \frac{y^2}{b^2} - \frac{z^2}{c^2} = 1$ | 单叶双曲面 |
| | $\frac{x^2}{a^2} - \frac{y^2}{b^2} - \frac{z^2}{c^2} = 1$ | 双叶双曲面 |
| | $\frac{x^2}{a^2} + \frac{y^2}{b^2} - \frac{z^2}{c^2} = 0$ | 二次锥面 |
| **抛物型** | $z = \frac{x^2}{a^2} + \frac{y^2}{b^2}$ | 椭圆抛物面 |
| | $z = \frac{x^2}{a^2} - \frac{y^2}{b^2}$ | 双曲抛物面（马鞍面） |
| **二次柱面** | $\frac{x^2}{a^2} + \frac{y^2}{b^2} = 1$ | 椭圆柱面 |
| | $\frac{x^2}{a^2} - \frac{y^2}{b^2} = 1$ | 双曲柱面 |
| | $y^2 = 2px$ | 抛物柱面 |

---

## 4.6 分类步骤（以例15为例）

**例15**：$4x^2 + 8xy + 4y^2 + 13x + 3y + 4 = 0$

**步骤1**：写出二次部分矩阵 $A = \begin{pmatrix} 4 & 4 \\ 4 & 4 \end{pmatrix}$，求特征值 $\lambda_1 = 8, \lambda_2 = 0$。

**步骤2**：求正交矩阵 $P = \begin{pmatrix} \frac{\sqrt{2}}{2} & \frac{\sqrt{2}}{2} \\ \frac{\sqrt{2}}{2} & -\frac{\sqrt{2}}{2} \end{pmatrix}$，使 $P^T A P = \text{diag}(8, 0)$。

**步骤3**：做正交替换 $\begin{pmatrix} x \\ y \end{pmatrix} = P \begin{pmatrix} \bar{x} \\ \bar{y} \end{pmatrix}$，方程化为 $8\bar{x}^2 + 8\sqrt{2}\bar{x} + 5\sqrt{2}\bar{y} + 4 = 0$。

**步骤4**：配方消去一次项：$8\left(\bar{x} + \frac{\sqrt{2}}{2}\right)^2 + 5\sqrt{2}\bar{y} = 0$。

**步骤5**：令 $\tilde{x} = \bar{x} + \frac{\sqrt{2}}{2}, \tilde{y} = \bar{y}$，得 $\tilde{y} = -\frac{4\sqrt{2}}{5}\tilde{x}^2$。

**结论**：该曲线为**抛物线**。

---

# §5 正定二次型 {#sec5}

> 🔥 本章最重要的部分，考试必考！

## 5.1 定义 (Definition 5.1)

实二次型 $Q(x_1, \ldots, x_n)$ 称为**正定的**，若对任意非零的 $(c_1, \ldots, c_n) \in \mathbb{R}^n$，都有：
$$Q(c_1, \ldots, c_n) > 0$$

实对称矩阵 $A$ 称为**正定的**（记为 $A > 0$），若 $X^T A X$ 正定。

---

## 5.2 正定性的等价判定条件

> ⭐ **Theorem 5.4 / 小结**：设 $A$ 为 $n$ 阶实对称矩阵，则以下条件**等价**：

| 序号 | 条件 |
|------|------|
| (1) | $A$ 正定 |
| (2) | $A$ 的标准形为 $\text{diag}(b_1, \ldots, b_n)$，$b_i > 0$ |
| (3) | $A$ 的规范形为 $I_n$ |
| (4) | $A$ 的正惯性指数 $= n$ |
| (5) | $A$ 的 $n$ 个特征值**全为正实数** |
| (6) | 存在可逆矩阵 $P$，使得 $A = P^T P$ |
| (7) | $A$ 的 $n$ 个**顺序主子式全大于零** |
| (8) | $A$ 的所有**主子式全大于零** |

> 📌 **Lemma 5.2**：$b_1 x_1^2 + \cdots + b_n x_n^2$ 正定 $\Longleftrightarrow$ $b_i > 0$（$1 \leq i \leq n$）

> 📌 **Lemma 5.3**：非退化的线性替换**保持**二次型的正定性。

---

## 5.3 正定矩阵的性质

| 性质 | 说明 |
|------|------|
| $A$ 正定 $\Rightarrow$ $|A| > 0$ | 行列式大于零 |
| $A$ 正定 $\Rightarrow$ $a_{ii} > 0$（$\forall i$） | 对角元全正 |
| $A$ 正定 $\Rightarrow$ $\exists a_{kk} > |a_{ij}|$（$\forall i \neq j$） | 存在对角元绝对值最大 |
| $A$ 正定 $\Rightarrow$ $A^k$ 正定（$\forall k$） | 幂保持正定 |
| $A$ 正定 $\Rightarrow$ $A^{-1}$ 正定 | 逆保持正定 |
| $A$ 正定 $\Rightarrow$ $A^*$ 正定 | 伴随保持正定 |
| $A, B$ 正定 $\Rightarrow$ $A + B$ 正定 | 加法保持正定 |
| $A, B$ 正定，$AB = BA$ $\Longleftrightarrow$ $AB$ 正定 | 乘法保持正定的充要条件 |
| $M = \begin{pmatrix} A & 0 \\ 0 & B \end{pmatrix}$ 正定 $\Longleftrightarrow$ $A, B$ 都正定 | 分块对角矩阵 |

> ⭐ **Corollary 5.5**：$A$ 正定 $\Longleftrightarrow$ 存在可逆矩阵 $P$，使得 $A = P^T P$。

---

## 5.4 顺序主子式判定法

> ⭐ **Theorem 5.7**：$Q(x_1, \ldots, x_n) = X^T A X$ 正定 $\Longleftrightarrow$ $A$ 的**所有顺序主子式皆大于零**。

**顺序主子式**：
$$\Delta_k = \begin{vmatrix} a_{11} & a_{12} & \cdots & a_{1k} \\ a_{21} & a_{22} & \cdots & a_{2k} \\ \vdots & \vdots & \ddots & \vdots \\ a_{k1} & a_{k2} & \cdots & a_{kk} \end{vmatrix} > 0, \quad k = 1, 2, \ldots, n$$

**例22（习题八 16(1)）**：$Q = 2x_1^2 + x_2^2 + x_3^2 + 2x_1x_2 + t x_1x_3$，求 $t$ 使正定。

**解**：$A = \begin{pmatrix} 2 & 1 & \frac{t}{2} \\ 1 & 1 & 0 \\ \frac{t}{2} & 0 & 1 \end{pmatrix}$

顺序主子式：
- $\Delta_1 = 2 > 0$ ✓
- $\Delta_2 = \begin{vmatrix} 2 & 1 \\ 1 & 1 \end{vmatrix} = 1 > 0$ ✓
- $\Delta_3 = |A| = 2 \cdot 1 \cdot 1 + 1 \cdot 0 \cdot \frac{t}{2} + \frac{t}{2} \cdot 1 \cdot 0 - \frac{t}{2} \cdot 1 \cdot \frac{t}{2} - 0 - 2 \cdot 0 \cdot 0 = 2 - \frac{t^2}{4} > 0$

解得 $|t| < 2\sqrt{2}$，即 $\boxed{-2\sqrt{2} < t < 2\sqrt{2}}$。

---

## 5.5 其他定型

| 类型 | 定义 | 标准形系数 | 顺序主子式 |
|------|------|-----------|-----------|
| **负定** | $\forall X \neq 0, Q(X) < 0$ | 全负 | 奇数阶 $< 0$, 偶数阶 $> 0$ |
| **半正定** | $\forall X, Q(X) \geq 0$ | 全 $\geq 0$ | 全 $\geq 0$ |
| **半负定** | $\forall X, Q(X) \leq 0$ | 全 $\leq 0$ | — |
| **不定** | 既有正又有负 | 有正有负 | — |

> 📌 注：$Q$ 正定 $\Longleftrightarrow$ $-Q$ 负定；半正定 $\Longleftrightarrow$ 半负定。

---

## 5.6 半正定矩阵的等价条件

> ⭐ **Theorem 5.10**：对 $n$ 阶实对称矩阵 $A$，以下等价：
> 1. $A$ 半正定
> 2. 存在 $P \in M_n(\mathbb{R})$，使 $A = P^T P$（注：$P$ 不必可逆）
> 3. $A$ 的各阶**主子式**全 $\geq 0$（⚠️ 注意：不是顺序主子式！）
> 4. $\forall t > 0$，$A + tI_n$ 正定
> 5. $A$ 的所有特征值皆 $\geq 0$
> 6. $A$ 的规范形为 $\begin{pmatrix} I_r & 0 \\ 0 & 0 \end{pmatrix}$

> ⚠️ **易错提醒**：半正定时用"顺序主子式 $\geq 0$"判断是**不充分**的！必须要求**所有主子式** $\geq 0$。

---

## 5.7 典型例题

**例19（习题八 28）**：$A$ 正定 $\Longrightarrow \det(A) \leq \left(\frac{1}{n}\text{tr}(A)\right)^n$

> 📌 证明思路：$A$ 的特征值全正，由均值不等式 $\sqrt[n]{\lambda_1 \cdots \lambda_n} \leq \frac{\lambda_1 + \cdots + \lambda_n}{n}$ 即得。

**例29（习题八 19）**：$Q = X^T A X$ 负定 $\Longleftrightarrow$ $A$ 的奇数阶顺序主子式全小于零，偶数阶顺序主子式全大于零。

**例25**：$A$ 正定，$B$ 半正定 $\Longrightarrow A + B$ 正定。

**例32**：$A$ 正定，$B$ 实对称 $\Longrightarrow$ 存在可逆矩阵 $P$，使 $P^T A P$ 和 $P^T B P$ 同时对角化。

---

# 附录1：矩阵的奇异值分解 (SVD) {#svd}

> ⭐ **Theorem 6.1（奇异值分解）**：
> 设 $A \in \mathbb{R}^{m \times n}$，则存在 $m$ 阶正交阵 $U$ 和 $n$ 阶正交阵 $V$，使得：
> $$A = U \begin{pmatrix} \sigma_1 & & & & \\ & \ddots & & O & \\ & & \sigma_r & & \\ & O & & O & \end{pmatrix} V^T$$
> 其中 $\sigma_1 \geq \sigma_2 \geq \cdots \geq \sigma_r > 0$ 称为 $A$ 的**奇异值**。

> 📌 **几何意义**：SVD 将一个线性变换分解为"旋转 → 伸缩 → 旋转"，$\sigma_i$ 是将单位球映为椭球时各半轴的长度。这是线性代数中**最重要的矩阵分解之一**，在数据降维（PCA）、图像压缩等领域有广泛应用。

---

# 更多例题 {#more}

**例34**：
(1) $Q = x_1^2 + 2x_2^2 + 3x_3^2 - 4x_1x_2 - 4x_2x_3$ 的规范形为 $\boxed{y_1^2 + y_2^2 + y_3^2}$（正定）。

(2) $Q = (x_1 - x_2)^2 + (x_2 - x_3)^2 + (x_3 - x_1)^2$ 的规范形为 $\boxed{y_1^2 + y_2^2}$（秩为 2，不是正定）。

(3) 是非题：该二次型是正定的。**答：否**（因为 $Q(1, 1, 1) = 0$，不满足正定性）。

**例35**：
(1) $A = \begin{pmatrix} 1 & 1 & 2 \\ 1 & 2 & 3 \\ 2 & 3 & t \end{pmatrix}$，秩为 2，则 $t = \boxed{6}$，正惯性指数为 $\boxed{2}$。

(2) $f(X) = \text{tr}(X^2)$ 关于 $x_{ij}$ 是二次型，正惯性指数为 $\boxed{6}$。

**例36(1)**：$A = \begin{pmatrix} t & 1 & -2 \\ 1 & 2 & 0 \\ -2 & 0 & 3 \end{pmatrix}$ 正定 $\Longrightarrow \boxed{t > \frac{11}{6}}$。

**例39**：$A$ 正定 $\Longrightarrow$ (1) 存在实对称矩阵 $P$ 使 $A = P^2$；(2) 存在**唯一的**正定矩阵 $P$ 使 $A = P^2$。（正定矩阵的平方根）

---

# 小测速查卡片 {#cheatsheet}

## 核心概念

| 名词 | 英文 | 定义 |
|------|------|------|
| 线性函数 | linear function | $\varphi: V \to F$，满足线性性 |
| 双线性函数 | bilinear function | $\varphi: V \times V \to F$，对两个变元分别线性 |
| 对称双线性函数 | symmetric bilinear function | $\varphi(\alpha, \beta) = \varphi(\beta, \alpha)$ |
| 二次型函数 | quadratic form function | $Q_\varphi(\alpha) = \varphi(\alpha, \alpha)$ |
| 实二次型 | real quadratic form | $Q(X) = X^T A X$，$A$ 为实对称矩阵 |
| 标准形 | standard form | $b_1 y_1^2 + \cdots + b_n y_n^2$ |
| 规范形 | canonical form | $y_1^2 + \cdots + y_p^2 - y_{p+1}^2 - \cdots - y_r^2$ |
| 正惯性指数 | positive index of inertia | 规范形中正平方项的个数 $p$ |
| 负惯性指数 | negative index of inertia | 规范形中负平方项的个数 $r-p$ |
| 符号差 | signature | $2p - r$ |
| 正定二次型 | positive definite quadratic form | $\forall X \neq 0, Q(X) > 0$ |
| 负定二次型 | negative definite quadratic form | $\forall X \neq 0, Q(X) < 0$ |
| 半正定 | positive semi-definite | $\forall X, Q(X) \geq 0$ |
| 不定 | indefinite | 既有正又有负 |
| 顺序主子式 | leading principal minor | $\Delta_k = \det(A[1:k, 1:k])$ |
| 相合 | congruence | $A, B$ 相合 $\Longleftrightarrow \exists$ 可逆 $P$，$B = P^T A P$ |
| 线性替换 | linear substitution | $X = PY$ |
| 非退化线性替换 | non-degenerate linear substitution | $X = PY$，$P$ 可逆 |
| 等距变换 | isometry | $|\mathcal{A}(X) - \mathcal{A}(Y)| = |X - Y|$ |
| 奇异值 | singular value | SVD 中的 $\sigma_i$，$A^T A$ 特征值的正平方根 |

## 核心定理速记

| 定理 | 内容 |
|------|------|
| **对称双线性函数基本定理** | 对称矩阵必相合于对角阵 |
| **惯性定理** | 规范形唯一（正/负惯性指数是不变量） |
| **正定性等价条件** | 特征值全正 ⇔ 顺序主子式全正 ⇔ $A = P^T P$ ⇔ 正惯性指数 = n |
| **度量分类定理** | 二次曲面由 rank 关系分三种情形 |
| **SVD 定理** | $A = U \Sigma V^T$，$\Sigma$ 对角元为奇异值 |

## 三种化标准形方法

| 方法 | 适用场景 |
|------|----------|
| 配平方法 | 手工计算，最直观 |
| 初等变换法 | 行列成对操作，最系统 |
| 正交变换法 | 需要保持几何/度量性质 |

## 正定性判定流程

```
给定实对称矩阵 A
  ↓
方法A：计算特征值 → 全 > 0 → 正定
方法B：计算顺序主子式 → 全 > 0 → 正定
方法C：配方法化标准形 → 系数全 > 0 → 正定
方法D：化规范形 → = I_n → 正定
方法E：找可逆 P 使 A = P^T P → 正定
```

## 易错提醒

1. ⚠️ 半正定不能用"顺序主子式 $\geq 0$"判断（反例：$\begin{pmatrix} 0 & 0 \\ 0 & -1 \end{pmatrix}$），必须用**所有主子式** $\geq 0$
2. ⚠️ $A$ 正定 $\Rightarrow$ $|A| > 0$，但反过来**不成立**！
3. ⚠️ 二次型的秩 = 矩阵 $A$ 的秩 = 标准形中非零系数的个数
4. ⚠️ 规范形中系数只能是 $1, -1, 0$
5. ⚠️ 相合变换不保持特征值，只保持惯性指数！

---

> *本章内容对应 PPT 第 1-74 页 | 乐珏老师 · 线性代数 B1 · 2026.03*
