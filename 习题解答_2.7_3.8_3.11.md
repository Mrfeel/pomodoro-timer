# 数据结构 · 习题解答（2.7 / 3.8 / 3.11）

> 杨思辰 · 未来技术学院 · 物理学专业 · PB25992094
>
> 来源：《数据结构及算法习题集》第2章线性表、第3章栈与队列

---

# 2.7 顺序表删除值相同的多余元素

## 题干

已知线性表 L 采用顺序存储结构存放。对两种不同情况分别写出算法，删除 L 中值相同的多余元素，使得 L 中没有重复元素：

1. L 中数据元素无序排列；
2. L 中数据元素非递减有序排列。

---

## 解答

### (1) 无序排列

**算法思想**：从左到右逐元素处理，维护一个"已去重区" `a[0..k-1]`。对每个元素，检查它是否在已去重区内出现过；若未出现，则加入去重区。

**时间复杂度**：$O(n^2)$（对每个元素扫描去重区做查找）；**辅助空间**：$O(1)$（原地进行，无额外数组）。

**类 C 代码**：

```c
int delDupUnordered(int a[], int n){
    int k = 0;                         // 去重区末尾（最终也是去重后长度）
    for(int i = 0; i < n; i++){
        int dup = 0;
        for(int j = 0; j < k; j++)     // 在已去重区中查找 a[i]
            if(a[i] == a[j]){ dup = 1; break; }
        if(!dup) a[k++] = a[i];        // 未出现过 → 保留
    }
    return k;                          // 返回去重后的表长
}
```

**示例**：`{5,3,5,1,3,2,1,5,4}` → `{5,3,1,2,4}`，长度 5。

---

### (2) 非递减有序排列

**算法思想**：有序时重复元素必定相邻，可用**双指针**一趟完成。`i` 扫描全表，`j` 指向"干净区"的末尾。当 `a[i] ≠ a[j]` 说明遇到了一个新的值，将其存入 `a[++j]`。

**时间复杂度**：$O(n)$（一趟扫描）；**辅助空间**：$O(1)$。

**类 C 代码**：

```c
int delDupOrdered(int a[], int n){
    if(n <= 1) return n;
    int j = 0;                         // j 指向去重区的最后一个元素
    for(int i = 1; i < n; i++)
        if(a[i] != a[j])               // 与去重区末元素不同 → 新值
            a[++j] = a[i];             // 扩展去重区
    return j + 1;
}
```

**示例**：`{1,1,2,2,2,3,5,5,5,7,8,8}` → `{1,2,3,5,7,8}`，长度 6。

---

### 两种情形对比

| | 无序 | 有序 |
|---|---|---|
| 为何复杂度不同 | 重复元素分散，需要查找确认是否出现过 | 重复必定相邻，无需查找 |
| 时间复杂度 | $O(n^2)$ | $O(n)$ |
| 技巧 | 维护"已去重区"辅助查找 | 双指针就地扫描 |

> 💡 关联题 2.8 要求将本题存储结构改为 **单链表** 再实现一次——那时无序情形仍需两层循环（每遇重复走一遍已去重链），有序情形同样是双指针一趟完成。

---

# 3.8 中缀表达式转逆波兰式

## 题干

设表达式由单字母变量、双目运算符和圆括号组成。如：`(a*(b+c)-d)/e`。试写一个算法，将一个书写正确的表达式转换为逆波兰式。

---

## 解答

### 算法思想

使用一个**算符栈**。从左到右扫描中缀表达式：

- **操作数**（单字母变量）：直接输出到结果串
- **左括号 `(`**：直接入栈
- **右括号 `)`**：反复弹栈输出，直到遇到左括号并弹掉它
- **运算符**：弹出栈中**优先级 ≥ 当前算符**的所有算符并输出，再将当前算符入栈
- **扫描完毕**：弹出栈中所有剩余算符

### 优先级定义

| 算符 | 优先级 |
|:--:|:--:|
| `+` `-` | 1（低） |
| `*` `/` | 2（高） |

### 类 C 代码

```c
int prec(char op){                    // 返回优先级数值
    if(op=='+'||op=='-') return 1;
    if(op=='*'||op=='/') return 2;
    return 0;                         // '(' 的优先级为 0（最低）
}

void infixToPostfix(const char *infix, char *postfix){
    char st[100]; int top = -1, k = 0;

    for(int i = 0; infix[i]; i++){
        char c = infix[i];
        if(isalpha(c))                        // 操作数 → 直接输出
            postfix[k++] = c;
        else if(c == '(')                     // 左括号入栈
            st[++top] = c;
        else if(c == ')'){
            while(top>=0 && st[top]!='(')     // 弹至左括号
                postfix[k++] = st[top--];
            if(top >= 0) top--;               // 弹掉 '('
        } else {                              // 运算符
            while(top>=0 && st[top]!='(' &&
                  prec(st[top]) >= prec(c))
                postfix[k++] = st[top--];
            st[++top] = c;
        }
    }
    while(top >= 0) postfix[k++] = st[top--];  // 弹出剩余算符
    postfix[k] = '\0';
}
```

### 跟踪示例

以 `(a*(b+c)-d)/e` 为例（用表记录关键步骤）：

| 读入 | 输出 | 算符栈 | 动作 |
|:--:|:--|:--|:--|
| `(` | | `(` | `(` 入栈 |
| `a` | `a` | `(` | 操作数直接输出 |
| `*` | `a` | `( *` | `(` < `*`，入栈 |
| `(` | `a` | `( * (` | `*` < `(`，入栈 |
| `b` | `ab` | `( * (` | 输出 |
| `+` | `ab` | `( * ( +` | `(` < `+`，入栈 |
| `c` | `abc` | `( * ( +` | 输出 |
| `)` | `abc+` | `( *` | `+` > `)` → 弹；`(` = `)` → 脱 |
| `-` | `abc+*` | `( -` | `*` > `-` → 弹；`(` < `-` → 入栈 |
| `d` | `abc+*d` | `( -` | 输出 |
| `)` | `abc+*d-` | | `-` > `)` → 弹；`(` = `)` → 脱 |
| `/` | `abc+*d-` | `/` | 入栈 |
| `e` | `abc+*d-e` | `/` | 输出 |
| 结束 | `abc+*d-e/` | | 弹出剩余 |

**结果**：`(a*(b+c)-d)/e` → **`abc+*d-e/`**。

### 复杂度分析

- **时间** $O(n)$：每个字符进出栈最多一次。
- **空间** $O(n)$：最坏情况下全部为算符/左括号。

> 💡 关联题 3.9 要求对逆波兰式**求值**——只需一个操作数栈，遇操作数入栈、遇算符弹两个运算后压回。

---

# 3.11 循环队列（rear + length 方案）

## 题干

假设将循环队列定义为：以 `rear` 和 `length` 分别指示**队尾元素**和**队列长度**。试给出此循环队列的队满条件，并写出相应的入队和出队算法（在出队算法中要传递回队头元素的值）。

---

## 解答

### 约定说明

与教材标准方案的关键区别：**不存 `front` 变量**。`front` 由 `rear` 和 `length` 推导得出：

$$front = (rear - length + 1 + MAXSIZE) \bmod MAXSIZE$$

- `rear` 指向**队尾元素**（不是"队尾下一空位"）
- `length` 为当前位置中的元素个数

### 队满条件

$$\boxed{length = MAXSIZE}$$

### 队空条件

$$\boxed{length = 0}$$

### 初始化

`rear` 初值取 `MAXSIZE - 1`，使第一次入队时 `(rear+1)%MAXSIZE = 0`，从下标 0 开始存放。

```c
void initCQ(CQueue *q){
    q->rear = MAXQ - 1;
    q->length = 0;
}
```

### 入队算法

```c
int enCQ(CQueue *q, int x){
    if(q->length == MAXSIZE) return 0;             // 队满 → 失败
    q->rear = (q->rear + 1) % MAXSIZE;             // rear 后移
    q->data[q->rear] = x;                          // 存入
    q->length++;                                   // 长度 +1
    return 1;
}
```

### 出队算法

```c
int deCQ(CQueue *q, int *x){
    if(q->length == 0) return 0;                   // 队空 → 失败
    int front = (q->rear - q->length + 1 + MAXSIZE) % MAXSIZE;
    *x = q->data[front];                           // 取队头元素，通过参数传回
    q->length--;                                   // 长度 -1（无需移动 rear）
    return 1;
}
```

> ⚠️ 出队时**只 `length--`**，不改变 `rear`。`front` 是通过公式计算得到的，`length` 减小自然会改变 `front` 的值，相当于队头"向后走了一步"。

### 手算验证（MAXSIZE=6）

| 操作 | rear | length | front（公式） | 队列内容（队头→队尾） |
|:--|:--:|:--:|:--:|:--|
| 入 10 | 0 | 1 | 0 | `[10]` |
| 入 20 | 1 | 2 | 0 | `[10,20]` |
| 入 30 | 2 | 3 | 0 | `[10,20,30]` |
| 出队(10) | 2 | 2 | 1 | `[20,30]` |
| 出队(20) | 2 | 1 | 2 | `[30]` |
| 入 40 | 3 | 2 | 2 | `[30,40]` |
| 入到满 6 个 | 5 | 6 | 0 | `[10..15]` |
| 试图入队 | 5 | 6 | — | **队满，入队失败** |

### 与教材标准方案对比

| | 标准方案（front+rear） | 本题方案（rear+length） |
|---|---|---|
| 存储变量 | `front`, `rear` | `rear`, `length` |
| 判空 | `front == rear` | `length == 0` |
| 判满 | `(rear+1)%MAXQ == front` | `length == MAXSIZE` |
| 浪费一个单元 | **是**（始终留一个空位） | **否**（全部可用） |
| front 维护方式 | 出队时显式更新 | 通过公式间接计算 |
| 入队操作 | 两步 | 三步（多一步 `length++`） |
| 适用场景 | 存储空间足够时 | 存储空间紧张、希望用满容量 |

> 💡 核心洞察：标准方案省去了 `length` 计数器，但代价是浪费一个存储单元；本题多花了一个 `int` 存 `length`，换来了全部空间的利用率。这是典型的**空间换功能**取舍。

---

<div style="text-align: center; margin-top: 2em; color: #666;">

—— 习题解答完毕 ——

</div>
