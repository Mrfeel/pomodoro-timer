# 磁耦合多摆系统中 FPUT 回归的实现与模态熵的定义与测量

> 基于《磁耦合多摆系统的实验标定、数值建模与百球尺度涌现行为》(v2) 及仿真源码 `fput_recurrence_verify.py`
>
> 杨思辰 · 未来技术学院 · 物理学专业
>
> 🔥 核心要点：**弱耦合+弱非线性+低阻尼+长时程 → 模态能量周期性回归 → 用 Shannon 模态熵定量刻画**

> ⚠️ **重要说明**：本文所述的 FPUT 回归**并非台架实测结果**，而是在标定后的耦合 Duffing 模型上做的 **N=20 数值仿真预测**。台架实验只做到四球、时长约 21 s，远不足以观测周期约 544 s 的回归。因此下文的"实现"与"测量"均指**数值实验流程**。

---

## 目录

1. [§1 FPUT 回归的物理背景](#sec1)
2. [§2 回归是如何实现的（仿真流程）](#sec2)
3. [§3 模态熵的定义](#sec3)
4. [§4 模态熵的测量流程](#sec4)
5. [§5 回归的判定与结果](#sec5)
6. [§6 速查卡片](#cheatsheet)

---

# §1 FPUT 回归的物理背景 {#sec1}

Fermi–Pasta–Ulam–Tsingou (FPUT) 问题问的是：一条**非线性耦合振子链**，若把能量注入到少数几个简正模，它是否会像统计力学预言的那样走向**能量均分（热化）**？

FPUT 的经典发现是：能量**并不**热化，而是先扩散到多个模态、随后又几乎**完整地回流**到初始的少数模态，呈现**周期性回归 (recurrence)**。

> 💡 直观图像：把能量想成分装在 $N$ 个抽屉（简正模）里。一开始只有一两个抽屉是满的；随时间能量往其他抽屉渗，看似要"摊平"；但过一阵子能量又几乎全部回到最初那一两个抽屉。这一"聚集 → 扩散 → 再聚集"的循环就是 FPUT 回归。

要在本系统中观测到回归，需同时满足三个条件：

| 条件 | 物理原因 | 本仿真取值 |
|------|---------|-----------|
| **弱耦合** $\kappa \ll 1$ | 模态能量可分离、交换缓慢 | $\kappa = 0.004\text{–}0.012$ |
| **弱非线性**（小 Duffing） | 保证简正模近似成立、模态清晰可辨 | $\varepsilon^{\text{hard}}=10,\ \varepsilon^{\text{soft}}=-5$（近线性） |
| **极低阻尼** $\gamma \ll 1$ | 能量在回归周期内几乎不耗散 | $\gamma = 0.001\text{–}0.002$ |
| **长时程** $T$ | 给回归足够时间发育 | $T = 5000\text{–}8000\ \text{s}$ |

---

# §2 回归是如何实现的（仿真流程） {#sec2}

## 2.1 系统与参数

在 $N=20$ 的耦合 Duffing 摆链上求解运动方程

$$
\ddot{\theta}_i + 2\Gamma_i\dot{\theta}_i + \Omega_{0,i}^2\theta_i
+ \mathcal{E}_i(A_i)\theta_i^3
+ \Omega_0^2\!\!\sum_{j\in\text{nn}(i)}\!\!\kappa_{ij}(\theta_i-\theta_j) = 0
$$

为逼近"近线性"的 FPUT 条件，仿真刻意把非线性压到极弱：各球固有频率取 $f_{0,i}=1.0+\mathcal{N}(0,\ 0.001)$ Hz（$\sigma=1$ mHz 的微小无序），Duffing 系数取近似对称的极小值。

| 参数 | 符号 | 取值 | 说明 |
|------|------|------|------|
| 球数 | $N$ | 20 | 链长 |
| 耦合强度 | $\kappa$ | 0.004 / 0.008 / 0.012 | 弱耦合扫描 |
| 阻尼 | $\gamma$ | 0.002 / 0.001 | 极低阻尼 |
| 仿真时长 | $T$ | 5000 / 8000 s | 长时程 |
| Duffing（硬/软） | $\varepsilon^{\text{hard}},\varepsilon^{\text{soft}}$ | $+10,\ -5$ | 近线性 |
| 过渡临界角/锐度 | $A_c,\ k$ | 0.05 rad, 200 | sigmoid 过渡 |

## 2.2 能量注入（初始条件）

**只激发 A 球**：$\theta_A(0)=0.05$ rad，其余各球给一个 $0.001$ rad 量级的微扰、初速度为零。

> ⭐ 关键：单端激发在简正模基下**天然把能量集中到少数几个模态**，从而制造出"初始低熵态"——这正是回归得以显现的前提。若一开始能量就均分，就无所谓"回流"了。

## 2.3 数值求解

- 求解器：`scipy.integrate.solve_ivp`，**BDF** 隐式多步法（适合刚性 ODE）
- 容差：$r_{\text{tol}}=10^{-8}$，$a_{\text{tol}}=10^{-9}$，最大步长 $0.005$ s
- 输出：完整的 $\theta_i(t)$ 时间序列，供后续模态分解使用

---

# §3 模态熵的定义 {#sec3}

模态熵 (modal entropy) 是把**能量在各简正模上的分布**当作一个概率分布，再取其 **Shannon 熵**。它是"能量集中/分散"程度的单一标量指标。

## 3.1 第一步：线性化刚度矩阵与简正模

忽略非线性项，构造三对角刚度矩阵 $\mathbf{K}$（对角为固有刚度加耦合，次对角为耦合项）：

$$
K_{ii}=\omega_{0,i}^2 + (\text{左右邻居数})\cdot\kappa\,\omega_0^2,\qquad
K_{i,i\pm1}=-\kappa\,\omega_0^2
$$

对 $\mathbf{K}$ 做特征分解：

$$
\mathbf{K}\,\mathbf{v}_n=\lambda_n\,\mathbf{v}_n,\qquad n=1,\dots,N
$$

得到 $N$ 个简正模：本征值 $\lambda_n$（即模态角频率平方，$\omega_n=\sqrt{\lambda_n}$）与正交本征矢量 $\mathbf{v}_n$。（代码中模态基固定用 $\kappa=0.006$ 构造。）

## 3.2 第二步：把运动投影到模态坐标

将各球位移 $\boldsymbol{\theta}(t)=(\theta_1,\dots,\theta_N)$ 投影到简正模上，得到**模态坐标**

$$
Q_n(t)=\sum_{i=1}^{N}\theta_i(t)\,v_{i,n}
= \boldsymbol{\theta}(t)\cdot\mathbf{v}_n
$$

模态速度由数值微分（`np.gradient`）得到：$\dot{Q}_n(t)=\dfrac{\mathrm{d}Q_n}{\mathrm{d}t}$。

## 3.3 第三步：模态能量与能量占比

第 $n$ 个模态的能量为动能加势能：

$$
E_n(t)=\tfrac{1}{2}\dot{Q}_n^2(t)+\tfrac{1}{2}\lambda_n\,Q_n^2(t)
$$

归一化为**能量占比**（一个随时间变化的概率分布）：

$$
p_n(t)=\frac{E_n(t)}{\displaystyle\sum_{m=1}^{N}E_m(t)},
\qquad \sum_{n=1}^{N}p_n(t)=1
$$

## 3.4 第四步：Shannon 模态熵

$$
\boxed{\,S(t)=-\sum_{n=1}^{N} p_n(t)\,\ln p_n(t)\,}
$$

（数值上只对 $p_n>10^{-15}$ 的项求和，避免 $\ln 0$。）

> ⭐ **物理意义（熵的两个极限）**
>
> - 能量**全部集中在单一模态**：$p=(1,0,\dots,0)$ → $S=0$（最低熵，最有序）
> - 能量**在 $N$ 个模态上均分（热化）**：$p_n=1/N$ → $S=\ln N$（最高熵）
>
> 对 $N=20$，$S_{\max}=\ln 20\approx 3.0$。论文实测 $S$ 在约 **2.30（能量较集中）↔ 2.82（能量较分散）** 之间往复振荡——**永不达到 $\ln 20$，即系统始终不热化**，这正是 FPUT 回归的定量证据。

---

# §4 模态熵的测量流程 {#sec4}

从仿真轨迹得到熵的**时间序列**，采用**分窗平均**以抑制快速振荡、突出慢变的回归包络：

**第1步：** 把全时程等分为 $W=200$ 个窗口。

**第2步：** 在每个窗口内对能量占比取时间平均并重新归一化：

$$
\bar{p}_n^{(w)}=\frac{\big\langle p_n(t)\big\rangle_{t\in\text{窗口}w}}
{\sum_m \big\langle p_m(t)\big\rangle_{t\in\text{窗口}w}}
$$

**第3步：** 对每个窗口计算一个熵值，得到离散时间序列 $\{S_w\}_{w=1}^{200}$：

$$
S_w=-\sum_{n=1}^{N}\bar{p}_n^{(w)}\ln\bar{p}_n^{(w)},
\qquad t_w=\text{窗口中点时刻}
$$

**第4步：** 同时记录**能量最高的前 5 个模态**的占比，用于检验模态间的能量交换（见 §5）。

---

# §5 回归的判定与结果 {#sec5}

## 5.1 回归的自动判定

回归 = 能量"少数模态 → 多数模态 → 再回少数模态"，在熵曲线上表现为**周期性振荡（低→高→低）**。判定算法：

1. **平滑**熵曲线（滑动平均，窗宽约为序列长度的 1/50）。
2. **找极小值**：对 $-S_{\text{smooth}}$ 用 `find_peaks`（最小间隔 15、显著度 $>0.02\,S_{\max}$）。每个极小值 = 一次能量重新聚集 = 一个**回归点**。
3. **回归次数** $= (\text{极小值个数}-1)$；**回归周期** $T_{\text{FPUT}}=\overline{\Delta t_{\text{极小值}}}$。
4. **判据（同时满足）**：

$$
\text{回归次数}\ge 2
\quad\text{且}\quad
(S_{\max}-S_{\min}) > 0.1\,S_{\max}
$$

5. **模态能量交换的佐证**：检查前两个主模态的能量占比是否**显著反相关**（相关系数 $<-0.3$）——一个模态涨、另一个落，正是能量在模态间往复交换的直接标志。

## 5.2 结果

在 4 组配置下均观测到明确的 FPUT 回归（**确认率 4/4**）：

| 配置 | 回归次数 | 平均周期 (s) | 熵振荡幅度 (占 $S_{\max}$) |
|------|:---:|:---:|:---:|
| $\kappa=0.004,\ \gamma=0.002$ | 8 | $544\pm61$ | 17.4% |
| $\kappa=0.008,\ \gamma=0.002$ | 5 | $700\pm193$ | 13.4% |
| $\kappa=0.012,\ \gamma=0.002$ | 6 | $571\pm210$ | 13.9% |
| $\kappa=0.004,\ \gamma=0.001$ | 7 | $971\pm283$ | 16.6% |

> ⭐ **两条物理结论**
>
> 1. **回归周期与耦合强度反相关**：$\kappa$ 越小，模态间能量交换越慢，回归周期越长（如 $\gamma$ 减半后周期从 544 s 拉长到 971 s）。
> 2. 熵振荡幅度始终 $<20\%$ 且熵**从不触及 $\ln 20$**，说明系统在整个时程内保持非遍历（不热化），与 FPUT 原始发现一致。

---

# 📋 速查卡片 {#cheatsheet}

**FPUT 回归实现（数值，N=20）：** 弱耦合 $\kappa\!\sim\!0.004$ + 弱非线性 + 极低阻尼 $\gamma\!\sim\!0.002$ + 长时程 $T\!\sim\!5000$ s，单端激发 A 球 → BDF 求解 → 得 $\theta_i(t)$。

**模态熵五步：**

$$
\mathbf{K}\mathbf{v}_n=\lambda_n\mathbf{v}_n
\ \Rightarrow\
Q_n=\boldsymbol{\theta}\cdot\mathbf{v}_n
\ \Rightarrow\
E_n=\tfrac12\dot{Q}_n^2+\tfrac12\lambda_n Q_n^2
\ \Rightarrow\
p_n=\frac{E_n}{\sum_m E_m}
\ \Rightarrow\
S=-\sum_n p_n\ln p_n
$$

**熵的读法：** $S=0$ 能量集中单模；$S=\ln N$ 完全热化。$N=20$ 时 $S_{\max}=\ln 20\approx3.0$，实测在 $2.30\!\leftrightarrow\!2.82$ 往复。

**回归判据：** 熵极小值 $\ge$ 2 个（即 $\ge$ 2 次回归）**且** 熵振幅 $>0.1\,S_{\max}$；前两主模态能量反相关（$<-0.3$）为佐证。

**关键结果：** $T_{\text{FPUT}}\approx544$ s，4/4 配置确认，周期随 $\kappa,\gamma$ 减小而增大。

---

<div style="text-align: center;">

—— 完 ——

</div>
