# 数据结构 · 队列（Queue）知识点与考点总结

> 《数据结构及其算法》复习资料 | 杨思辰 · 未来技术学院 · 物理学专业
>
> 🔥 考试重点：**循环队列判空判满、队列长度公式、链队列出队边界（只剩一个结点）、循环队列/链队列对比、杨辉三角、停车场模型**

## 目录

1. [§1 队列的基本概念](#sec1)
2. [§2 队列的存储结构](#sec2)
3. [§3 队列的基本操作](#sec3)
4. [§4 队列的典型应用](#sec4)
5. [§5 队列与栈的对比](#sec5)
6. [§6 易错点与速查卡片](#sec6)

---

# §1 队列的基本概念 {#sec1}

## 1.1 定义

**队列（Queue）** 是一种只允许**在一端插入（入队）、在另一端删除（出队）**的线性表。允许删除的一端叫**队头（front）**，允许插入的一端叫**队尾（rear）**。

> ⭐ 核心特性：**先进先出（FIFO，First In First Out）**。先来的先服务。

> 💡 直观类比：排队买饭——排前面的人先走（出队），新来的人排最后（入队）。谁先排上谁就先买到，公平排队。

## 1.2 基本术语

| 术语 | 含义 |
|------|------|
| 入队（enqueue） | 在队尾插入一个元素 |
| 出队（dequeue） | 删除队头元素并返回 |
| 队头（front） | 允许删除的一端 |
| 队尾（rear） | 允许插入的一端 |
| 空队列 | 不含任何元素的队列 |
| 假溢出 | 队尾已到数组末尾，但队头前有空位（顺序队列特有） |
| 循环队列 | 把数组"首尾相连"成环，解决假溢出 |

> ⭐ 队列和栈一样是**操作受限的线性表**：逻辑结构仍是线性表，只是插入/删除限制在两端。

---

# §2 队列的存储结构 {#sec2}

队列有两种实现：**循环队列**（顺序）和**链队列**（链表）。

## 2.1 循环队列

> 🔥 **考试第一大考点**：循环队列的判空判满。两种约定必须分清！

一维数组 `data[MAXSIZE]`，`front` 指向队头元素，`rear` 指向**队尾元素的下一个空位**。队列"假溢出"时用取模运算实现循环：

$$next\_pos = (pos+1) \bmod MAXSIZE$$

**约定一（常见，浪费一个单元）**：

| 状态 | 条件 |
|------|------|
| 空 | `front == rear` |
| 满 | `(rear + 1) % MAXSIZE == front` |
| 队长 | `(rear - front + MAXSIZE) % MAXSIZE` |
| 实际可用容量 | `MAXSIZE - 1`（有一个空位不用，用来区分空/满） |

**约定二（另设标志位/计数器，不浪费单元）**：
- 设一个 `count` 变量计数当前元素数，判空 `count==0`，判满 `count==MAXSIZE`。

> ⚠️ 考试看清题目给的是哪种约定！两种都考过。**不浪费单元法**不常用但偶尔出现。

**循环队列核心操作的代码：**

```c
// 入队：先判满
int enQ(SqQueue *q, int x){
    if((q->rear + 1) % MAXQ == q->front) return 0;  // 队满
    q->data[q->rear] = x;
    q->rear = (q->rear + 1) % MAXQ;
    return 1;
}
// 出队：先判空
int deQ(SqQueue *q, int *x){
    if(q->front == q->rear) return 0;               // 队空
    *x = q->data[q->front];
    q->front = (q->front + 1) % MAXQ;
    return 1;
}
```

## 2.2 链队列

以单链表实现，**带头结点**，`front` 指向头结点，`rear` 指向队尾结点（真正的最后一个元素，不是头结点）。

- 入队 = **尾插法**：`rear->next = p; rear = p;`
- 出队 = 删除头结点后的第一个结点（相当于**删首元素**）

> 🔥 **链队列出队的边界**（必考！）：若出队的是最后一个结点，需 `rear = front`，否则 rear 变成野指针。

## 2.3 循环队列 vs 链队列 对比

| 对比项 | 循环队列 | 链队列 |
|--------|---------|--------|
| 存储 | 数组，定容 | 链表，动态 |
| 队满 | 可能满（需判满） | 一般不会满 |
| 判空 | `front == rear` | `front == rear`（均指向头结点）|
| 队长计算 | `(rear-front+MAXQ)%MAXQ` | 需遍历计数 |
| 空间 | 无指针开销，但可能浪费或不足 | 有指针开销（每个结点多一个 next）|
| 适用场景 | 最大容量已知 | 容量不确定或变动大 |

---

# §3 队列的基本操作 {#sec3}

标准操作（**时间复杂度均为 $O(1)$**）：

- `InitQueue(Q)`：初始化空队列
- `QueueEmpty(Q)`：判空
- `EnQueue(Q, x)`：入队
- `DeQueue(Q, &x)`：出队
- `GetHead(Q, &x)`：读队头（不删除）

---

# §4 队列的典型应用 {#sec4}

> 🔥 队列应用的核心都在"**按到达顺序（FIFO）排队处理**"上。

## 4.1 停车场 / 缓冲区排队

**核心模式**：`到达 → 入队；服务完 → 出队`。实验二停车场里，场外便道就是队列——车排队等候，先来的先进。

**代码示例**（链队列模拟候车便道）：

```c
// 车辆排队等候
enLQ(&q, 104); enLQ(&q, 105); enLQ(&q, 106);
// 依次进入停车场（FIFO）
while(!emptyLQ(&q)){ deLQ(&q, &x); printf("车辆 %d 进入\n", x); }
// 输出：104 → 105 → 106（严格按到达顺序）
```

## 4.2 BFS / 图的层序遍历

广度优先搜索（BFS）和树的层序遍历，核心是"**访问当前层 → 把下一层结点入队 → 一层层展开**"。队列保证了"先遇到先处理"的横向扩张次序。

> 💡 直观理解：BFS 就像"在朋友圈里按一度人脉→二度人脉→…逐圈扩散"。

## 4.3 杨辉三角（队列递推经典题）

> 🔥 教科书必讲，也是考试常考"队列的巧妙应用"的典型。

**算法思想**：队列存上一行系数，**前后各补一个 0**，两两求和即得新一行。

**计算方法**（生成第1~4行，全程跟踪队列）：

| 轮次 | `prev` | 取 `curr` | 输出 `prev+curr` | 入队 `prev+curr` | 本轮后队列 |
|:--:|:--:|:--:|:--:|:--:|:--|
| 初始化 | — | — | 第1行：`1` | 入 `1` | `[1]` |
| i=2, j=1 | 0 | 1 | 1 | 入1 | `[0]→[0,1]` |
| i=2, j=2 | 1 | 0(哨兵) | 1 | 入1 | `[1,1]` |
| i=3, j=1 | 0 | 1 | 1 | 入1 | `[1,0,1]` |
| i=3, j=2 | 1 | 1 | **2** | 入2 | `[0,1,2]` |
| i=3, j=3 | 1 | 0 | 1 | 入1 | `[1,2,1]` |

队列初始 `[1]`（第1行）。每轮生成下一行：输出 1 2 1（第3行）、1 3 3 1（第4行）、1 4 6 4 1（第5行）……

**代码示例**（队列递推，无二项式公式，纯靠队列）：

```c
void yanghui(int n){
    SqQueue q; initQ(&q);
    printf("1\n"); enQ(&q, 1);   // 第 1 行
    for(int i=2; i<=n; i++){
        enQ(&q, 0);              // 队尾补 0（哨兵）
        int prev = 0;
        for(int j=1; j<=i; j++){
            int curr; deQ(&q, &curr);
            int val = prev + curr;
            printf("%d%c", val, j<i?' ':'\n');
            enQ(&q, val);        // 新系数存入
            prev = curr;
        }
    }
}
```

> 💡 要点：`prev` 模拟"左侧补 0"，队尾 `0` 模拟"右侧补 0"。队列总保持上一行系数，两两求和即得新行。

## 4.4 操作系统的作业调度

打印机任务队列、CPU 进程就绪队列——**先到先服务（FCFS）**是队列最基本的调度策略。

## 4.5 舞伴配对 / 轮转调度

队头两人出队配对，配对完（或等待超时）重新入队尾——利用队头出、队尾入的循环实现**轮转**。

> 💡 直观理解：就像"抢椅子游戏"，每轮淘汰一个后重新列队。循环队列天然支持这种"转盘式"操作。

---

# §5 队列与栈的对比 {#sec5}

| 对比项 | 栈（Stack） | 队列（Queue） |
|--------|-----------|-------------|
| 操作端 | 仅栈顶（一端） | 队头出、队尾入（两端）|
| 顺序原则 | **LIFO**（后进先出） | **FIFO**（先进先出）|
| 判空 | `top == -1` | `front == rear` |
| 判满 | `top == MAXSIZE - 1` | `(rear+1) % MAXSIZE == front` |
| 典型应用 | 括号匹配、表达式求值、递归 | 排队、BFS、缓冲区、杨辉三角 |
| 本质 | 逆序 | 保序 |

> ⭐ 核心区别记忆：栈是"**先来的压在底**"（后出），队列是"**先来的先走**"（先出）。

---

# §6 易错点与速查卡片 {#sec6}

## 6.1 常见易错点

| 易错点 | 正确做法 |
|--------|----------|
| 循环队列判空/判满 | 空：`front==rear`；满：`(rear+1)%MAXQ==front`（浪费一个单元约定）|
| 链出队只剩一个元素 | 出队后是空队列 → 必须执行 `rear = front`，否则 rear 野指针 |
| 队长公式忘加 `MAXSIZE` | `(rear - front + MAXSIZE) % MAXSIZE`——加 MAXSIZE 防止负数 |
| 顺序队列假溢出 | 用**循环队列**解决（取模运算）|
| 队列与栈的操作混 | 队列是"一端进另一端出"（保序），不是"同一端进出"（逆序）|

## 6.2 核心速查

- **特性**：先进先出（FIFO）
- **两种实现**：循环队列（取模，浪费一个单元）、链队列（带头结点，尾插头删）
- **判空条件**：`front == rear`（两种实现通用）
- **判满条件**（循环队列）：`(rear + 1) % MAXQ == front`
- **队长公式**（循环队列）：$(rear - front + MAXSIZE) \bmod MAXSIZE$
- **所有基本操作复杂度**：$O(1)$
- **链队列出队边界**：删光后 `rear = front`
- **三大经典应用**：杨辉三角（队列递推）、排队缓冲（FIFO）、BFS（层次展开）
- **实验二相关**：停车场场外便道用**链队列**

---

—— 栈队列双璧在手，线性结构无忧！祝复习顺利！ ——
