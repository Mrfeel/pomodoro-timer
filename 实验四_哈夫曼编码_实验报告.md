# 数据结构 实验报告

## 实验名称：实验四——哈夫曼编码与解码

**班级：** ________　**姓名：** 杨思辰　**学号：** PB25992094　**实验日期：** 2026-07-23

---

## 一、实验项目与实验要求

### 1. 实验项目

哈夫曼（Huffman）编码与解码。给定一组字符及其出现频率，构建哈夫曼树，为每个字符分配最优前缀编码，并实现编码（文本→二进制串）和解码（二进制串→文本）功能。

### 2. 核心功能要求

- **输入字符与频率**：支持从键盘输入任意数量的字符及其出现频率；
- **构建哈夫曼树**：使用贪心算法，每次选取频率最小的两棵子树合并，直到只剩一棵树；
- **生成哈夫曼编码**：从根出发，左分支为 0、右分支为 1，生成每个字符的变长编码；
- **编码**：输入文本，输出对应的二进制编码串；
- **解码**：输入二进制编码串，输出原始文本（编码必须是当前哈夫曼树生成的有效编码）；
- **显示树结构**：支持查看哈夫曼树的结点表（含父子关系）。

### 3. 准备测试数据与预期结果

**标准测试数据**：

字符集 {a, b, c, d, e}，频率依次为 {5, 7, 3, 8, 1}。

**预期哈夫曼树构建过程**：

```
合并 e(1)+c(3)=4 → 合并 a(5)+4=9 → 合并 b(7)+d(8)=15 → 合并 9+15=24
```

**预期编码**（不唯一，取决于合并时左右顺序）：

| 字符 | 频率 | 编码 |
|:--|:--|:--|
| a | 5 | 00 |
| b | 7 | 10 |
| c | 3 | 011 |
| d | 8 | 11 |
| e | 1 | 010 |

**WPL** = 5×2 + 7×2 + 3×3 + 8×2 + 1×3 = 10+14+9+16+3 = 52

等长编码每字符需 ⌈log₂5⌉ = 3 位，总长 = (5+7+3+8+1) × 3 = 24×3 = 72 位。
压缩率 = (72-52)/72 ≈ 27.8%

**额外测试**：编码文本 "bad" → 预期编码 "100011"，解码后还原为 "bad"。

---

## 二、实验原理与详细设计

### 1. 选择数据结构的原因

| 数据结构 | 用途 | 选择理由 |
|:--|:--|:--|
| **顺序存储的哈夫曼树**（HTNode 数组） | 存储哈夫曼树的所有结点 | 哈夫曼树结点数固定 = 2n-1（n=叶结点数）。用数组存储便于随机访问父结点和子结点，编码时从叶子上溯到根无需递归 |
| **编码表**（HCode 数组） | 存储每个字符的二进制编码 | 编码/解码时需要快速查找字符对应的编码，数组索引即可 O(1) 访问 |

哈夫曼树结点结构：

```c
typedef struct {
    char ch;               /* 叶结点对应的字符 */
    int  freq;             /* 频率/权值 */
    int  parent, left, right;  /* 父子关系，-1 表示无 */
} HTNode;
```

编码表结构：

```c
typedef struct {
    char ch;               /* 字符 */
    char code[MAX_CODE];   /* 二进制编码字符串 */
    int  len;              /* 编码长度 */
} HCode;
```

### 2. 核心函数说明

| 函数 | 功能 | 关键算法 |
|:--|:--|:--|
| `inputData()` | 输入 n 种字符及频率，初始化哈夫曼树前 n 个叶结点 | 初始化 parent/left/right 均为 -1 |
| `buildTree()` | 构建哈夫曼树，共 n-1 次合并 | 每次从未合并结点中选 freq 最小的两个，创建新父结点 |
| `generateCode()` | 从叶子上溯到根，反转得到编码 | 反转是因为上溯时先得到的是编码的最后一位 |
| `encodeText()` | 文本→二进制编码串 | 逐字符查编码表，拼接 |
| `decodeText()` | 二进制编码串→文本 | 从根出发，遇 0 走左、遇 1 走右，到叶子输出字符并回到根 |
| `showTree()` | 显示哈夫曼树结点表 | 方便调试 |

### 3. 核心算法详解

**构建哈夫曼树**（贪心算法）：

以测试数据 {a:5, b:7, c:3, d:8, e:1} 为例：

```
初始: [a:5] [b:7] [c:3] [d:8] [e:1]   共 5 个独立结点

第1次合并: 选 e(1) 和 c(3)
  → 新结点 HT[5]: freq=4, left=4(e), right=2(c)
  → {a:5, b:7, d:8, 新(4)}

第2次合并: 选 a(5) 和 新(4)
  → HT[6]: freq=9, left=0(a), right=5(新)
  → {b:7, d:8, 新(9)}

第3次合并: 选 b(7) 和 d(8)
  → HT[7]: freq=15, left=1(b), right=3(d)
  → {新(9), 新(15)}

第4次合并: 选 新(9) 和 新(15)
  → HT[8]: freq=24, left=6, right=7
  → {根(24)}
```

**编码生成**（上溯法）：

以字符 c 为例——c 是 HT[2]，频率 3：

```
HT[2].parent = 5  →  5 的左孩子=4(e), 右孩子=2(c)
                      c 是右孩子 → 编码最后一位 = '1'
HT[5].parent = 6  →  6 的左孩子=0(a), 右孩子=5
                      5 是右孩子 → 编码倒数第二位 = '1'
HT[6].parent = 8  →  8 的左孩子=6, 右孩子=7
                      6 是左孩子 → 编码倒数第三位 = '0'
HT[8].parent = -1 → 到达根，停止

上溯结果（反序）: 1 → 1 → 0
反转后编码: 0 1 1  → "011"
```

### 4. 程序执行逻辑与函数调用关系

```
main() 循环菜单
  ├─ [1] inputData()      → 读入 n 及 n 个字符频率 → 初始化 HT[0..n-1]
  ├─ [2] buildTree()      → n-1 次合并 → 填充 HT[n..2n-2], 树完成
  ├─ [3] generateCode()   → 每个叶子上溯到根 → 反转 → 填入 HC[]
  ├─ [4] encodeText()     → 输入文本 → 逐字符查 HC[] → 输出编码串
  ├─ [5] decodeText()     → 输入编码串 → 从根沿树下降 → 输出字符
  ├─ [6] showTree()       → 打印 HT[] 全部结点
  └─ [0] 退出
```

功能依赖关系：1 → 2 → 3 → (4 或 5)。必须先有数据，再建树，再生成编码表，才能进行编码或解码。

---

## 三、算法时空复杂度分析

### 1. 构建哈夫曼树

- 每次合并需从 O(n) 个未合并结点中选两个最小值 → O(n)
- 共 n-1 次合并 → 总时间 **O(n²)**
- 可优化为小根堆 → O(n log n)，但本次实验 n 通常较小（≤ 256），O(n²) 足够
- 空间：数组 HT[2n-1] → **O(n)**

### 2. 生成编码

- 每个叶结点上溯到根，路径长度 ≤ n → O(n)
- n 个叶结点 → 总时间 **O(n²)**
- 空间：编码表 HC[n] → **O(n)**

### 3. 编码文本

- 文本长度 L，每个字符查编码表 O(1) → 总时间 **O(L)**
- 空间：输出编码串 O(L·maxCodeLen)

### 4. 解码

- 编码串长度 B，每位沿树下降一步 O(1) → 总时间 **O(B)**
- 空间：输出文本 O(B)

### 5. 总结

| 操作 | 时间 | 空间 |
|:--|:--|:--|
| 构建树 | O(n²) | O(n) |
| 生成编码 | O(n²) | O(n) |
| 编码 | O(L) | O(L·log n) |
| 解码 | O(B) | O(B) |

核心操作（构建树+生成编码）为 O(n²)，对字符集大小 n≤256 完全可接受。

---

## 四、程序使用说明与测试运行结果

### 1. 使用说明

- **运行环境**：Windows 11，gcc 16.1.0（MSYS2 ucrt64），命令行交互
- **编译**：`gcc lab4_huffman.c -o lab4_huffman.exe`
- **运行**：`./lab4_huffman.exe`
- **操作流程**：按菜单顺序——先选 1 输入数据→选 2 构建树→选 3 生成编码→选 4 编码或选 5 解码

### 2. 测试结果

**测试 1：标准数据**

```
── 功能1：输入字符与频率 ──
请输入字符种类数: 5
第1个: a 5
第2个: b 7
第3个: c 3
第4个: d 8
第5个: e 1

── 功能2：构建哈夫曼树 ──
  合并 'e'(1) + 'c'(3) → 新结点 freq=4
  合并 'a'(5) + 新结点(4) → 新结点 freq=9
  合并 'b'(7) + 'd'(8) → 新结点 freq=15
  合并 新结点(9) + 新结点(15) → 新结点 freq=24

哈夫曼树构建完成！根结点为 HT[8]，总权值 = 24

── 功能3：生成哈夫曼编码 ──
  'a'(频5): 00
  'b'(频7): 10
  'c'(频3): 011
  'd'(频8): 11
  'e'(频1): 010

WPL = 52
等长编码: 3位/字符 → 总长 72 位
哈夫曼编码总长: 52 位
压缩率: 27.8%
```

**测试 2：编码**

```
── 功能4：编码 ──
请输入要编码的文本: bad

编码结果: 100011
原文 3 字符 → 编码后 7 位
```

验证：b→"10", a→"00", d→"11" → "100011" ✓

**测试 3：解码**

```
── 功能5：解码 ──
请输入二进制编码串: 100011

解码结果: bad
```

验证：解码后还原为 "bad" ✓

**测试 4：树结点表（功能6）**

```
── 哈夫曼树结点表 ──
  编号  字符  频率    parent left right
  [ 0]   a      5       6    -1    -1
  [ 1]   b      7       7    -1    -1
  [ 2]   c      3       5    -1    -1
  [ 3]   d      8       7    -1    -1
  [ 4]   e      1       5    -1    -1
  [ 5]   -      4       6     4     2
  [ 6]   -      9       8     0     5
  [ 7]   -     15       8     1     3
  [ 8]   -     24      -1     6     7
```

所有父子关系正确 ✓

---

## 五、测试分析与实验总结

### 1. 测试过程中可能遇到的问题及解决

| 问题 | 原因 | 解决方法 |
|:--|:--|:--|
| 编码生成顺序不对 | 从叶到根的上溯法得到的编码是反序的 | 用一个临时数组暂存，然后反转填入 HC[] |
| 解码时死循环 | 非叶子结点的 left/right 可能指向 -1（未初始化），导致 cur 跳到 -1 | 在建树时将所有结点的 left/right/parent 初始化为 -1 |
| scanf 读字符被跳过 | 前一次 scanf 的 `\n` 留在缓冲区，被 `%c` 读走 | 使用 `scanf(" %c", &ch)`（%c 前加空格跳过空白字符） |
| 编码串中有未定义字符 | 用户输入了不在字符集中的字母 | 增加 found 标记，未找到时输出 `[?X]` 提示 |
| 菜单死循环 | scanf 读到非法字符不消费 | 检测 scanf 返回值，失败时清空缓冲区 |

### 2. 实验收获

- 深入理解了**哈夫曼树的贪心构造算法**——每次选最小的两个合并，这是最优前缀编码的数学基础；
- 掌握了**哈夫曼编码的上溯生成法**——从叶子走向根，记录路径（0=左，1=右），再反转；无需递归遍历整个树；
- 理解了**前缀编码的性质**：哈夫曼编码中没有一个编码是另一个的前缀，这是它能被无歧义解码的关键条件；
- 体会到**压缩率的计算**：比较等长编码（⌈log₂n⌉ 位/字符）与哈夫曼编码（按频率加权平均码长）的总比特数；
- 学会了**解码的自动机模型**：从根出发，沿 0/1 下降，到叶子自动复位——这本质是一个确定性有限自动机（DFA）。

### 3. 可选扩展与优化思路

- **堆优化建树**：用最小堆维护未合并结点，构建时间从 O(n²) 降为 O(n log n)；
- **按位存储编码**：当前编码以字符串形式存储（每位一个 byte），实际可以压缩到真正的比特流中（用位操作），大幅减小输出体积；
- **文件 I/O**：支持从文件读入频率表、从文件读入待编码文本、将编码写入二进制文件；
- **动态哈夫曼编码**：适用于流式数据的自适应哈夫曼编码（adaptive Huffman coding），无需预先知道频率分布。

---

## 六、附录：完整 C 语言源代码

```c
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <windows.h>

#define MAX_CHAR 256
#define MAX_CODE 256

/* ── 哈夫曼树结点 ── */
typedef struct {
    char ch;
    int  freq;
    int  parent, left, right;
} HTNode;

/* ── 编码表 ── */
typedef struct {
    char ch;
    char code[MAX_CODE];
    int  len;
} HCode;

HTNode HT[2 * MAX_CHAR - 1];
HCode  HC[MAX_CHAR];
int    n, totalNodes, freqSum;

/* 功能1：输入字符与频率 */
void inputData() {
    printf("\n── 功能1：输入字符与频率 ──\n");
    printf("请输入字符种类数: ");
    scanf("%d", &n);
    getchar();
    totalNodes = 2 * n - 1;
    freqSum = 0;
    for (int i = 0; i < totalNodes; i++) {
        HT[i].ch = '\0'; HT[i].freq = 0;
        HT[i].parent = HT[i].left = HT[i].right = -1;
    }
    printf("请逐行输入字符和频率（格式: 字符 频率）:\n");
    for (int i = 0; i < n; i++) {
        printf("  第%d个: ", i + 1);
        scanf(" %c %d", &HT[i].ch, &HT[i].freq);
        freqSum += HT[i].freq;
        HT[i].parent = HT[i].left = HT[i].right = -1;
    }
    printf("\n已输入 %d 种字符，总频率 = %d\n", n, freqSum);
}

/* 功能2：构建哈夫曼树 */
void buildTree() {
    printf("\n── 功能2：构建哈夫曼树 ──\n");
    if (n == 0) { printf("请先输入数据！\n"); return; }
    for (int i = n; i < totalNodes; i++) {
        int min1 = -1, min2 = -1;
        int f1 = 0x7FFFFFFF, f2 = 0x7FFFFFFF;
        for (int j = 0; j < i; j++) {
            if (HT[j].parent != -1) continue;
            if (HT[j].freq < f1) {
                f2 = f1; min2 = min1;
                f1 = HT[j].freq; min1 = j;
            } else if (HT[j].freq < f2) {
                f2 = HT[j].freq; min2 = j;
            }
        }
        HT[i].freq = f1 + f2;
        HT[i].left  = min1;
        HT[i].right = min2;
        HT[i].parent = -1;
        HT[min1].parent = i;
        HT[min2].parent = i;
        printf("  合并 freq %d + %d → %d\n", f1, f2, f1 + f2);
    }
    printf("\n哈夫曼树构建完成！\n");
}

/* 功能3：生成哈夫曼编码 */
void generateCode() {
    printf("\n── 功能3：生成哈夫曼编码 ──\n");
    if (n == 0) { printf("请先输入数据！\n"); return; }
    char temp[MAX_CODE];
    int WPL = 0;
    for (int i = 0; i < n; i++) {
        HC[i].ch = HT[i].ch;
        int len = 0, cur = i;
        while (HT[cur].parent != -1) {
            int p = HT[cur].parent;
            temp[len++] = (HT[p].left == cur) ? '0' : '1';
            cur = p;
        }
        HC[i].len = len;
        for (int j = 0; j < len; j++)
            HC[i].code[j] = temp[len - 1 - j];
        HC[i].code[len] = '\0';
        WPL += len * HT[i].freq;
        printf("  '%c'(频%d): %s\n", HC[i].ch, HT[i].freq, HC[i].code);
    }
    printf("\nWPL = %d\n", WPL);
    int eqBits = (n <= 2) ? 1 : ((n <= 4) ? 2 : ((n <= 8) ? 3 : 4));
    printf("等长编码: %d位/字符 → 总长 %d 位\n", eqBits, freqSum * eqBits);
    printf("哈夫曼编码总长: %d 位\n", WPL);
    printf("压缩率: %.1f%%\n", 100.0 * (1.0 - (double)WPL / (freqSum * eqBits)));
}

/* 功能4：编码 */
void encodeText() {
    printf("\n── 功能4：编码 ──\n");
    if (n == 0) { printf("请先完成前3步！\n"); return; }
    getchar();
    char text[4096];
    printf("请输入要编码的文本: ");
    fgets(text, sizeof(text), stdin);
    text[strcspn(text, "\n")] = '\0';
    printf("\n编码结果:\n  ");
    int totalBits = 0;
    for (int i = 0; text[i]; i++) {
        int found = 0;
        for (int j = 0; j < n; j++) {
            if (HC[j].ch == text[i]) {
                printf("%s", HC[j].code);
                totalBits += HC[j].len;
                found = 1; break;
            }
        }
        if (!found) printf("[?%c]", text[i]);
    }
    printf("\n\n原文 %d 字符 → 编码后 %d 位\n", (int)strlen(text), totalBits);
}

/* 功能5：解码 */
void decodeText() {
    printf("\n── 功能5：解码 ──\n");
    if (n == 0) { printf("请先完成前3步！\n"); return; }
    getchar();
    char bits[8192];
    printf("请输入二进制编码串: ");
    fgets(bits, sizeof(bits), stdin);
    bits[strcspn(bits, "\n")] = '\0';
    printf("\n解码结果:\n  ");
    int root = totalNodes - 1, cur = root;
    for (int i = 0; bits[i]; i++) {
        cur = (bits[i] == '0') ? HT[cur].left : HT[cur].right;
        if (HT[cur].left == -1 && HT[cur].right == -1) {
            putchar(HT[cur].ch);
            cur = root;
        }
    }
    printf("\n");
}

/* 功能6：显示树结构 */
void showTree() {
    if (n == 0) { printf("请先输入数据！\n"); return; }
    printf("\n── 哈夫曼树结点表 ──\n");
    printf("  编号  字符  频率    parent left right\n");
    for (int i = 0; i < totalNodes; i++)
        printf("  [%2d]   %c    %4d    %3d   %3d   %3d\n",
               i, HT[i].ch ? HT[i].ch : '-', HT[i].freq,
               HT[i].parent, HT[i].left, HT[i].right);
}

int main() {
    SetConsoleOutputCP(CP_UTF8);
    printf("╔═══════════════════════════════════════╗\n");
    printf("║  实验四：哈夫曼编码 / 解码          ║\n");
    printf("╚═══════════════════════════════════════╝\n");
    int choice;
    while (1) {
        printf("\n┌──────────────────────────┐\n");
        printf("│  1. 输入字符与频率        │\n");
        printf("│  2. 构建哈夫曼树          │\n");
        printf("│  3. 生成哈夫曼编码        │\n");
        printf("│  4. 编码（文本→编码）    │\n");
        printf("│  5. 解码（编码→文本）    │\n");
        printf("│  6. 显示哈夫曼树结构      │\n");
        printf("│  0. 退出                  │\n");
        printf("└──────────────────────────┘\n");
        printf("请选择 (0-6): ");
        if (scanf("%d", &choice) != 1) { choice = -1; while(getchar()!='\n'); }
        switch (choice) {
            case 1: inputData();     break;
            case 2: buildTree();     break;
            case 3: generateCode();  break;
            case 4: encodeText();    break;
            case 5: decodeText();    break;
            case 6: showTree();      break;
            case 0: printf("再见！\n"); return 0;
            default: printf("无效选项。\n");
        }
    }
}
```

---

<div style="text-align: center; margin-top: 2em;">

—— 实验报告完 ——

</div>
