# 朗道《力学》中的 ∂ 与 δ 区别详解

---

## 一、三个符号的一览表

| 符号 | 名称 | 作用对象 | 数学含义 | 物理含义 |
|---|---|---|---|---|
| $d$ | 全微分 | 单变量函数 | $df = f'(x) dx$ | 沿曲线的微小变化 |
| $\partial$ | 偏微分 | 多元函数 | $\frac{\partial f}{\partial x_i}$ 只变一个变量 | 多个自变量的"部分"变化 |
| $\delta$ | 变分 | 泛函 | $\delta S[q] = S[q + \delta q] - S[q]$ | 整条路径被替换 |

---

## 二、$\partial$（偏微分）详解

### 出现场景

多元函数 $f(x_1, x_2, \dots, x_n)$，自变量有多个。

### 定义

$$\frac{\partial f}{\partial x_1} = \lim_{\Delta x_1 \to 0} \frac{f(x_1 + \Delta x_1, x_2, \dots, x_n) - f(x_1, x_2, \dots, x_n)}{\Delta x_1}$$

**只变 $x_1$，其他所有变量保持不变。**

### 在朗道中的使用

拉格朗日量 $L(q, \dot{q}, t)$ 是三个自变量的函数：

$$\frac{\partial L}{\partial q}, \quad \frac{\partial L}{\partial \dot{q}}, \quad \frac{\partial L}{\partial t}$$

这三个偏导数**互不干扰**——求 $\partial L/\partial q$ 时把 $\dot{q}$ 和 $t$ 当作常数。

### 形象理解

你站在一座山上，海拔 $h(x, y)$：
- $\partial h/\partial x$ = 只往东走，y 坐标不动，海拔怎么变
- $\partial h/\partial y$ = 只往北走，x 坐标不动，海拔怎么变

---

## 三、$\delta$（变分）详解——这是朗道力学的核心

### 出现场景

作用量 $S[q]$ 是泛函——输入一整条路径，输出一个数。

### 定义

有两条**不同的路径** $q(t)$ 和 $\tilde{q}(t)$，它们在每一点的差：

$$\delta q(t) = \tilde{q}(t) - q(t)$$

这在整个时间区间 $[t_1, t_2]$ 上的**每一点**都有定义。$\delta q$ 本身也是时间的函数。

### 与 $d$ 的关键区别

| | $dq$ | $\delta q$ |
|---|---|---|
| **是什么在变** | 自变量 $t$ 改变 | 整条函数曲线被替换 |
| **时间是否变化** | 是，$t \to t + dt$ | **否**，比较的是同一时刻两条路径的差 |
| **相当于** | 沿着同一条路多走一段 | 换一条路走 |

> 关键：$\delta$ 变分中，**时间是固定的**。$\delta q(t)$ 是比较同一时刻 $t$ 下，两条路径在该时刻的坐标值之差。

### 两个核心性质

**性质1：$\delta$ 与 $d/dt$ 可交换**

$$\delta \dot{q} = \frac{d}{dt} \delta q$$

证明：
$$\delta \dot{q} = \tilde{\dot{q}} - \dot{q} = \frac{d}{dt}\tilde{q} - \frac{d}{dt}q = \frac{d}{dt}(\tilde{q} - q) = \frac{d}{dt}\delta q$$

> 这个性质是推导欧拉-拉格朗日方程的关键一步。

**性质2：端点固定**

$$\delta q(t_1) = \delta q(t_2) = 0$$

> 所有备选路径有相同的起点和终点。变分只在中间"鼓起来"。

---

## 四、在朗道 §2 推导中的具体运用

### 作用量变分

$$S[q + \delta q] = \int_{t_1}^{t_2} L(q + \delta q, \dot{q} + \delta \dot{q}, t)\, dt$$

### 展开到一阶

$$\delta S = S[q + \delta q] - S[q] = \int_{t_1}^{t_2} \left( \frac{\partial L}{\partial q}\delta q + \frac{\partial L}{\partial \dot{q}}\delta \dot{q} \right) dt$$

> 注意：这里 $\partial L/\partial q$ 和 $\partial L/\partial \dot{q}$ 是**偏导数**（$L$ 是普通函数），而 $\delta q$ 和 $\delta \dot{q}$ 是**变分**。

### 分部积分

对第二项用 $\delta \dot{q} = \frac{d}{dt}\delta q$（性质1）：

$$\int_{t_1}^{t_2} \frac{\partial L}{\partial \dot{q}} \delta \dot{q}\, dt = \int_{t_1}^{t_2} \frac{\partial L}{\partial \dot{q}} \frac{d}{dt}\delta q\, dt$$

分部积分：

$$= \left[ \frac{\partial L}{\partial \dot{q}} \delta q \right]_{t_1}^{t_2} - \int_{t_1}^{t_2} \frac{d}{dt}\left( \frac{\partial L}{\partial \dot{q}} \right) \delta q\, dt$$

边界项为零（性质2：$\delta q(t_1) = \delta q(t_2) = 0$）。

### 得到欧拉-拉格朗日方程

$$\delta S = \int_{t_1}^{t_2} \left[ \frac{\partial L}{\partial q} - \frac{d}{dt}\frac{\partial L}{\partial \dot{q}} \right] \delta q(t)\, dt = 0$$

由于 $\delta q(t)$ 是**任意**函数：

$$\boxed{\frac{d}{dt}\frac{\partial L}{\partial \dot{q}} - \frac{\partial L}{\partial q} = 0}$$

> 这里 $\delta$ 的任务完成了——它帮我们把"泛函取极值"转化成了"微分方程"，然后退出舞台。方程里只剩下 $\partial$ 和 $d/dt$。

---

## 五、一张图总结三者的层级关系

$$\boxed{
\begin{aligned}
&\text{泛函}\ S[q] &&\xrightarrow{\text{取变分}}\ &&\delta S = 0 &&\rightarrow\ &&\text{运动方程（微分方程）}\\
\\
&\text{多元函数}\ L(q, \dot{q}, t) &&\xrightarrow{\text{取偏导}}\ &&\frac{\partial L}{\partial q}, \frac{\partial L}{\partial \dot{q}} &&\rightarrow\ &&\text{代入运动方程}\\
\\
&\text{路径}\ q(t) &&\xrightarrow{\text{时间求导}}\ &&\frac{dq}{dt}, \frac{d}{dt}\frac{\partial L}{\partial \dot{q}} &&\rightarrow\ &&\text{运动方程中的项}
\end{aligned}}$$

$$\boxed{\delta\ \text{找正确的方程}\quad \longrightarrow \quad \partial\ \text{和}\ d/dt\ \text{解这个方程}}$$

---

> 📅 创建日期：2026-08-04
> 📖 参考：朗道《力学》第五版 §2（第2-5页）
