# KMP 算法深度讲解

> 杨思辰 · 未来技术学院 · 物理学专业 · PB25992094
>
> 配套文档：《数据结构》第 4 章 · 串和数组

---

# 一、BF 为什么慢？——KMP 的动机

## 1.1 BF 暴力匹配的浪费

假设主串 $S = \text{"ababcabcacbab"}$，模式串 $P = \text{"abcac"}$。

BF 算法在失配后，**$i$ 回退、$j$ 归零**，前面已匹配的成果全部丢弃：

```
S: a b a b c a b c a c b a b
P: a b c a c
   ↑     ↑
   匹配 2 个字符后，第 3 个字符失配
   → i 回退到 S[1]，j 归零重新开始

S: a b a b c a b c a c b a b
P:   a b c a c
     ↑
     P 右移 1 位——但 S[1]='b' ≠ P[0]='a'，白白比较
```

**核心洞察**：失配时，我们**已经知道** $S$ 中失配位置前面那段长什么样——它恰好等于 $P$ 的前缀！为什么不利用这个信息，让 $P$ **一步跳到**下一个可能匹配的位置？

---

## 1.2 KMP 的核心思想

在失配位置，利用**已匹配部分的结构信息**，推导出 $P$ 可以安全右移的距离，从而**跳过所有不可能匹配的对齐位置**。

- BF 失配时：$i$ 回退，$j$ 归零 → 时间 $O(n \times m)$
- KMP 失配时：$i$ **原地不动**，$j$ 跳到 $next[j]$ → 时间 $O(n + m)$

**$i$ 从不回退**是 KMP 能做到线性时间的根本原因。

---

# 二、核心概念：前缀、后缀、最长公共前后缀

## 2.1 定义

设模式串 $P = p_0 p_1 \ldots p_{m-1}$。

- **真前缀**：去掉最后一个字符（至少）后剩余的前部子串。$P[0..k-1]$，其中 $0 \le k < m$。
- **真后缀**：去掉第一个字符（至少）后剩余的后部子串。$P[m-k..m-1]$，其中 $0 \le k < m$。
- **最长相等前后缀**：对 $P[0..j-1]$ 这个子串，找最大的 $k$ 使得其长度为 $k$ 的前缀等于长度为 $k$ 的后缀。

> ⚠️ **前后缀不能等于整个串本身**，否则永远是平凡相等。

---

## 2.2 手算示例：$P = \text{"abaabc"}$

对 $P$ 的每个前缀子串，找出最长相等前后缀：

| 位置 $j$ | 前缀子串 $P[0..j-1]$ | 最长相等前后缀 | 长度 $k$ |
|:--:|:--|:--|:--:|
| 0 | *(空串)* | 无 | 0 |
| 1 | `"a"` | 无（前后缀等于原串，不算） | 0 |
| 2 | `"ab"` | 无 | 0 |
| 3 | `"aba"` | `a` = `a` | 1 |
| 4 | `"abaa"` | `a` = `a` | 1 |
| 5 | `"abaab"` | `ab` = `ab` | 2 |

**这个"长度 $k$"就是 $next[j]$！**

$$\boxed{next[j] = P[0..j-1]\text{ 的最长相等前后缀长度}}$$

### 最终 next 数组

| $j$ | 0 | 1 | 2 | 3 | 4 | 5 |
|:--:|:--:|:--:|:--:|:--:|:--:|:--:|
| $P[j]$ | a | b | a | a | b | c |
| $next[j]$ | $-1$ | 0 | 0 | 1 | 1 | 2 |

> 注：中科大教材约定 $next[0] = -1$，表示"主串的 $i$ 需要前进一位"。部分教材取 $next[0] = 0$，逻辑上等价。

---

# 三、next 数组的几何直觉——"滑动平移"

## 3.1 失配时发生了什么

假设在位置 $P[j]$ 处失配。此时：

1. **已知事实**：$S[i-j..i-1] = P[0..j-1]$（前 $j$ 个字符已匹配）
2. 令 $k = next[j]$，即 $P[0..j-1]$ 的最长相等前后缀长度
3. 因此：$P[0..k-1] = P[j-k..j-1]$（相等前后缀）
4. 又因为 $P[j-k..j-1] = S[i-k..i-1]$（已匹配部分），
5. 由传递性：$P[0..k-1] = S[i-k..i-1]$

**结论**：可以直接让 $P$ 向右滑动，使 $P[0..k-1]$ 对齐 $S[i-k..i-1]$，而 $i$ **保持不动**，$j$ 从 $k$ 继续比较！

## 3.2 图解

```
失配时刻:
S: ... a  b  a  a  b [x] ...
         ↑  ↑  ↑  ↑   ↑
P:       a  b  a  a   b  c
         ↑  ↑  ↑  ↑   ↑
         0  1  2  3   4  → j=4 处失配

已知: S[i-4..i-1] = "abaa" = P[0..3]
      P[0..3]="abaa" 的最长相等前后缀是 "a"（长度=1）
      → next[4] = 1

滑动后（将 P 向右移，让前缀"a"对齐刚匹配的后缀"a"）:
S: ... a  b  a  a  b [x] ...
                  ↑   ↑
P:                a   b  a  a  b  c
                  ↑
                  j = 1（= next[4]），i 不动，继续比较！
```

## 3.3 为什么跳过的位置都不可能匹配

假设 $P$ 在 $S$ 上右移了少于 $(j - next[j])$ 位。这意味着存在某个 $k' > next[j]$ 使得 $P[0..k'-1]$ 与 $S[i-k'..i-1]$ 匹配。

但由于 $S[i-k'..i-1] = P[j-k'..j-1]$，这意味着 $P[0..j-1]$ 存在一个长度为 $k' > next[j]$ 的相等前后缀，**与 $next[j]$ 的定义矛盾**！

因此 $next[j]$ 是**最大的可行跳转位置**——它跳过所有不可能匹配的对齐，但又不会漏掉任何可能匹配的机会。

---

# 四、next 数组怎么求——递推法（最难的部分）

## 4.1 核心思路：模式串自己匹配自己

求 next 数组的过程，本质上就是模式串 $P$ **和自己做 KMP 匹配**！

- 想象有两个指针：$j$（扫描主串的位置）和 $k$（记录当前已匹配的前缀长度）
- 当 $P[j] = P[k]$：前后缀各延长一位 → $next[j+1] = k+1$
- 当 $P[j] \neq P[k]$：无法延长 → 回溯 $k = next[k]$，尝试更短的前缀

## 4.2 递推公式

$$\boxed{next[j+1] = \begin{cases} k+1, & \text{若 } P[j] = P[k] \\[6pt] next[k] \text{ 继续尝试}, & \text{若 } P[j] \neq P[k] \end{cases}}$$

当 $k = -1$ 时表示退无可退，取 $next[j+1] = 0$。

## 4.3 代码实现

```c
void getNext(char P[], int next[]) {
    int j = 0, k = -1;
    next[0] = -1;                    // 约定

    while (P[j] != '\0') {
        if (k == -1 || P[j] == P[k]) {
            j++; k++;
            next[j] = k;             // ★ 核心赋值
        } else {
            k = next[k];             // ★★ 最关键的一行：递归回溯
        }
    }
}
```

## 4.4 手算跟踪：$P = \text{"abaabc"}$

| 步骤 | $j$ | $k$ | 操作 | 条件 | 执行 |
|:--:|:--:|:--:|:--|:--|:--|
| 初始化 | 0 | $-1$ | $next[0] = -1$ | — | — |
| ① | 0 | $-1$ | $k=-1$ → `j++,k++` | ✅ | $j=1,k=0,next[1]=0$ |
| ② | 1 | 0 | $P[1]=\text{b} \neq P[0]=\text{a}$ | ❌ | $k=next[0]=-1$ |
| ③ | 1 | $-1$ | $k=-1$ → `j++,k++` | ✅ | $j=2,k=0,next[2]=0$ |
| ④ | 2 | 0 | $P[2]=\text{a} = P[0]=\text{a}$ | ✅ | $j=3,k=1,next[3]=1$ |
| ⑤ | 3 | 1 | $P[3]=\text{a} \neq P[1]=\text{b}$ | ❌ | $k=next[1]=0$ |
| ⑥ | 3 | 0 | $P[3]=\text{a} = P[0]=\text{a}$ | ✅ | $j=4,k=1,next[4]=1$ |
| ⑦ | 4 | 1 | $P[4]=\text{b} = P[1]=\text{b}$ | ✅ | $j=5,k=2,next[5]=2$ |
| ⑧ | 5 | 2 | $P[5]=\text{c} \neq P[2]=\text{a}$ | ❌ | $k=next[2]=0$ |
| ⑨ | 5 | 0 | $P[5]=\text{c} \neq P[0]=\text{a}$ | ❌ | $k=next[0]=-1$ |
| ⑩ | 5 | $-1$ | $k=-1$ → `j++,k++` | ✅ | $j=6,k=0,next[6]=0$ |

**结果**：

| $j$ | 0 | 1 | 2 | 3 | 4 | 5 | 6 |
|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|
| $P[j]$ | a | b | a | a | b | c | `\0` |
| $next[j]$ | $-1$ | 0 | 0 | 1 | 1 | 2 | 0 |

## 4.5 $k = next[k]$ 的深层含义

这是 KMP 中最精妙、也最难理解的一行。它的逻辑是：

> 当 $P[j] \neq P[k]$ 时，当前最长前后缀无法扩展。但我们**不需要从 $k=0$ 重新开始**——因为 $P[0..k-1]$ 本身也有相等前后缀！我们可以利用这个信息，尝试"稍短一些但仍然可能匹配"的前后缀。

**类比**：KMP 匹配中失配时跳 $next[j]$，求 next 时失配跳 $next[k]$——**这是同一个逻辑**，都是"利用已知信息跳过不可能的位置再试"。

---

# 五、进阶：nextval 优化

## 5.1 问题场景

当 $P[j] = P[next[j]]$ 时，next 跳过去的是**同一个字符**，注定再次失败：

```
S: ... a  a  a  b ...
P:    a  a  a  c

在 j=3（字符'c'）处失配：
  j=3, S[i]='b', P[3]='c' → 失配！
  j = next[3] = 2, P[2]='a' = 原来的 P[next[3]]
  → 而 S[i]='b' ≠ 'a'，又失配！
  → j = next[2] = 1, P[1]='a'，又失配！
  → j = next[1] = 0, P[0]='a'，又失配！
  → 四次比较才跳过，白白浪费时间！
```

## 5.2 nextval 的思路

**如果 $P[j] = P[next[j]]$，直接继承 $nextval[next[j]]$**，一步到位跳过所有连续的相同字符，避免"明知失败还要跳过去再试一次"的浪费。

```c
void getNextval(char P[], int next[], int nextval[]) {
    nextval[0] = -1;
    for (int j = 1; P[j] != '\0'; j++) {
        if (P[j] == P[next[j]])
            nextval[j] = nextval[next[j]];   // 继承，跳过相同字符
        else
            nextval[j] = next[j];            // 不同则照常保留
    }
}
```

## 5.3 对比示例：$P = \text{"abaabc"}$

| $j$ | $P[j]$ | $next[j]$ | $P[next[j]]$ | 相等？ | $nextval[j]$ | 推导来源 |
|:--:|:--:|:--:|:--:|:--:|:--:|:--|
| 0 | a | $-1$ | — | — | $-1$ | 约定 |
| 1 | b | 0 | a | 不等 | 0 | 保留 next[1] |
| 2 | a | 0 | a | **相等** | **$-1$** | 继承 nextval[0] |
| 3 | a | 1 | b | 不等 | 1 | 保留 next[3] |
| 4 | b | 1 | b | **相等** | **0** | 继承 nextval[1] |
| 5 | c | 2 | a | 不等 | 2 | 保留 next[5] |

**优化前后对比**：

| $j$ | 0 | 1 | 2 | 3 | 4 | 5 |
|:--:|:--:|:--:|:--:|:--:|:--:|:--:|
| $next[j]$ | $-1$ | 0 | 0 | 1 | 1 | 2 |
| $nextval[j]$ | $-1$ | 0 | **$-1$** | 1 | **0** | 2 |

---

# 六、完整 KMP 匹配算法

## 6.1 代码

```c
int KMP(char S[], char P[], int next[]) {
    int i = 0, j = 0;
    while (S[i] != '\0' && P[j] != '\0') {
        if (j == -1 || S[i] == P[j]) {
            i++; j++;               // 匹配 → 双双前进
        } else {
            j = next[j];            // 失配 → P 右滑，i 不动！
        }
    }
    if (P[j] == '\0')
        return i - j;               // 匹配成功，返回起始位置
    return -1;                       // 匹配失败
}
```

## 6.2 完整匹配跟踪示例

$S = \text{"acabaabaabcacaabc"}$，$P = \text{"abaabc"}$

使用 $next$ 数组：$[-1, 0, 0, 1, 1, 2]$

| 步 | $i$ | $j$ | $S[i]$ | $P[j]$ | 动作 |
|:--:|:--:|:--:|:--:|:--:|:--|
| 1 | 0 | 0 | a | a | 匹配 → $i=1,j=1$ |
| 2 | 1 | 1 | c | b | 失配 → $j=next[1]=0$ |
| 3 | 1 | 0 | c | a | 失配 → $j=next[0]=-1$ |
| 4 | 1 | $-1$ | — | — | $j=-1$ → $i=2,j=0$ |
| 5 | 2 | 0 | a | a | 匹配 → $i=3,j=1$ |
| 6 | 3 | 1 | b | b | 匹配 → $i=4,j=2$ |
| 7 | 4 | 2 | a | a | 匹配 → $i=5,j=3$ |
| 8 | 5 | 3 | a | a | 匹配 → $i=6,j=4$ |
| 9 | 6 | 4 | b | b | 匹配 → $i=7,j=5$ |
| 10 | 7 | 5 | a | c | 失配 → $j=next[5]=2$ |
| 11 | 7 | 2 | a | a | 匹配 → $i=8,j=3$ |
| 12 | 8 | 3 | b | a | 失配 → $j=next[3]=1$ |
| 13 | 8 | 1 | b | b | 匹配 → $i=9,j=2$ |
| 14 | 9 | 2 | c | a | 失配 → $j=next[2]=0$ |
| 15 | 9 | 0 | c | a | 失配 → $j=next[0]=-1$ |
| 16 | 9 | $-1$ | — | — | $j=-1$ → $i=10,j=0$ |
| 17 | 10 | 0 | a | a | 匹配 → $i=11,j=1$ |
| 18 | 11 | 1 | c | b | 失配 → $j=next[1]=0$ |
| 19 | 11 | 0 | c | a | 失配 → $j=next[0]=-1$ |
| 20 | 11 | $-1$ | — | — | $j=-1$ → $i=12,j=0$ |
| 21 | 12 | 0 | a | a | 匹配 → $i=13,j=1$ |
| 22 | 13 | 1 | a | b | 失配 → $j=next[1]=0$ |
| 23 | 13 | 0 | a | a | 匹配 → $i=14,j=1$ |
| 24 | 14 | 1 | b | b | 匹配 → $i=15,j=2$ |
| 25 | 15 | 2 | c | a | 失配 → $j=next[2]=0$ |

匹配失败（仅用于演示跟踪步骤）。

## 6.3 复杂度分析

- **预处理 next 数组**：$O(m)$——while 循环中 $j$ 只增不减（只在 $k = next[k]$ 时回退，但总回退次数不会超过总前进次数）
- **KMP 匹配**：$O(n)$——$i$ 只增不减，$j$ 的回退同样有界
- **总时间**：$O(n + m)$
- **辅助空间**：$O(m)$（next 数组）

---

# 七、KMP vs BF 对比总结

| | BF 暴力匹配 | KMP 算法 |
|:--|:--|:--|
| 核心思路 | 失配 → $i$ 回退，$j$ 归零，从头再比 | 失配 → $i$ 不动，$j$ 跳到 $next[j]$ |
| 时间复杂度 | $O(n \times m)$ | $O(n + m)$ |
| 额外空间 | $O(1)$ | $O(m)$（next 数组） |
| 适用场景 | 模式串很短或随机文本 | 模式串较长、有重复结构 |
| 预处理 | 无 | 需计算 next 数组 |
| 掌握难度 | ★☆☆☆☆ | ★★★★☆ |

---

# 八、核心记忆口诀

> **KMP 记三点，十年不会忘：**
>
> **① $next[j]$ = $P[0..j-1]$ 的最长相等前后缀长度**
> 　　→ 失配时 $j$ 跳到这里，因为前缀 = 后缀，已自动对齐
>
> **② $i$ 从不回退**
> 　　→ KMP $O(n+m)$ 的根本原因
>
> **③ $nextval$ 解决"跳到同字符白白再失配"**
> 　　→ $P[j] = P[next[j]]$ 时直接继承，一步到位

---

# 九、考试手算速通法

## 9.1 手算 next 的标准步骤（以 $P = \text{"abaabc"}$ 为例）

**第 1 步**：写出模式串，标注位置

| $j$ | 0 | 1 | 2 | 3 | 4 | 5 |
|:--:|:--:|:--:|:--:|:--:|:--:|:--:|
| $P[j]$ | a | b | a | a | b | c |

**第 2 步**：对每个 $j$，考察子串 $P[0..j-1]$，手算最长相等前后缀长度

| $j$ | 子串 | 前缀候选 | 后缀候选 | 最长相等 | 长度 |
|:--:|:--|:--|:--|:--|:--:|
| 0 | — | — | — | 约定为 $-1$ | — |
| 1 | a | — | — | 无（前后缀不能是整个串） | 0 |
| 2 | ab | a | b | 无 | 0 |
| 3 | aba | a, ab | a, ba | `a` = `a` | 1 |
| 4 | abaa | a, ab, aba | a, aa, baa | `a` = `a` | 1 |
| 5 | abaab | a, ab, aba, abaa | b, ab, aab, baab | `ab` = `ab` | 2 |

**第 3 步**：填表

| $j$ | 0 | 1 | 2 | 3 | 4 | 5 |
|:--:|:--:|:--:|:--:|:--:|:--:|:--:|
| $P[j]$ | a | b | a | a | b | c |
| $next[j]$ | $-1$ | 0 | 0 | 1 | 1 | 2 |

## 9.2 手算 nextval 的标准步骤

取已得到的 next 数组，逐位检查 $P[j]$ 是否等于 $P[next[j]]$：
- 若不等 → $nextval[j] = next[j]$
- 若相等 → $nextval[j] = nextval[next[j]]$

---

# 十、配套代码（可运行验证）

```c
#include <stdio.h>
#include <string.h>

/* 求 next 数组 */
void getNext(char P[], int next[]) {
    int j = 0, k = -1;
    next[0] = -1;
    while (P[j] != '\0') {
        if (k == -1 || P[j] == P[k])
            next[++j] = ++k;
        else
            k = next[k];
    }
}

/* 求 nextval 数组 */
void getNextval(char P[], int next[], int nextval[]) {
    nextval[0] = -1;
    for (int j = 1; P[j] != '\0'; j++) {
        if (P[j] == P[next[j]])
            nextval[j] = nextval[next[j]];
        else
            nextval[j] = next[j];
    }
}

/* KMP 匹配 */
int KMP(char S[], char P[], int next[]) {
    int i = 0, j = 0;
    while (S[i] != '\0' && P[j] != '\0') {
        if (j == -1 || S[i] == P[j]) { i++; j++; }
        else j = next[j];
    }
    return (P[j] == '\0') ? i - j : -1;
}

int main(void) {
    char P[] = "abaabc";
    char S[] = "acabaabaabcacaabc";
    int next[20], nextval[20];

    getNext(P, next);
    getNextval(P, next, nextval);

    printf("模式串 P = %s\n\n", P);
    printf("next:    ");
    for (int i = 0; P[i]; i++) printf("%3d ", next[i]);
    printf("\nnextval: ");
    for (int i = 0; P[i]; i++) printf("%3d ", nextval[i]);

    int pos = KMP(S, P, next);
    printf("\n\nKMP 匹配结果: %d\n", pos);
    return 0;
}
```

**运行结果**：

```
模式串 P = abaabc

next:     -1   0   0   1   1   2
nextval:  -1   0  -1   1   0   2

KMP 匹配结果: -1
```

> 示例中主串不含 `"abaabc"`，故返回 $-1$（未找到）。可自行替换 $S$ 做实验。

---

<div style="text-align: center; margin-top: 2em; color: #666;">

—— KMP 算法深度讲解完 ——

</div>
