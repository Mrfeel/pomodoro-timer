# 数据结构 实验报告

## 实验名称：实验五——管道铺设（最小生成树 Prim 算法）

**班级：** ________　**姓名：** 杨思辰　**学号：** PB25992094　**实验日期：** 2026-07-25

---

## 一、实验项目与实验要求

### 1. 实验项目

某开发区有 $n$ 个小区，需要铺设供水管道使所有小区连通。已知任意两个小区之间铺设管道的费用（部分路段因地形原因无法直连，用无穷大表示），要求设计算法求出使总费用最小的管道铺设方案。

该问题的数学模型是**图的最小生成树（Minimum Spanning Tree，MST）**：给定一个无向带权连通图 $G = (V, E)$，每条边 $e$ 有一个权值 $w(e)$（代表管道铺设费用），求一个包含全部 $n$ 个顶点的无环连通子图（即一棵生成树），使得树中所有边的权值之和最小。

生成树必须满足三个条件：（1）包含全部 $n$ 个顶点；（2）恰好有 $n-1$ 条边（极小连通）；（3）无回路。

### 2. 核心功能要求

- 采用**邻接表**存储无向带权图，每条无向边插入两次（$u \rightarrow v$ 和 $v \rightarrow u$）；
- 实现 **Prim 算法**（加点法），维护三个辅助数组：`lowcost[]`、`closest[]`、`visited[]`；
- **逐步输出构造过程**：每一步输出所选边、费用、新加入的顶点；
- 统计并输出**最小总费用**；
- 程序结束时正确**释放邻接表中所有动态分配的边结点内存**。

### 3. 准备测试数据与预期结果

测试数据（6 个顶点，10 条边，顶点编号 0~5）：

```
6 10
0 1 6
0 2 1
0 3 5
1 2 5
1 4 3
2 3 5
2 4 6
2 5 4
3 5 2
4 5 6
```

**边权分布**：

| 边 | 权值 | 边 | 权值 | 边 | 权值 | 边 | 权值 |
|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|
| 0-1 | 6 | 0-2 | 1 | 0-3 | 5 | 1-2 | 5 |
| 1-4 | 3 | 2-3 | 5 | 2-4 | 6 | 2-5 | 4 |
| 3-5 | 2 | 4-5 | 6 | | | | |

以顶点 0 为起点，Prim 算法的**预期 MST 边集**：

```
V0 — V2（费用 1）
V2 — V5（费用 4）
V5 — V3（费用 2）
V2 — V1（费用 5）
V1 — V4（费用 3）
```

**预期最小总费用 = 1 + 4 + 2 + 5 + 3 = 15**

---

## 二、实验原理与详细设计

### 1. 选择数据结构的原因

本实验采用**邻接表**存储无向带权图，理由如下：

**(1) Prim 算法的核心操作决定了数据结构的选择**

Prim 算法以**顶点为中心**工作：每加入一个新顶点到 MST 后，需要遍历该顶点的所有邻接边来更新 `lowcost` 数组。邻接表使得「遍历某个顶点的所有邻边」这一操作的时间为 $O(\deg(v))$（$\deg(v)$ 为该顶点的度）。若使用邻接矩阵，同样操作需要 $O(n)$ 扫描一整行。

**(2) 空间效率**

邻接表空间复杂度为 $O(n + e)$（每条边存两次），优于邻接矩阵的 $O(n^2)$。对于稀疏图（$e \ll n^2$），邻接表的空间优势显著。

**(3) 为什么不选 Kruskal 常用的边集数组**

Kruskal 算法需要将全部边排序，用边集数组更自然。但 Prim 以顶点为中心逐步扩展，邻接表是更匹配的数据结构。本实验选用 Prim + 邻接表的组合，体现了「数据结构由算法核心操作决定」的设计原则。

### 2. 结构体 / 类型定义

```c
/* ── 边表结点：代表一条边（的终点和权值）── */
typedef struct EdgeNode {
    int adjvex;               /* 邻接顶点编号 */
    int weight;               /* 边权（管道铺设费用） */
    struct EdgeNode *next;    /* 指向下一条邻接边 */
} EdgeNode;

/* ── 顶点表：每个顶点维护一条边链表的头指针 ── */
typedef struct {
    EdgeNode *firstedge;      /* 第一条邻接边的指针 */
} VNode;

/* ── 邻接表图 ── */
typedef struct {
    VNode adjlist[MAXV];      /* 顶点数组（每个元素是一个链表头） */
    int n, e;                 /* 顶点数、边数 */
} ALGraph;
```

**邻接表示意图**（以测试数据顶点 0 为例，头插法）：

```
adjlist[0].firstedge
       │
       ▼
  ┌─────────┬──────┬──────┐    ┌─────────┬──────┬──────┐    ┌─────────┬──────┬──────┐
  │ adjvex=3│weight│ next │───→│ adjvex=2│weight│ next │───→│ adjvex=1│weight│ next │→ NULL
  │         │  =5  │      │    │         │  =1  │      │    │         │  =6  │      │
  └─────────┴──────┴──────┘    └─────────┴──────┴──────┘    └─────────┴──────┴──────┘
```

注意：头插法使边以插入的逆序出现，不影响算法正确性。每条无向边存两次（$u \rightarrow v$ 和 $v \rightarrow u$）。

### 3. Prim 算法的三个辅助数组

Prim 算法的核心是维护三个数组，共同描述「当前 MST 状态」与「未选顶点到 MST 的关系」：

| 数组 | 含义 | 初始值 | 更新时机 |
|:--|:--|:--|:--|
| `lowcost[i]` | 顶点 $i$ 到当前 MST 的**最小边权**（候选边）。若 `INF` 表示 $i$ 与 MST 无直接边相连 | `INF`，将起点邻居设为实际权值 | 新顶点 $k$ 入树后，遍历其所有邻边 $t$，若 $w(k,t) < lowcost[t]$ 则更新 |
| `closest[i]` | MST 中离顶点 $i$ **最近的顶点**编号 | 起点编号 | 与 `lowcost` 同步更新——`lowcost[t]` 被更新时，`closest[t] = k` |
| `visited[i]` | 标记 $i$ 是否已在 MST 中 | 全 0，起点设为 1 | 每轮选出 $k$ 后 `visited[k] = 1` |

**三个数组的协作逻辑**（每轮循环）：

1. **选边**：扫描所有 `visited[j]==0` 的顶点 $j$，找 `lowcost[j]` 最小的那个 $k$；
2. **入树**：$k$ 加入 MST（`visited[k]=1`），选中边 `(closest[k], k)`，费用 += `lowcost[k]`；
3. **修正**：遍历 $k$ 的所有邻边，对每个未选邻居 $t$：若 `weight(k,t) < lowcost[t]`，说明找到了更便宜的「接入 MST 的方式」→ 更新 `lowcost[t]` 和 `closest[t]`。

**与 Dijkstra 算法的关键区别**：

| | Prim | Dijkstra |
|:--|:--|:--|
| 更新的公式 | `lowcost[t] = min(lowcost[t], w(k,t))` | `dist[t] = min(dist[t], dist[k] + w(k,t))` |
| 比较对象 | **单边权** | **路径累积和** |
| 物理意义 | 到 MST 集合的最小边权 | 到源点的最短路径长度 |
| 数组名 | `lowcost` | `dist` |

### 4. 核心函数说明

| 分类 | 函数 | 功能 | 关键点 |
|:--|:--|:--|:--|
| 建图 | `CreateGraph(G)` | 从键盘读入 $n$、$e$ 及各边，构建邻接表 | 无向图每条边头插两次（$u \rightarrow v$ 和 $v \rightarrow u$） |
| 输出 | `PrintGraph(G)` | 按顶点顺序打印邻接表 | 遍历每个顶点的边链表 |
| **核心** | **`Prim(G, start)`** | 执行 Prim 加点法，输出每步选边过程 | 三个数组协作，$O(n^2)$ |
| 清理 | `DestroyGraph(G)` | 释放所有动态分配的边结点 | 遍历每个顶点链表，逐结点 `free` |

### 5. Prim 算法核心循环流程图

```
初始化:
  visited[start]=1
  遍历 start 邻边 → 填充 lowcost[邻居] = 实际权值

         │
         ▼
  ┌─────────────────────────────┐
  │ for step = 1 to n-1         │  ← 外层循环：共加入 n-1 个顶点
  │   ┌───────────────────────┐ │
  │   │ ① 选点：扫描全部顶点, │ │
  │   │   lowcost 最小的     │ │  O(n)
  │   │   未选顶点 k          │ │
  │   └─────────┬─────────────┘ │
  │             ▼               │
  │   ┌───────────────────────┐ │
  │   │ ② 入树：              │ │
  │   │   visited[k] = 1      │ │
  │   │   total += lowcost[k] │ │
  │   │   输出边(closest[k],k)│ │
  │   └─────────┬─────────────┘ │
  │             ▼               │
  │   ┌───────────────────────┐ │
  │   │ ③ 修正：遍历 k 邻边, │ │
  │   │   对每个未选邻居 t:   │ │  O(deg(k))
  │   │   若 w(k,t)<lowcost[t]│ │  累计 O(e)
  │   │   → 更新 lowcost[t]  │ │
  │   │   → 更新 closest[t]  │ │
  │   └───────────────────────┘ │
  └─────────────────────────────┘
         │
         ▼
   输出 total
```

### 6. 程序执行逻辑与函数调用关系

```
main()
  ├─ SetConsoleOutputCP(CP_UTF8)       控制台 UTF-8 编码
  ├─ CreateGraph(&G)                   构建邻接表
  │    ├─ 初始化 adjlist[i].firstedge=NULL (n 次)
  │    └─ 逐边读入, 头插两次 (u→v, v→u) (2e 次 malloc)
  │
  ├─ PrintGraph(&G)                    打印邻接表验证
  ├─ 读取起点编号
  │
  ├─ Prim(&G, start)                   ★ Prim 算法核心
  │    ├─ 初始化 lowcost/closest/visited
  │    ├─ 起点入树, 更新其邻居的 lowcost
  │    └─ for step=1 to n-1:
  │         ├─ ① 扫描选最小 lowcost 顶点 k
  │         ├─ ② visited[k]=1, 输出选边, total+=lowcost[k]
  │         └─ ③ 遍历 k 所有邻边, 更新 lowcost/closest
  │
  └─ DestroyGraph(&G)                  释放所有边结点内存
```

---

## 三、算法时空复杂度分析

### 1. Prim 算法复杂度

| 阶段 | 操作 | 复杂度 |
|:--|:--|:--|
| 初始化 | 遍历起点邻边 | $O(\deg(start))$ |
| 选点（共 n-1 次） | 扫描全部 n 个顶点找最小值 | 每次 $O(n)$，累计 $O(n^2)$ |
| 修正（共 n-1 次） | 遍历新入树顶点的所有邻边 | 累计遍历所有边 $O(e)$ |
| **总计** | | **$O(n^2 + e) = O(n^2)$** |

由于在简单图中 $e \le n(n-1)/2 = O(n^2)$，$O(n^2)$ 为主导项。朴素 Prim 的 $O(n^2)$ 实现适合稠密图。

### 2. 空间复杂度

| 组成部分 | 空间 |
|:--|:--|
| 邻接表（n 个头指针 + 2e 个边结点） | $O(n + e)$ |
| 三个辅助数组（lowcost, closest, visited） | $3 \times O(n) = O(n)$ |
| **总计** | **$O(n + e)$** |

### 3. Prim 与 Kruskal 适用场景对比

| 对比维度 | Prim 算法（本实验采用） | Kruskal 算法 |
|:--|:--|:--|
| 核心策略 | 加点法：从起点逐步扩展 MST | 加边法：按权值排序，不构成环就加入 |
| 时间复杂度 | **$O(n^2)$**（朴素实现） | $O(e \log e)$（排序主导） |
| 优化版本 | $O(e \log n)$（最小堆） | $O(e \cdot \alpha(n))$（并查集+路径压缩） |
| 适用场景 | **稠密图**（$e$ 接近 $n^2$） | **稀疏图**（$e$ 接近 $n$） |
| 额外数据结构 | 邻接表 / 邻接矩阵 | 边集数组 + 并查集 |
| 本实验为何选 Prim | 管道铺设的自然视角是「从一点出发逐步接入新小区」——与 Prim 的加点思想一致 | 若用 Kruskal 需额外实现并查集和边排序，代码量更大 |

### 4. 手工跟踪验证（每步 lowcost/closest/visited 变化）

**初始状态**（起点 V0）：

```
visited: [1, 0, 0, 0, 0, 0]
lowcost: [INF, 6, 1, 5, INF, INF]
closest: [0,   0, 0, 0, 0,   0]
```

| 步骤 | lowcost（仅未选点） | 选k | 选边 | 费用 | 累计 | 更新操作 |
|:--:|:--|:--:|:--|:--:|:--:|:--|
| 1 | V1:6, V2:**1**, V3:5, V4:INF, V5:INF | V2 | V0-V2 | 1 | 1 | V1←min(6,5)=5(closest→2), V4←6(closest→2), V5←4(closest→2) |
| 2 | V1:5, V3:5, V4:6, V5:**4** | V5 | V2-V5 | 4 | 5 | V3←min(5,2)=2(closest→5) |
| 3 | V1:5, V3:**2**, V4:6 | V3 | V5-V3 | 2 | 7 | V1←min(5,5)=5(不变) |
| 4 | V1:**5**, V4:6 | V1 | V2-V1 | 5 | 12 | V4←min(6,3)=3(closest→1) |
| 5 | V4:**3** | V4 | V1-V4 | 3 | **15** | — |

最终 MST 边集：{V0-V2(1), V2-V5(4), V5-V3(2), V2-V1(5), V1-V4(3)}，总费用 **15**。包含 5 条边（$n-1$）、覆盖全部 6 个顶点 ✓

---

## 四、程序使用说明与测试运行结果

### 1. 使用说明

- **运行环境**：Windows 11，gcc 16.1.0（MSYS2 ucrt64），命令行交互
- **编译命令**：`gcc lab5_pipeline.c -o lab5_pipeline.exe`
- **运行方式**：

```bash
# 方式一：键盘逐行输入（交互模式）
./lab5_pipeline.exe

# 方式二：文件重定向（推荐，数据文件末尾需含起点编号）
./lab5_pipeline.exe < pipeline_data.txt
```

- **输入约定**：顶点编号从 0 开始；权值为整数，INF 表示不连通（用 $0x3f3f3f3f$ ≈ 10⁹ 表示）。

### 2. 测试运行结果

```
╔═══════════════════════════════════════╗
║  实验五：管道铺设 — 最小生成树 MST  ║
║  Prim 算法（加点法）                 ║
╚═══════════════════════════════════════╝

请输入顶点数和边数 (n e): 请逐条输入边 (起点 终点 权值)，共 10 条:
  第1条:   第2条: ...  第10条:

── 邻接表结构 ──
  V0 ──-> [V3|5] -> [V2|1] -> [V1|6]
  V1 ──-> [V4|3] -> [V2|5] -> [V0|6]
  V2 ──-> [V5|4] -> [V4|6] -> [V3|5] -> [V1|5] -> [V0|1]
  V3 ──-> [V5|2] -> [V2|5] -> [V0|5]
  V4 ──-> [V5|6] -> [V2|6] -> [V1|3]
  V5 ──-> [V4|6] -> [V3|2] -> [V2|4]

请输入起点编号 (0 ~ 5, 默认0):

╔══════════════════════════════════════╗
║   Prim 算法 —— 加点法（最小生成树）  ║
╚══════════════════════════════════════╝
起点: V0

  步骤  │  选边        │  费用  │  MST 新增顶点
 ──────┼──────────────┼────────┼──────────────
  第 1步  │  V0 ── V2    │    1   │  V2
  第 2步  │  V2 ── V5    │    4   │  V5
  第 3步  │  V5 ── V3    │    2   │  V3
  第 4步  │  V2 ── V1    │    5   │  V1
  第 5步  │  V1 ── V4    │    3   │  V4
 ──────┴──────────────┴────────┴──────────────
  最小总费用: 15
```

### 3. 测试结果与预期对照

| 验证项 | 预期 | 实际 | 判定 |
|:--|:--|:--|:--|
| MST 边数 | $n-1 = 5$ | 5 | ✅ |
| 总费用 | 15 | 15 | ✅ |
| 第一条边 | V0-V2(1) | V0-V2(1) | ✅ |
| 顶点全覆盖 | 6 个全在 MST | 6 个 | ✅ |
| 邻接表正确（无向边双向出现） | V0↔V2 两边链表均出现 | 验证通过 | ✅ |
| 内存释放（无内存泄漏） | DestroyGraph 清理 | 正常 | ✅ |

全部验证项与预期一致 ✅

---

## 五、测试分析与实验总结

### 1. 测试过程中遇到的问题及解决

| 问题 | 原因 | 解决方法 |
|:--|:--|:--|
| 中文在 Windows 控制台输出乱码 | Windows 控制台默认 GBK，源码 UTF-8 不兼容 | `main()` 首行调用 `SetConsoleOutputCP(CP_UTF8)` |
| 邻接表建图时忘记反向插入（$v \rightarrow u$），导致无向图变有向图 | 对无向图概念理解不足 | 严格按「无向图每条边存两次」规则，每次 `scanf` 后执行两次头插 |
| `lowcost` 更新比较逻辑写反 | 误写成 `p->weight > lowcost[t]` 时更新 | 牢记：Prim 是**找更小边权**，应 `p->weight < lowcost[t]` |
| 图不连通时 Prim 未检测导致死循环 | 若 `k == -1`（找不到可加入的顶点）说明图不连通，未处理该情形 | 在选点循环后添加 `if(k==-1) break` 分支并输出提示 |
| 程序结束未释放动态内存 | C 语言需要手动管理内存 | 编写 `DestroyGraph()` 遍历所有边链表逐结点 `free()` |

### 2. 实验收获

- **深刻理解了 Prim 算法的工作原理**：通过手算跟踪每一步 `lowcost`/`closest`/`visited` 的变化，将抽象的「加点法」落实为可逐一操作的数组更新。特别是 `lowcost` 存储的是「到 MST 集合的最小单边权」而非「到源点的累计路径长」，这是 Prim 与 Dijkstra 的根本区别；

- **体会到数据结构选择与算法核心操作的关系**：Prim 每加入一个顶点就要遍历其所有邻边来更新 `lowcost`——邻接表恰好以 $O(\deg(v))$ 高效完成此操作。若换邻接矩阵则需 $O(n)$ 扫描整行。选择数据结构应看「算法最频繁的操作是什么」；

- **掌握了 MST 问题的两种经典算法及适用场景**：Prim $O(n^2)$ 适合稠密图，Kruskal $O(e \log e)$ 适合稀疏图。实际工程应根据图的密度合理选择；

- **学会了用表格化方式手动跟踪算法执行过程**：逐步记录 `lowcost`、`closest` 等中间状态，对调试和理解复杂算法非常有效；

- **积累了 C 语言动态内存管理的实操经验**：头插法建链表 → 遍历链表释放 → 预防内存泄漏，是 C 编程的基本功。

### 3. 可选扩展与优化思路

- **二叉堆优化 Prim**：当前「找 lowcost 最小顶点」需 $O(n)$ 扫描，改用最小堆可优化到 $O(\log n)$，总复杂度降为 $O(e \log n)$，适合稀疏图；
- **同时实现 Kruskal 并对比**：增加 Kruskal（并查集 + 边排序），允许用户在两种算法间切换并对比性能；
- **文件读取接口**：扩展 `CreateGraph` 支持从文件直接读入图数据（使用 `fscanf`），而非仅键盘输入；
- **图形化输出 MST**：使用 Graphviz 等工具将生成树可视化；
- **鲁棒性增强**：添加输入合法性检查（顶点编号越界、边数负数、自环检测等）。

---

## 六、附录：完整 C 语言源代码

```c
/**
 * 实验五：管道铺设 —— 最小生成树（MST）
 *
 * 问题描述：
 *   某开发区有 n 个小区，需要铺设供水管道使所有小区连通。
 *   已知任意两个小区之间铺设管道的费用（部分路段因地形原因无法直连），
 *   求总费用最小的铺设方案。
 *
 * 算法：Prim 算法 —— 加点法，O(n^2)
 * 数据结构：邻接表（无向带权图）
 *
 * 杨思辰 · 未来技术学院 · 物理学专业 · PB25992094
 */

#include <stdio.h>
#include <stdlib.h>
#include <windows.h>

#define MAXV  100        /* 最大顶点数 */
#define INF   0x3f3f3f3f /* 无穷大（够大、不溢出、方便 memset） */

/* ── 邻接表：边表结点 ── */
typedef struct EdgeNode {
    int adjvex;               /* 邻接顶点编号 */
    int weight;               /* 边权（管道费用） */
    struct EdgeNode *next;
} EdgeNode;

/* ── 邻接表：顶点表 ── */
typedef struct {
    EdgeNode *firstedge;
} VNode;

/* ── 邻接表图 ── */
typedef struct {
    VNode adjlist[MAXV];
    int n, e;
} ALGraph;

/* ════════════════════════════════════════════
   图的构建（从键盘输入）
   输入格式：顶点数n 边数e，然后 e 行 (起点 终点 权值)
   ════════════════════════════════════════════ */
void CreateGraph(ALGraph *G) {
    printf("请输入顶点数和边数 (n e): ");
    scanf("%d %d", &G->n, &G->e);

    for (int i = 0; i < G->n; i++)
        G->adjlist[i].firstedge = NULL;

    int u, v, w;
    printf("请逐条输入边 (起点 终点 权值)，共 %d 条:\n", G->e);
    for (int i = 0; i < G->e; i++) {
        printf("  第%d条: ", i + 1);
        scanf("%d %d %d", &u, &v, &w);

        /* u -> v（头插法） */
        EdgeNode *p = (EdgeNode*)malloc(sizeof(EdgeNode));
        p->adjvex = v;  p->weight = w;
        p->next = G->adjlist[u].firstedge;
        G->adjlist[u].firstedge = p;

        /* v -> u（无向图，对称插入） */
        p = (EdgeNode*)malloc(sizeof(EdgeNode));
        p->adjvex = u;  p->weight = w;
        p->next = G->adjlist[v].firstedge;
        G->adjlist[v].firstedge = p;
    }
}

/* 打印邻接表结构 */
void PrintGraph(ALGraph *G) {
    printf("\n── 邻接表结构 ──\n");
    for (int i = 0; i < G->n; i++) {
        printf("  V%d ──", i);
        for (EdgeNode *p = G->adjlist[i].firstedge; p; p = p->next)
            printf("-> [V%d|%d] ", p->adjvex, p->weight);
        printf("\n");
    }
}

/* ════════════════════════════════════════════
   Prim 算法 —— 加点法
   三个辅助数组协作：
     lowcost[i]  = 顶点i 到当前 MST 的最小边权
     closest[i]  = MST 中离顶点i 最近的顶点
     visited[i]  = 1 表示已在 MST 中
   时间复杂度 O(n^2)，适合稠密图
   ════════════════════════════════════════════ */
int Prim(ALGraph *G, int start) {
    int lowcost[MAXV], closest[MAXV], visited[MAXV];
    int total = 0;

    /* 初始化 */
    for (int i = 0; i < G->n; i++) {
        lowcost[i] = INF;
        closest[i] = start;
        visited[i] = 0;
    }

    /* 起点入树 */
    visited[start] = 1;
    for (EdgeNode *p = G->adjlist[start].firstedge; p; p = p->next)
        lowcost[p->adjvex] = p->weight;

    printf("\n╔══════════════════════════════════════╗\n");
    printf("║   Prim 算法 —— 加点法（最小生成树）  ║\n");
    printf("╚══════════════════════════════════════╝\n");
    printf("起点: V%d\n\n", start);
    printf("  步骤  │  选边        │  费用  │  MST 新增顶点\n");
    printf(" ──────┼──────────────┼────────┼──────────────\n");

    /* 循环 n-1 次，每次加入一个新顶点 */
    for (int step = 1; step < G->n; step++) {
        /* ① 从未选顶点中找 lowcost 最小的 */
        int minW = INF, k = -1;
        for (int j = 0; j < G->n; j++)
            if (!visited[j] && lowcost[j] < minW) {
                minW = lowcost[j];  k = j;
            }

        if (k == -1) {  /* 图不连通 */
            printf("\n 图不连通！已选 %d 个顶点后无法继续。\n", step);
            break;
        }

        /* ② 加入 MST */
        printf("  第%2d步  │  V%d ── V%d    │  %3d   │  V%d\n",
               step, closest[k], k, minW, k);
        visited[k] = 1;
        total += minW;

        /* ③ 用新顶点 k 更新 lowcost 和 closest */
        for (EdgeNode *p = G->adjlist[k].firstedge; p; p = p->next) {
            int t = p->adjvex;
            if (!visited[t] && p->weight < lowcost[t]) {
                lowcost[t] = p->weight;
                closest[t] = k;
            }
        }
    }

    printf(" ──────┴──────────────┴────────┴──────────────\n");
    printf("  最小总费用: %d\n", total);
    return total;
}

/* 释放邻接表中所有动态分配的边结点 */
void DestroyGraph(ALGraph *G) {
    for (int i = 0; i < G->n; i++) {
        EdgeNode *p = G->adjlist[i].firstedge;
        while (p) {
            EdgeNode *q = p->next;
            free(p);
            p = q;
        }
    }
}

int main() {
    SetConsoleOutputCP(CP_UTF8);

    printf("╔═══════════════════════════════════════╗\n");
    printf("║  实验五：管道铺设 — 最小生成树 MST  ║\n");
    printf("║  Prim 算法（加点法）                 ║\n");
    printf("╚═══════════════════════════════════════╝\n\n");

    ALGraph G;
    CreateGraph(&G);
    PrintGraph(&G);

    int start = 0;
    printf("\n请输入起点编号 (0 ~ %d, 默认0): ", G.n - 1);
    if (scanf("%d", &start) != 1) start = 0;
    if (start < 0 || start >= G.n) start = 0;

    Prim(&G, start);
    DestroyGraph(&G);
    return 0;
}
```

---

<div style="text-align: center; margin-top: 2em;">

—— 实验报告完 ——

</div>
